# PARK-LIST

| # | Parked item | Why | Deposit path / unblock | Verification that unblocks |
|---|---|---|---|---|
| P1 | Live payload builder (schedules → ExhibitPayload) | CP-basis engine artifacts (driving tree w/ edge sets, CIC, τ-b, null-model churn, recompute deltas, six-state assignment) do not exist at HEAD; fabricating them violates the fidelity law | Build `engine/cp_basis/` producing the §3 fields; then add `exhibits/build.py::build_payload(schedules, cpms, config)` | `schedule-forensics-report --inputs <files> --target-uid N` exits 0 and its pack passes the determinism + structural tests |
| P2 | §5.1 volData six-state migration + §5.4 new interactive exhibits (EX-03/04/07 live, provenance rail w/ recompute marks) | Same engine dependency as P1 | After P1: extend `_volatility_data` with `state` vectors + transitions; update volatility.js in the SAME commit (no dual format) | `tests/web/test_volatility.py` pins `t.state[c] ∈ {0..5}`; embedded payload == `canonical_json` (parity test) |
| P3 | Full SSI rename (vendor→StructuredSolutions*, metric→ScheduleSensitivityIndex*, `sra_ssi.js` rename) | References tangled through the ~600KB `app.py` + tests + docs; exceeds the three-attempt budget for this session | Dedicated refactor session; trace list = `grep -rn "\bSSI\b" src/ tests/ docs/` | gate test `test_no_bare_ssi_in_exhibits` widened to the whole tree; allowlist emptied |
| P4 | `weighted_instability` on the interactive heatmap | entropy × remaining-duration needs the per-update cell payload (P1); flips-based sort shipped as the honest available measure | P1/P2 land → switch the heatmap sort key to `weighted_instability` | heatmap note names weighted instability instead of flips |
