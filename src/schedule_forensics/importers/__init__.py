"""Importer layer — native ingestion into the domain model.

MSPDI / XER parsers (M3), native ``.mpp`` via the vendored MPXJ runner and the
multi-file loader (≤10, UniqueID-keyed) (M4), and an optional Windows COM path.
See ``docs/PLAN/BUILD-PLAN.md``.
"""

from __future__ import annotations

__all__: list[str] = []
