# Metric dictionary

> Generated from `schedule_forensics.web.help.METRIC_DICTIONARY`. To regenerate:
> `python -c "from schedule_forensics.web.help import render_dictionary_markdown as r; open('docs/METRIC-DICTIONARY.md','w').write(r())"`

Every metric/measure the tool emits, with its formula and source. Each computed value also cites **file + UniqueID + task name** so it can be verified in the parent schedule (§6). The **Dimension** column tags each metric with the NASA Schedule Management Handbook reliability dimension it most informs (Comprehensiveness / Construction / Realism / Affordability) — an organizational lens, not a computed figure.

| Metric | Dimension | Definition | Formula | Source |
|--------|-----------|------------|---------|--------|
| Logic | Comprehensiveness | Incomplete activities missing a predecessor and/or successor. | `count(incomplete without pred or succ) / incomplete <= 5%` | DCMA 14-Point Assessment / reference Analyst Report (PARITY-TARGETS §B, ADR-0012). |
| Leads | Construction | Relationships with a negative lag (a lead). Counted as DISTINCT incomplete-successor activities (the Fuse-validated activity scope), not raw relationships: a task with two lead predecessors is ONE offender (QC audit D22). | `count(distinct incomplete successors of lag < 0 links) == 0` | DCMA 14-Point Assessment / reference Analyst Report (PARITY-TARGETS §B, ADR-0012). |
| Lags | Construction | Relationships with a positive lag into an incomplete successor. Counted as DISTINCT incomplete-successor activities over a total-links denominator (the Fuse-validated scope): a task with two lagged predecessors is ONE offender (QC audit D22). | `count(distinct incomplete successors of lag > 0 links) / links <= 5%` | DCMA 14-Point Assessment / reference Analyst Report (PARITY-TARGETS §B, ADR-0012). |
| FS Relationships | Construction | Share of relationships that are Finish-to-Start. | `count(FS) / links >= 90%` | DCMA 14-Point Assessment / reference Analyst Report (PARITY-TARGETS §B, ADR-0012). |
| SS/FF Relationships | Construction | Start-Start / Finish-Finish links into incomplete work. | `count(SS|FF into incomplete)` | DCMA 14-Point Assessment / reference Analyst Report (PARITY-TARGETS §B, ADR-0012). |
| SF Relationships | Construction | Start-to-Finish relationships (discouraged). | `count(SF into incomplete)` | DCMA 14-Point Assessment / reference Analyst Report (PARITY-TARGETS §B, ADR-0012). |
| Hard Constraints | Construction | Activities with a hard/mandatory constraint (MSO/MFO/SNLT/FNLT). | `count(hard constraint) / activities <= 5%` | DCMA 14-Point Assessment / reference Analyst Report (PARITY-TARGETS §B, ADR-0012). |
| High Float | Construction | Incomplete activities with total float > 44 working days. | `count(total_float > 44d) / incomplete <= 5%` | DCMA 14-Point Assessment / reference Analyst Report (PARITY-TARGETS §B, ADR-0012). |
| Negative Float | Construction | Incomplete activities with total float < 0. | `count(total_float < 0) == 0` | DCMA 14-Point Assessment / reference Analyst Report (PARITY-TARGETS §B, ADR-0012). |
| High Duration | Construction | Incomplete activities with baseline duration > 44 working days. | `count(baseline_dur > 44d) / incomplete <= 5%` | DCMA 14-Point Assessment / reference Analyst Report (PARITY-TARGETS §B, ADR-0012). |
| Invalid Dates | Construction | Actuals after the status date, or a stored forecast (early) date already in the past without the matching actual — the Bible's Invalid Forecast Dates conditions, scored on the file's OWN stored start/finish dates (Acumen basis, ADR-0176), with recomputed CPM only as a fallback for files carrying no stored dates. Task-level count (Fuse's Metric History counts fields, so its figure can be up to 2x this activity count). | `((EarlyStart<Now)*(ActualStart="")) + ((EarlyFinish<Now)*(ActualFinish="")) + actuals after Now == 0` | DCMA 14-Point Assessment / reference Analyst Report (PARITY-TARGETS §B, ADR-0012). |
| Resources | Comprehensiveness | Incomplete, real-duration activities with no resource assigned. | `count(no resource) / incomplete-with-duration <= 5%` | DCMA 14-Point Assessment / reference Analyst Report (PARITY-TARGETS §B, ADR-0012). |
| Missed Activities | Realism | Baselined-due-by-status activities not finished on time. | `count(due not finished on time) / due <= 5%` | DCMA 14-Point Assessment / reference Analyst Report (PARITY-TARGETS §B, ADR-0012). |
| Critical Path Test | Construction | A delay on a critical activity must flow to the project finish. | `inject delay on critical task -> finish moves by the delay` | DCMA 14-Point Assessment / reference Analyst Report (PARITY-TARGETS §B, ADR-0012). |
| CPLI | Realism | Critical Path Length Index. | `(remaining crit-path length + project total float) / remaining crit-path length >= 0.95` | DCMA 14-Point Assessment / reference Analyst Report (PARITY-TARGETS §B, ADR-0012). |
| BEI | Realism | Baseline Execution Index (BEI - Value Tasks; Normal activities only). Cumulative: BOTH terms score the same baselined-due population — tasks completed ahead of a baseline finish that is not yet due do not inflate the numerator (ADR-0176; verified vs every Acumen oracle incl. Hard_File_updated 0.27). | `complete among baselined-due / Normal tasks baselined-to-finish-by-status >= 0.95` | DCMA 14-Point Assessment / reference Analyst Report (PARITY-TARGETS §B, ADR-0012). |
| Missing Logic | Comprehensiveness | Activities with an open start or finish (no pred/succ). | `count(open-ended) / activities <= 5%` | Reference Schedule-Quality summary (PARITY-TARGETS §A, ADR-0012). |
| Logic Density | Construction | Average relationships per activity. | `2 x links / activities` | Reference Schedule-Quality summary (PARITY-TARGETS §A, ADR-0012). |
| Critical | Construction | Incomplete activities on the critical path. | `count(total_float <= 0 and incomplete)` | Reference Schedule-Quality summary (PARITY-TARGETS §A, ADR-0012). |
| Hard Constraints | Construction | Activities with a hard/mandatory constraint. | `count(hard constraint) / activities` | Reference Schedule-Quality summary (PARITY-TARGETS §A, ADR-0012). |
| Negative Float | Construction | Incomplete activities with total float < 0. | `count(total_float < 0) / incomplete` | Reference Schedule-Quality summary (PARITY-TARGETS §A, ADR-0012). |
| Insufficient Detail | Construction | Activities whose (current) duration exceeds 10% of the project's calendar span. | `count(OriginalDuration_workdays / (ProjectFinish - ProjectStart)_days > 0.1) <= 5%` | Reference Schedule-Quality summary (PARITY-TARGETS §A, ADR-0012). |
| Number of Lags | Construction | Relationships carrying a positive lag. | `count(lag > 0) / activities <= 5%` | Reference Schedule-Quality summary (PARITY-TARGETS §A, ADR-0012). |
| Number of Leads | Construction | Relationships carrying a negative lag. | `count(lag < 0) / activities` | Reference Schedule-Quality summary (PARITY-TARGETS §A, ADR-0012). |
| Merge Hotspot | Construction | Activities with 3 or more predecessors (a merge point). | `count(predecessors >= 3) / activities` | Reference Schedule-Quality summary (PARITY-TARGETS §A, ADR-0012). |
| Forecast to be Finished | Realism | Activities the baseline placed on/before the status date. | `count(baseline_finish <= status) / activities` | Reference baseline-compliance / Half-Step-Delay (PARITY-TARGETS §C, ADR-0013). |
| Completed On Time | Realism | Due activities completed on/before their baseline finish. | `count(complete and actual_finish <= baseline_finish) / due` | Reference baseline-compliance (§C, ADR-0013); pass threshold derived from the DCMA 14-Point BEI/CPLI 0.95 standard + GAO-16-89G Best Practice 9 (established ADR-0161). |
| Completed Late | Realism | Due activities completed after their baseline finish. | `count(complete and actual_finish > baseline_finish) / due` | Reference baseline-compliance (§C, ADR-0013); pass threshold derived from the DCMA 14-Point BEI/CPLI 0.95 standard + GAO-16-89G Best Practice 9 (established ADR-0161). |
| Not Completed | Realism | Due activities not yet complete. | `count(due and incomplete) / due` | Reference baseline-compliance / Half-Step-Delay (PARITY-TARGETS §C, ADR-0013). |
| Baseline Finish Compliance | Realism | Share of due activities finished on time (BFC) — the DCMA Baseline-Execution index on the finish side. | `completed_on_time / forecast_to_be_finished` | Reference baseline-compliance (§C, ADR-0013); pass threshold derived from the DCMA 14-Point BEI/CPLI 0.95 standard + GAO-16-89G Best Practice 9 (established ADR-0161). |
| Dates Not Supported by Logic | Construction | Unstarted activities whose stored dates network logic does not produce: manually-scheduled tasks pinned where MS Project stored them, and unlinked auto tasks floored at their stored start (a pure CPM would pack them at the project start). The tool honors the file's dates and cites each divergence. | `count(unstarted tasks where honored stored start != pure logic early start)` | Stored-date CPM mandate (ADR-0034); MS Project manual-task semantics. |
| Logic on Summary Tasks | Construction | Summary (roll-up) tasks carrying predecessor or successor relationships. MS Project applies that logic to the summary's children, so the tool lowers it onto the leaf descendants and schedules the children to match the file — but logic on a summary is a DCMA/PMI anti-pattern that hides the true driver and breaks when the summary's contents change. Each offending summary is cited. | `count(summary tasks that are a predecessor or successor in any relationship)` | Summary-logic handling (ADR-0043); MS Project summary-scheduling semantics. |
| Forecast to be Started | Realism | Activities the baseline placed to start on/before the status date. | `count(baseline_start <= status) / activities` | Reference baseline-compliance / Half-Step-Delay (PARITY-TARGETS §C, ADR-0013). |
| Started On Time | Realism | Start-due activities started on/before their baseline start. | `count(actual_start <= baseline_start) / start-due` | Reference baseline-compliance (§C, ADR-0013); pass threshold derived from the DCMA 14-Point BEI/CPLI 0.95 standard + GAO-16-89G Best Practice 9 (established ADR-0161). |
| Started Late | Realism | Start-due activities started after their baseline start. | `count(actual_start > baseline_start) / start-due` | Reference baseline-compliance (§C, ADR-0013); pass threshold derived from the DCMA 14-Point BEI/CPLI 0.95 standard + GAO-16-89G Best Practice 9 (established ADR-0161). |
| Not Started | Realism | Start-due activities not yet started. | `count(start-due without actual_start) / start-due` | Reference baseline-compliance / Half-Step-Delay (PARITY-TARGETS §C, ADR-0013). |
| Baseline Start Compliance | Realism | Share of start-due activities that started before their baseline FINISH (the reference tool's Half-Step-Delay definition — distinct from Started On Time, which uses baseline start). | `count(actual start <= baseline finish) / forecast_to_be_started` | Reference baseline-compliance (§C, ADR-0013); pass threshold derived from the DCMA 14-Point BEI/CPLI 0.95 standard + GAO-16-89G Best Practice 9 (established ADR-0161). |
| SPI | Affordability | Schedule Performance Index (cost-based; N/A unless the schedule is cost-loaded — this is a data limitation of the file, not a missing threshold: without BCWP/BCWS the ratio is undefined and is never fabricated as 0). | `BCWP / BCWS  (pass >= 1.0 when cost-loaded)` | EVM performance indices (METRICS-CATALOG §3, ADR-0013). |
| CPI | Affordability | Cost Performance Index (N/A unless cost-loaded — a data limitation, not a missing threshold). | `BCWP / ACWP  (pass >= 1.0 when cost-loaded)` | EVM performance indices (METRICS-CATALOG §3, ADR-0013). |
| TCPI | Affordability | To-Complete Performance Index (N/A unless cost-loaded — a data limitation, not a missing threshold). | `(BAC - BCWP) / (BAC - ACWP)  (pass <= 1.0 when cost-loaded)` | EVM performance indices (METRICS-CATALOG §3, ADR-0013). |
| CEI (Finish) | Realism | Current Execution Index, finish side — single-schedule, baseline-anchored (= BFC). | `completed_on_time / forecast_to_be_finished` | Reference baseline-compliance (§C, ADR-0013); pass threshold derived from the DCMA 14-Point BEI/CPLI 0.95 standard + GAO-16-89G Best Practice 9 (established ADR-0161). |
| CEI (Start) | Realism | Current Execution Index, start side — single-schedule, baseline-anchored (= Started On Time %, distinct from Baseline Start Compliance's Half-Step-Delay numerator). | `started_on_time / forecast_to_be_started` | Reference baseline-compliance (§C, ADR-0013); pass threshold derived from the DCMA 14-Point BEI/CPLI 0.95 standard + GAO-16-89G Best Practice 9 (established ADR-0161). |
| CEI (Bow Wave) | Realism | Current Execution Index of the /cei view — pairwise and forecast-anchored. Of the activities the prior snapshot's current schedule forecast to finish in the month after its data date, the share that actually finished by the end of that month. A distinct metric from the baseline-anchored CEI (Finish) above — they answer different questions and must not be conflated. | `finished_by_end_of_P / prior_forecast_for_P  (P = month after prior data date)` | Bow-wave / Current Execution Index (engine/bow_wave.py, §6.D; re-verified ADR-0052). |
| SPI(t) | Realism | Time-based Earned-Schedule index (count-based; informational). Answers 'how far along the baseline finish curve is the completed work vs the time actually elapsed' — it sees stalled work (AT keeps growing while ES freezes). Reported alongside the Acumen per-activity SPI(t) below; the two methods answer different questions and can legitimately disagree in direction (ADR-0176). | `Earned Schedule / Actual Time` | EVM performance indices (METRICS-CATALOG §3, ADR-0013). |
| SPI(t) — Acumen | Realism | Acumen Fuse's per-activity SPI(t): the average duration-efficiency of STARTED activities. Each completed activity contributes baselined span / actual span (calendar); an in-progress activity contributes 0 until it finishes (the Fuse formula's blank-ActualFinish term — faithfully reproduced); zero-span completions (milestones) are excluded. >1 = completed work ran faster than baselined. Verified EXACT vs the Fuse Metric History on Hard_File_updated/2/3 (0.80 / 1.14 / 1.25). Only sees started work — read it together with the Earned-Schedule SPI(t) (ADR-0176). | `AVERAGE(IF(Status="Complete", (BaselineFinish-BaselineStart)/(ActualFinish-ActualStart), ((BaselineFinish-BaselineStart)-(Finish-ProjectTimeNow))/(ActualFinish-ActualStart)))` | NASA Acumen metric library (SPI(t)); Fuse v8.11.0 Metric History parity. |
| Total Activities | Comprehensiveness | Count of schedulable activities in the version. | `count(non-summary activities)` | Reference Schedule-Network / PP & Change (PARITY-TARGETS §E, ADR-0013/0016). |
| Activities Added | Comprehensiveness | Activities present in the current version but not the prior (by UID). | `current UIDs - prior UIDs` | Reference Schedule-Network / PP & Change (PARITY-TARGETS §E, ADR-0013/0016). |
| New Critical | Construction | Activities newly on the critical path vs the prior version. | `critical-incomplete now and not before (present in both)` | Reference Schedule-Network / PP & Change (PARITY-TARGETS §E, ADR-0013/0016). |
| No Longer Critical | Construction | Activities that left the critical path while still incomplete. | `critical-incomplete before, not now, still incomplete` | Reference Schedule-Network / PP & Change (PARITY-TARGETS §E, ADR-0013/0016). |
| Finish Date Slips | Realism | Activities the prior plan placed to finish by now, still incomplete. | `prior forecast_finish <= status and incomplete now` | Reference Schedule-Network / PP & Change (PARITY-TARGETS §E, ADR-0013/0016). |
| Start Date Slips | Realism | Activities the prior plan placed to start by now, still not started. | `prior forecast_start <= status and not started now` | Reference Schedule-Network / PP & Change (PARITY-TARGETS §E, ADR-0013/0016). |
| Remaining Duration Increases | Realism | Activities whose duration grew vs the prior version. Basis note (QC audit D14, CLOSED ADR-0151): the formula compares TOTAL duration (planned+actual), not the stored remaining_duration — and that basis is now Fuse-validated UID-EXACT against the Fuse Forensic 'Original Duration' change sheet for the golden pair (9/9 UIDs). The .aft Bible (1,443 metrics) carries no 'Remaining Duration Increases' metric, so the name is this tool's §E label, not a Bible formula; a remaining-duration basis would count the 7-UID subset that excludes progressed activities whose total grew (both sets recorded in fuse_exports_2026-06.json). | `count(duration_now > duration_prior)` | Reference Schedule-Network / PP & Change (PARITY-TARGETS §E, ADR-0013/0016). |
| Float Erosion | Realism | Incomplete activities whose total float decreased vs the prior version. Fuse-validated UID-exact on the golden pair (ADR-0151): the Fuse Forensic 'Total Float' change sheet, scoped the same way (non-summary + incomplete), erodes exactly the same single activity (UID 131). | `count(total_float_now < total_float_prior and incomplete)` | Reference Schedule-Network / PP & Change (PARITY-TARGETS §E, ADR-0013/0016). |
| Completed | Comprehensiveness | Activities at 100% complete. | `count(percent_complete >= 100)` | Reference Schedule-Network / PP & Change (PARITY-TARGETS §E, ADR-0013/0016). |
| In-Progress | Comprehensiveness | Activities between 0% and 100% complete. | `count(0 < percent_complete < 100)` | Reference Schedule-Network / PP & Change (PARITY-TARGETS §E, ADR-0013/0016). |
| Net Finish Impact | Realism | Calendar-day move of the project finish vs the prior version. | `(prior CPM finish date) - (current CPM finish date), in days` | Reference baseline-compliance / Half-Step-Delay (PARITY-TARGETS §C, ADR-0013). |
| Driving Slack | Construction | Days an activity may slip before it delays the focus (target UID). | `anchored backward pass to the focus task; days of slack to the focus` | Reference MS Project driving-slack add-on (ADR-0011). |
| Driving Path (between two UIDs) | Construction | The chain of activities controlling target B's date that lie on a logic route from source A — the work that, if it slips, moves B. Traced across every loaded version. | `driving path to B (driving_slack < 1 working day) ∩ descendants(A), ordered A → … → B` | Reference MS Project driving-slack add-on (ADR-0011). |
| Total Float 0 Days | Construction | Incomplete activities with no total float left (critical or negative). | `count(incomplete, total_float <= 0) / incomplete` | Reference Power BI deck measure, reconstructed — the deck's DataModel DAX is XPress9-compressed and unreadable (M15, ADR-0030). |
| Total Float < 5 Days | Construction | Incomplete activities with under 5 working days of total float (cumulative band). | `count(incomplete, total_float < 5d) / incomplete` | Reference Power BI deck measure, reconstructed — the deck's DataModel DAX is XPress9-compressed and unreadable (M15, ADR-0030). |
| Total Float < 10 Days | Construction | Incomplete activities with under 10 working days of total float (cumulative band). | `count(incomplete, total_float < 10d) / incomplete` | Reference Power BI deck measure, reconstructed — the deck's DataModel DAX is XPress9-compressed and unreadable (M15, ADR-0030). |
| Free Float 0 Days | Construction | Incomplete activities with no free float left. | `count(incomplete, free_float <= 0) / incomplete` | Reference Power BI deck measure, reconstructed — the deck's DataModel DAX is XPress9-compressed and unreadable (M15, ADR-0030). |
| Free Float < 5 Days | Construction | Incomplete activities with under 5 working days of free float (cumulative band). | `count(incomplete, free_float < 5d) / incomplete` | Reference Power BI deck measure, reconstructed — the deck's DataModel DAX is XPress9-compressed and unreadable (M15, ADR-0030). |
| Free Float < 10 Days | Construction | Incomplete activities with under 10 working days of free float (cumulative band). | `count(incomplete, free_float < 10d) / incomplete` | Reference Power BI deck measure, reconstructed — the deck's DataModel DAX is XPress9-compressed and unreadable (M15, ADR-0030). |
| Completed Ahead | Realism | Completed activities that finished before their baseline finish. | `count(actual_finish < baseline_finish) / completed-with-both-dates` | Reference Power BI deck measure, reconstructed — the deck's DataModel DAX is XPress9-compressed and unreadable (M15, ADR-0030). |
| Completed On Schedule | Realism | Completed activities that finished exactly on their baseline finish date. | `count(actual_finish == baseline_finish) / completed-with-both-dates` | Reference Power BI deck measure, reconstructed — the deck's DataModel DAX is XPress9-compressed and unreadable (M15, ADR-0030). |
| Completed Behind Baseline | Realism | Completed activities that finished after their baseline finish. | `count(actual_finish > baseline_finish) / completed-with-both-dates` | Reference Power BI deck measure, reconstructed — the deck's DataModel DAX is XPress9-compressed and unreadable (M15, ADR-0030). |
| Average Days Ahead | Realism | Average calendar days gained, among the early finishers. | `mean(baseline_finish - actual_finish) over completed-ahead` | Reference Power BI deck measure, reconstructed — the deck's DataModel DAX is XPress9-compressed and unreadable (M15, ADR-0030). |
| Average Days Late | Realism | Average calendar days lost, among the late finishers. | `mean(actual_finish - baseline_finish) over completed-behind` | Reference Power BI deck measure, reconstructed — the deck's DataModel DAX is XPress9-compressed and unreadable (M15, ADR-0030). |
| Average Completion Variance | Realism | Signed average finish variance across all completed activities (+ = late). | `mean(actual_finish - baseline_finish) over completed` | Reference Power BI deck measure, reconstructed — the deck's DataModel DAX is XPress9-compressed and unreadable (M15, ADR-0030). |
| Activities Longer Than Planned | Realism | Completed activities whose duration exceeded their baseline duration. | `count(duration > baseline_duration) / completed-with-baseline-duration` | Reference Power BI deck measure, reconstructed — the deck's DataModel DAX is XPress9-compressed and unreadable (M15, ADR-0030). |
| Activities Shorter Than Baseline | Realism | Completed activities that took less than their baseline duration. | `count(duration < baseline_duration) / completed-with-baseline-duration` | Reference Power BI deck measure, reconstructed — the deck's DataModel DAX is XPress9-compressed and unreadable (M15, ADR-0030). |
| Duration Ratio Min | Realism | Smallest actual-to-baseline duration ratio among completed activities. | `min(duration / baseline_duration)` | Reference Power BI deck measure, reconstructed — the deck's DataModel DAX is XPress9-compressed and unreadable (M15, ADR-0030). |
| Duration Ratio Average | Realism | Average actual-to-baseline duration ratio among completed activities. | `mean(duration / baseline_duration)` | Reference Power BI deck measure, reconstructed — the deck's DataModel DAX is XPress9-compressed and unreadable (M15, ADR-0030). |
| Duration Ratio Max | Realism | Largest actual-to-baseline duration ratio among completed activities. | `max(duration / baseline_duration)` | Reference Power BI deck measure, reconstructed — the deck's DataModel DAX is XPress9-compressed and unreadable (M15, ADR-0030). |
| MEI | Realism | Milestone Execution Index — BEI restricted to milestones. | `milestones finished by status / milestones baselined-to-finish by status` | Reference Power BI deck measure, reconstructed — the deck's DataModel DAX is XPress9-compressed and unreadable (M15, ADR-0030). |
| HMI (Tasks) | Realism | Hit or Miss Index for tasks — period-over-period, not cumulative. A 'hit' is a task the baseline placed to finish in the current status period that actually completed in it. | `tasks baselined-due this period AND completed this period / tasks baselined-due in it` | NASA metric library ('Bible') — extracted formula, ADR-0087. |
| HMI (Milestones) | Realism | Hit or Miss Index for milestones — the milestone counterpart of HMI (Tasks): milestones the baseline placed in the current status period that actually completed in it. | `milestones baselined-due this period AND completed this period / milestones baselined-due the period` | NASA metric library ('Bible') — extracted formula, ADR-0087. |
| CEI (Tasks) | Realism | Current Execution Index for tasks — period-over-period, forecast-anchored. Of the tasks the PRIOR schedule forecast to finish in the current status period, the share that actually completed by the data date. Needs two versions (N/A for a single schedule). | `tasks the prior schedule forecast to finish this period AND now complete / tasks the prior schedule forecast to finish this period` | NASA metric library ('Bible') — extracted formula, ADR-0087. |
| CEI (Milestones) | Realism | Current Execution Index for milestones — the milestone counterpart of CEI (Tasks): milestones the prior schedule forecast to finish this period that completed by the data date. | `milestones the prior schedule forecast to finish this period AND now complete / milestones the prior schedule forecast to finish this period` | NASA metric library ('Bible') — extracted formula, ADR-0087. |
| CEI (Task Starts) | Realism | CEI start cut — of the activities the prior schedule forecast to START in the period, the share that actually started by the data date. | `count(ActualStart > 0) / count(prior Start in (prev data date, data date])` | NASA metric library ('Bible') — extracted formula, ADR-0087. |
| Critical CEI | Realism | CEI restricted to the critical-path activities — of the CRITICAL activities the prior schedule forecast to finish this period, the share that actually completed. | `CEI (Tasks), population filtered to current critical-path activities` | NASA metric library ('Bible') — extracted formula, ADR-0087. |
| CEI (adjusted) | Realism | CEI finish cut that credits early completions — same denominator as CEI (Tasks), but the numerator also counts activities the prior schedule forecast to finish later but done ahead of plan. | `count(complete AND prior Finish > prev data date) / count(prior Finish in (prev, now])` | NASA metric library ('Bible') — extracted formula, ADR-0087. |
| FEI (Starts) | Realism | Forecast Execution Index, start cut — forward-looking (to-go). Of the remaining work, the count of activities still forecast to START in the remaining window over the count the baseline placed there. | `count(Start >= now) / count(BaselineStart >= now), over Normal value tasks` | NASA metric library ('Bible') — extracted formula, ADR-0087. |
| FEI (Finish) | Realism | Forecast Execution Index, finish cut — the remaining activities still forecast to FINISH in the to-go window over the count the baseline placed there. | `count(Finish >= now AND not finished early) / count(BaselineFinish >= now), value tasks` | NASA metric library ('Bible') — extracted formula, ADR-0087. |
| BRI | Realism | Baseline Realism Index (cumulative) — of the activities the baseline placed to finish by now, how many actually finished by now. Backward-looking realism of the baseline. | `count(BaselineFinish <= now AND actually finished <= now) / count(BaselineFinish <= now)` | NASA metric library ('Bible') — extracted formula, ADR-0087. |
| Float Ratio™ | Construction | Average, across the live activities, of each activity's total float divided by its remaining duration — how much breathing room the remaining work has relative to how much work is left. Over Normal planned/in-progress activities (completed work excluded). The cited offenders are the very-tight activities (per-activity ratio < 0.1). | `AVERAGE(TotalFloat / RemainingDuration), Normal planned/in-progress activities` | NASA metric library ('Bible') — extracted formula, ADR-0087. |
| Float Ratio (aggregate) | Construction | The ratio-of-means companion to Float Ratio™: total float over total remaining duration across the same population. More robust than the mean-of-ratios to activities with a tiny remaining duration (which can otherwise dominate the average). | `AVERAGE(TotalFloat) / AVERAGE(RemainingDuration), Normal planned/in-progress activities` | NASA metric library ('Bible') — extracted formula, ADR-0087. |
| EPI | Realism | Execution Progress Index — the deck's DAX adopted verbatim (ADR-0033): the recorded execution events over the events the plan expected once started. | `(n actual starts + n actual finishes) / (n actual starts + n baseline finishes)` | Reference Power BI deck measure, reconstructed — the deck's DataModel DAX is XPress9-compressed and unreadable (M15, ADR-0030). |
| Start-to-Finish Ratio | Realism | The deck's DAX adopted verbatim (ADR-0033): scheduled start/finish pairs per actually-completed pair; falls toward 1.0 as the schedule finishes things. | `n(start & finish present) / n(actual start & actual finish present)` | Reference Power BI deck measure, reconstructed — the deck's DataModel DAX is XPress9-compressed and unreadable (M15, ADR-0030). |
| % Schedule Elapsed Since Latest Actual Finish | Realism | Share of the elapsed schedule that has passed since anything actually finished. | `(status - max(actual_finish)) / (status - project_start)` | Reference Power BI deck measure, reconstructed — the deck's DataModel DAX is XPress9-compressed and unreadable (M15, ADR-0030). |
| Finish Forecast — Schedule Logic (CPM) | Realism | The network's own computed finish, given its logic, durations, and calendar. | `CPM forward-pass project finish` | Reference Power BI deck measure, reconstructed — the deck's DataModel DAX is XPress9-compressed and unreadable (M15, ADR-0030). |
| Finish Forecast — Completion Rate | Realism | Throughput extrapolation: to-go activities at the historical completions-per-month pace. | `status + (remaining / (completed / elapsed_months)) months` | Reference Power BI deck measure, reconstructed — the deck's DataModel DAX is XPress9-compressed and unreadable (M15, ADR-0030). |
| Finish Forecast — Earned Schedule | Realism | The standard Earned-Schedule estimate-at-completion on the working-time axis. | `IEAC(t) = AT + (PD - ES) / SPI(t)` | Reference Power BI deck measure, reconstructed — the deck's DataModel DAX is XPress9-compressed and unreadable (M15, ADR-0030). |
| DCMA Checks Passed (derived) | Construction | Headline schedule-health figure: the share of APPLICABLE DCMA-14 checks that pass. Not-applicable checks are excluded from the denominator (DCMA convention), so it reads the same way an assessor tallies the 14-point card. Derived from the audit's own pass/fail tally — it cannot disagree with the per-check results. | `passing applicable checks / (passing + failing applicable checks) x 100` | DCMA 14-Point Assessment / reference Analyst Report (PARITY-TARGETS §B, ADR-0012). |
| Population Share (derived) | Realism | The percentage of a metric's population that its count represents — the standard normalisation that turns a raw 'N of M' count into a comparable rate (e.g. 12 of 126 hard constraints = 9.5%). Computed from the primary metric's own count and population, to one decimal place; undefined (not a fabricated 0) when the population is empty. | `count / population x 100` | Standard ratio normalisation / the Acumen population-ratio metrics (NASA metric library). |
