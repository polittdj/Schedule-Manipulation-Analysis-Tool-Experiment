# The approved queue — 2026-07-30 (operator-approved; decisions A1 · B1 · C1 recorded)

> The operator approved this plan (with a self-attack pass) on 2026-07-30 and, in the same flow,
> **answered the three decisions that had gated rank 12's remainder** plus the OR-04 behavior
> ruling. This file is the durable record of both — future sessions execute from here and do NOT
> re-ask the decisions. PR-1 shipped under ADR-0315 the same day; the queue below is what remains.

## Decisions recorded (asked from `DECISION-BRIEFS-20260730.md`, answered 2026-07-30)

| Decision | Answer | Carried sub-answers |
|---|---|---|
| A — AXIS-TITLES 3b scope | **A1: `margin_dashboard` first**, rest as 3c | drop `y2Label` where the code shows one scale; retire the local quasi-captions into the helper; tornados recorded as not-axis-charts; deliberate md5 re-baseline is the accepted procedure |
| B — NO_SVG_AXES captions | **B1: native `<caption>` + one SFGantt timescale label slot** | new executable ledger detector; the ADR records "one convention per medium" |
| C — `data-noprint` | **C1: one rule in base.css's A5 print block** (`[data-noprint]{display:none!important}`) | `/analysis` version chips stay print-hidden as marked; single-theme print check; keep the two redundant attributes |
| OR-04 exit behavior | **Free GPU on exit** | stop what the tool started; unload-only for an external service it merely used; never kill a process the tool didn't start (shipped, ADR-0315) |

## The queue — strictly serial (every PR touches the three state docs; parallel branches self-conflict)

| # | Item | Size | Key anchors (verified by recon 2026-07-30) |
|---|---|---|---|
| PR-1 | **OR-04 GPU release — SHIPPED (ADR-0315)** | — | three-tier shutdown + marker + reconciliation; park artifacts #1/#3/#5 open in `audit/VERIFICATION-REPORT-ollama-lifecycle.md` §8 |
| PR-2 | `/performance` first-paint race | S | one word: `defer` on the `performance.js` script tag (app.py ~:19456; mirrors `/resources` `:17856`); guard `performance.js:472` like `:504`; `pageerror` test mirroring `test_r10_resources_contract.py:352-361/:454`; the throw leaves the three quad tiles empty until first interaction |
| PR-3 | `/analysis` scatter panel's two ⛶ → one | S–M | scatter.js adopts the `curves.js:120-149` single-button pattern (`data-sf-big` + `tile-expanded` in one handler); deliberately update the pinned markers in `test_trends_animation.py:42-50/:160-170` (replace with a stronger contract: ONE control whose click changes the measured box, incl. a scrollbar-visible run); close the r11 guard's JS-injection hole (`test_r11_panel_contract.py:583-643`); select by identity, never index (scatter is panel 4 without a target UID, 5 with) |
| PR-4 | `data-noprint` per C1 | S | one line in base.css's A5 print block (`:145-161`; `!important` — `.tile-actions` and the later `.sf-tools` otherwise win); print-emulation measurement, single theme; DESIGN-SYSTEM §7's DoD becomes satisfiable |
| PR-5 | `/resources` X-caption collision | S–M | ADR-0303 "data label yields": thin/clamp the 40°-rotated tick labels in `resources.js:230-234` (~36–40×2–4 px overlap in 8/12 theme×scale combos); add `/resources` to `PAGES` (`test_axis_titles_visual.py:73`) in the same change; `chartframe.js` placement is pinned — do not touch |
| PR-6 | `/evolution` exports honor the banner | M | app.py only: `_export_bar("evolution")` at `:3938` gains the live query string (precedent `:3445`); `export_evolution` (`:5578-5591`) gains Query params (`target`/`tier`/`ignore_constraints`/`ignore_leveling`) and routes through `_optioned_versions` (the banner at `:16849-16857` already promises it); fix the two state-dropping forms (`:18402` hidden `ignore_*`; `:18636` hidden `target`/`tier`/`ignore_*`); export headings state the applied scope |
| PR-7 | OR-01 roll-up titles | M | `_portfolio_body` `app.py:7930-8001`: headings state the applied rule (verified: latest included version, `:7947-7949`; NO average exists in the body today); add "Average DCMA-14 across N versions" as VIEW-LAYER arithmetic over per-version engine outputs only, rule in the heading, "—" when degenerate; per-file surface (card views + home manifest `:2273-2296`) gains the six requested fields; i18n entries |
| PR-8 | AXIS-TITLES 3b-i `margin_dashboard` per A1 | M | `axisTitles` on both charts (`frame(...)` at `:101`/`:232`); drop `y2Label` (single scale verified); retire the two `"status date"` quasi-captions (`:227`/`:297`) into the helper; nudge legends off `(L+4, T+2)`; named re-baselines: the 16-site census md5s (`test_r11_panel_contract.py:452-518`); add `/margin` to the visual `PAGES` + close its measured collisions same change; "not an axis chart" calls recorded in the batch ADR |
| PR-9 | rank-12 toolbar/read-me + B1 mechanism | M–L (may split) | none of the six pages includes `panelkit.js` today; per page: one include (contract), `_panel_head` + `_shell_tools` (⤓ only where a real export exists — dead ⤓ is a defect class; tables ⤓-only per §3:78; ▦ needs a `.sf-drawer`), read-me `<p class=muted>` after `<p class=sf-take>` (canonical `app.py:8559-8566`); B1: native `<caption>` (new `.ch-at` sibling with `color:var(--muted)` — the SVG class styles `fill`) + one SFGantt timescale slot (labels `driving_path`/`path`/`path_evolution`/`sra_grid` at a stroke) + a NEW executable ledger detector + "one convention per medium" ADR |
| PR-10 | OR-03 launch motion + hum | M–L | NO existing audio anywhere (the "Boot Audio Hum" is being defined); the load ends in `window.location` — the hum spans gesture→POST-resolution and fades ≤200 ms before navigation (cross-page audio out of scope, in the ADR); WebAudio synthesis, no asset (wheel+9 installers stay lean; a ≥60 s ogg ≈ +10 MB of artifacts held as fallback); primed only in genuine gesture handlers (pick/folder/example-submit/drop — NOT `input.onchange`); shuffled-pitch-bag swell scheduler, all gains ramped; visible mute+volume on `.load-card`, persisted in localStorage (the verified house pattern: theme.js, sysmon.js, persist.js…); audible-at-low-gain default (operator asked for it; WCAG 1.4.2 wants a control, not silence) + DESIGN-SYSTEM gains the audio rule; motion = CSS-only orbiting craft dots (transform-only, token colors, zero JS — the pinned `_AUTOPLAY_JS` list stays untouched; reduced-motion gets its own `animation:none` line BESIDE the pinned `.load-spinner{animation:none}` literal); tests: `test_launch_sequence.py` (content) + `test_launch_audio_chromium.py` (float-tip skip posture; no context pre-gesture; mute persists; 4-theme × 2-viewport scrollbar-visible geometry) |

Stretch behind the queue: batch 3c (remaining four PENDING modules), rank 13 (vendored woff2), rank 14. Phase 3 (CC-01 rendering half — Fable 5 Max) and Phase 4 (P1–P6) stay behind the UI queue.

## Red-team digest (the plan attacked itself before approval; full versions in the session log)

OR-04 unprovable in the build container → fakes + the operator smoke script are the acceptance (R1, done); deployed-version check rides the smoke (R2); shared-model eviction disclosed (R3); the hum cannot survive the redirect — scope is the load phase, stated not hidden (R4); autoplay satisfied — every overlay entry follows a real gesture (R5); synthesized-hum taste risk — operator's ear is the acceptance, vendored-ogg fallback held (R6); PR-3 updates deliberately-pinned tests — replacement must be stronger, never a deletion (R7); every re-baseline named old→new and isolated in its own commit (R8); the serial pipeline spans sessions — each PR independently shippable, HANDOFF per PR is the checkpoint (R9); re-verify the tip before each branch (R10); mid-session finds get LOGGED, not chased, unless an active Law-1/2 violation (R11); diff the tree after any remote resume (R12); OR-01's average must never mislead — the heading carries the formula, "—" over a plausible wrong number (R13); "panel 5" is positional — select by identity (R14).

## Model & effort (operator-informed choice, ADR-0240 mapping)

Fable 5 at high effort for the queue's risky members (process lifecycle, measured-box merges,
freeze re-baselines, audio); Opus 5 suffices for the mechanical slices (PR-2, PR-4, batch-3c
replication, doc refreshes) with lead re-validation; Fable 5 Max stays reserved for Phase 3's
CC-01 deep dive. Escalate only on an ADR-0240 trigger.
