# Kickoff prompt — next session

**`main` @ `5b605970` (#701, R-70 / ADR-0512, v1.0.277) — its OWN runs were read this session by their JOBS: CI 1945 (`35408559575`): `cui-guard` 00:13:01Z · `browser` 00:30:16Z · `floor` 00:40:20Z · `test (3.13)` 00:59:03Z · `test (3.11)` 01:04:07Z · `check` 01:04:13Z — six of six; installer-smoke 783 (`35408559573`): `linux` 00:13:31Z · `windows` 00:15:44Z. Always `git fetch origin` and read `git log origin/main` before trusting any sha written here, including this one.**

**R-72 is CLOSED — ADR-0513, draft PR [#702](https://github.com/polittdj/Schedule-Manipulation-Analysis-Tool-Experiment/pull/702) on branch `claude/dazzling-knuth-68vcoq` (the OPERATOR merges; if it is merged when you arrive, `main` has moved past `5b605970` — read ITS own six jobs AND its installer-smoke run to conclusion, since the diff touches `installer/**`; expect EIGHT checks on the PR; the `check` job is listed only once its `needs` complete; a red cell on a tree identical to the green PR head is the RUNNER's claim — compare tree hashes before believing it; do NOT open a docs-only PR to record the merge).** Out-of-sequence progress — a started activity whose logic start lies past its recorded actual start — now starts at its RECORD and resumes its REMAINING work at the later of the stored Resume and the link bounds evaluated for the remaining (FF / SF retreating the remaining; a constraint binds nothing once work has begun); the backward pass retreats it by the remaining; a start-type (SS / SF) successor need binds NO started predecessor; a predecessor's free float anchors at the remaining portion's start. Version 1.0.278; highest ADR 0513; SCHEMA 2.17.0 unchanged.

**FIRST: read PR #702's EIGHT checks on its FINAL head to conclusion and record them in the session log (code commit `6bfbeac0`; docs `1ea854a8` / `dc862dbf`; then the pin re-derivation commit — tests + docs, no `src/` — after `dc862dbf`'s first CI read went red on `floor` and `test (3.11)` for three `+12 wd` pins of the 188→187 counterfactual that ADR-0513 legitimately moved to **+6** (UID 187 resumes its remaining after the restored link; the other checks green, `test (3.13)` unread at the time); read the PR's commits, never the prose, for the final head).** Baselines to attribute against: the previous unit's worktree run read 4 failed (attributable) / 5,667 passed / 7 skipped at its code commit; this unit adds 18 synthetic pins + 1 oracle pin and re-pins the two Large Test File oracle rows UPWARD; the worktree suite DIED at 57 % unread — CI's `test` jobs on the final head are this unit's figures (the first read: 3 failed / 5,306 passed / 269 skipped on `floor` and `test (3.11)`, the three re-derived).

Environment (re-measured 2026-09-20): the clone arrives SHALLOW — `git fetch --unshallow origin` first (59 s), then `git fetch --prune origin && git remote set-head origin -a`; the package is NOT installed — `uv pip install --python /usr/local/bin/python3 --system -e '.[dev]' build playwright` (never `playwright install`); re-`uv pip install -e . --no-deps` after a version bump. Run the full sweep with `-v`, never `-q`; never `-o cache_dir=`; a `(…; echo) > log` wrapper reports exit 0 — read the log's `PYTEST_EXIT` line. Keep the token-guardian's `token_audit.py` in the SCRATCHPAD (`ruff check .` is whole-tree). `/root/.local/bin/ruff` 0.15.8 shadows CI's 0.16.8 — `uvx ruff@0.16.8` runs the other; run both. A `.pth` editable install lets `PYTHONPATH` shadow the package — the `-p mutcheck` plugin (`MUTCHECK_ROOT` + `pytest_sessionstart`, written into `<scratchpad>/plugins/mutcheck.py`) asserts the imported module IS the copy every time; the same trick runs the working tree's tests against a pristine worktree's `src/` for the red-first proof, and a SHADOW COPY of `src/` lacks `tools/mpxj` (ten importer-backed tests error there — an artefact of the copy; a git WORKTREE has it). Take the pristine baseline in a SEPARATE WORKTREE (`git worktree add <scratch> origin/main`); run the full suite in a worktree at the CODE commit while the docs are written. The 29 intake `.mpp` convert once with `java -cp tools/mpxj/classes:"tools/mpxj/lib/*" MpxjToMspdi <in> <out>` — key every census by PATH with a lossless encoding (`Large Test File.mpp` and `Large_Test_File.mpp` collide under space→underscore; count the files: 44 = 15 goldens + 29). A file-only census reads every stored date from the XML (the model carries no late dates); its offset arithmetic is a suspect at lunch hours and on EVM1 — the engine on a shadow copy is the instrument for the rule, and the census scripts (`dump.py` / `compare.py` / `variant.py`, the last one building a shadow engine per candidate from exact anchors) are worth rewriting in the scratchpad on day one. `pkill -f <pattern>` matches its own shell — use `[p]attern`.

Run the session-token-guardian's `scripts/token_audit.py` as the FIRST action (copy it to the scratchpad) and before each operator prompt. Then, under QC-3, write the plan down and attack it before the first edit.

The next §3 unit is **R-03** (T1, S — `dcma14.py`'s two parity-mode classifications `round(eff / mpd) < 0` and `> 44` use banker's rounding at exactly .5: build a fixture whose total float is exactly −0.5 d and 44.5 d, pin what the engine classifies today WITH its delta against `round_half_up`, and ask the operator for the Acumen run that settles it — a parity-locked classification, never a blind flip; if no Acumen export can be produced, the row becomes ASK). Then R-04 (the 327 `round()` sites by exposure) · **R-73** (T1, M — the general `Resume + remaining` model for every started activity: 1,113 of 1,159 by the file; 4616 106 days early, 5505 43 early, 7378 three late, the 24Hour_Calendar file's 17 and its project finish 64 days late under the shipped rule; decide the stored instant's projection FIRST — ADR-0322's two-ruler rule refuses a second ruler, and the wide model's away movers were exactly the after-lunch Resumes: EVM1 18's 15:00, TP4 19's 15:00, 5505's 14:45:36) · R-09 · **R-74** (T2, S — free float above the total: MS Project's stored FreeSlack never exceeds its TotalSlack, 0 of 3,315; the engine's does on 1,870 incomplete activities, UID 408's 18,960 for 16,740) · R-69 · **R-71** (T3: the record's own late dates — LS = AS / LF = AF on finished work, LS = AS on started work, the 22 clamped LateFinishes; decide the Critical flag first; the start-type needs and the out-of-sequence late start are already taken) · R-13 · R-18 · R-21 · R-22 · R-32 · R-39; R-68 waits on the operator's reading (question (f)).

⇢ Traps paid for, by name (2026-09-20 first): the trap a kickoff names can belong to the rule it rejected — re-measure it under the candidate before designing around it (1489) · a row's "floor" can be a CONSTRAINT: read the mechanism per case (4581's SNET) · the whole-task bound is the wrong bound for a portion (FF / SF needs on the remaining) · decompose "away" movers by mechanism before rejecting a rule (the axis hour is ADR-0322's designed cost; the SS need was a real finding; UID 34 the record) · write every pin on BOTH paths — six battery survivors were six wall-path pins, and a Resume > Stop shape is an equivalent mutant under ADR-0309's floor · derive from the calendar and verify against the file, never from the engine's answer (three derivations wrong: the project-start clamp, a weekday, the axis's hour on a 13:24 Resume) · a count needs its mechanism (1,119 "Resume > Stop" were the status date's weekend) · MS Project's stored FreeSlack ≤ TotalSlack always (R-74) · `pkill -f` matches its own shell. (2026-09-18 (e), still live:) a row's remedy can be half right — measure every candidate on the class beside the row · a file-only probe's arithmetic is a suspect — the engine on a shadow copy measures the rule · read every miss case by case · an "away" count needs a progress-class column · the battery's survivor is a missing pin — derive it by hand, then cut again · a control's expectation can be mis-derived · a kickoff's "final head" is stale one push later — read the PR's commits · a shadow copy of `src/` has no `tools/mpxj`. (Earlier, still live:) the oracle the row said did not exist was in the same folder under another name · the scalar beside a record is the one to distrust · assert a patch per step · attack the plan before the first edit · measure both candidate rules on the population before choosing · key an oracle by PATH · a T2 row inserted after a T3 row fails the tier-order guard · a register row can be RIGHT about the arithmetic and WRONG about the mechanism · run the census before pricing the fix · negative pins are green on the pristine tree by construction; prove them with a mutant · MS Project's stored dates are a per-activity CPM oracle · MPXJ writes no zero. ⇢ Measured-false / deliberately held — do NOT re-chase: (ADR-0513:) the wide `Resume + remaining` model (R-73, registered with its blocker) · a segment-aware read of one stored instant (ADR-0322 refused it: one instant, two offsets) · the respanned start left at the logic start (a span that lies on 4581) · ADR-0476's pin + full span (60 away, reproduced) · the dropped-zero remaining as an inference (it reads 0 on 302 / 385 and the whole duration on EVM1's 17 and 267) · UID 3849's holiday actual start · the witness file as a golden · the free-float cap (R-74, registered) · (ADR-0512:) dropping a started successor · binding to a started successor's record or its resume alone · the unfloored remaining-work late start · a late-finish census that counts finished work · plus every earlier ADR's held items (see the previous kickoffs in git log). ⇢ Steward posture: draft PRs the OPERATOR merges (never mark ready, never merge, never approve); EIGHT checks when `installer/**` changes, SIX for docs-only; `main`'s own run for a squash is read from its JOBS; `pull_request_read get_status` returns pending / 0 on a fully green PR — use `get_check_runs`; the post-merge safety check is `HEAD^{tree}` vs `origin/main^{tree}`; after a squash-merge restart the branch with `git fetch --prune origin && git remote set-head origin -a && git checkout -B <branch> origin/main`, never amend or rebase the squash commit. Review cover is still absent — Codex quota EXHAUSTED.

Work the POLARIS² audit's plan-forward (Schedule-Manipulation-Analysis-Tool). Read `docs/STATE/HANDOFF.md` FIRST (auto-injected), then `docs/STATE/AUDIT-2026-08-27-REPORT.md` §3 — the roadmap by testimony tier, pinned by `tests/guards/test_audit_report_wp8.py`. QC-1 / QC-2 / QC-3 bind every session (ADR-0393, ADR-0509). `git fetch origin` before you branch, number an ADR, or commit.
