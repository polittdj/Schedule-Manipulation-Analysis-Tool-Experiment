# Kickoff prompt — next session

**`main` @ `60d75e93` (#699, R-67 / ADR-0510 + QC-3 / ADR-0509, v1.0.275) — its OWN CI was read TO CONCLUSION on 2026-09-18: run 1929 (`35362114564`): `cui-guard` 15:23:43Z · `browser` 15:41:20Z · `test (3.13)` 15:50:30Z · `floor` 15:50:59Z · `test (3.11)` 16:14:20Z · `check` 16:14:27Z — six of six; installer-smoke 767 (`35362114719`): `linux` 15:24:05Z · `windows` 15:28:42Z. Nothing about `60d75e93` is outstanding. Always `git fetch origin` and read `git log origin/main` before trusting any sha written here, including this one.**

**R-45 is CLOSED — ADR-0511, a draft PR on branch `claude/ci-verification-task-scheduling-7y0gh8` (the OPERATOR merges; if it is merged when you arrive, `main` has moved past `60d75e93` — read ITS own six jobs AND its installer-smoke run to conclusion, since the diff touches `installer/**`; expect EIGHT checks on the PR; the `check` job is listed only once its `needs` complete; a red cell on a tree identical to the green PR head is the RUNNER's claim — compare tree hashes before believing it; do NOT open a docs-only PR to record the merge).** EV (BCWP) and AC (ACWP) now follow the booking's TIME-PHASED record (`Assignment.performed_work_seconds` / `performed_overtime_seconds` — the file's own resolution; per-booking minute rounding reads 64,104.17 where the ribbon prints 64,105), each booking earning its baseline cost in the performed share of its booked work and spending that record at the STATUS-DATE rate rows (`Resource.overtime_rate` joins `standard_rate`); the budget no booking carries earns at the task's percent; a task whose bookings do not all record an actual cost spends its own, as before. The ribbon's BAC 121,800 is the workbook's time line (2026-07 to 2026-11; updated3's 15 December-starting activities carry the 11,600) and the engine's 133,400 is Fuse's own whole-file Budget Cost — no engine change, pinned as a reconciliation. The disagreement (UID 290: 100 % complete, 22 of 40 booked hours performed) rides SPI and the EVM page. SCHEMA 2.17.0; version 1.0.276; highest ADR 0511.

**Baselines to attribute against:** the previous unit's 5,650 green / 0 failed / 7 skipped plus this unit's additions (10 new tests: 2 parity, 2 engine, 2 importer, 1 Save, 1 web; the schema freeze re-pinned) — verify the count in the worktree log, never transcribe it. `-m parity` was 198 / 0 before this unit; expect 200.

**Environment (unchanged from 2026-09-18 (c), re-measured):** the clone arrives SHALLOW — `git fetch --unshallow origin` first (53 s), then `git fetch --prune origin && git remote set-head origin -a`; the package is NOT installed — `uv pip install --python /usr/local/bin/python3 --system -e '.[dev]' build playwright` (never `playwright install`); re-`uv pip install -e . --no-deps` after a version bump. Run the full sweep with `-v`, never `-q`; never `-o cache_dir=`; a `(…; echo) > log` wrapper reports exit 0 — read the log's `PYTEST_EXIT` line. Keep the token-guardian's `token_audit.py` in the SCRATCHPAD (`ruff check .` is whole-tree). `/root/.local/bin/ruff` 0.15.8 shadows CI's 0.16.8 — run both. A `.pth` editable install lets `PYTHONPATH` shadow the package — the `-p mutcheck` plugin (`MUTCHECK_ROOT` + `pytest_sessionstart`, written into `<scratchpad>/plugins/mutcheck.py`) asserts the imported module IS the copy every time; the same trick runs the working tree's tests against a pristine worktree's `src/` for the red-first proof. Take the pristine baseline in a SEPARATE WORKTREE (`git worktree add <scratch> origin/main`); run the full suite in a worktree at the CODE commit while the docs are written. The parity test module's own `_load_workbook` reads any intake `.xlsx` with the std-lib (SpreadsheetGear omits `r=`; a home-grown reader dies on it) — import it with `importlib` for probes. MPXJ 16.2.0 reads NO task / assignment / resource BCWP / BCWS / ACWP from a `.mpp`; the vendored converter is `java -cp tools/mpxj/classes:"tools/mpxj/lib/*" MpxjToMspdi <in> <out>` (29 intake `.mpp`, 25 distinct basenames — three are duplicated outside `mpp/`). A patch script that asserts each anchor and aborts on the first miss leaves the rest silently unapplied — twice this session an anchor had been reflowed by `ruff format`; assert PER STEP and report per step. The Forensic report's sheets pair every label over a change-marker column and a value column — take the LAST column of a label. `support.microsoft.com` is blocked; Microsoft's MSPDI element reference is reachable through the Microsoft Learn connector's search.

**Run the session-token-guardian's `scripts/token_audit.py` as the FIRST action (copy it to the scratchpad) and before each operator prompt. Then, under QC-3, write the plan down and attack it before the first edit.**

**The next §3 unit is R-70** (T2, M — the backward pass runs THROUGH a completed / started successor MS Project drops: Hard_File_updated3's UID 188 is stored LateFinish 12-12 17:00 while its only successor, the completed UID 291, is stored with a late start of 09-08; the engine binds 188 to 291's need and reads 09-08, three months early, and 189 / 181 / 178 / 179 / 180 follow; the 67 / 34 census in ADR-0510). Candidate rule: drop a recorded-complete successor's need from the backward pass (its dates are a record — ADR-0476 / ADR-0507's reading); a STARTED successor's remaining work needs its own measurement first (the logic-reestablished file's 187 is the witness). Attack first: the row's mechanism (walk the engine's own network to the first disagreement), the class beside the row (started successors; summaries; the 24-hour snapshots' three), and both candidate rules measured on the 44-file population before choosing. Then R-03 · R-04 · R-09 · R-69 · R-13 · R-18 · R-21 · R-22 · R-32 · R-39; R-68 waits on the operator's reading (question (f)).

⇢ **Traps paid for, by name (2026-09-18 (d) first):** the oracle the row said did not exist was in the same folder under another name (the field map; the Forensic report) · a ribbon's population is the workbook's time line, not the file's (four figures down by the same 11,600 / 160 h with actuals unmoved is not-started work leaving a window) · the scalar the file writes beside a record it also writes is the one to distrust (290's 31 h vs 22 h; the reference follows the record) · an assumption measured on the probe's form can fall on the shipped form (per-block vs per-booking rounding: 64,105.33 vs 64,104.17) · a patch script aborting on one anchor leaves the rest unapplied — assert and report per step · a paired-column sheet keys its value on the LAST column of a label · a module-level fixture on a new field errors at collection on the pristine package instead of failing by name — build fixtures per call. (2026-09-18 (c):) attack the plan before the first edit — two of four assumptions fell · a survivor is a missing pin as often as a code hole · measure both candidate rules on the population before choosing · key an oracle's stored values by PATH, never by project name · read the count, not the && chain · say what moved and why · a T2 row inserted after a T3 row fails the tier-order guard · a stale kickoff steers a fresh session at finished work. (Earlier, still live:) a register row can be RIGHT about the arithmetic and WRONG about the mechanism · run the census before pricing the fix · proving a patch inert requires proving it FIRED · a fixed bug is evidence against the hypothesis it lived under · negative pins are green on the pristine tree by construction; prove them with a mutant · MS Project's stored dates are a per-activity CPM oracle · MPXJ writes no zero · Large_Test_File.mpp ≠ Large Test File.mpp. ⇢ **Measured-false / deliberately held — do NOT re-chase:** (ADR-0511:) the booking's scalar `PercentWorkComplete` as the earned share (56,590) · actual work over baseline work (51,885; 47,700 where the oracle reads 49,700) · pricing the record at the block's own date (63,763.07 / 65,903.07) · "ACWP to time now" (refuted by sign) · the record in integer minutes (64,104.17) · a per-booking rate table (B to E) · the 24-hour snapshots' four EV movers (no oracle — a Fuse ribbon of `Hard_File_updated4 24 hour calendar` settles them) · MS Project's stored per-task BCWP / ACWP via MPXJ (not read) · plus every earlier ADR's held items (see the previous kickoff in `git log`). ⇢ **Steward posture:** draft PRs the OPERATOR merges (never mark ready, never merge, never approve); EIGHT checks when `installer/**` changes, SIX for docs-only; `main`'s own run for a squash is read from its JOBS; `pull_request_read get_status` returns pending / 0 on a fully green PR — use `get_check_runs`; the post-merge safety check is `HEAD^{tree}` vs `origin/main^{tree}`; after a squash-merge restart the branch with `git fetch --prune origin && git remote set-head origin -a && git checkout -B <branch> origin/main`, never amend or rebase the squash commit. Review cover is still absent — Codex quota EXHAUSTED.

Work the POLARIS² audit's plan-forward (Schedule-Manipulation-Analysis-Tool). Read `docs/STATE/HANDOFF.md` FIRST (auto-injected), then `docs/STATE/AUDIT-2026-08-27-REPORT.md` §3 — the roadmap by testimony tier, pinned by `tests/guards/test_audit_report_wp8.py`. QC-1 / QC-2 / QC-3 bind every session (ADR-0393, ADR-0509). `git fetch origin` before you branch, number an ADR, or commit.
