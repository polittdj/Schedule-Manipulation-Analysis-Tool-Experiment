# Kickoff prompt — next session (handed over 2026-09-20 (f), after the R-76 unit's push)

**`main` @ `601be5d3` (#706, R-73 / ADR-0517, v1.0.281) — MERGED by the operator 2026-09-20 17:44:09Z, the squash TREE-IDENTICAL to PR #706's final head `62c65a4a` (tree `e409412d…`, re-verified with `git rev-parse <sha>^{tree}`). PR #706's FINAL head read to conclusion — EIGHT of eight green (CI `35518839525`: `cui-guard` 15:12:53Z · `browser` 15:28:41Z · `floor` 15:43:45Z · `test (3.13)` 16:02:26Z · `test (3.11)` 16:07:34Z · `check` 16:07:41Z; installer-smoke `35518839551`: `linux` 15:13:00Z · `windows` 15:18:05Z). `main`'s OWN runs for `601be5d3` by their JOBS: CI 1962 (`35526809555`) `cui-guard` 17:44:27Z · `browser` 18:00:26Z · `floor` 18:15:01Z · `test (3.13)` 18:32:48Z · `test (3.11)` 18:32:00Z · `check` 18:32:55Z — six of six; installer-smoke 796 (`35526809525`) `linux` 17:44:51Z · `windows` 17:49:40Z. Nothing about `601be5d3` outstanding. The R-76 unit ships on `claude/blissful-cori-2nukng` (branched from the squash) as a **draft PR** (its number is in the session log's 2026-09-20 (f) follow-up) that the OPERATOR merges — `src/` changed, wheel + nine installers rebuilt, v1.0.282, so EIGHT checks (CI's six + installer-smoke's `linux` / `windows`). Read that PR's FINAL head's EIGHT checks to conclusion FIRST; if it is merged, restart the branch on the squash (`git fetch --prune origin && git remote set-head origin -a && git checkout -B <branch> origin/main`) and compare `HEAD^{tree}` with the PR's final head's tree. Always `git fetch origin` and read `git log origin/main` before trusting any sha written here, including this one.**

**R-76 is CLOSED — ADR-0518.** Fuse's *Original* / *Baseline Duration* fields are the minutes over the task's own CALENDAR day (else the project's), whole days half-even, the ELAPSED flag IGNORED (146's 2,880 → 6; Jacked Up 1's elapsed UID 20 → 96); *Remaining Duration* takes 1440 for an elapsed activity (146 → 2, UID 20 → 32). The DCMA-14 parity population ("Baseline Duration > 0") reads the Baseline FIELD on that day — the 24-hour Hard_File's 302 / 385 (480 minutes on 1440 → 0) leave every population (Negative Float 11 / 12 → the ribbon's 0.92; DCMA-09 1 = Fuse's 1) — and the "8. High Duration" tile is that field > 44 over the baselined-incomplete population (Large Test File 87 / 927 → 0.09, File2 86 / 904 → 0.10). Shipped: `activity_calendar_day_minutes`, `acumen_duration_field` (no new `round()`), dcma14's parity `_baselined` and DCMA-08 site; the pure-logic mode byte-identical. Measured on 12,088 committed duration cells (three named residuals: the minute-grid 5267, the dropped-zero summaries 315 / 129) and 44 files (population movers: the 24-hour copies' 302 / 385 only; DCMA-08 movers: `24Hour Calendar.mpp` 17 only, no oracle). Version 1.0.282; highest ADR 0518; SCHEMA 2.17.0 unchanged.

**FIRST:** read the R-76 PR's final head (EIGHT checks) to conclusion — and, if the operator has merged it, `main`'s own run for the squash by its JOBS — and record them in the session log as part of the next unit's docs — do NOT open a docs-only PR to record a merge or a run. Do NOT start a background suite that outlives the turn; CI's `test` jobs are the suite verdict, and any local run is read within the turn that started it. A docs-only edit made AFTER the local gate re-runs the doc guards (`tests/test_state_docs.py`, the standing-rules guard under ADR-0393 / ADR-0509, `tests/guards/test_audit_report_wp8.py`) before the push.

Environment (re-measured 2026-09-20 (f)): the clone arrives SHALLOW — `git fetch --unshallow origin` first (54 s), then `git fetch --prune origin && git remote set-head origin -a` (the prune deletes the harness's stale remote-tracking ref for a designated branch that was never pushed — expected); the package is NOT installed — `uv pip install --python /usr/local/bin/python3 --system -e '.[dev]' build playwright` (3 s; never `playwright install`); re-`uv pip install -e . --no-deps` after a version bump. A Bash call caps at 10 minutes: the full suite (~40 min) runs in the background to a log with a `PYTEST_EXIT` line and is read within the turn; targeted suites (`tests/engine tests/test_projects` 32 s; `-m parity` ~5 min) fit a foreground call. Keep the token-guardian's `token_audit.py` in the SCRATCHPAD (`ruff check .` is whole-tree). `/root/.local/bin/ruff` 0.15.8 shadows CI's 0.16.8 — `uvx ruff@0.16.8` runs the other; run both. A `.pth` editable install lets `PYTHONPATH` shadow the package — the `-p mutcheck` plugin (`MUTCHECK_ROOT` + `pytest_sessionstart`, written into `<scratchpad>/plugins/mutcheck.py`) asserts the imported module IS the copy every time. Take a pristine baseline (a copy of `src/` at the squash, md5 manifest) BEFORE the first `src/` edit; red-first runs use it. The 29 intake `.mpp` convert once with `java -cp tools/mpxj/classes:"tools/mpxj/lib/*" MpxjToMspdi <in> <out>` (3 minutes) — key every census by PATH with a manifest (44 = 15 goldens + 29). The xlsx reader lives in `tests/parity/test_fuse_hardfile_float_divisor_oracle.py` (`_sheets`, column-addressed) and `tests/parity/test_fuse_duration_fields_oracle.py` (`_ribbon` with an anchor label — the AlltheProjects ribbon carries no "Status Date " header; anchor on "Float Ratio™"). `pkill -f <pattern>` matches its own shell — use `[p]attern`.

Run the session-token-guardian's `scripts/token_audit.py` as the FIRST action (copy it to the scratchpad) and before each operator prompt. Then, under QC-3, write the plan down and attack it before the first edit.

The next §3 unit is **R-78** (T1, S — Fuse's Float Ratio™ is `AVERAGE(TotalFloat/RemainingDuration)` over Normal, planned-or-in-progress activities on the WHOLE-DAY fields — `acumen_total_float_field` and `acumen_duration_field` on `activity_day_minutes` — and reads N/A whenever a Remaining Duration field is 0; recomputed from Fuse's own displayed fields it reproduces every numeric tile of `AlltheProjects Analysis Report - Quick Add Metrics.xlsx`'s ribbon to 4 dp (−10.9446 → −10.94 on Hard_File_updated3, 14.0076 → 14.01, 0.7678, 0.2667, 0.3427) and every N/A (9 / 9 ⇔ a zero field: the 24-hour file's UID 389, Hard_File's UID 99); the engine averages minutes and skips a zero remaining (−11.85 on the rev-5 updated3, 119.61 / 3.98 / 3.21 where Fuse reads N/A). Decide the tool's OWN reading of the N/A first (a divide-by-zero, not a finding — the pure-logic mode may keep the minute ratio as a second figure, as dcma14 keeps its modes), then move `float_ratio._scored` to the fields, red-first on the rev-5 updated3 and the Large Test File; `duration_days_axis` retires with it and `test_float_ratio.py`'s elapsed pin is re-derived from Fuse's fields; the registration pin in `test_fuse_duration_fields_oracle.py` flips when the row closes). Then **R-79** (T1, S — the DCMA-09 tile's denominator is the baselined INCOMPLETE population: File2 322 / 904 → 0.36 (the engine's every-baselined 1,568 → 0.21), the 24-hour file 1 / 12 → 0.08 (84 → 0.01); the tile counts FIELDS (322) where the engine counts activities (173, ADR-0283's divergence); "9. Invalid Actual Dates" 4 / 0.01 fits neither (267 < n ≤ 800)) · R-09 · **R-74** (T2, S — free float above the total) · **R-77** (T2, M — the stored-date family and the rendering project CONTIGUOUSLY; census the 212 / 25 / 4 and every rendered-time pin FIRST) · R-69 · **R-71** (T3) · R-13 · R-18 · R-21 · R-22 · R-32 · R-39; R-68 waits on the operator's reading (question (f)); the probe fixture's Fuse run is optional (ADR-0514's assumption 5).

⇢ Traps paid for, by name (2026-09-20 (f) first): a ribbon's ratio block is a DENOMINATOR oracle to within a handful of activities — 0.92 is 11 / 12 and only 12 · two grids with one label are two metrics (the tile's filtered detail [267] vs the Quick-Add's [267, 302, 385]) — key a grid by the metric that owns it · price a "site" by its CONSUMER's own figure before changing its divisor (`duration_days_axis` → Float Ratio™'s whole formula) · match a workbook to its SAVE by measurement and print the DIRECTORY (two goldens share the basename `Hard_File_updated3.mspdi.xml.gz`; the AlltheProjects report is rev 5, the update2-vs-update3 report rev 2) · the surviving mutant names the UNVERIFIED claim (raw minutes at the tile ↔ no oracle in 44 < d < 44.5) · the oracle the row said did not exist was in the AlltheProjects grids (JU1 UID 20: 96 / 32) · a duration the file stores to the SECOND (`PT107H59M36S`) is the minute grid's residual, not a rule's. (2026-09-20 (e), still live:) an instrument that projects BOTH sides with the same ruler reads agreement by construction · the axis IS working minutes; only the PROJECTION of a mid-day instant is wrong · a segment-aware read is safe only as a FLOOR under `max()` · a tie is a supported date · the dropped zero has two shapes · `compute_cpm`'s local names are shared across its two paths. (2026-09-20 (d), still live:) a reader must NEVER filter the value it judges · Fuse's xlsx writer omits the cell reference on consecutive cells · two figures with one name are two metrics · the divisor Fuse uses is the TASK's own calendar · an elapsed activity's float field is RAW · a scratch module with top-level work re-runs on import. (2026-09-20 (c), still live:) the reference tool's own REPORT is the rounding oracle · a "measured" example in a docstring is testimony · a row the rule does not reproduce is a mechanism question first · the model's minute grid manufactures ties the file does not have · Fuse writes a grid's Id column as a STRING · say which pin has teeth on what · a CPM change must run the counterfactual's consumers in `tests/web` · a suite without a `PYTEST_EXIT` line was never run. (Earlier, still live:) the scalar beside a record is the one to distrust · assert a patch per step · attack the plan before the first edit · measure both candidate rules on the population before choosing · key an oracle by PATH · a T2 row inserted after a T3 row fails the tier-order guard · a register row can be RIGHT about the arithmetic and WRONG about the mechanism · run the census before pricing the fix · negative pins are green on the pristine tree by construction; prove them with a mutant · MS Project's stored dates are a per-activity CPM oracle · MPXJ writes no zero · `pkill -f` matches its own shell. ⇢ Measured-false / deliberately held — do NOT re-chase: (ADR-0518:) `duration_days_axis` as a divisor fix (the consumer's whole formula is R-78) · reading an absent remaining as zero for the Remaining Duration field (315 / 129 disclosed by name) · the "≥ 1 own day" population alternative (no witness in (0.5, 1)) · the pure-logic thresholds and the five project-day display sites · the AlltheProjects labels whose saves the repo does not hold (EVM2, Hard_File_updated2, TP4 v1–v5) and the intake-only `.mpp` labels as committed oracles · (ADR-0517:) the wall-computed finish projected contiguously · reading an absent remaining as zero in the CPM · a seconds-aware Resume · the late START of started work (R-71) · the four split bookings (R-60's tail) · the free-float cap (R-74) · a per-site segment-aware read of any OTHER stored instant (R-77) · (ADR-0516:) the crew's calendar or the execution calendar as the float divisor · rounding the elapsed activity's float field · (ADR-0515:) a `round_half_up` sweep of any display family · reverting MF-08 · (ADR-0514:) `round_half_up` at the DCMA ties · truncation of the float field · a raw-slack filter · plus every earlier ADR's held items (see the previous kickoffs in git log). ⇢ Steward posture: draft PRs the OPERATOR merges (never mark ready, never merge, never approve); EIGHT checks when `installer/**` changes, SIX for docs-only; `main`'s own run for a squash is read from its JOBS; `pull_request_read get_status` returns pending / 0 on a fully green PR — use `get_check_runs`; the post-merge safety check is `HEAD^{tree}` vs `origin/main^{tree}`; after a squash-merge restart the branch with `git fetch --prune origin && git remote set-head origin -a && git checkout -B <branch> origin/main`, never amend or rebase the squash commit. Review cover is still absent — Codex quota EXHAUSTED (its bot said so on #702 and #704).

Work the POLARIS² audit's plan-forward (Schedule-Manipulation-Analysis-Tool). Read `docs/STATE/HANDOFF.md` FIRST (auto-injected), then `docs/STATE/AUDIT-2026-08-27-REPORT.md` §3 — the roadmap by testimony tier, pinned by `tests/guards/test_audit_report_wp8.py`. QC-1 / QC-2 / QC-3 bind every session (ADR-0393, ADR-0509). `git fetch origin` before you branch, number an ADR, or commit.
