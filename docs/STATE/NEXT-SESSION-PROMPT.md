# Kickoff prompt — next session (handed over 2026-09-20 (e), after the R-73 unit's push)

**`main` @ `b0545572` (#705, R-75 / ADR-0516, v1.0.280) — MERGED by the operator 2026-09-20 10:50:27Z, the squash TREE-IDENTICAL to PR #705's final head `418a7da9` (tree `211074bc…`, re-verified with `git rev-parse <sha>^{tree}`). PR #705's FINAL head read to conclusion — EIGHT of eight green (CI `35503523545`: `cui-guard` 09:53:27Z · `browser` 10:11:07Z · `floor` 10:16:54Z · `test (3.13)` 10:41:46Z · `test (3.11)` 10:49:20Z · `check` 10:49:26Z; installer-smoke `35503523552`: `linux` 09:53:38Z · `windows` 09:57:43Z). `main`'s OWN runs for `b0545572` by their JOBS: CI 1959 (`35506181085`) `cui-guard` 10:50:48Z · `browser` 11:07:13Z · `floor` 11:13:40Z · `test (3.13)` 11:40:47Z · `test (3.11)` 11:37:26Z · `check` 11:40:54Z — six of six; installer-smoke 793 (`35506181084`) `linux` 10:51:08Z · `windows` 10:55:23Z. Nothing about `b0545572` outstanding. The R-73 unit ships on `claude/document-review-continuation-bvzkwm` (restarted on the squash) as **draft PR [#706](https://github.com/polittdj/Schedule-Manipulation-Analysis-Tool-Experiment/pull/706)** (code + docs `f46a9189`, then a docs-only follow-up) that the OPERATOR merges — the full suite at `f46a9189` read 5,729 passed / 7 skipped / 0 failed; `src/` changed, wheel + nine installers rebuilt, v1.0.281, so EIGHT checks (CI's six + installer-smoke's `linux` / `windows`). Read that PR's FINAL head's EIGHT checks to conclusion FIRST; if it is merged, restart the branch on the squash (`git fetch --prune origin && git remote set-head origin -a && git checkout -B <branch> origin/main`) and compare `HEAD^{tree}` with the PR's final head's tree. Always `git fetch origin` and read `git log origin/main` before trusting any sha written here, including this one.**

**R-73 is CLOSED — ADR-0517.** Every started, incomplete activity with a recorded actual start, a restart instant and a STORED remaining (or the SRA's override) resumes it at the later of its stored Resume — read SEGMENT-AWARE by `_stored_instant_offset`, a floor under `max()` against the link bounds — and the link bounds for the remaining, on both paths; the backward pass retreats by the remaining; an in-sequence started successor keeps its RECORD as its predecessor's free-float anchor; a reschedule is disclosed only past logic and the record's own plan; an absent remaining is MPXJ's dropped zero (two shapes, no single reading fits) and keeps `actual_start + duration`. Measured on 44 files: started finishes exact 1,050 → 1,114, late-finish instants 11,543 → 11,645, stored slacks 11,177 → 11,383, free slacks 2,367 → 2,428, Critical 22,069 → 22,095, no boolean measure lost a member, the 24Hour_Calendar file's project finish exact; 4616 / 7378 / 1489 exact, 5505 within the minute grid, EVM1 unmoved. Version 1.0.281; highest ADR 0517; SCHEMA 2.17.0 unchanged.

**FIRST:** read the R-73 PR's final head (EIGHT checks) to conclusion — and, if the operator has merged it, `main`'s own run for the squash by its JOBS — and record them in the session log as part of the next unit's docs — do NOT open a docs-only PR to record a merge or a run. Do NOT start a background suite that outlives the turn; CI's `test` jobs are the suite verdict, and any local run is read within the turn that started it. A docs-only edit made AFTER the local gate re-runs the doc guards (`tests/test_state_docs.py`, the standing-rules guard under ADR-0393 / ADR-0509, `tests/guards/test_audit_report_wp8.py`) before the push.

Environment (re-measured 2026-09-20 (e)): the clone arrives SHALLOW — `git fetch --unshallow origin` first (54 s), then `git fetch --prune origin && git remote set-head origin -a` (the prune deletes the harness's stale remote-tracking ref for a designated branch that was never pushed — expected); the package is NOT installed — `uv pip install --python /usr/local/bin/python3 --system -e '.[dev]' build playwright` (3 s; never `playwright install`); re-`uv pip install -e . --no-deps` after a version bump. A Bash call caps at 10 minutes: the full suite (~40 min) runs in the background to a log with a `PYTEST_EXIT` line and is read within the turn; targeted suites (`tests/engine tests/parity tests/test_projects`, 6:22) fit a foreground call. Keep the token-guardian's `token_audit.py` in the SCRATCHPAD (`ruff check .` is whole-tree). `/root/.local/bin/ruff` 0.15.8 shadows CI's 0.16.8 — `uvx ruff@0.16.8` runs the other; run both. A `.pth` editable install lets `PYTHONPATH` shadow the package — the `-p mutcheck` plugin (`MUTCHECK_ROOT` + `pytest_sessionstart`, written into `<scratchpad>/plugins/mutcheck.py`) asserts the imported module IS the copy every time; a SHADOW COPY of `src/` lacks `tools/mpxj` (importer-backed tests error there — an artefact of the copy). Take a pristine baseline (a copy of `src/` at the squash) BEFORE the first `src/` edit; red-first runs use it. The 29 intake `.mpp` convert once with `java -cp tools/mpxj/classes:"tools/mpxj/lib/*" MpxjToMspdi <in> <out>` (3 minutes) — key every census by PATH with a manifest (count the files: 44 = 15 goldens + 29). `compute_cpm` is ONE function: a new local name can collide with a datetime of the same name in the other path's block (`mypy --strict` catches it; `resumed` did). `pkill -f <pattern>` matches its own shell — use `[p]attern`.

Run the session-token-guardian's `scripts/token_audit.py` as the FIRST action (copy it to the scratchpad) and before each operator prompt. Then, under QC-3, write the plan down and attack it before the first edit.

The next §3 unit is **R-76** (T1, S — Fuse's Original / Remaining Duration fields divide by the task's own day too: the 7/15 grids show UID 14's 1,440 minutes on "24 Hours" as 1 (not 3), the 24-hour file's 302 / 385 / 389 as 2 / 3 / 1 (not 5 / 9 / 3), an ELAPSED original duration on the PROJECT day (146: 2,880 minutes → 6) but its remaining on 1440 (→ 2), and two remaining rows (315 / 129: 247 / 242 minutes shown 0) a different mechanism; decide DCMA-08's population rule FIRST — baseline or remaining, the `.aft` filter and the Detailed Metric Reports' "8. High Duration" marks — then the 282 duration cells of the 7/15 grids under the own-day rule, then the DCMA-08 parity site and `duration_days_axis`, red-first on the four 24-hour rows; two corpus crossings of 44 days (`24Hour Calendar.mpp` 17: 100 → 33, `Jacked Up Schedule 1.mpp` 20's remaining 96 → 32) have no Fuse oracle). Then R-09 · **R-74** (T2, S — free float above the total: MS Project's stored FreeSlack never exceeds its TotalSlack, 0 of 3,315; the engine's does on 1,870 incomplete activities, UID 408's 18,960 for 16,740) · **R-77** (T2, M — the stored-date family and the rendering project CONTIGUOUSLY on an axis that is working minutes: 4 after-lunch actual starts, 25 pinned completed finishes 54–60 min late, 212 unstarted finishes exactly one lunch gap off, EVM2's UID 23 chain; ADR-0322's trap makes it a family — `datetime_to_offset` / `offset_to_datetime` as a segment-aware PAIR, every rendered end-of-day time re-pinned 16:00 → 17:00; census the three classes and every rendered-time pin FIRST) · R-69 · **R-71** (T3: the record's own late dates — LS = AS / LF = AF on finished work, LS = AS on started work, the 22 clamped LateFinishes; decide the Critical flag first) · R-13 · R-18 · R-21 · R-22 · R-32 · R-39; R-68 waits on the operator's reading (question (f)); the probe fixture's Fuse run is optional (ADR-0514's assumption 5).

⇢ Traps paid for, by name (2026-09-20 (e) first): an instrument that projects BOTH sides with the same ruler reads agreement by construction — the pristine "776 exact" and the first candidate's "1,111" were the contiguous ruler judging itself; the stored slack is the oracle no ruler touches, keep it beside every date measure · the axis IS working minutes; only the PROJECTION of a mid-day instant (and its rendering) is wrong — never "fix" a projection by projecting a different instant with the same ruler · a segment-aware read is safe only as a FLOOR under `max()` (ADR-0322's trap bites a pin, not a floor) · a tie is a supported date — a floor that binds by the width of a ruler's error is not a floor (35 disclosures) · the dropped zero has two shapes; state the class, do not infer a zero · a candidate that lifts every date and drops the slacks is wrong about the independent quantity · `compute_cpm`'s local names are shared across its two paths. (2026-09-20 (d), still live:) a reader must NEVER filter the value it judges · Fuse's xlsx writer omits the cell reference on consecutive cells · match a workbook to its SAVE by measurement · two figures with one name are two metrics · the divisor Fuse uses is the TASK's own calendar · an elapsed activity's field is RAW · measure the neighbouring column while the grid is open (R-76) · a scratch module with top-level work re-runs on import. (2026-09-20 (c), still live:) the reference tool's own REPORT is the rounding oracle · a "measured" example in a docstring is testimony · a row the rule does not reproduce is a mechanism question first · the model's minute grid manufactures ties the file does not have (5505's 614 for 614.4) · derive a tie inside the test · Fuse writes a grid's Id column as a STRING · say which pin has teeth on what · a CPM change must run the counterfactual's consumers in `tests/web` · a suite without a `PYTEST_EXIT` line was never run. (2026-09-20, still live:) the trap a kickoff names can belong to the rule it rejected · a row's "floor" can be a CONSTRAINT · the whole-task bound is the wrong bound for a portion · decompose "away" movers by mechanism · write every pin on BOTH paths · derive from the calendar and verify against the file · a count needs its mechanism · MS Project's stored FreeSlack ≤ TotalSlack always (R-74) · `pkill -f` matches its own shell. (Earlier, still live:) the oracle the row said did not exist was in the same folder under another name · the scalar beside a record is the one to distrust · assert a patch per step · attack the plan before the first edit · measure both candidate rules on the population before choosing · key an oracle by PATH · a T2 row inserted after a T3 row fails the tier-order guard · a register row can be RIGHT about the arithmetic and WRONG about the mechanism · run the census before pricing the fix · negative pins are green on the pristine tree by construction; prove them with a mutant · MS Project's stored dates are a per-activity CPM oracle · MPXJ writes no zero. ⇢ Measured-false / deliberately held — do NOT re-chase: (ADR-0517:) the wall-computed finish projected contiguously (refuted by 1,247 slacks) · reading an absent remaining as zero (302 / 385 exact, 17 / 267 the morning they began) · a seconds-aware Resume (5505's minute is R-65's grid) · a finish-slack-only total for the wide class (unneeded) · the late START of started work (R-71) · the four split bookings (R-60's tail) · the free-float cap (R-74) · a per-site segment-aware read of any OTHER stored instant (R-77 — a family, ADR-0322's trap) · (ADR-0516:) the crew's calendar or the execution calendar as the float divisor · rounding the elapsed activity's field · moving the five project-day display sites or the pure-logic DCMA thresholds to the activity's day · re-reading the LTF field oracle's document-order reader · (ADR-0515:) a `round_half_up` sweep of any display family · reverting MF-08 · a 2-dp precision change toward MS Project's display · (ADR-0514:) `round_half_up` at the DCMA ties · truncation of the float field · a raw-slack filter · the truncated Fuse grids as oracles · (ADR-0513:) a segment-aware read of one stored instant AS A PIN · the respanned start left at the logic start · ADR-0476's pin + full span · UID 3849's holiday actual start · the witness file as a golden · plus every earlier ADR's held items (see the previous kickoffs in git log). ⇢ Steward posture: draft PRs the OPERATOR merges (never mark ready, never merge, never approve); EIGHT checks when `installer/**` changes, SIX for docs-only; `main`'s own run for a squash is read from its JOBS; `pull_request_read get_status` returns pending / 0 on a fully green PR — use `get_check_runs`; the post-merge safety check is `HEAD^{tree}` vs `origin/main^{tree}`; after a squash-merge restart the branch with `git fetch --prune origin && git remote set-head origin -a && git checkout -B <branch> origin/main`, never amend or rebase the squash commit. Review cover is still absent — Codex quota EXHAUSTED (its bot said so on #702 and #704).

Work the POLARIS² audit's plan-forward (Schedule-Manipulation-Analysis-Tool). Read `docs/STATE/HANDOFF.md` FIRST (auto-injected), then `docs/STATE/AUDIT-2026-08-27-REPORT.md` §3 — the roadmap by testimony tier, pinned by `tests/guards/test_audit_report_wp8.py`. QC-1 / QC-2 / QC-3 bind every session (ADR-0393, ADR-0509). `git fetch origin` before you branch, number an ADR, or commit.
