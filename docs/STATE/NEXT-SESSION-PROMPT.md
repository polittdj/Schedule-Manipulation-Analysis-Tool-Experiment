# Kickoff prompt for the next session

Paste the block below verbatim to start the next session. (This file is a pointer, not a status snapshot — `docs/STATE/HANDOFF.md` is ALWAYS the authoritative "where we are"; if any number here disagrees with HANDOFF, HANDOFF wins. Refresh this file whenever the queue changes — a stale kickoff steers a fresh session at work that is already done.)

It once went EIGHT SLICES stale and handed a session work finished five slices earlier; only the auto-injected handoff caught it. It carries no drift guard, unlike `HANDOFF.md` — which is exactly why it rots. Refreshing it is part of session close, not an optional tidy. It has been wrong FOUR times: it claimed the data-date gap was "guarded by NO test" when a guard had existed since #536; it named the data date as the mechanism when the mechanism was an ignored `ActualStart`; its "mpxj_ref has no shallow-clone guard — add refuse-if-not-ancestor" was half right — the guard was missing but ancestry is the WRONG check (ADR-0397 used tree-identity); and it said SEC-01's "behavioural half" was wholly missing when `test_sec_hardening.py` already behaviourally covered both consumers — the real holes were narrower (sample-blindness to a named widening; the Origin scheme conjunct caught by nothing; ADR-0400 re-measured before building). Do not trust its diagnoses — re-derive from measurement. (It ALSO mispredicted the ADR number three times — two same-day sessions once both took 0395. Take the next free number after `git fetch origin`, never the one a doc predicted — and fetch AGAIN before committing.)

Resume POLARIS (Schedule-Manipulation-Analysis-Tool). Read `docs/STATE/HANDOFF.md` FIRST (auto-injected). As of last close: v1.0.201, highest ADR 0400, SCHEMA 2.11.0. THE REPOSITORY IS PRIVATE (DISC-01 remediation) — anonymous raw.githubusercontent fetches 404, the container clone is SHALLOW, and any workflow that assumed a public repo must be re-derived, not assumed. Recent sessions, oldest first: QC-1/QC-2 standing rules (ADR-0393); DoD 001a loopback-allowlist pin (ADR-0394); the read-only ten-role audit as docs + `tests/audit/test_audit_findings.py` (ADR-0395: xfails ENCODE live defects — drive an xfail green when you close its finding); DoD 001b observed sovereignty banner (ADR-0396); DoD 117 MPXJ shallow-clone pin guard (ADR-0397); the private-repo installer fetch (ADR-0398); HOOK-01 disguise/container detectors for the CUI pre-commit guard (ADR-0399); and three verification-gap closures (ADR-0400): the Fuse transcription oracle (`tests/parity/test_fuse_transcription_oracle.py` — PO-03 xfail flipped and marker removed), the ADR-0391 own-calendar floor guard (`tests/engine/test_actual_start_floor_own_calendar.py`), and the `_ALLOWED_HOSTS` behavioural closure (`tests/web/test_sec01_host_allowlist_closure.py`). `tests/audit` now has ONE live xfail: TEST-01.

⇢ BEFORE ANYTHING: `CLAUDE.md` carries two binding working rules — QC-1 and QC-2 (ADR-0393), pinned by `tests/test_standing_rules.py`. Read them in full. QC-1: before any change, conclusion or document update, build an executable pass/fail check, run it in a sandbox, and use it to try to REFUTE the claim — before the result is reported (red before green; prove the check has teeth by mutation; say UNVERIFIED rather than assert silently). QC-2: read everything, skip nothing, assume nothing, verify everything — and if an error is found, QC-1 applies before the correction.

⇢ DISC-01 — RELEASABILITY, still gating (audit 2026-08-13; REQUIRES AUTHORIZING OFFICIAL) The gateway hostname and ITAR-tagged model identifier are in git HISTORY since `a19b969` (and in `docs/PLAN/APPROVED-GATEWAY-INTEGRATION.md`, older STATE docs, ADR-0394). Making the repo private MITIGATES but does not DECIDE releasability — history rewrite (filter-repo) vs accept-as-is is an authorizing-official call, not an engineering commit. Do not add new copies of those strings (this file deliberately contains none; post-001b files use fictional placeholders — `.example`/`.invalid` hosts). Treat 001c gateway wiring as design-gated until the determination lands.

⇢ WHAT'S DONE — do not re-open

* 001a (ADR-0394): `net_guard`'s two frozensets pinned, data + behaviour, 9/9 mutations by name.
* 001b (ADR-0396): every sovereignty claim rides ONE observed derivation; 18 guard tests, 15/15 mutations by name. GW-02 is a plain passing test.
* 117 (ADR-0397): `mpxj_ref()` refuses shallow-graft-boundary resolutions; installers pin `42d92dc`.
* Private-repo fetch (ADR-0398): installers use the contents API when a token is set; `installer-smoke.yml` passes `github.token`.
* HOOK-01 (ADR-0399): disguise-suffix chain, anchored serialization-start sniffs, OLE2/ZIP/PDF container checks; 42-case battery; 8/8 mutations; the C-quoting fail-open fixed. THE HOOK IS ITSELF AN EXTENSION-LESS SNIFFED FILE — never write a signature literal in its comments (census-enforced).
* PO-03 (ADR-0400): the Fuse transcription is machine-guarded — `test_fuse_transcription_oracle.py` re-derives every derivable `fuse_exports_2026-06.json` value from the four vendor workbooks (115/115 locations mapped, transcription CLEAN); battery flips JSON-side AND workbook-byte-side by name. The audit xfail is flipped, marker removed.
* exec_cal floor (ADR-0400): the ADR-0391 own-calendar floor is guarded — red 3/3 with the branch deleted, green with the project-axis SIBLING deleted (isolation control). It was deletable with engine + parity green while moving UID 5230 six years on the primary golden.
* SEC-01 (ADR-0400): `_ALLOWED_HOSTS` closure through the HTTP layer — M1-M5 batteries by name incl. the previously-uncovered Origin-scheme hole (M3) and the middleware-unwired case (M5).

⇢ NEXT — in order

1. DISC-01 release determination — operator / authorizing official. Blocks nothing below except gateway wiring.
2. 001c — OPERATOR DECISION, then its ADR (next free number after `git fetch origin`). Delete the dead `cloud` option, or build a first-class `ai/gateway.py` with `is_local=False`, its OWN named allowlist (never a widening of the loopback set), a classification gate, and an AI transaction log (`APPROVED-GATEWAY-INTEGRATION.md` §6 steps 4–6). ADR-0396 made the honest path mechanical. `openai_compat.py` is std-lib `urllib` and already speaks the wire format — no new dependency.
3. PO-04/05 — CEI/bow-wave and HMI have NO independent reference oracle in the repo. BLOCKED ON A MISSING PRIMARY ORACLE: needs an operator-delivered vendor export for those families; engineering cannot conjure one (a fixture generated by the rule under test cannot validate the rule).
4. `actual_start_driven` computed but consumed NOWHERE in `src/` (ENG-DEAD-01) — wire as an INFO finding or a column beside "Date-driven" (ADR-0391's promised disclosure). SHIPPED-code change: version bump + wheel + nine installers in the same unit (ADR-0148 lockstep).
5. TEST-01 — 22 playwright modules hard-pin a chromium BUILD NUMBER (the audit module's last live xfail); CI's browser job runs only the one fixed module. Propagate r11's glob fix; flip the xfail and remove its marker in the same commit.
6. FINAL-REPORT overclaims on parity and "No data off-machine" — condition on the observed backend (`_observed_banner` is the derivation to cite), do not weaken.
7. 8 stale remote branches, 148–611 commits behind (DoD 091).

⇢ TWO PREMISES REVERSED BY MEASUREMENT — do NOT "fix" these

* SSI Mean/StdDev: ours sits 3 days / 3.4% from SSI's own exported distribution; P80 EXACT. Sampling noise, not a defect.
* SRA sensitivity VALUES match SSI exactly; only the LABELS are wrong (tornado prints the host task's name — SRA R-2, still open).

⇢ TRAPS PAID FOR — check BY NAME THE REPO IS PRIVATE AND THE CLONE IS SHALLOW — anonymous fetches 404; per-path git history lies (IN A SHALLOW CLONE, `git log -1 -- <path>`, `--stat` AND `merge-base` CAN ALL AGREE ON A FALSEHOOD — break the loop with an independent oracle: tree hashes, the remote commits API). A TRANSCRIPTION ORACLE MUST BE PROVEN TO READ THE SOURCE SIDE — mutate the workbook BYTES, not just the JSON; a guard comparing the expectation to itself stays green (ADR-0400 B3). AN ENGINE BRANCH CAN MOVE A MEMBER SIX YEARS WHILE EVERY AGGREGATE PIN STAYS BYTE-IDENTICAL — pin the member the branch moves, and run an ISOLATION control (sibling-branch deleted → new guard stays green). A POPULATION ROW MUST BE TRANSPORTABLE — the raw-Unicode homograph Host ERRORED (httpx refuses to encode it) instead of failing; sweep the punycode form a browser actually sends. THE SPREADSHEETGEAR TRAP — the vendor workbooks omit `r=` coords; a reader requiring them sees EMPTY sheets (the SSI test's `_cells` has exactly that requirement — do not reuse it on Fuse exports). A CI FAILURE ON AN UNCHANGED CODE PATH IS A QUESTION ABOUT THE ENVIRONMENT FIRST — and a "fix" that undoes a deliberate security remediation is not a fix. A DIAGNOSIS THAT REVERSED ONCE IS NOT DONE REVERSING — record what the LAST measurement proved and name the measurement. THE ADR-NUMBER COLLISION IS REAL — fetch before numbering AND before committing. WHEN ONE CLAIM SPANS MANY SURFACES, ROUTE THEM THROUGH ONE DERIVATION. A DATA PIN GUARDS THE LITERAL, NOT THE GUARANTEE — pin the constant AND sweep the behaviour (ADR-0394; SEC-01's M3 was the conjunct-drop nothing saw). WHEN A BATTERY MUTATES A SANDBOX, PROVE THE SANDBOX IS THE TREE BEING MEASURED (import-origin canary; md5 the instruments); BEFORE TRUSTING A SANDBOX CENSUS, PROVE ITS TRACKED SET EQUALS THE REAL REPO'S. A GUARD THAT SNIFFS A FILE CLASS IT BELONGS TO WILL READ ITSELF. A PREDICTED MUTATION OUTCOME IS NOT A MEASURED ONE — write the flip list the battery printed. MUTATION-GREEN IS NOT ADVERSARIALLY VERIFIED — after 8/8 by name, HOOK-01's attack round still found five defect classes; adversaries probe BETWEEN the mutations. A GUARD'S INPUT PLUMBING IS ATTACK SURFACE (`git diff --name-only` C-quotes paths; read raw with `-z`). MONKEYPATCH REPOINT IS PER CALL SITE, NOT PER NAME. AN ARCHITECTURE THAT OFFERS NO LEGITIMATE PATH TO A REAL NEED WILL GET THE DANGEROUS PATH TAKEN (001c). A CLAIM DERIVED FROM CONFIG DESCRIBES INTENT, NOT BEHAVIOUR (FINAL-REPORT's instance still open). A DOCUMENT THAT FLAGS A DISCLOSURE MUST NOT REPEAT THE DISCLOSED LITERAL. `xfail(strict=True)` IS HOW A VALIDATED-BUT-UNFIXED FINDING LANDS — a fix flips it loudly; remove the marker in the fixing commit (GW-02, HOOK-01, PO-03 all followed the design). A SWEEP'S GLOB/POPULATION/PATTERN ARE PART OF ITS CLAIM. `| head -N` CAN SIGPIPE-KILL A BUILD MID-WAY. Plus the standing set (instrument shown to FAIL first · never MEASURE a tree a battery is mutating · never MUTATE an instrument a measurement is using · `grep -c` exits 1 on zero · two ruffs on PATH, use `python -m ruff` · bare `pytest` does not prepend CWD).

⇢ TIMING / ENVIRONMENT — MEASURED The container starts with NO deps installed: `python -m pip install -e ".[dev]"` and `pip install build` before anything runs. Full local suite ~30 min. `pytest -m parity` alone exceeds 900 s — give it room or it dies to your own `timeout`. CI ~60 min end-to-end; `check` is a gate job and its absence early is sequencing, not failure; `cancel-in-progress: true`, so never push while you need a run's signal. Installer-smoke is path-filtered to `installer/**`, `tools/installer/**`, and its own workflow file — seven checks on PRs touching those, four otherwise. After a squash-merge, restart the branch: `git fetch --prune origin && git remote set-head origin -a && git checkout -B <branch> origin/main` — never amend GitHub's squash commit.

⇢ Standing rules (binding) QC-1 — PROVE OR REFUTE IT BEFORE YOU REPORT IT. QC-2 — READ EVERYTHING, VERIFY EVERYTHING. Both are ADR-0393, binding, NO EXCEPTIONS, pinned by `tests/test_standing_rules.py`. Read them in full in CLAUDE.md before you start. Law 1 CUI · Law 2 fidelity ("—" never 0; never weaken a test) · ADR-0240 model protocol (Ultracode for the overall audit; one lead re-verifies every finding against code evidence before it is reported) · full gate before every commit · handoff rotation + SESSION-LOG + LESSONS-LEARNED + THIS FILE in the same commit · wheel + nine installers ONCE per shipped-code change · a number written mid-session is not a measurement (`wc` decides). Skills: `full-gate`, `prove-able-to-fail`, `metric-parity`, `ui-change`, `cui-guard`, `render-verify`, `session-close`.
