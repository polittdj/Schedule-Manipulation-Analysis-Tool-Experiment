# Kickoff prompt — next session

PR state (2026-09-07, session b): #645 and #646 MERGED → `main` @ `0408c56`; this session's draft PR (number in the SESSION-LOG follow-up) carries ADR-0473 at **v1.0.243** — merge it, then branch FRESH (`git fetch --prune origin && git checkout -B <branch> origin/main`).

Work the POLARIS² audit's plan-forward (Schedule-Manipulation-Analysis-Tool). Read `docs/STATE/HANDOFF.md` FIRST (auto-injected), then `docs/STATE/AUDIT-2026-08-27-REPORT.md` §3 — the roadmap by testimony tier, pinned by `tests/guards/test_audit_report_wp8.py` (every row priced or owned, tier order, a nine-figure census recomputed by method — re-measure, never edit by hand). QC-1/QC-2 bind every session (ADR-0393). `git fetch origin` before you branch, number an ADR, or commit. The container may have NO project install: `python3 -m pip install -e '.[dev,browser]'` and `pip install build` before measuring anything; the vendored Chromium is under `/opt/pw-browsers`; `java` is present for `tools/mpxj/` (`java -cp tools/mpxj/classes:tools/mpxj/lib/* MpxjToMspdi <in.mpp> <out.xml>`).

⇢ WHAT'S DONE — do not re-open. WP0–WP8 (ADR-0440..0472). **ADR-0473:** R-01 CLOSED (the Bible's blank-as-zero ACWP is Fuse's own — CPI 0.81/0.78 exact; the started, budgeted activities with no actual cost ride CPI/TCPI as count/population/offenders and /evm prints them) · the §C compliance block scores the Bible's CURRENT Finish/Start (exact on 12 oracles) · MSPDI currency ÷ 100 (Hard_File BAC 133,400 exact) · BCWS linear over the baseline span (64,240 / 110,440 exact) · the ribbon Negative Float in whole days (LTF2 122) · the five operator questions DECIDED (the report's §5: probe kept · P6 planned basis kept with disclosure · rulings stand, `.pptx` fails in LibreOffice · UID 152 does not move on the repo's pair · chips/folder ask pinned) · `tests/parity/test_fuse_metric_history_oracle.py` reads the vendor workbooks with the std-lib (15) · the parity report's DCMA08 row names High BASELINE Duration.

⇢ NEXT — the report's §3 in order, one row per unit of work (red-first → mutation proofs by name → the full gate → an ADR → the state docs → a draft PR): **R-44** FIRST — Hard_File's CPM finish is 42 days later than MS Project's stored 2026-11-05 because its resources sit on 16-hour / 24-hour calendars the base CPM does not model; an assignment-calendar pass behind a flag, the four Hard_File stored finishes the oracle (46331 / 46332 / 46368), `pytest -m parity` unmoved · **R-49** — MPXJ omits a ZERO `TotalSlack` (62 of Fuse's 66 zero-float activities on LTF2 carry none): the importer infers 0 when the file carries the element elsewhere and the task carries `Critical`; red-first on Fuse's Zero Days Float 66 / 2; the SSI driving-slack gates are the arbiter · R-46 (the BCWS +150 on a resource calendar) · R-47 (SPI(t) 8.24 vs 8.22, per-activity vs the Detailed report's column) · R-52 (the `.pptx` package LibreOffice refuses — diff against a PowerPoint-authored minimal deck) · R-50 (expose the History variants as their own metrics) · then R-03 (ask for the Acumen half-day run) · R-04 · R-09 · R-13 · R-18 · R-20 · R-21 · R-22 · R-32 · R-39. PLUS the design page owed each session: /standards (`setScreen('sd')`) or /scorecards (`'sk'`) — the recipe in ADR-0471/0472.

⇢ Traps paid for, by name (2026-09-07 b first): the reference library carries the SAME display name with DIFFERENT inclusion sets per workbook section — a Fuse figure is an oracle only for the tile its own section carries; read the inclusion block, prove the mapping on a ribbon oracle that discriminates, THEN change a rule · a harness's first-occurrence label lookup collides with same-named rows · a 2-dp ratio derives from the counts, never the 1-dp percentage · MPXJ writes no zero (Cost / ActualCost / TotalSlack omitted at 0) · `Large_Test_File.mpp` ≠ `Large Test File.mpp` (the `ssi_uid152` fixture is the former, 31 vs 41) · `<= 0.01` needs `0.0101` · `git archive HEAD src` on PYTHONPATH is the pristine tree · never bump the version under a running battery · a figure without a method is a rumour · a census instrument that names the literal it censuses self-matches · element boxes cannot see a pseudo-element · the preflight's `[ok] python` is an interpreter check · rebuild the wheel + nine installers as the LAST step · never measure a tree a battery is mutating.

⇢ Measured-false / deliberately held — do NOT re-chase: the ribbon tiles' scopes (Insufficient Detail every status · Merge Hotspot every status · DCMA-03 planned + in-progress · Number of Lags every status — each proven by a ribbon oracle or a golden when the History variant was tried) · TP3's ribbon 8 / Lags 3 (a differently-progressed `.mpp`; R-53) · the DCMA08 baseline basis (ADR-0110's drift; R-48 needs a discriminating ribbon) · Fuse's ACWP-to-time-now and updated3's BAC (R-45) · the HELD and CLOSED rows of the report · late_start for a floored task · the S-curve & finish walk · CI-03 as a slow first paint · the 176 (retired).
