# Kickoff prompt — next session (handed over 2026-09-21, after the R-78 unit's push)

**`main` @ `01251bb2` (#707, R-76 / ADR-0518, v1.0.282) — MERGED by the operator 2026-09-20 21:21:51Z, the squash TREE-IDENTICAL to PR #707's final head `5deb78d6` (tree `78b981fb…`, re-verified with `git rev-parse <sha>^{tree}`). PR #707's FINAL head read to conclusion — EIGHT of eight green (CI `35535165650`: `cui-guard` 20:19:54Z · `browser` 20:35:30Z · `floor` 20:53:27Z · `test (3.13)` 21:11:14Z · `test (3.11)` 21:16:55Z · `check` 21:17:36Z; installer-smoke `35535165662`: `linux` 20:20:05Z · `windows` 20:24:06Z). `main`'s OWN runs for `01251bb2` by their JOBS: CI 1965 (`35538468705`) `cui-guard` 21:22:10Z · `browser` 21:38:14Z · `floor` 21:53:45Z · `test (3.11)` 22:08:40Z · `test (3.13)` 22:11:19Z · `check` 22:11:25Z — six of six; installer-smoke 799 (`35538468545`) `linux` 21:22:34Z · `windows` 21:26:46Z. Nothing about `01251bb2` outstanding. The R-78 unit ships on `claude/charming-archimedes-ihwapd` (branched from the squash) as a **draft PR** (its number is in the session log's 2026-09-21 follow-up) that the OPERATOR merges — `src/` changed, wheel + nine installers rebuilt, v1.0.283, so EIGHT checks (CI's six + installer-smoke's `linux` / `windows`). Read that PR's FINAL head's EIGHT checks to conclusion FIRST; if it is merged, restart the branch on the squash (`git fetch --prune origin && git remote set-head origin -a && git checkout -B <branch> origin/main`) and compare `HEAD^{tree}` with the PR's final head's tree. Always `git fetch origin` and read `git log origin/main` before trusting any sha written here, including this one.**

**R-78 is CLOSED — ADR-0519.** Fuse's `Float Ratio™` is **TWO metrics under one name**: the `.aft` defines it under BOTH Bible forms (GUID `a536d1a4` `AVERAGE(TotalFloat/RemainingDuration)`; `8aec0857` / `697a1c84` / `dc1bd267` / `838991ea` / `9c556fbd` `AVERAGE(TotalFloat)/AVERAGE(RemainingDuration)`) and Fuse prints both under that tile name — the rev-5 `Hard_File_updated3` save reads **-10.94** in the AlltheProjects ribbon and **-8.01** in the 7/15 Analyst ribbon, the 24-hour file **N/A** and **-3.58**. Both terms are the whole-day FIELDS (`acumen_total_float_field` / `acumen_duration_field` on `activity_day_minutes`). The N/A is Excel's per-activity divide-by-zero propagating through `AVERAGE`, so it is the **mean-of-ratios form's alone**; the ratio-of-means keeps the zero-remaining activities in BOTH sums (the 24-hour file's four zeros: kept -3.58, dropped -9.58) and is the figure that survives. N/A is carried by `_na()` (population 0) — **not** by the status, which is already spent on "informational, no threshold". Both forms present through `round_half_up`: the `.aft` declares `FormulaFormat='{0:N}'` and the ribbon's only tie, `TP4_DataCenter_v1`'s exact 5/8, is WRITTEN 0.63. `duration_days_axis` retired; `engine/trend.py` / `web/trend.py` / `web/standards.py` / `static/trend.js` unchanged. Measured: 39 / 39 ribbon labels from Fuse's own cells (37 / 39 under half-to-even — the two misses ARE the tie), 19 / 19 mapped labels from the engine, 3 / 3 ratio-of-means tiles; 30 fixtures re-scored, 16 moved, N/A 1 → 10. Version 1.0.283; highest ADR 0519; SCHEMA 2.17.0 unchanged.

**FIRST:** read the R-78 PR's final head (EIGHT checks) to conclusion — and, if the operator has merged it, `main`'s own run for the squash by its JOBS — and record them in the session log as part of the next unit's docs — do NOT open a docs-only PR to record a merge or a run. Do NOT start a background suite that outlives the turn; CI's `test` jobs are the suite verdict, and any local run is read within the turn that started it. A docs-only edit made AFTER the local gate re-runs the doc guards (`tests/test_state_docs.py`, the standing-rules guard under ADR-0393 / ADR-0509, `tests/guards/test_audit_report_wp8.py`) before the push.

Environment (re-measured 2026-09-21): the clone arrives SHALLOW — `git fetch --unshallow origin` first (13 s this session), then `git fetch --prune origin && git remote set-head origin -a` (the prune deletes the harness's stale remote-tracking ref for a designated branch that was never pushed — expected); the package is NOT installed — `uv pip install --python /usr/local/bin/python3 --system -e '.[dev]' build playwright` (never `playwright install`); re-`uv pip install -e . --no-deps` after a version bump. A Bash call caps at 10 minutes: the full suite (~40 min) runs in the background to a log with a `PYTEST_EXIT` line and is read within the turn; `tests/engine` + `tests/test_projects` is 50 s, `tests/guards` 68 s, the duration-fields parity oracle ~75 s, `-m parity` ~5 min. Keep the token-guardian's `token_audit.py` in the SCRATCHPAD (`ruff check .` is whole-tree). `/root/.local/bin/ruff` 0.15.8 shadows CI's 0.16.8 — run both. **PYTHONPATH DOES shadow the `.pth` editable install** (verified this session with the `-p mutcheck` plugin: `MUTCHECK_ROOT` + `pytest_sessionstart` asserting the imported module IS the copy), so red-first and mutation runs against a scratch copy of `src/` work — take a pristine baseline (a copy of `src/` at the squash, md5 manifest) BEFORE the first `src/` edit. The xlsx reader lives in `tests/parity/test_fuse_duration_fields_oracle.py` (`_sheets` column-addressed, `_displayed`, `_grids`, `_ribbon` with an anchor label, `_fuse_pairs`) — anchor the AlltheProjects ribbon on `"Float Ratio™"`, which works for all three ribbon-bearing workbooks. `pkill -f <pattern>` matches its own shell — use `[p]attern`.

Run the session-token-guardian's `scripts/token_audit.py` as the FIRST action (copy it to the scratchpad) and before each operator prompt. Then, under QC-3, write the plan down and attack it before the first edit.

The next §3 unit is **R-79** (T1, S — the DCMA-09 tile's denominator is the baselined INCOMPLETE population: File2 322 / 904 → 0.36 (the engine's every-baselined 1,568 → 0.21), the 24-hour file 1 / 12 → 0.08 (84 → 0.01); the tile counts FIELDS (322) where the engine counts activities (173, ADR-0283's divergence); "9. Invalid Actual Dates" 4 / 0.01 fits neither (267 < n ≤ 800). Scope the parity denominator red-first on File2 and the 24-hour file, then read the tile's own detail grid for the field-vs-activity count and name the Actual Dates denominator from the started-or-complete population). Then R-09 · **R-74** (T2, S — free float above the total) · **R-77** (T2, M — the stored-date family and the rendering CONTIGUOUSLY; census the 212 / 25 / 4 and every rendered-time pin FIRST) · R-69 · **R-71** (T3) · R-13 · R-18 · R-21 · R-22 · R-32 · R-39; R-68 waits on the operator's reading (question (f)); the probe fixture's Fuse run is optional (ADR-0514's assumption 5).

⇢ Traps paid for, by name (2026-09-21 first): **the oracle a row says does not exist is in the OTHER workbook — twice running now** (sweep EVERY committed reference artefact for that tile before accepting an absence claim; an absence claim describes its author's sweep, not the corpus) · **one tile NAME can be two metrics and the `.aft` says so** (key a tile by (workbook, name); the same save read -10.94 and -8.01) · **a row's prescribed REMEDY is a claim too** (R-78's "move the None-test to the status" would have blanked the whole trend chart — the status is spent on "no threshold") · **a field that already means two things cannot carry a third** (`population == 0` is the only "no figure" signal every consumer reads) · **a refuted blanket sweep does not refute a single site** (ADR-0515 refused the `round_half_up` sweep *because no reference display was known*; `FormulaFormat='{0:N}'` plus the corpus's ONLY tie is a display) · **write the witness from the measurement, not from the prose** (the kickoff's single UID 389 was really 267 / 302 / 385 / 389; the pin failed, the code was right). (2026-09-20 (f), still live:) a ribbon's ratio block is a DENOMINATOR oracle · two grids with one label are two metrics · price a "site" by its CONSUMER's own figure before changing its divisor · match a workbook to its SAVE by measurement and print the DIRECTORY · the surviving mutant names the UNVERIFIED claim · a duration the file stores to the SECOND is the minute grid's residual. (2026-09-20 (e), still live:) an instrument that projects BOTH sides with the same ruler reads agreement by construction · the axis IS working minutes; only the PROJECTION of a mid-day instant is wrong · a segment-aware read is safe only as a FLOOR under `max()` · a tie is a supported date · `compute_cpm`'s local names are shared across its two paths. (2026-09-20 (d), still live:) a reader must NEVER filter the value it judges · Fuse's xlsx writer omits the cell reference on consecutive cells · the divisor Fuse uses is the TASK's own calendar · an elapsed activity's float field is RAW · a scratch module with top-level work re-runs on import. (Earlier, still live:) the reference tool's own REPORT is the rounding oracle · a "measured" example in a docstring is testimony · the scalar beside a record is the one to distrust · assert a patch per step · attack the plan before the first edit · measure both candidate rules on the population before choosing · key an oracle by PATH · a T2 row inserted after a T3 row fails the tier-order guard · a register row can be RIGHT about the arithmetic and WRONG about the mechanism · run the census before pricing the fix · negative pins are green on the pristine tree by construction; prove them with a mutant · MS Project's stored dates are a per-activity CPM oracle · MPXJ writes no zero. ⇢ Measured-false / deliberately held — do NOT re-chase: (ADR-0519:) the aggregate form as an UNVERIFIED analogy (it has three tiles' worth of oracle) · a status-keyed "no value" test in the trend · an N/A that keeps its candidate population · a pure-logic second Float Ratio mode (no independent standard exists for a Deltek/NASA metric) · the update2-vs-update3 `-5.59` tile and its `CP - Float Ratio™` twin `-11.9` (reproduce from no committed save under either form; named in the oracle) · the Analyst's `Avg Float` tile (-11 / -4 vs `AVERAGE(TotalFloat field)` -13.4038 / -2.8095 — a different metric) · a NEGATIVE rounding tie (none in the corpus; away-from-zero is convention, UNVERIFIED) · `CP - Float Ratio™` / `Near CP - Float Ratio™` as engine metrics · the rest of the `value_dp` family · (ADR-0518:) `duration_days_axis` as a divisor fix (retired outright by ADR-0519) · reading an absent remaining as zero for the Remaining Duration field · the "≥ 1 own day" population alternative · the pure-logic thresholds and the five project-day display sites · the AlltheProjects labels whose saves the repo does not hold (EVM2, Hard_File_updated2, TP4 v1–v5) as ENGINE oracles — they remain valid evidence for a RULE, from Fuse's own displayed cells · (ADR-0517:) the wall-computed finish projected contiguously · reading an absent remaining as zero in the CPM · a seconds-aware Resume · the late START of started work (R-71) · the four split bookings (R-60's tail) · the free-float cap (R-74) · (ADR-0516:) the crew's calendar or the execution calendar as the float divisor · rounding the elapsed activity's float field · (ADR-0515:) a `round_half_up` sweep of any display family (the two Float Ratio sites left it on their OWN measured display, which is the family-by-family reading ADR-0515 asked for) · reverting MF-08 · (ADR-0514:) `round_half_up` at the DCMA ties · truncation of the float field · a raw-slack filter · plus every earlier ADR's held items (see the previous kickoffs in git log). ⇢ Steward posture: draft PRs the OPERATOR merges (never mark ready, never merge, never approve); EIGHT checks when `installer/**` changes, SIX for docs-only; `main`'s own run for a squash is read from its JOBS; `pull_request_read get_status` returns pending / 0 on a fully green PR — use `get_check_runs`; the post-merge safety check is `HEAD^{tree}` vs `origin/main^{tree}`; after a squash-merge restart the branch with `git fetch --prune origin && git remote set-head origin -a && git checkout -B <branch> origin/main`, never amend or rebase the squash commit. Review cover is still absent — Codex quota EXHAUSTED (its bot said so on #702 and #704).

Work the POLARIS² audit's plan-forward (Schedule-Manipulation-Analysis-Tool). Read `docs/STATE/HANDOFF.md` FIRST (auto-injected), then `docs/STATE/AUDIT-2026-08-27-REPORT.md` §3 — the roadmap by testimony tier, pinned by `tests/guards/test_audit_report_wp8.py`. QC-1 / QC-2 / QC-3 bind every session (ADR-0393, ADR-0509). `git fetch origin` before you branch, number an ADR, or commit.
