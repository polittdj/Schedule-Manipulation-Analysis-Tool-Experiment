# Kickoff prompt — next session

**`main` @ `85f0c6ce` (#702, R-72 / ADR-0513, v1.0.278) — MERGED by the operator 2026-09-20 02:57:43Z, the squash TREE-IDENTICAL to PR #702's final head `044cf15a` (tree `8e0c283c…`); its OWN runs were read to conclusion by their JOBS this session: CI 1949 (`35485335370`): `cui-guard` 02:58:01Z · `browser` 03:13:07Z · `floor` 03:25:24Z · `test (3.11)` 03:30:09Z · `test (3.13)` 03:45:38Z · `check` 03:45:42Z — six of six; installer-smoke 787 (`35485335341`): `linux` 02:58:38Z · `windows` 03:02:57Z. Nothing about `85f0c6ce` is outstanding. Always `git fetch origin` and read `git log origin/main` before trusting any sha written here, including this one.**

**R-03 is CLOSED — ADR-0514, draft PR [#703](https://github.com/polittdj/Schedule-Manipulation-Analysis-Tool-Experiment/pull/703) on branch `claude/dazzling-knuth-68vcoq` (code + docs `2d44fa5b`, eight of eight green on it, then a docs-only follow-up; the OPERATOR merges; if it is merged when you arrive, `main` has moved past `85f0c6ce` — read ITS own six jobs AND its installer-smoke run to conclusion, since the diff touches `installer/**`; expect EIGHT checks on the PR; the `check` job is listed only once its `needs` complete; a red cell on a tree identical to the green PR head is the RUNNER's claim — compare tree hashes before believing it; do NOT open a docs-only PR to record the merge).** Acumen Fuse's Total Float FIELD is the stored slack rounded HALF-TO-EVEN to whole days — measured on 3,637 displayed activities of the Large Test File pair (196 exact half-day floats: 118 odd-part UP, 78 even-part DOWN), the "6. High Float" / "7. Negative Float" sets UID-exact on both snapshots (814 / 35, 660 / 112) — so dcma14's two parity roundings were the reference's own rule and `round_half_up` there would have been a parity defect; the rule is named `_common.acumen_whole_day_float` (dcma14 ×2 and the ribbon's Negative Float call it; R-04's sweep exempts them) and pinned by `tests/parity/test_fuse_total_float_field_oracle.py`; the probe fixture `NEGFLOAT_SubDay_Probe.xml` now carries the 44-day side (UIDs 109–113) — its Fuse run is the operator's OPTIONAL confirmation of the filter at the two ties (expected: 102 and 111 NOT counted). Version 1.0.279; highest ADR 0514; SCHEMA 2.17.0 unchanged.

**FIRST: read PR #703's EIGHT checks on its FINAL head to conclusion and record them in the session log (read the PR's commits, never the prose, for the final head — the docs-only follow-up after `2d44fa5b` restarted CI).** Baselines to attribute against: `2d44fa5b` read eight of eight green (CI `35486842926`, installer-smoke `35486842918`); PR #702's final head read 5,306 passed / 269 skipped / 0 failed on both `test` jobs; this unit adds 2 parity oracle pins (32 s), 1 synthetic tie pin, 4 guard pins and 5 probe activities. Do NOT start a background worktree suite: it died unread at 57 % and 47 % on the last two units (the container's idle gap between wakes kills it) — CI's `test` jobs are the suite verdict, and any local run is read within the turn that started it.

Environment (re-measured 2026-09-20): the clone arrives SHALLOW — `git fetch --unshallow origin` first (59 s), then `git fetch --prune origin && git remote set-head origin -a`; the package is NOT installed — `uv pip install --python /usr/local/bin/python3 --system -e '.[dev]' build playwright` (never `playwright install`); re-`uv pip install -e . --no-deps` after a version bump. Run the full sweep with `-v`, never `-q`; never `-o cache_dir=`; a `(…; echo) > log` wrapper reports exit 0 — read the log's `PYTEST_EXIT` line. Keep the token-guardian's `token_audit.py` in the SCRATCHPAD (`ruff check .` is whole-tree). `/root/.local/bin/ruff` 0.15.8 shadows CI's 0.16.8 — `uvx ruff@0.16.8` runs the other; run both. A `.pth` editable install lets `PYTHONPATH` shadow the package — the `-p mutcheck` plugin (`MUTCHECK_ROOT` + `pytest_sessionstart`, written into `<scratchpad>/plugins/mutcheck.py`) asserts the imported module IS the copy every time; the same trick runs the working tree's tests against a pristine worktree's `src/` for the red-first proof, and a SHADOW COPY of `src/` lacks `tools/mpxj` (ten importer-backed tests error there — an artefact of the copy; a git WORKTREE has it). Take the pristine baseline in a SEPARATE WORKTREE (`git worktree add <scratch> origin/main`); a background suite does NOT survive the session's idle gap between wakes (two units' worktree runs died unread at 57 % and 47 %) — CI's `test` jobs on the final head are the suite verdict; a local run must be read within the turn that started it. The 29 intake `.mpp` convert once with `java -cp tools/mpxj/classes:"tools/mpxj/lib/*" MpxjToMspdi <in> <out>` — key every census by PATH with a lossless encoding (`Large Test File.mpp` and `Large_Test_File.mpp` collide under space→underscore; count the files: 44 = 15 goldens + 29). A file-only census reads every stored date from the XML (the model carries no late dates); its offset arithmetic is a suspect at lunch hours and on EVM1 — the engine on a shadow copy is the instrument for the rule, and the census scripts (`dump.py` / `compare.py` / `variant.py`, the last one building a shadow engine per candidate from exact anchors) are worth rewriting in the scratchpad on day one. `pkill -f <pattern>` matches its own shell — use `[p]attern`.

Run the session-token-guardian's `scripts/token_audit.py` as the FIRST action (copy it to the scratchpad) and before each operator prompt. Then, under QC-3, write the plan down and attack it before the first edit.

The next §3 unit is **R-04** (T1, M — the 327 builtin `round()` sites outside `engine/metrics` by exposure: classify with the census script (a figure that reaches a page or an export vs internal arithmetic); render /sra and /jcl on a half-day fixture and read each displayed rounding against the reference tool's OWN rule for that figure — MEASURED per figure, never assumed to be `round_half_up` (ADR-0514: Fuse's Total Float is half-to-even; the three `acumen_whole_day_float` callers are exempt); fix family by family, red-first, parity unmoved). Then **R-73** (T1, M — the general `Resume + remaining` model for every started activity: 1,113 of 1,159 by the file; 4616 106 days early, 5505 43 early, 7378 three late, the 24Hour_Calendar file's 17 and its project finish 64 days late under the shipped rule; decide the stored instant's projection FIRST — ADR-0322's two-ruler rule refuses a second ruler, and the wide model's away movers were exactly the after-lunch Resumes: EVM1 18's 15:00, TP4 19's 15:00, 5505's 14:45:36) · R-09 · **R-74** (T2, S — free float above the total: MS Project's stored FreeSlack never exceeds its TotalSlack, 0 of 3,315; the engine's does on 1,870 incomplete activities, UID 408's 18,960 for 16,740) · R-69 · **R-71** (T3: the record's own late dates — LS = AS / LF = AF on finished work, LS = AS on started work, the 22 clamped LateFinishes; decide the Critical flag first; the start-type needs and the out-of-sequence late start are already taken) · R-13 · R-18 · R-21 · R-22 · R-32 · R-39; R-68 waits on the operator's reading (question (f)); the probe fixture's Fuse run is optional (ADR-0514's assumption 5).

⇢ Traps paid for, by name (2026-09-20 (b) first): read the intake's DISPLAYED values before asking the operator for a licensed-tool run — a rule visible on thousands of other activities decides a case the corpus never shows (196 half-day displays settled a tie no activity occupies) · grep `tests/fixtures` before building a fixture an audit row prescribes (the negative-side probe had existed since PR #576, unrun, its ask dropped) · Fuse writes a grid's Id column as a STRING — a numeric reader reads 0 rows and reports it confidently · say which pin has teeth on what: a set oracle stays green under a tie mutant when the population has no tie · the same session's earlier lesson still stands: a CPM change must run the counterfactual's consumers in `tests/web` (`grep -rl compute_change_effects tests/`), and a worktree suite without a `PYTEST_EXIT` line was never run. (2026-09-20, still live:) the trap a kickoff names can belong to the rule it rejected — re-measure it under the candidate before designing around it (1489) · a row's "floor" can be a CONSTRAINT: read the mechanism per case (4581's SNET) · the whole-task bound is the wrong bound for a portion (FF / SF needs on the remaining) · decompose "away" movers by mechanism before rejecting a rule · write every pin on BOTH paths — six battery survivors were six wall-path pins · derive from the calendar and verify against the file, never from the engine's answer · a count needs its mechanism · MS Project's stored FreeSlack ≤ TotalSlack always (R-74) · `pkill -f` matches its own shell. (2026-09-18 (e), still live:) a row's remedy can be half right — measure every candidate on the class beside the row · a file-only probe's arithmetic is a suspect — the engine on a shadow copy measures the rule · read every miss case by case · an "away" count needs a progress-class column · the battery's survivor is a missing pin — derive it by hand, then cut again · a control's expectation can be mis-derived · a kickoff's "final head" is stale one push later — read the PR's commits · a shadow copy of `src/` has no `tools/mpxj`. (Earlier, still live:) the oracle the row said did not exist was in the same folder under another name · the scalar beside a record is the one to distrust · assert a patch per step · attack the plan before the first edit · measure both candidate rules on the population before choosing · key an oracle by PATH · a T2 row inserted after a T3 row fails the tier-order guard · a register row can be RIGHT about the arithmetic and WRONG about the mechanism · run the census before pricing the fix · negative pins are green on the pristine tree by construction; prove them with a mutant · MS Project's stored dates are a per-activity CPM oracle · MPXJ writes no zero. ⇢ Measured-false / deliberately held — do NOT re-chase: (ADR-0514:) `round_half_up` at the DCMA ties (refuted on 196 half-day displays) · truncation of the float field (refuted 118 + 154.75 → 155) · a raw-slack filter (refuted by 5283's −0.29 d displayed 0 and absent) · the truncated Fuse grids as oracles · the generic "Negative Float" / "High Float 44d" library variants without the baseline filter · (ADR-0513:) the wide `Resume + remaining` model (R-73, registered with its blocker) · a segment-aware read of one stored instant (ADR-0322 refused it: one instant, two offsets) · the respanned start left at the logic start (a span that lies on 4581) · ADR-0476's pin + full span (60 away, reproduced) · the dropped-zero remaining as an inference (it reads 0 on 302 / 385 and the whole duration on EVM1's 17 and 267) · UID 3849's holiday actual start · the witness file as a golden · the free-float cap (R-74, registered) · (ADR-0512:) dropping a started successor · binding to a started successor's record or its resume alone · the unfloored remaining-work late start · a late-finish census that counts finished work · plus every earlier ADR's held items (see the previous kickoffs in git log). ⇢ Steward posture: draft PRs the OPERATOR merges (never mark ready, never merge, never approve); EIGHT checks when `installer/**` changes, SIX for docs-only; `main`'s own run for a squash is read from its JOBS; `pull_request_read get_status` returns pending / 0 on a fully green PR — use `get_check_runs`; the post-merge safety check is `HEAD^{tree}` vs `origin/main^{tree}`; after a squash-merge restart the branch with `git fetch --prune origin && git remote set-head origin -a && git checkout -B <branch> origin/main`, never amend or rebase the squash commit. Review cover is still absent — Codex quota EXHAUSTED (its bot said so again on #702).

Work the POLARIS² audit's plan-forward (Schedule-Manipulation-Analysis-Tool). Read `docs/STATE/HANDOFF.md` FIRST (auto-injected), then `docs/STATE/AUDIT-2026-08-27-REPORT.md` §3 — the roadmap by testimony tier, pinned by `tests/guards/test_audit_report_wp8.py`. QC-1 / QC-2 / QC-3 bind every session (ADR-0393, ADR-0509). `git fetch origin` before you branch, number an ADR, or commit.
