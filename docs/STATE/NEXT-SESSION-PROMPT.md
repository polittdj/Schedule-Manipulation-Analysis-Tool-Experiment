Kickoff prompt for the next session

Paste the block below verbatim to start the next session. (This file is a pointer, not a status snapshot — `docs/STATE/HANDOFF.md` is ALWAYS the authoritative "where we are"; if any number here disagrees with HANDOFF, HANDOFF wins. Refresh this file whenever the queue changes — a stale kickoff steers a fresh session at work that is already done.)

It once went EIGHT SLICES stale and handed a session work finished five slices earlier; only the auto-injected handoff caught it. It carries no drift guard, unlike `HANDOFF.md` — which is exactly why it rots. Refreshing it is part of session close, not an optional tidy. **It has been wrong twice in a row**: it claimed the data-date gap was "guarded by NO test" when a guard had existed since #536, and it named the data date as the mechanism when the mechanism was an ignored `ActualStart`. **Do not trust its diagnoses — re-derive from measurement.** (It also predicted ADR-0392 would be the gateway decision; an operator-reported defect took 0392 and the QC-1/QC-2 standing rules took 0393, so the gateway decision is **ADR-0394**. **Take the next free number after `git fetch origin`, never the one a doc predicted** — this file has now mispredicted an ADR number twice, and a session that trusted it collided with a parallel merge.)

---

Resume POLARIS (Schedule-Manipulation-Analysis-Tool). Read `docs/STATE/HANDOFF.md` FIRST (auto-injected). As of last close: **v1.0.200**, highest ADR **0393**, SCHEMA 2.11.0. The monolith's page-family queue is EMPTY. Recent sessions, oldest first: a **full-repo audit** (7 dimensions, adversarially verified — 57 findings, 37 confirmed, 20 narrowed, 0 refuted); an **operator-reported Ask-panel defect fix (ADR-0392)**; and **the QC-1/QC-2 standing working rules (ADR-0393)**. **Band 1 (001a/001b/001c) is still UNTOUCHED and first in the queue.**

⇢ **BEFORE ANYTHING: `CLAUDE.md` now carries two binding working rules — QC-1 and QC-2 (ADR-0393), pinned by `tests/test_standing_rules.py`.** Read them in full. QC-1: before any change, conclusion or document update, build an executable pass/fail check, run it in a sandbox, and use it to try to REFUTE the claim — before the result is reported (red before green; prove the check has teeth by mutation; say UNVERIFIED rather than assert silently). QC-2: read everything, skip nothing, assume nothing, verify everything — and if an error is found, QC-1 applies before the correction.

⇢ **WHAT THE ASK-PANEL SESSION SHIPPED — read `docs/adr/0392-*.md` if you touch the Ask panel or the fact base.** The operator loaded 31 `.mpp` versions and the AI answered that it could see "only two file versions, not 31". Measured: the workbook fact base held 23 facts, **exactly ONE** naming more than one version, and **zero** carrying any per-version series — so the model was describing its evidence accurately. Fixed by making the population a pinned FACT (`ai/version_facts.py`) over a new per-version series (`engine/version_series.py`), plus: the 500-char question cap is gone and `GET /export/{fmt}/ask` exports the answer + cited facts. **Do not "optimize" `compute_version_series` into reading `compute_s_curve`** — it recomputes on purpose (the animated curve's 60-month axis cap blinds early versions), and `test_matches_the_s_curve_at_every_on_axis_status_month` keeps the two paths agreeing.

⇢ **READ THIS BEFORE ANYTHING ELSE: `docs/PLAN/APPROVED-GATEWAY-INTEGRATION.md`**

The operator now runs POLARIS against a **NASA-approved AI gateway** (`https://proxy.fast.luna.nasa.gov`, model `claude-opus-4.8-thinking-itar`) **on their own Windows machine**. A separate assistant session patched `ai/backend.py` / `web/app.py` / `web/settings.py` and added a hostname allowlist entry **in that local install**.

**None of it is in this repository** — zero hits for `POLARIS_GATEWAY` / `luna.nasa.gov` / `opus-4.8` in `src/`, `tests/`, `tools/`, `installer/`. **Do NOT read that absence as evidence the gateway is unused. It is in use.** The repo simply has no record of it, and that is the thing to fix.

⇢ BAND 1 — do these first, in this order (`docs/PLAN/DEFINITION-OF-DONE-V2.md`; item 001 is now CLOSED by ADR-0391)

1. **001a — pin the allowlist contents.** `net_guard.py:127` `_LOOPBACK_HOSTNAMES = {"localhost","ip6-localhost"}` is the single point every Law-1 check funnels through (`is_loopback_host:223` → `is_local_http_endpoint:237`, consumed at `ai/ollama.py:130`, `ai/openai_compat.py:39`, `web/app.py:6406/6408/6447`, `launcher.py:232`, `web/app.py:8018`). **NO test pins its contents.** An agent EXECUTED the widening in memory: adding the gateway hostname → **226 tests passed**, and **854** across a wider selection. The suites pin only sampled negatives, so a *named* entry is invisible. Assert the frozensets exactly, add a property test that any non-loopback name is rejected, and MUTATION-PROVE it fails. **Land this alone, first — it closes the gap whether or not a gateway is ever built.**
2. **001b — make the banner OBSERVED, not config-derived.** `route_backend` returns the literal `Local-only — no data leaves this machine.` for BOTH the ollama and openai paths (`ai/backend.py:129-131`, `:144-147`) with no classification check and no endpoint inspection; `banner_for` (`:99-108`) describes CONFIG, and `chrome.py:175` renders that, so a DOWN gateway still displays as up. `route_backend`'s own Banner is dead code, and `AIBackend.is_local` is a hardcoded constant nothing reads. Six user-visible claims plus four i18n translations are affected — **two print inside EXPORTED exhibits** (`ai/brief.py:625`, `web/sra.py:1076`). Prove the new banner can go red by routing a non-local fake.
3. **001c — OPERATOR DECISION, then ADR-0394.** `ai/cloud.py` does not exist and no caller supplies `cloud_backend`, yet `settings.py:449` still offers "Cloud (UNCLASSIFIED only)". So the only working route to an approved gateway is **widening the loopback validator** — the architecture channels a lawful need into the most dangerous change. Either delete the dead option, or build a first-class `ai/gateway.py` with `is_local=False`, its OWN named allowlist (never a widening of the loopback set), a mandatory non-local banner, a classification gate, and an AI transaction log. **Good news: `openai_compat.py` is std-lib `urllib` and already speaks the OpenAI wire format, so this needs NO new dependency** — the egress guard stays intact.

⇢ CONFIRMED AUDIT FINDINGS WORTH DOING NEXT

* **`actual_start_driven` is computed but consumed NOWHERE in `src/`.** ADR-0391 promised it as the disclosure surface; the engine now moves dates on every progressed schedule with zero analyst-facing notice. Wire it as an INFO finding (evidence, not a concern) or a column beside "Date-driven".
* **ADR-0391's own-calendar (`exec_cal`) floor branch is behaviorally unguarded** — deleting it entirely leaves the engine suite AND the parity gate green. Add an assertion-bearing test (Large_Test_File floors 391 tasks; or a synthetic own-calendar progressed task).
* **`mpxj_ref()` still has no shallow-clone guard** and the test that looks like it guards the pin only checks 40-hex shape (DoD 117). It FIRED last session — pinned `79865bc` instead of `42d92dc`. Add: refuse if `git rev-parse --is-shallow-repository`, and refuse if the SHA is not an ancestor of `origin/main`.
* **The pre-commit CUI guard has NO image detector** (`.githooks/pre-commit:36`) and 120 PNGs are tracked. Rendered screenshots of a real IMS would not be caught. Real Law-1 hole.
* **22 playwright modules hard-pin a chromium BUILD NUMBER** in a module-level skipif; only one module was fixed and CI's browser job runs only that one.
* **FINAL-REPORT overclaims** on both parity and "No data off-machine" — its cited controls (build-time dep hygiene, construction-time endpoint validation, served-asset air-gap) structurally cannot prove a running instance's AI transport stays on loopback.
* 8 stale remote branches, all 148-611 commits behind (DoD 091).

⇢ SCOPING CORRECTION THE ADVERSARIAL PASS FORCED — get this right

"The product docs are false" is **wrong**; "the docs are false of a PATCHED install" is right. The shipped build's validators hold, so as shipped the no-egress claims are TRUE. They are unconditional where they should be conditional, guarding a promise nothing pins. Do not weaken the docs to accommodate the gateway, and do not silently "fix" them — condition them on the observed backend.

⇢ TWO PREMISES REVERSED BY MEASUREMENT — do NOT "fix" these

* SSI Mean/StdDev: SSI's headline cells are not reproducible from SSI's OWN exported distribution. Ours sits 3 days / 3.4% from SSI's own data; P80 EXACT. Sampling noise, not a defect.
* SRA sensitivity VALUES match SSI exactly (same order, same UIDs). Only the LABELS are wrong (the tornado prints the host task's name — SRA R-2, still open).

⇢ ALSO STILL OPEN (engine)

Completed-task actual **FINISH** anchoring (the named remaining half of ADR-0391 — cause of the accepted Hard_File_updated/updated2 drift) · EVM2's sub-day / segmented-calendar divergence (DoD 050; all six divergent activities are 0% complete with NO actuals, diverging at UID 23 `PT12H` on a lunch-break calendar — **why MSP spans a 12-hour duration across three days is still unexplained**) · TP3's −5d Fuse gap · SRA R-1/R-2/R-3→R-4→R-6.

⇢ TRAPS PAID FOR — check BY NAME

A GUARD IS ONLY AS STRONG AS THE TEST THAT PINS ITS DATA (sampled negatives prove nothing about a named allowlist entry — cost: the whole Law-1 guarantee). AN ARCHITECTURE THAT OFFERS NO LEGITIMATE PATH TO A REAL NEED WILL GET THE DANGEROUS PATH TAKEN. A CLAIM DERIVED FROM CONFIG DESCRIBES INTENT, NOT BEHAVIOUR. VERIFY THE SCOPE OF A DOC-TRUTH FINDING. A FIXTURE GENERATED BY A RULE CANNOT VALIDATE THAT RULE. THE CORROBORATING ORACLE MAY ALREADY BE IN A DOC NOTHING CROSS-REFERENCES. AN ADR'S OBSERVATION CAN BE RIGHT AND ITS DIAGNOSIS WRONG. A NEW DISCLOSURE NEEDS ITS OWN CHANNEL WHEN THE EXISTING ONE CARRIES A JUDGEMENT. A SWEEP'S GLOB/POPULATION/PATTERN ARE PART OF ITS CLAIM. `| head -N` CAN SIGPIPE-KILL A BUILD MID-WAY. THE MPXJ PIN DRIFTS IN A SHALLOW CLONE (fired). Plus the standing set (instrument shown to FAIL first · never MEASURE a tree a battery is mutating · never MUTATE an instrument a measurement is using · monkeypatch repoint is per CALL SITE · `grep -c` exits 1 on zero · two ruffs on PATH, use `python -m ruff` · bare `pytest` does not prepend CWD · `git fetch origin` before taking an ADR number and again before committing).

⇢ TIMING / ENVIRONMENT — MEASURED

The container starts with **NO deps installed**: `python -m pip install -e ".[dev]"` and `pip install build` before anything runs. Full local suite **~30 min** (3636 passed / 45 skipped at last green). **`pytest -m parity` alone exceeds 900 s** — give it room or it dies to your own `timeout`. CI ~60 min end-to-end; `check` is a gate job and its absence early is sequencing, not failure; `cancel-in-progress: true`, so never push while you need a run's signal. After a squash-merge, restart the branch: `git fetch --prune origin && git remote set-head origin -a && git checkout -B <branch> origin/main` — **never** amend GitHub's squash commit.

⇢ Standing rules (binding)

**QC-1 — PROVE OR REFUTE IT BEFORE YOU REPORT IT.** Before any change, any conclusion, or any document update: build an executable pass/fail check, run it in a sandbox, and use it to try to REFUTE the claim — before the result is reported. Red before green (the check must be OBSERVED TO FAIL); executable beats inspectional; never mutate the instrument; prove the check has teeth by MUTATION; if a check is genuinely impossible say UNVERIFIED in the deliverable rather than assert it silently. **QC-2 — READ EVERYTHING, VERIFY EVERYTHING.** Skip nothing, assume nothing — docs, code, comments, tests, config, instructions, prior sessions' claims, and CLAUDE.md itself. If an error is found, QC-1 applies BEFORE the correction. Both are ADR-0392, binding, NO EXCEPTIONS, pinned by `tests/test_standing_rules.py`. Read them in full in CLAUDE.md before you start.

Law 1 CUI · Law 2 fidelity ("—" never 0; never weaken a test) · READ EVERYTHING, ASSUME NOTHING, VERIFY EVERYTHING · ADR-0240 model protocol (Ultracode for the overall audit; one lead re-verifies every finding against code evidence before it is reported) · full gate before every commit · handoff rotation + SESSION-LOG + LESSONS-LEARNED + THIS FILE in the same commit · wheel + nine installers ONCE per shipped-code change · a number written mid-session is not a measurement (`wc` decides). Skills: `full-gate`, `prove-able-to-fail`, `metric-parity`, `ui-change`, `cui-guard`, `render-verify`, `session-close`.
