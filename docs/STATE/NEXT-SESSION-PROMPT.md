# Kickoff prompt for the next session

Paste the block below verbatim to start the next session. (This file is a pointer, not a status snapshot — `docs/STATE/HANDOFF.md` is ALWAYS the authoritative "where we are"; if any number here disagrees with HANDOFF, HANDOFF wins. Refresh this file whenever the queue changes — a stale kickoff steers a fresh session at work that is already done.)

It once went EIGHT SLICES stale and handed a session work finished five slices earlier; only the auto-injected handoff caught it. It carries no drift guard, unlike `HANDOFF.md` — which is exactly why it rots. Refreshing it is part of session close, not an optional tidy. It has been wrong FOUR times: it claimed the data-date gap was "guarded by NO test" when a guard had existed since #536; it named the data date as the mechanism when the mechanism was an ignored `ActualStart`; its "mpxj_ref has no shallow-clone guard — add refuse-if-not-ancestor" was half right — the guard was missing but ancestry is the WRONG check (ADR-0397 used tree-identity); and it said SEC-01's "behavioural half" was wholly missing when `test_sec_hardening.py` already behaviourally covered both consumers — the real holes were narrower (ADR-0400 re-measured before building). Do not trust its diagnoses — re-derive from measurement. (It ALSO mispredicted the ADR number three times — two same-day sessions once both took 0395. Take the next free number after `git fetch origin`, never the one a doc predicted — and fetch AGAIN before committing.)

Resume POLARIS (Schedule-Manipulation-Analysis-Tool). Read `docs/STATE/HANDOFF.md` FIRST (auto-injected). As of last close: v1.0.205 (unchanged by the 0406 unit — tests+docs only), highest ADR 0406, SCHEMA 2.11.0. ADR-0406 closed TEST-01 (22 chromium build pins → sorted-glob resolvers; the audit xfail flipped — tests/audit is xfail-FREE, the census canary-proved and permanent; JCL-BR-01 is the ONE strict xfail repo-wide) and landed OR-10's ledger in OPERATOR-REQUESTS.md (the V-1/V-2/V-3 pending-operator-verification table + blocked-on-operator + live agent queue). ADR-0405 closed DOC-01 (FINAL-REPORT's locality claim is now CONDITIONAL exactly as _observed_banner states it, parity evidence tempered, the M15 DoD contradiction resolved — its pinning test repointed) and ADR-0401's JCL docs follow-ups (the eac gloss transcribes engine/jcl.py incl. the (1-tau) split + the clean-EVM precondition; METRIC-DICTIONARY regenerated). ADR-0404 made ALL AI settings persist across launches (ai/config_store.py — DPAPI-wrapped key on Windows, load-boundary sanitizers incl. the allowlist, conftest SF_SETTINGS_DIR/SF_AI_LOG_DIR isolation; 6/6 mutations by name): the operator arms the gateway ONCE and every desktop-icon launch comes up armed. 001c is CLOSED (ADR-0402): the approved AI gateway is a first-class backend (allowlist, consent gate, `ai/txlog.py` transaction log, settings option; 15/15 mutations by name). ADR-0403 added Bearer authentication after a field-reported HTTP 401 from the real NASA machine (`AIConfig.gateway_api_key` repr=False, header-only, blank-means-keep, `SF_GATEWAY_API_KEY` fallback; 8/8 mutations by name). THE REPOSITORY IS PUBLIC AGAIN (operator flip, 2026-08-14, to restore free Actions CI after the private-repo minute allowance was exhausted — see the DISC-01 block below; the DISC-01 private-visibility mitigation is thereby REVERSED). The container clone still arrives SHALLOW — per-path git history still lies until you unshallow. Recent sessions, oldest first: the read-only ten-role audit (ADR-0395: xfails ENCODE live defects — drive an xfail green when you close its finding); DoD 001b observed sovereignty banner (ADR-0396); DoD 117 MPXJ shallow-clone pin guard (ADR-0397); the private-repo installer fetch (ADR-0398); HOOK-01 disguise/container detectors (ADR-0399); three verification-gap closures — Fuse transcription oracle, own-calendar floor guard, Host-allowlist closure (ADR-0400); and the first `engine/jcl.py` mutation battery + `tests/engine/test_jcl_joint_statement_closure.py` (21 tests, 44/44 distinct mutants killed by name), the JCL-BR-01 strict xfail, and the SMAT-SANDBOX wipe (ADR-0401). `tests/audit` is xfail-FREE (TEST-01 closed, ADR-0406); the ONE strict xfail repo-wide is JCL-BR-01 in `tests/web/test_jcl_web.py`.
⇢ BEFORE ANYTHING: `CLAUDE.md` carries two binding working rules — QC-1 and QC-2 (ADR-0393), pinned by `tests/test_standing_rules.py`. Read them in full. QC-1: before any change, conclusion or document update, build an executable pass/fail check, run it in a sandbox, and use it to try to REFUTE the claim — before the result is reported (red before green; prove the check has teeth by mutation; say UNVERIFIED rather than assert silently). QC-2: read everything, skip nothing, assume nothing, verify everything — and if an error is found, QC-1 applies before the correction.
⇢ DISC-01 — RELEASABILITY, still gating and NOW MORE URGENT (audit 2026-08-13; REQUIRES AUTHORIZING OFFICIAL) The gateway hostname and ITAR-tagged model identifier are in git HISTORY since `a19b969` (and in `docs/PLAN/APPROVED-GATEWAY-INTEGRATION.md`, older STATE docs, ADR-0394). The 2026-08-13 private flip MITIGATED the exposure; the operator's 2026-08-14 public flip (to restore free Actions CI) REVERSED that mitigation — the history is publicly reachable again while the release determination is still pending. History rewrite (filter-repo) vs accept-as-is remains an authorizing-official call, not an engineering commit. The 001c build (ADR-0402, operator-directed 2026-08-14) added the ONE sanctioned, load-bearing copy of the gateway hostname — `net_guard.APPROVED_GATEWAY_ENDPOINTS` plus its pin tests and the air-gap text exemption (no NEW disclosure class; the literal was already public in the plan doc and ADR-0394's pin test). Do not add copies beyond that pinned set (this file deliberately contains none; guard tests otherwise use fictional placeholders — `.example`/`.invalid` hosts).
⇢ SMAT-SANDBOX IS AN EMPTY GREENFIELD AND OUT OF SCOPE (operator directive 2026-08-14, mid-session): all 12 branch names point at one parentless empty commit (`91b3395`); NO backup of this repository exists there; the kickoff's earlier backup objective was explicitly ABANDONED. Do not push to, clone from, or otherwise operate on SMAT-SANDBOX. The residual branch NAMES (and the old closed July PRs) are operator UI cleanup — the session git proxy 403-blocks ref-deletion pushes (measured, ADR-0401).
⇢ WHAT'S DONE — do not re-open

* 001a (ADR-0394) · 001b (ADR-0396) · 117 (ADR-0397) · private-repo fetch (ADR-0398) · HOOK-01 (ADR-0399) — see the archive for details; all battery-proven by name.
* PO-03 (ADR-0400): the Fuse transcription is machine-guarded; the audit xfail flipped and its marker is removed. exec_cal floor + SEC-01 Host closure (ADR-0400) likewise.
* JCL battery (ADR-0401): `engine/jcl.py`'s semantic surface is battery-guarded — 45 specs / 44 distinct across three rounds (16 lead + 14 workflow + 3 teeth + 12 adversarial), every one killed by name (`sampling` passthrough is guarded by `test_lhs.py`, measured). The closure module is `tests/engine/test_jcl_joint_statement_closure.py` (21 tests). Do NOT weaken its fixtures: the quadrant fixture's multipliers-on decoupling, explicit seeds, boundary margins, and liveness self-checks are all load-bearing (ADR-0401 records why each exists).
* 001c (ADR-0402): the approved gateway is BUILT and battery-proven (15/15 by name, including the layer-pin lesson). Growing the allowlist is a three-place conscious act: `net_guard` + `tests/guards/test_gateway_allowlist.py` + `tests/web/test_airgap.py`'s text exemption. Deliberately NOT done (do not "fix"): gateway as second model; i18n keys for endpoint-interpolated warnings; env-var endpoint seeding. (The 0402-era "no config persistence" clause was SUPERSEDED by ADR-0404 at operator direction — settings now persist by design.)

⇢ NEXT — in order

1. Operator verifies v1.0.205 (or later) on the NASA machine: arm once (endpoint + acknowledgment + key + model), then a plain double-click launch must come up armed with the catalog populated (still 401 WITH the key → capture the AI Hub's documented auth scheme, then a follow-on ADR implements it on evidence).
2. DISC-01 release determination — operator / authorizing official. Nothing below blocks on it now.
3. PO-04/05 — CEI/bow-wave and HMI: BLOCKED ON A MISSING PRIMARY ORACLE (operator-delivered vendor export; a fixture generated by the rule under test cannot validate the rule).
4. `actual_start_driven` computed but consumed NOWHERE in `src/` (ENG-DEAD-01) — wire as an INFO finding or a column beside "Date-driven" (ADR-0391's promised disclosure). SHIPPED-code change: version bump + wheel + nine installers in the same unit (ADR-0148 lockstep).
5. JCL-BR-01 (ADR-0401) — session branches feed `/api/sra/ssi` but `compute_jcl` accepts NO branch/conditional inputs; with a branch configured the JCL finish marginal silently leaves the SSI S-curve (measured through the real routes). The strict xfail in `tests/web/test_jcl_web.py` flips loudly when fixed — carry the branches through `compute_jcl` and EXTEND the equivalence pins to branch configurations, or honest-gate the panel while branches exist. SHIPPED-code change (ADR-0148 lockstep). Design decision first; conditionals share the gap.
6. 8 stale remote branches, 148–611 commits behind (DoD 091); SMAT-SANDBOX branch-name/repo cleanup (operator UI). Operator also verifies the gateway option on the NASA-connected machine — the unversioned local patch is obsolete; reinstall from a fresh v1.0.202 tier installer.

⇢ TWO PREMISES REVERSED BY MEASUREMENT — do NOT "fix" these

* SSI Mean/StdDev: ours sits 3 days / 3.4% from SSI's own exported distribution; P80 EXACT. Sampling noise, not a defect.
* SRA sensitivity VALUES match SSI exactly; only the LABELS are wrong (tornado prints the host task's name — SRA R-2, still open).

⇢ TRAPS PAID FOR — check BY NAME THE REPO IS PRIVATE AND THE CLONE IS SHALLOW — anonymous fetches 404; per-path git history lies in a shallow clone (break the loop with an independent oracle: tree hashes, the remote commits API). THE SESSION GIT PROXY BLOCKS REF-DELETION PUSHES — a zero-object deletion 403s while additive pushes of ANY size pass; a whole-pack push that carries `--prune` fails ON THE DELETES, and the size-cap diagnosis was WRONG (ADR-0401 — a diagnosis that reversed once is not done reversing; name the measurement). `| tail` MASKS EXIT CODES — the ref comparator's RED was the honest instrument. A CONSISTENCY ORACLE NEEDS A FIXTURE PROVEN TO POPULATE ITS DISCRIMINATING CELLS — the JCL late-but-cheap quadrant was STRUCTURALLY empty with multipliers off; decouple, MEASURE the populations, pin the seed the liveness rides on. A SURVIVOR OF A NARROW SUITE IS A HYPOTHESIS — widen the suite before declaring a blind spot (L13 died in `test_lhs`). SPENT=0.0 MAKES ANY FLOAT RE-ASSOCIATION EXACT — a bit-identity contract is only testable where the bits can differ. A TRANSCRIPTION ORACLE MUST BE PROVEN TO READ THE SOURCE SIDE (ADR-0400 B3). AN ENGINE BRANCH CAN MOVE A MEMBER SIX YEARS WHILE EVERY AGGREGATE PIN STAYS BYTE-IDENTICAL — pin the member, run an ISOLATION control. A POPULATION ROW MUST BE TRANSPORTABLE (punycode, not raw Unicode). THE SPREADSHEETGEAR TRAP — vendor workbooks omit `r=` coords. A CI FAILURE ON AN UNCHANGED CODE PATH IS A QUESTION ABOUT THE ENVIRONMENT FIRST. THE ADR-NUMBER COLLISION IS REAL — fetch before numbering AND before committing. WHEN ONE CLAIM SPANS MANY SURFACES, ROUTE THEM THROUGH ONE DERIVATION. A DATA PIN GUARDS THE LITERAL, NOT THE GUARANTEE — pin the constant AND sweep the behaviour. WHEN A BATTERY MUTATES A SANDBOX, PROVE THE SANDBOX IS THE TREE BEING MEASURED (import-origin canary; md5 the instruments). A PREDICTED MUTATION OUTCOME IS NOT A MEASURED ONE — write the flip list the battery printed. MUTATION-GREEN IS NOT ADVERSARIALLY VERIFIED — adversaries probe BETWEEN the mutations (ADR-0399's five defect classes; ADR-0401's twelve attack survivors). A GUARD'S INPUT PLUMBING IS ATTACK SURFACE. MONKEYPATCH REPOINT IS PER CALL SITE, NOT PER NAME. AN ARCHITECTURE THAT OFFERS NO LEGITIMATE PATH TO A REAL NEED WILL GET THE DANGEROUS PATH TAKEN (001c — closed by building the path, ADR-0402). A DEFENSE-IN-DEPTH TWIN CAN HIDE A LAYER'S MUTATION FROM AN OUTCOME ASSERTION — pin the LAYER (ADR-0402's constructor bomb). `pkill -f` SELF-MATCHES ON PLAIN STRINGS ELSEWHERE IN YOUR OWN COMMAND LINE even with the bracket trick — kill by recorded PID. BROWSER DRIVERS GO IN AN ISOLATED VENV or ~19 CI-invisible playwright tests start executing in your gate. A CLAIM DERIVED FROM CONFIG DESCRIBES INTENT, NOT BEHAVIOUR. A DOCUMENT THAT FLAGS A DISCLOSURE MUST NOT REPEAT THE DISCLOSED LITERAL. `xfail(strict=True)` IS HOW A VALIDATED-BUT-UNFIXED FINDING LANDS — a fix flips it loudly; remove the marker in the fixing commit (GW-02, HOOK-01, PO-03, JCL-BR-01 all follow the design). A SWEEP'S GLOB/POPULATION/PATTERN ARE PART OF ITS CLAIM. `| head -N` CAN SIGPIPE-KILL A BUILD MID-WAY. Plus the standing set (instrument shown to FAIL first · never MEASURE a tree a battery is mutating · never MUTATE an instrument a measurement is using · `grep -c` exits 1 on zero · two ruffs on PATH, use `python -m ruff` · bare `pytest` does not prepend CWD).
⇢ TIMING / ENVIRONMENT — MEASURED The container starts with NO deps installed: `python -m pip install -e ".[dev]"` and `pip install build` before anything runs. Full local suite ~30 min. `pytest -m parity` alone exceeds 900 s — give it room or it dies to your own `timeout`. CI ~60 min end-to-end; `check` is a gate job and its absence early is sequencing, not failure; `cancel-in-progress: true`, so never push while you need a run's signal. Installer-smoke is path-filtered to `installer/**`, `tools/installer/**`, and its own workflow file. After a squash-merge, restart the branch: `git fetch --prune origin && git remote set-head origin -a && git checkout -B <branch> origin/main` — never amend GitHub's squash commit.
⇢ Standing rules (binding) QC-1 — PROVE OR REFUTE IT BEFORE YOU REPORT IT. QC-2 — READ EVERYTHING, VERIFY EVERYTHING. Both are ADR-0393, binding, NO EXCEPTIONS, pinned by `tests/test_standing_rules.py`. Read them in full in CLAUDE.md before you start. Law 1 CUI · Law 2 fidelity ("—" never 0; never weaken a test) · ADR-0240 model protocol (Ultracode for the overall audit; one lead re-verifies every finding against code evidence before it is reported) · full gate before every commit · handoff rotation + SESSION-LOG + LESSONS-LEARNED + THIS FILE in the same commit · wheel + nine installers ONCE per shipped-code change · a number written mid-session is not a measurement (`wc` decides). Skills: `full-gate`, `prove-able-to-fail`, `metric-parity`, `ui-change`, `cui-guard`, `render-verify`, `session-close`.
