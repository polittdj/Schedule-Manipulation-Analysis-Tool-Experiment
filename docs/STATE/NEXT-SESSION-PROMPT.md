Kickoff prompt for the next session

Paste the block below verbatim to start the next session. (This file is a pointer, not a status snapshot — `docs/STATE/HANDOFF.md` is ALWAYS the authoritative "where we are"; if any number here disagrees with HANDOFF, HANDOFF wins. Refresh this file whenever the queue changes — a stale kickoff steers a fresh session at work that is already done.)

It once went EIGHT SLICES stale and handed a session work finished five slices earlier; only the auto-injected handoff caught it. It carries no drift guard, unlike `HANDOFF.md` — which is exactly why it rots. Refreshing it is part of session close, not an optional tidy. **It has been wrong three times**: it claimed the data-date gap was "guarded by NO test" when a guard had existed since #536; it named the data date as the mechanism when the mechanism was an ignored `ActualStart`; and its "mpxj_ref has no shallow-clone guard — add refuse-if-not-ancestor" was half right — the guard was missing, but ancestry is the WRONG check (unverifiable in exactly the clones that lie; ADR-0396 used tree-identity instead). **Do not trust its diagnoses — re-derive from measurement.** (It has also mispredicted the ADR number twice. **Take the next free number after `git fetch origin`, never the one a doc predicted.**)

---

Resume POLARIS (Schedule-Manipulation-Analysis-Tool). Read `docs/STATE/HANDOFF.md` FIRST (auto-injected). As of last close: **v1.0.201, highest ADR 0396**, SCHEMA 2.11.0. The monolith's page-family queue is EMPTY. Recent sessions, oldest first: the QC-1/QC-2 standing rules (ADR-0393); DoD 001a — loopback allowlist pinned (ADR-0394); DoD 001b — the sovereignty banner is OBSERVED, not config-derived (ADR-0395) **plus** DoD 117 — the MPXJ shallow-clone pin guard (ADR-0396), which fired mid-session on its own build. Band 1's remaining item is **001c, and it is the OPERATOR'S decision** — an agent session without that decision should take the post-Band-1 queue below.

⇢ BEFORE ANYTHING: `CLAUDE.md` carries two binding working rules — QC-1 and QC-2 (ADR-0393), pinned by `tests/test_standing_rules.py`. Read them in full. QC-1: before any change, conclusion or document update, build an executable pass/fail check, run it in a sandbox, and use it to try to REFUTE the claim — before the result is reported (red before green; prove the check has teeth by mutation; say UNVERIFIED rather than assert silently). QC-2: read everything, skip nothing, assume nothing, verify everything — and if an error is found, QC-1 applies before the correction.

⇢ WHAT'S DONE — do not re-open

* 001a (ADR-0394): `net_guard`'s two frozensets pinned, data + behaviour, 9/9 mutations by name.
* 001b (ADR-0395): every sovereignty claim rides ONE observed derivation. `is_local` is an INSTANCE measurement (validator's verdict on the actual endpoint); `banner_for_backend` is the core (missing/falsy `is_local` = presumed NON-local); `route_backend`'s Banner derives from the chosen backend (dead code no more); `banner_for` constructs candidates via the NEW `ai/factory.py`; `chrome._observed_banner` adds the routed-cache veto; consumers = banner, CUI drawer, hero, takeaway, settings tip, brief + SRA exports (`brief_blocks(…, *, ai_is_local)` is a REQUIRED kw). 18 guard tests (`tests/guards/test_observed_banner.py`), 15/15 mutations caught by name, re-run against the final tree. i18n key + four translations untouched and pinned.
* 117 (ADR-0396): `mpxj_ref()` refuses shallow-graft-boundary resolutions; `SF_MPXJ_REF` override accepted only with verified tree-identity; installer test `test_the_converter_pin_is_a_real_touch_not_a_shallow_graft_artifact` covers the built artifacts. v1.0.201 installers pin `42d92dc` (the true last touch per the GitHub API — the committed pin had been RIGHT all along; nothing in the wild was broken).

⇢ NEXT — in order

1. **001c — OPERATOR DECISION, then its ADR** (next free number after `git fetch origin`). `ai/cloud.py` does not exist and no caller supplies `cloud_backend`, yet `settings.py` still offers "Cloud (UNCLASSIFIED only)". Either delete the dead option, or build a first-class `ai/gateway.py` with `is_local=False`, its OWN named allowlist (never a widening of the loopback set), a classification gate, and an AI transaction log (`APPROVED-GATEWAY-INTEGRATION.md` §6 steps 4–6). ADR-0395 made the honest path mechanical: a backend declaring `is_local=False` gets warning banners + export disclosures on every surface with zero further plumbing. `openai_compat.py` is std-lib `urllib` and already speaks the wire format — no new dependency.
2. **Pin `web/app.py` `_ALLOWED_HOSTS`** — the DNS-rebinding Host-header allowlist, the last unpinned security frozenset. Measured (ADR-0394 session): widened with the gateway hostname, `test_sec_hardening.py` + `test_launcher.py` = 23 passed, 0 failed. Same recipe as 001a: data pin + behavioural sweep + mutation proof.
3. `actual_start_driven` is computed but consumed NOWHERE in `src/` — ADR-0391 promised it as the disclosure surface; wire it as an INFO finding or a column beside "Date-driven".
4. ADR-0391's own-calendar (`exec_cal`) floor branch is behaviorally unguarded — deleting it leaves the engine suite AND parity green. Add an assertion-bearing test.
5. The pre-commit CUI guard has NO image detector and 120 PNGs are tracked — rendered screenshots of a real IMS would not be caught. Real Law-1 hole.
6. 22 playwright modules hard-pin a chromium BUILD NUMBER in a module-level skipif; CI's browser job runs only the one fixed module.
7. FINAL-REPORT overclaims on parity and "No data off-machine" — condition on the observed backend (ADR-0395's `_observed_banner` is the derivation to cite), do not weaken.
8. 8 stale remote branches, 148–611 commits behind (DoD 091).

⇢ TWO PREMISES REVERSED BY MEASUREMENT — do NOT "fix" these

* SSI Mean/StdDev: ours sits 3 days / 3.4% from SSI's own exported distribution; P80 EXACT. Sampling noise, not a defect.
* SRA sensitivity VALUES match SSI exactly; only the LABELS are wrong (tornado prints the host task's name — SRA R-2, still open).

⇢ TRAPS PAID FOR — check BY NAME
IN A SHALLOW CLONE, `git log -1 -- <path>`, `--stat` AND `merge-base` CAN ALL AGREE ON A FALSEHOOD — the graft boundary owns every path; break the loop with an independent oracle (tree hashes, the remote commits API); ADR-0396's guard now refuses the boundary and verifies TREE-IDENTITY, not ancestry (fired TWICE; the second time on the v1.0.201 build itself). A DIAGNOSIS THAT REVERSED ONCE IS NOT DONE REVERSING — record what the LAST measurement proved and name the measurement. WHEN ONE CLAIM SPANS MANY SURFACES, ROUTE THEM THROUGH ONE DERIVATION — independently-derived surfaces WILL disagree and nobody notices (the router said local while the page said cloud). A DATA PIN GUARDS THE LITERAL, NOT THE GUARANTEE — pin the constant AND sweep the behaviour (ADR-0394). WHEN A BATTERY MUTATES A SANDBOX, PROVE THE SANDBOX IS THE TREE BEING MEASURED (canary that must go red; md5 the instruments). MONKEYPATCH REPOINT IS PER CALL SITE, NOT PER NAME — construction moved to `ai/factory.py` and `test_second_backend_caches_…` had to move its patch WITH the call site (verified live that the patched constructor is reached). AN ARCHITECTURE THAT OFFERS NO LEGITIMATE PATH TO A REAL NEED WILL GET THE DANGEROUS PATH TAKEN (001c exists to close this). A CLAIM DERIVED FROM CONFIG DESCRIBES INTENT, NOT BEHAVIOUR (001b closed the banner instance of this; FINAL-REPORT's instance is open). A FIXTURE GENERATED BY A RULE CANNOT VALIDATE THAT RULE. AN ADR'S OBSERVATION CAN BE RIGHT AND ITS DIAGNOSIS WRONG. A SWEEP'S GLOB/POPULATION/PATTERN ARE PART OF ITS CLAIM. `| head -N` CAN SIGPIPE-KILL A BUILD MID-WAY. Plus the standing set (instrument shown to FAIL first · never MEASURE a tree a battery is mutating · never MUTATE an instrument a measurement is using · `grep -c` exits 1 on zero · two ruffs on PATH, use `python -m ruff` · bare `pytest` does not prepend CWD · `git fetch origin` before taking an ADR number and again before committing).

⇢ TIMING / ENVIRONMENT — MEASURED
The container starts with NO deps installed: `python -m pip install -e ".[dev]"` and `pip install build` before anything runs. Full local suite ~30 min. `pytest -m parity` alone exceeds 900 s — give it room or it dies to your own `timeout`. CI ~60 min end-to-end; `check` is a gate job and its absence early is sequencing, not failure; `cancel-in-progress: true`, so never push while you need a run's signal. The container clone is SHALLOW — see the graft-boundary trap above before trusting any per-path git history. After a squash-merge, restart the branch: `git fetch --prune origin && git remote set-head origin -a && git checkout -B <branch> origin/main` — never amend GitHub's squash commit.

⇢ Standing rules (binding)
QC-1 — PROVE OR REFUTE IT BEFORE YOU REPORT IT. QC-2 — READ EVERYTHING, VERIFY EVERYTHING. Both are ADR-0393, binding, NO EXCEPTIONS, pinned by `tests/test_standing_rules.py`. Read them in full in CLAUDE.md before you start.
Law 1 CUI · Law 2 fidelity ("—" never 0; never weaken a test) · ADR-0240 model protocol (Ultracode for the overall audit; one lead re-verifies every finding against code evidence before it is reported) · full gate before every commit · handoff rotation + SESSION-LOG + LESSONS-LEARNED + THIS FILE in the same commit · wheel + nine installers ONCE per shipped-code change · a number written mid-session is not a measurement (`wc` decides). Skills: `full-gate`, `prove-able-to-fail`, `metric-parity`, `ui-change`, `cui-guard`, `render-verify`, `session-close`.
