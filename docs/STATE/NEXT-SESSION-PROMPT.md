# Kickoff prompt — next session (handed over 2026-09-20 (d), after the R-75 unit's push)

**`main` @ `9d5541af` (#704, R-04 / ADR-0515, v1.0.279 — docs + tests only) — MERGED by the operator 2026-09-20 08:24:02Z, the squash TREE-IDENTICAL to PR #704's final head `bee00008` (tree `c6b4fbad…`, re-verified with `git rev-parse <sha>^{tree}`). `main`'s OWN run for `9d5541af` — CI 1956 (`35499451753`) — read by its JOBS: `cui-guard` 08:24:19Z · `browser` 08:42:22Z · `floor` 08:53:47Z · `test (3.13)` 09:11:46Z · `test (3.11)` 09:20:00Z · `check` 09:20:11Z — SIX OF SIX GREEN, nothing about `9d5541af` outstanding. The R-75 unit ships on `claude/dreamy-euler-vjd862` (started on the squash) as **draft PR [#705](https://github.com/polittdj/Schedule-Manipulation-Analysis-Tool-Experiment/pull/705)** (code + docs `b534a9dd`, then a docs-only follow-up) that the OPERATOR merges: `src/` changed, wheel + nine installers rebuilt, v1.0.280, so EIGHT checks (CI's six + installer-smoke's `linux` / `windows`); the full suite at `b534a9dd` read 5,718 passed / 7 skipped / 0 failed. Read PR #705's FINAL head's EIGHT checks to conclusion FIRST; if it is merged, restart the branch on the squash (`git fetch --prune origin && git remote set-head origin -a && git checkout -B <branch> origin/main`) and compare `HEAD^{tree}` with the PR's final head's tree. Always `git fetch origin` and read `git log origin/main` before trusting any sha written here, including this one.**

**R-75 is CLOSED — ADR-0516.** Acumen Fuse's Total Float field divides the stored slack by the ACTIVITY's own day: 1440 for an elapsed duration (and then RAW — the 24-hour Hard_File displays UID 146 at −13.333333333333334 where UID 14, on a 24 Hours task calendar with the same −19,200 minutes, reads −13), else the task's own calendar's working minutes per day, else the project's; never the crew's calendar, never the engine's execution calendar (ADR-0474 / ADR-0503). Measured on 771 displayed floats over five Hard_File snapshots, each workbook matched to its save by measurement; the alternatives refuted by name; the 24-hour ribbon's High Float 0 / 0 and Negative Float 40 / 11 UID-exact from `compute_dcma14(acumen_parity=True)` (the engine had read High Float 2). `_common.activity_day_minutes` + `acumen_total_float_field`; the three parity callers read the field; the Forensic oracle's reader corrected (810 / 855 / 1,514 rows). Version 1.0.280; highest ADR 0516; SCHEMA 2.17.0 unchanged.

**FIRST:** read PR #705's final head (EIGHT checks) to conclusion — and, if the operator has merged it, `main`'s own run for the squash by its JOBS — and record them in the session log as part of the next unit's docs — do NOT open a docs-only PR to record a merge or a run. Do NOT start a background worktree suite that outlives the turn; CI's `test` jobs are the suite verdict, and any local run is read within the turn that started it. A docs-only edit made AFTER the local gate re-runs the doc guards (`tests/test_state_docs.py`, the standing-rules guard under ADR-0393 / ADR-0509, `tests/guards/test_audit_report_wp8.py`) before the push.

Environment (re-measured 2026-09-20 (d)): the clone arrives SHALLOW — `git fetch --unshallow origin` first (54 s), then `git fetch --prune origin && git remote set-head origin -a` (the prune deletes the harness's stale remote-tracking ref for a designated branch that was never pushed — expected); the package is NOT installed — `uv pip install --python /usr/local/bin/python3 --system -e '.[dev]' build playwright` (5 s; never `playwright install`); re-`uv pip install -e . --no-deps` after a version bump. Run the full sweep with `-v`, never `-q`; never `-o cache_dir=`; a `(…; echo) > log` wrapper reports exit 0 — read the log's `PYTEST_EXIT` line. Keep the token-guardian's `token_audit.py` in the SCRATCHPAD (`ruff check .` is whole-tree). `/root/.local/bin/ruff` 0.15.8 shadows CI's 0.16.8 — `uvx ruff@0.16.8` runs the other; run both. A `.pth` editable install lets `PYTHONPATH` shadow the package — the `-p mutcheck` plugin (`MUTCHECK_ROOT` + `pytest_sessionstart`, written into `<scratchpad>/plugins/mutcheck.py`) asserts the imported module IS the copy every time; a SHADOW COPY of `src/` lacks `tools/mpxj` (importer-backed tests error there — an artefact of the copy; a git WORKTREE has it). Take a pristine baseline by running the new tests BEFORE the first `src/` edit (this unit's value-reds came from an inline probe using only `compute_dcma14`; the modules that import a new helper are red at import, which proves nothing about the number). The 29 intake `.mpp` convert once with `java -cp tools/mpxj/classes:"tools/mpxj/lib/*" MpxjToMspdi <in> <out>` (3 minutes) — key every census by PATH with a manifest (`Large Test File.mpp` and `Large_Test_File.mpp` collide under space→underscore; count the files: 44 = 15 goldens + 29). A scratch module with top-level work re-runs it on every import — guard it with `if __name__ == "__main__"` before a second script imports it. `pkill -f <pattern>` matches its own shell — use `[p]attern`.

Run the session-token-guardian's `scripts/token_audit.py` as the FIRST action (copy it to the scratchpad) and before each operator prompt. Then, under QC-3, write the plan down and attack it before the first edit.

The next §3 unit is **R-73** (T1, M — the general `Resume + remaining` model for every started activity: 1,113 of 1,159 by the file; 4616 106 days early, 5505 43 early, 7378 three late, the 24Hour_Calendar file's 17 and its project finish 64 days late under the shipped rule; decide the stored instant's projection FIRST — ADR-0322's two-ruler rule refuses a second ruler, and the wide model's away movers were exactly the after-lunch Resumes: EVM1 18's 15:00, TP4 19's 15:00, 5505's 14:45:36). Then **R-76** (T1, S — Fuse's Original / Remaining Duration fields divide by the task's own day too: the 7/15 grids show UID 14's 1,440 minutes on "24 Hours" as 1 (not 3), the 24-hour file's 302 / 385 / 389 as 2 / 3 / 1 (not 5 / 9 / 3), an ELAPSED original duration on the PROJECT day (146: 2,880 minutes → 6) but its remaining on 1440 (→ 2), and two remaining rows (315 / 129: 247 / 242 minutes shown 0) a different mechanism; decide DCMA-08's population rule FIRST — baseline or remaining, the `.aft` filter and the Detailed Metric Reports' "8. High Duration" marks — then the 282 duration cells of the 7/15 grids under the own-day rule, then the DCMA-08 parity site and `duration_days_axis`, red-first on the four 24-hour rows; two corpus crossings of 44 days (`24Hour Calendar.mpp` 17: 100 → 33, `Jacked Up Schedule 1.mpp` 20's remaining 96 → 32) have no Fuse oracle) · R-09 · **R-74** (T2, S — free float above the total: MS Project's stored FreeSlack never exceeds its TotalSlack, 0 of 3,315; the engine's does on 1,870 incomplete activities, UID 408's 18,960 for 16,740) · R-69 · **R-71** (T3: the record's own late dates — LS = AS / LF = AF on finished work, LS = AS on started work, the 22 clamped LateFinishes; decide the Critical flag first) · R-13 · R-18 · R-21 · R-22 · R-32 · R-39; R-68 waits on the operator's reading (question (f)); the probe fixture's Fuse run is optional (ADR-0514's assumption 5).

⇢ Traps paid for, by name (2026-09-20 (d) first): a reader must NEVER filter the value it judges — an integer filter dropped the one non-integer cell in 771 and read "140 of 140" for the very case the row asked about; pin the population and keep the emitter's value · Fuse's xlsx writer omits the cell reference on consecutive cells and writes it only where it SKIPPED a column (an empty ratio at a zero 'before'; every date-sheet row) — place a cell at the column its `r` names, never merely append; ADR-0515's reader had slid 70 Hard_File rows out of its population while green · match a workbook to its SAVE by measurement (two `updated3` goldens; 141 / 141 vs 129 / 141) · two figures with one name are two metrics (`schedule_quality.negative_float` 49 / 15 vs the DCMA tile's 40 / 11 — no baseline filter) — pin "unmoved", not "equal" · the divisor Fuse uses is the TASK's own calendar, not the calendar the engine schedules the work on (ADR-0474 / ADR-0503 stay right for dates and wrong for this field) · an elapsed activity's field is RAW and the corpus cannot show its tie — implement as displayed, say UNVERIFIED · measure the neighbouring column while the grid is open (the duration fields, R-76) · a scratch module with top-level work re-runs on import. (2026-09-20 (c), still live:) the reference tool's own REPORT is the rounding oracle · a "measured" example in a docstring is testimony · a row the rule does not reproduce is a mechanism question first · the model's minute grid manufactures ties the file does not have (`PT107H59M36S` → 13.5) · derive a tie inside the test · Fuse writes a grid's Id column as a STRING · say which pin has teeth on what · a CPM change must run the counterfactual's consumers in `tests/web` · a worktree suite without a `PYTEST_EXIT` line was never run. (2026-09-20, still live:) the trap a kickoff names can belong to the rule it rejected · a row's "floor" can be a CONSTRAINT · the whole-task bound is the wrong bound for a portion · decompose "away" movers by mechanism · write every pin on BOTH paths · derive from the calendar and verify against the file · a count needs its mechanism · MS Project's stored FreeSlack ≤ TotalSlack always (R-74) · `pkill -f` matches its own shell. (Earlier, still live:) the oracle the row said did not exist was in the same folder under another name · the scalar beside a record is the one to distrust · assert a patch per step · attack the plan before the first edit · measure both candidate rules on the population before choosing · key an oracle by PATH · a T2 row inserted after a T3 row fails the tier-order guard · a register row can be RIGHT about the arithmetic and WRONG about the mechanism · run the census before pricing the fix · negative pins are green on the pristine tree by construction; prove them with a mutant · MS Project's stored dates are a per-activity CPM oracle · MPXJ writes no zero. ⇢ Measured-false / deliberately held — do NOT re-chase: (ADR-0516:) the crew's calendar or the execution calendar as the float divisor (refuted by name on 14 / 94 / 13 / 389) · rounding the elapsed activity's field (the reference prints −13.333…) · moving the five project-day display sites (the brief ×2, driving ×2, recommendations ×1) or the pure-logic DCMA thresholds to the activity's day (the tool's own reading on the declared day; MS Project's display convention UNVERIFIED — a view export of UID 14's Total Slack settles it) · re-reading the LTF field oracle's document-order reader (full-width grids, 3,637 rows match by value) · (ADR-0515:) a `round_half_up` sweep of any display family · reverting MF-08 · a 2-dp precision change toward MS Project's display · reading `round(x, 2)` as the reference's `round(x * 100) / 100` · (ADR-0514:) `round_half_up` at the DCMA ties · truncation of the float field · a raw-slack filter · the truncated Fuse grids as oracles · (ADR-0513:) the wide `Resume + remaining` model (R-73, registered with its blocker) · a segment-aware read of one stored instant · the respanned start left at the logic start · ADR-0476's pin + full span · the dropped-zero remaining as an inference · UID 3849's holiday actual start · the witness file as a golden · the free-float cap (R-74) · (ADR-0512:) dropping a started successor · binding to a started successor's record or its resume alone · the unfloored remaining-work late start · a late-finish census that counts finished work · plus every earlier ADR's held items (see the previous kickoffs in git log). ⇢ Steward posture: draft PRs the OPERATOR merges (never mark ready, never merge, never approve); EIGHT checks when `installer/**` changes, SIX for docs-only; `main`'s own run for a squash is read from its JOBS; `pull_request_read get_status` returns pending / 0 on a fully green PR — use `get_check_runs`; the post-merge safety check is `HEAD^{tree}` vs `origin/main^{tree}`; after a squash-merge restart the branch with `git fetch --prune origin && git remote set-head origin -a && git checkout -B <branch> origin/main`, never amend or rebase the squash commit. Review cover is still absent — Codex quota EXHAUSTED (its bot said so again on #702 and #704).

Work the POLARIS² audit's plan-forward (Schedule-Manipulation-Analysis-Tool). Read `docs/STATE/HANDOFF.md` FIRST (auto-injected), then `docs/STATE/AUDIT-2026-08-27-REPORT.md` §3 — the roadmap by testimony tier, pinned by `tests/guards/test_audit_report_wp8.py`. QC-1 / QC-2 / QC-3 bind every session (ADR-0393, ADR-0509). `git fetch origin` before you branch, number an ADR, or commit.
