# Kickoff prompt — next session

**`main` @ `4d931ba7` (#700, R-45 / ADR-0511, v1.0.276) — its OWN runs were read this session: CI 1938 (`35383096831`): `cui-guard` 18:56:51Z · `browser` 19:12:33Z · `floor` 19:25:42Z · `test (3.11)` 19:29:59Z green, `test (3.13)` and `check` still running at 19:40Z (the docs-only follow-up records what the session saw last — if it does not say six of six, read them); installer-smoke 776 (`35383096874`): `linux` 18:57:16Z · `windows` 19:01:22Z. Always `git fetch origin` and read `git log origin/main` before trusting any sha written here, including this one.**

**R-70 is CLOSED — ADR-0512, draft PR [#701](https://github.com/polittdj/Schedule-Manipulation-Analysis-Tool-Experiment/pull/701) on branch `claude/fervent-ramanujan-ttxvxk` (the OPERATOR merges; if it is merged when you arrive, `main` has moved past `4d931ba7` — read ITS own six jobs AND its installer-smoke run to conclusion, since the diff touches `installer/**`; expect EIGHT checks on the PR; the `check` job is listed only once its `needs` complete; a red cell on a tree identical to the green PR head is the RUNNER's claim — compare tree hashes before believing it; do NOT open a docs-only PR to record the merge).** The backward pass now stops at finished work: a recorded-complete successor presents no late need and anchors no free float (Hard_File_updated3's UID 188 reads the project finish, its FreeSlack == TotalSlack, as stored); a started successor presents its REMAINING portion — its late finish less its remaining, never earlier than its early finish less the same remaining (the logic-reestablished 188 is stored at 187's Resume); FF / SF keep the late finish; an absent remaining reads the percent-derived remainder. Version 1.0.277; highest ADR 0512; SCHEMA 2.17.0 unchanged.

**FIRST: read PR #701's EIGHT checks on its FINAL head to conclusion and record them in the session log (code commit `8e828bdb`; docs `56445980`; the two pins CI's floor job named re-derived in `fe2ae49d`; the docs-only follow-up after it — read the PR's commits, never the prose, for the final head: two sessions running, the kickoff's "final head" was one push stale).** Baselines to attribute against: the previous unit's worktree run read 4 failed (attributable) / 5,653 passed / 7 skipped; this unit adds 13 synthetic pins + 1 oracle pin and re-derives 4 pins.

Environment (re-measured 2026-09-18 (e)): the clone arrives SHALLOW — `git fetch --unshallow origin` first (55 s), then `git fetch --prune origin && git remote set-head origin -a`; the package is NOT installed — `uv pip install --python /usr/local/bin/python3 --system -e '.[dev]' build playwright` (never `playwright install`); re-`uv pip install -e . --no-deps` after a version bump. Run the full sweep with `-v`, never `-q`; never `-o cache_dir=`; a `(…; echo) > log` wrapper reports exit 0 — read the log's `PYTEST_EXIT` line. Keep the token-guardian's `token_audit.py` in the SCRATCHPAD (`ruff check .` is whole-tree). `/root/.local/bin/ruff` 0.15.8 shadows CI's 0.16.8 — `uvx ruff@0.16.8` runs the other; run both. A `.pth` editable install lets `PYTHONPATH` shadow the package — the `-p mutcheck` plugin (`MUTCHECK_ROOT` + `pytest_sessionstart`, written into `<scratchpad>/plugins/mutcheck.py`) asserts the imported module IS the copy every time; the same trick runs the working tree's tests against a pristine worktree's `src/` for the red-first proof, and a SHADOW COPY of `src/` lacks `tools/mpxj` (ten importer-backed tests error there — an artefact of the copy). Take the pristine baseline in a SEPARATE WORKTREE (`git worktree add <scratch> origin/main`); run the full suite in a worktree at the CODE commit while the docs are written. The 29 intake `.mpp` convert once with `java -cp tools/mpxj/classes:"tools/mpxj/lib/*" MpxjToMspdi <in> <out>` (25 distinct basenames; key every census by PATH). A file-only census reads every stored late date from the XML (`LateStart` / `LateFinish` / `FreeSlack` / `Stop` / `Resume`, the model carries none); its offset arithmetic is a suspect at lunch hours and on a golden whose calendar and stored times disagree (EVM1) — the engine on a shadow copy is the instrument for the rule.

Run the session-token-guardian's `scripts/token_audit.py` as the FIRST action (copy it to the scratchpad) and before each operator prompt. Then, under QC-3, write the plan down and attack it before the first edit.

The next §3 unit is **R-72** (T1, M — a started activity whose start the ADR-0391 floor moved past its actual start is re-spanned for its FULL duration from the floored start, where MS Project spans the REMAINING work from Resume: the logic-reestablished 187 (60 %, 48 h left, started 08-05 out of sequence) is floored to 188's finish 08-17 17:00 and finishes 08-27 08:00 for the stored 08-20 17:00; under R-70 its remaining portion's scheduled start, the need 188 reads, is four days late, and the chain 94 … 188 reads +4 days where the file stores 0 (the pre-R-70 engine read −8; Critical agreement 109 → 102 on that file). Census in ADR-0512: 192 of 1,159 started activities floored past their actual start; 25 finish more than a working day after the stored Finish, 7 of them floored (one on each of the six Large Test File copies, and 187). Candidate rule: a floored started activity spans its REMAINING from the later of the floor and Resume — ADR-0309's floor becomes the span once the floor moved the start. Attack first: measure the candidate against ADR-0476's three rejected variants on the WHOLE corpus (UID 1489's 136-day swing is the trap), the started activities' stored Finish as the oracle (1,094 of 1,159 already within a day), and the 18 unfloored late finishers as the class beside the row). Then R-03 · R-04 · R-09 · R-69 · **R-71** (T3: the record's own late dates — LS = AS / LF = AF on finished work, LS = AS on started work, the 22 clamped LateFinishes; decide the Critical flag first) · R-13 · R-18 · R-21 · R-22 · R-32 · R-39; R-68 waits on the operator's reading (question (f)).

⇢ Traps paid for, by name (2026-09-18 (e) first): a row's remedy can be half right — measure every candidate on the class beside the row, and read the links that do NOT bind (the 22 SS links chose the rule) · a file-only probe's arithmetic is a suspect (60-minute misses were lunch hours; EVM1's calendar disagrees with its stored times) — the engine on a shadow copy measures the rule · read every miss case by case (five misses were one activity's clamped date) · an "away" count needs a progress-class column (6,176 away were records the engine never modelled) · the battery's survivor was a missing pin for the third unit running — derive the pin by hand, then cut again · a control's expectation can be mis-derived: read the pristine value before trusting a "green before and after" · a kickoff's "final head" is stale one push later — read the PR's commits · a shadow copy of `src/` has no `tools/mpxj` (errors, not failures). (2026-09-18 (d), still live:) the oracle the row said did not exist was in the same folder under another name · the scalar beside a record is the one to distrust · assert a patch per step · a paired-column sheet keys on the LAST column · build fixtures per call. (2026-09-18 (c):) attack the plan before the first edit · a survivor is a missing pin as often as a code hole · measure both candidate rules on the population before choosing · key an oracle by PATH · read the count, not the && chain · a T2 row inserted after a T3 row fails the tier-order guard (R-72 went into the T1 block, R-71 after R-69). (Earlier, still live:) a register row can be RIGHT about the arithmetic and WRONG about the mechanism · run the census before pricing the fix · proving a patch inert requires proving it FIRED · negative pins are green on the pristine tree by construction; prove them with a mutant · MS Project's stored dates are a per-activity CPM oracle · MPXJ writes no zero · Large_Test_File.mpp ≠ Large Test File.mpp. ⇢ Measured-false / deliberately held — do NOT re-chase: (ADR-0512:) dropping a started successor (138 / 222) · binding to a started successor's record (193) or its resume alone (195) · the unfloored remaining-work late start (215; misses the witness) · a started successor's free-float anchor at its remaining portion (equal on the corpus; EVM1 says the record) · a late-finish census that counts finished work (records, never the engine's) · the importer's dropped-zero `RemainingDuration` (named) · a free float floored at zero (named) · the witness file as a golden (named) · (ADR-0511:) the booking's scalar as the earned share · actual work over baseline · pricing the record at the block's date · "ACWP to time now" · plus every earlier ADR's held items (see the previous kickoffs in git log). ⇢ Steward posture: draft PRs the OPERATOR merges (never mark ready, never merge, never approve); EIGHT checks when `installer/**` changes, SIX for docs-only; `main`'s own run for a squash is read from its JOBS; `pull_request_read get_status` returns pending / 0 on a fully green PR — use `get_check_runs`; the post-merge safety check is `HEAD^{tree}` vs `origin/main^{tree}`; after a squash-merge restart the branch with `git fetch --prune origin && git remote set-head origin -a && git checkout -B <branch> origin/main`, never amend or rebase the squash commit. Review cover is still absent — Codex quota EXHAUSTED.

Work the POLARIS² audit's plan-forward (Schedule-Manipulation-Analysis-Tool). Read `docs/STATE/HANDOFF.md` FIRST (auto-injected), then `docs/STATE/AUDIT-2026-08-27-REPORT.md` §3 — the roadmap by testimony tier, pinned by `tests/guards/test_audit_report_wp8.py`. QC-1 / QC-2 / QC-3 bind every session (ADR-0393, ADR-0509). `git fetch origin` before you branch, number an ADR, or commit.
