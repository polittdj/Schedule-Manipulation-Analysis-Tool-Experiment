Kickoff prompt for the next session

Paste the block below verbatim to start the next session. (This file is a pointer, not a status snapshot — `docs/STATE/HANDOFF.md` is ALWAYS the authoritative "where we are"; if any number here disagrees with HANDOFF, HANDOFF wins. Refresh this file whenever the queue changes — a stale kickoff steers a fresh session at work that is already done.)

It once went EIGHT SLICES stale and handed a session work finished five slices earlier; only the auto-injected handoff caught it. It carries no drift guard, unlike `HANDOFF.md` — which is exactly why it rots. Refreshing it is part of session close, not an optional tidy. **It has been wrong three times**: it claimed the data-date gap was "guarded by NO test" when a guard had existed since #536; it named the data date as the mechanism when the mechanism was an ignored `ActualStart`; and its "mpxj_ref has no shallow-clone guard — add refuse-if-not-ancestor" was half right — the guard was missing, but ancestry is the WRONG check (unverifiable in exactly the clones that lie; ADR-0397 used tree-identity instead). **Do not trust its diagnoses — re-derive from measurement.** (It ALSO mispredicted the ADR number three times — the third prediction collided for real: two same-day sessions both took 0395, and the later one renumbered at merge. **Take the next free number after `git fetch origin`, never the one a doc predicted — and fetch AGAIN before committing.**)

---

Resume POLARIS (Schedule-Manipulation-Analysis-Tool). Read `docs/STATE/HANDOFF.md` FIRST (auto-injected). As of last close: **v1.0.201, highest ADR 0399**, SCHEMA 2.11.0. The monolith's page-family queue is EMPTY. **THE REPOSITORY IS PRIVATE NOW** (DISC-01 remediation) — anonymous raw.githubusercontent fetches 404, the container clone is SHALLOW, and any workflow that assumed a public repo must be re-derived, not assumed. Recent sessions, oldest first: QC-1/QC-2 standing rules (ADR-0393); DoD 001a — loopback allowlist pinned (ADR-0394); a read-only ten-role audit landed as docs + `tests/audit/test_audit_findings.py` (ADR-0395: xfails ENCODE live defects — drive an xfail green when you close its finding); DoD 001b — the sovereignty banner is OBSERVED, not config-derived (ADR-0396, flipped GW-02 green); DoD 117 — the MPXJ shallow-clone pin guard (ADR-0397); the private-repo installer fetch (ADR-0398); and HOOK-01 — the CUI pre-commit guard learns disguise suffixes, anchored renames, and containers (ADR-0399, flipped the HOOK-01 xfail green; `tests/audit` now has 2 live xfails: TEST-01, PO-03).

⇢ BEFORE ANYTHING: `CLAUDE.md` carries two binding working rules — QC-1 and QC-2 (ADR-0393), pinned by `tests/test_standing_rules.py`. Read them in full. QC-1: before any change, conclusion or document update, build an executable pass/fail check, run it in a sandbox, and use it to try to REFUTE the claim — before the result is reported (red before green; prove the check has teeth by mutation; say UNVERIFIED rather than assert silently). QC-2: read everything, skip nothing, assume nothing, verify everything — and if an error is found, QC-1 applies before the correction.

⇢ **DISC-01 — RELEASABILITY, still gating (audit 2026-08-13; REQUIRES AUTHORIZING OFFICIAL)**
The gateway hostname and ITAR-tagged model identifier are in git HISTORY since `a19b969` (and in `docs/PLAN/APPROVED-GATEWAY-INTEGRATION.md`, older STATE docs, ADR-0394). Making the repo private MITIGATES but does not DECIDE releasability — history rewrite (filter-repo) vs accept-as-is is an authorizing-official call, not an engineering commit. **Do not add new copies of those strings** (this file deliberately contains none; new files on the 001b branch use fictional placeholders). Treat 001c gateway wiring as design-gated until the determination lands.

⇢ WHAT'S DONE — do not re-open

* 001a (ADR-0394): `net_guard`'s two frozensets pinned, data + behaviour, 9/9 mutations by name.
* 001b (ADR-0396): every sovereignty claim rides ONE observed derivation — instance-measured `is_local`, `banner_for_backend` (missing/falsy = presumed NON-local), `route_backend` derives from the chosen backend, `banner_for` constructs candidates via `ai/factory.py`, `chrome._observed_banner` adds the routed-cache veto; consumers = banner, CUI drawer, hero, takeaway, settings tip, brief + SRA exports (`brief_blocks(…, *, ai_is_local)` REQUIRED kw). 18 guard tests, 15/15 mutations by name. GW-02 in `tests/audit` is a plain passing test.
* 117 (ADR-0397): `mpxj_ref()` refuses shallow-graft-boundary resolutions; `SF_MPXJ_REF` override only with verified tree-identity. v1.0.201 installers pin `42d92dc`.
* Private-repo fetch (ADR-0398): installers download MPXJ via the GitHub contents API when `SF_GITHUB_TOKEN`/`GITHUB_TOKEN` is set (byte-identity proven), anonymous raw URL otherwise; `installer-smoke.yml` passes `github.token`.
* HOOK-01 (ADR-0399): the pre-commit guard's three detectors — CLOSED disguise-suffix chain (`data.mpp.png` blocks by name; zero tracked files claimed, measured), anchored serialization-start text rules for `.md`/image/`.pdf`/`.zip` renames (unanchored would block a tracked doc TODAY — measured), and OLE2/ZIP-member/Power-BI/PDF-attachment container checks in an embedded python3 batch with a LOUD bash floor fallback. The adversarial fan-out then found and the same unit FIXED five defect classes, including the PRE-EXISTING C-quoting fail-open (`schädule.mpp` bypassed every detector silently; now `-z` + `read -d ''`), trailing-whitespace names, single-quoted MSPDI xmlns, and two false positives (templated `.md`, PDF page-text conflation). 42-case battery red-then-green; whole-tree census with canary + population-equality proof; 8/8 mutations by name AGAINST THE FINAL HOOK; the audit xfail flipped and its marker is removed. `.jar`/`.whl` deliberately NOT sniffed (members would wedge rebuilds); the sniff set is CLOSED like the suffix set (`.dat`/`.html` renames are a documented limit, not drift). THE HOOK IS ITSELF AN EXTENSION-LESS SNIFFED FILE — never write a signature literal in its comments (census-enforced).
* SEC-01: `_ALLOWED_HOSTS` DATA pin landed inside `tests/audit/test_audit_findings.py`. Still open from the 001a recipe: the behavioural sweep + mutation proof.

⇢ NEXT — in order

1. **DISC-01 release determination** — operator / authorizing official. Blocks nothing below except gateway wiring.
2. **001c — OPERATOR DECISION, then its ADR** (next free number after `git fetch origin`). Delete the dead `cloud` option, or build a first-class `ai/gateway.py` with `is_local=False`, its OWN named allowlist (never a widening of the loopback set), a classification gate, and an AI transaction log (`APPROVED-GATEWAY-INTEGRATION.md` §6 steps 4–6). ADR-0396 made the honest path mechanical: a backend declaring `is_local=False` gets warning banners + export disclosures on every surface with zero further plumbing. `openai_compat.py` is std-lib `urllib` and already speaks the wire format — no new dependency.
3. **PO-03/04/05 — parity-oracle independence**: the Fuse transcription (`fuse_exports_2026-06.json`) is not machine-guarded against the source `.xlsx` (PO-03 xfail); CEI/bow-wave and HMI have no independent reference oracle.
4. `actual_start_driven` computed but consumed NOWHERE in `src/` — wire as an INFO finding or a column beside "Date-driven" (ADR-0391's promised disclosure).
5. ADR-0391's own-calendar (`exec_cal`) floor branch behaviorally unguarded — deleting it leaves engine + parity green. Add an assertion-bearing test.
6. **TEST-01** — 22 playwright modules hard-pin a chromium BUILD NUMBER (audit xfail); CI's browser job runs only the one fixed module.
7. `_ALLOWED_HOSTS` behavioural sweep + mutation proof (the data pin exists — see WHAT'S DONE).
8. FINAL-REPORT overclaims on parity and "No data off-machine" — condition on the observed backend (`_observed_banner` is the derivation to cite), do not weaken.
9. 8 stale remote branches, 148–611 commits behind (DoD 091).

⇢ TWO PREMISES REVERSED BY MEASUREMENT — do NOT "fix" these

* SSI Mean/StdDev: ours sits 3 days / 3.4% from SSI's own exported distribution; P80 EXACT. Sampling noise, not a defect.
* SRA sensitivity VALUES match SSI exactly; only the LABELS are wrong (tornado prints the host task's name — SRA R-2, still open).

⇢ TRAPS PAID FOR — check BY NAME
THE REPO IS PRIVATE AND THE CLONE IS SHALLOW — anonymous fetches 404; per-path git history lies (IN A SHALLOW CLONE, `git log -1 -- <path>`, `--stat` AND `merge-base` CAN ALL AGREE ON A FALSEHOOD — the graft boundary owns every path; break the loop with an independent oracle: tree hashes, the remote commits API). A CI FAILURE ON AN UNCHANGED CODE PATH IS A QUESTION ABOUT THE ENVIRONMENT FIRST — and a "fix" that undoes a deliberate security remediation is not a fix. A DIAGNOSIS THAT REVERSED ONCE IS NOT DONE REVERSING — record what the LAST measurement proved and name the measurement. THE ADR-NUMBER COLLISION IS REAL — two same-day sessions both took 0395; fetch before numbering AND before committing; renumbering is merge-commit work. WHEN ONE CLAIM SPANS MANY SURFACES, ROUTE THEM THROUGH ONE DERIVATION — independently-derived surfaces WILL disagree (the router said local while the page said cloud). A DATA PIN GUARDS THE LITERAL, NOT THE GUARANTEE — pin the constant AND sweep the behaviour (ADR-0394). WHEN A BATTERY MUTATES A SANDBOX, PROVE THE SANDBOX IS THE TREE BEING MEASURED (canary that must go red; md5 the instruments); BEFORE TRUSTING A SANDBOX CENSUS, PROVE ITS TRACKED SET EQUALS THE REAL REPO'S (audit: `git add -A` missed the force-added intake; ADR-0399's census asserts staged-count == population). A GUARD THAT SNIFFS A FILE CLASS IT BELONGS TO WILL READ ITSELF — never write a detector's signature literal inside the detector's own file (the hook is extension-less and sniffed; the first ADR-0399 draft wedged its own commit and the census caught it). A PREDICTED MUTATION OUTCOME IS NOT A MEASURED ONE — write the flip list the battery printed, not the one you expected. MUTATION-GREEN IS NOT ADVERSARIALLY VERIFIED — after 8/8 mutations by name, the attack round still found five in-scope defect classes; mutations prove the tests see the detectors, adversaries probe BETWEEN them (name encodings, spec-valid variants, template dialects). A GUARD'S INPUT PLUMBING IS ATTACK SURFACE — `git diff --name-only` emits an ENCODING of paths (C-quoting), not paths; every detector was correct and `schädule.mpp` still committed silently; read raw with `-z` + `read -d ''` or prove the decode. MONKEYPATCH REPOINT IS PER CALL SITE, NOT PER NAME. AN ARCHITECTURE THAT OFFERS NO LEGITIMATE PATH TO A REAL NEED WILL GET THE DANGEROUS PATH TAKEN (001c). A CLAIM DERIVED FROM CONFIG DESCRIBES INTENT, NOT BEHAVIOUR (FINAL-REPORT's instance still open). A DOCUMENT THAT FLAGS A DISCLOSURE MUST NOT REPEAT THE DISCLOSED LITERAL. `xfail(strict=True)` IS HOW A VALIDATED-BUT-UNFIXED FINDING LANDS — a fix flips it loudly; remove the marker in the fixing commit (GW-02 then HOOK-01 both followed the design). A SWEEP'S GLOB/POPULATION/PATTERN ARE PART OF ITS CLAIM — and a source-text guard whose derivation splits on a token its subject no longer contains passes VACUOUSLY (the suffix sweep was repointed in ADR-0399; check the derivation when you change a pattern's SHAPE). `| head -N` CAN SIGPIPE-KILL A BUILD MID-WAY. Plus the standing set (instrument shown to FAIL first · never MEASURE a tree a battery is mutating · never MUTATE an instrument a measurement is using · `grep -c` exits 1 on zero · two ruffs on PATH, use `python -m ruff` · bare `pytest` does not prepend CWD).

⇢ TIMING / ENVIRONMENT — MEASURED
The container starts with NO deps installed: `python -m pip install -e ".[dev]"` and `pip install build` before anything runs. Full local suite ~30 min. `pytest -m parity` alone exceeds 900 s — give it room or it dies to your own `timeout`. CI ~60 min end-to-end; `check` is a gate job and its absence early is sequencing, not failure; `cancel-in-progress: true`, so never push while you need a run's signal. Installer-smoke is path-filtered to `installer/**`, `tools/installer/**`, and its own workflow file — seven checks on PRs touching those, four otherwise. After a squash-merge, restart the branch: `git fetch --prune origin && git remote set-head origin -a && git checkout -B <branch> origin/main` — never amend GitHub's squash commit.

⇢ Standing rules (binding)
QC-1 — PROVE OR REFUTE IT BEFORE YOU REPORT IT. QC-2 — READ EVERYTHING, VERIFY EVERYTHING. Both are ADR-0393, binding, NO EXCEPTIONS, pinned by `tests/test_standing_rules.py`. Read them in full in CLAUDE.md before you start.
Law 1 CUI · Law 2 fidelity ("—" never 0; never weaken a test) · ADR-0240 model protocol (Ultracode for the overall audit; one lead re-verifies every finding against code evidence before it is reported) · full gate before every commit · handoff rotation + SESSION-LOG + LESSONS-LEARNED + THIS FILE in the same commit · wheel + nine installers ONCE per shipped-code change · a number written mid-session is not a measurement (`wc` decides). Skills: `full-gate`, `prove-able-to-fail`, `metric-parity`, `ui-change`, `cui-guard`, `render-verify`, `session-close`.
