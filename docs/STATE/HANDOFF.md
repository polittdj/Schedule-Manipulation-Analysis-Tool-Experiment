# Handoff — 2026-09-04 (d) (CI-03 ROOT-CAUSED — `chartframe.js` now loads in the layout HEAD, closing the fetch-callback race behind three "no captions rendered" strikes (ADR-0461); the operator's counterfactual report FIXED — working days, the finish activity named, the target's own move (ADR-0462); v1.0.236)

> ## STATUS (current) — **On branch `claude/polaris-audit-resume-xqte7c` from `origin/main` @ `66364af7` (the #634 docs-only merge; `main`'s run #1724 for it was `in_progress` at 12:16Z — read its conclusion; #1721 for the #633 merge concluded `success` in FULL at 08:17Z; #632 was closed unmerged at 08:13Z). This session's draft PR carries TWO product fixes at **v1.0.236** (wheel + nine installers rebuilt after the last source edit; the operator re-downloads from `main` once it merges — banner **v1.0.236**). QC-1/QC-2 bind every session — ADR-0393, pinned by `tests/test_standing_rules.py`.**
> Highest ADR **0462**. Campaign queue: **WP6** is next (CPM-01 · CPM-02 · MC-02 · MC-03 · MAN-01 · REC-02; parity-sensitive rows through the metric-parity skill; RC-02's endpoints are WP6/WP7 rows) → WP7 (`ai/txlog.py` first — Law 1) → WP8. The standing one-design-page-per-session ask was **NOT met this session** (named, not hidden): the operator's live counterfactual report took the slot under fix-as-verified — next candidates **/forecast (09 Where it lands)** and **/performance (07 How we execute)**; /compare (10) is a feature change.
>
> ## What this session shipped (ledger rows CI-03 → CLOSED and CF-01)
> **CI-03 (ADR-0461) — the kickoff's own hypothesis REFUTED and replaced.** The three `console@0.9 … no captions rendered` strikes (/forecast #626 · /curves #629 · /cei `main` #1717) were not a slow paint: every fetch-driven chart module is a sync script inside `<main>` that draws in its fetch callback, `chartframe.js` (the only `SFChartFrame`) was emitted AFTER `</main>`, and the parser yields while it downloads a sync script — so the callback could run first, throw `SFChartFrame is not defined`, and the module's `.catch` printed **"Failed to load the … data."** (a FALSE sentence, `pageerror` empty) with zero captions. Measured: R1 (the asset held 1.5 s) blanked /cei /curves /forecast /scurve /trend while the four `defer`red pages survived; R2 (NO delay, 6 CPU hogs + CDP 8×, fresh browser per iteration, 12 cold loads × 3 routes on the pristine tree) reproduced `Failed to load the forecast-drift data.` once in 36 with the first caption never later than 2,494 ms; #1717's log put the sweep in the job's first minute. FIX: one moved line — the `chartframe.js` tag sits at the end of the head script group (after `tooltips.js`). Five premise pins that asserted the old order as a REASON were re-derived (the DOM helpers stay in `gantt.js` by filing), `docs/DESIGN-SYSTEM.md` §4 re-worded, the `defer` attributes kept (byte-pinned, harmless). The sweep's zero-caption line now carries `ready / frame / hosts / svgs / failed / pageerrors` — against the pristine layout with a slow asset it reads `failed=['Failed to load the bow-wave data.']`. NEW `tests/web/test_chartframe_load_order_browser.py` (layout pin · 6 per-route pins · a Chromium proof under an async middleware holding the asset 2.5 s, the delay itself asserted): red 8/8 on the pristine tree, green 8/8 after; the sleep-neutralised mutation red by name; the six pinning modules 143 passed / 3 env skips. The `.catch` conflation stands (UI-map row).
> **CF-01 (ADR-0462) — the operator's mid-session report, TWO defects on one panel.** "31 working day(s)" on /integrity's counterfactual was `(cf_finish - actual_finish).days` — a calendar-date subtraction under a working-day label (2029-09-28 → 2029-10-29 IS 31 calendar days) while the change-effects table beneath it was already working days; and the two dates were two ACTIVITIES (the network's last finish, 2029, vs the target milestone, 2027) that the panel never told apart. Engine: both deltas are the CPM's working-minute move over the calendar's day; NEW `finish_uid` / `finish_name`. Page: "the project finish (the network's last activity, UID n “name”)", the target line carries its own working-day move (recovery · pushed out · no change), one muted line says the two are different activities; /evolution's `_delta_words` and the Ask-the-AI fact say "working day(s)". Red-first: engine `7 == 5` + `AttributeError: finish_uid` → green; page 3 pins red against a pristine scratch copy on `PYTHONPATH` → green; the one data pin on the old number re-baselined 7 → 5 with the reason; 449 affected tests green; `mypy --strict` clean.
>
> ## Operator-facing state
> Re-download once the PR merges; the banner must read **v1.0.236**. ASK (do not assume): (1) on /integrity with the same pair and UID 152 as the target — the first line now names the network's last activity; does the target line's working-day move read right against the file's calendar (five-day or seven-day)? (2) #631's asks stand (the parent-folder question on `/`; /trend's version chips); (3) #630's asks stand (I-01: which finding, which two files, folders or Titles · T-01: page, zoom, screenshot · (c): is the residual /analysis lag gone). UNKNOWABLE and closed: the blank-header banner, the 08-26 incident.
>
> ## Traps paid for this session, by name
> a wait whose failure reads the same as "not yet" reports nothing — make the zero case name what it saw · fix the class where the dependency LIVES (one layout line) rather than the consumer where it bites (`defer` ×4 and eleven still exposed) · a `.catch` that swallows a `ReferenceError` prints a false sentence and no oracle sees it — read the page's own text · a route-interception probe that shows no effect must first prove it matched (`?v=<version>` on every static URL; the glob needs a trailing `*`) · a premise pin that fires is re-derived, never deleted · a mutation must break what the test guards, not the test's constant (`SLOW_MS = 0` zeroed the threshold too) · a number's UNIT is part of its provenance — compare units across the panels of one page · two dates that look like a contradiction are two subjects — name them · a pip log's last line can look green while the install died on a read timeout — check the exit code · `cd` inside one Bash call moves the shell's cwd for every later call · the lessons log's own order (newest first) is a rule; the previous entry sat at the bottom and was moved verbatim.
>
> ## Next — campaign queue
> Branch FRESH from `origin/main` once this PR merges and open a NEW draft PR. **WP6** (ledger highs: CPM-01 `cpm.py:1316` · CPM-02 `driving_slack.py:314` · MC-02 · MC-03 `jcl.py:284` · MAN-01 · REC-02; any golden shift = CONFIRMED-DEFERRED, never a silent re-pin) → **WP7** (`ai/txlog.py` first — Law 1) → **WP8**; plus the design page owed from this session (/forecast 09 first — the `.cd-*` family carries `cd-grid-12`, `cd-stack`, `cd-master`, `cd-note`; a script-created master mounts into a `#<page>Master` slot; recover artboard 09 by EXECUTING the canvas, never from a screenshot's file name). CI-04 (the /driving-path header-row equality race on #632's docs-only diff) is the remaining CI candidate — a different oracle, diagnose the same way (measure first, induce the race, then fix where the dependency lives).

# (prior) handoffs — archived

> The earlier handoff sections now live in **[HANDOFF-ARCHIVE.md](HANDOFF-ARCHIVE.md)** (newest-first,
> verbatim), and the full append-only per-session history is in **[SESSION-LOG.md](SESSION-LOG.md)**.
> Per ADR-0246 this file keeps ONLY the current STATUS section above, so the entire live handoff is
> small enough to read in full in one pass every session (and the SessionStart hook auto-injects it). When you
> write the next handoff, MOVE the current section to the top of `HANDOFF-ARCHIVE.md` (demote its
> heading to `(prior) Handoff`) and REPLACE the section above — do not stack another archived heading
> here. This single pointer is intentionally the only such heading in the file; the size guard enforces
> that.
