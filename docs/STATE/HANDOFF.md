# Handoff — 2026-09-04 (f) (WP6 COMPLETE — the six ledger highs all CONFIRMED by execution and FIXED red-first, REC-02 widened to the whole app (ADR-0463); /forecast is the FOURTH page on the Claude Design layout, the drift stepper re-homed into the masthead strip (ADR-0464); v1.0.237)

> ## STATUS (current) — **WP6 shipped and the owed design page delivered on one branch (`claude/polaris-audit-campaign-kr8maj`, restarted FRESH on `origin/main` @ `3c3c398` — the #636 docs merge; `main`'s run #1728 for the #635 squash read `success` at 16:07:29Z, #1732 for #636 was `in_progress` at 17:20Z). Banner **v1.0.237** (wheel + nine installers rebuilt after the last source edit; the lockstep and the full-gate result are in the SESSION-LOG's follow-up line). Draft PR number in the SESSION-LOG. QC-1/QC-2 bind every session — ADR-0393, pinned by `tests/test_standing_rules.py`.**
> Highest ADR **0464**. Campaign queue: **WP6b / WP7** next — the ledger TAIL (CPM-03/04 · MF-03/04/06..10 · MC-04..08 · IMP-02..06 · MAN-02/03 · JS-02..06 · TST-02/03) and RC-02's 3 never-2xx + 15 never-adverse routes, then `ai/txlog.py` first (Law 1) → WP8. The standing one-design-page-per-session ask was **MET** (/forecast 09); next: **/performance (07 How we execute)**; /compare (10) is a feature change.
>
> ## What this session shipped
> **WP6 (ADR-0463) — six finder claims from 2026-08-16, every one re-derived from its cited line AS IT READ THEN (`git show 1b833c6a:…`), probed to refute, and every one CONFIRMED.** CPM-01: the ADR-0309 resume floor moved EF but total float was `LS - ES` with `LS = LF - stored duration`, so the floor's gap never reached the float — golden EVM2 UID 20 read **10 wd of float, non-critical** while MS Project flags it Critical and `LF - EF` is 0 → `total = min(LS - ES, LF - EF)` (byte-identical for contiguous tasks; Project2/5's reschedules have a zero gap). CPM-02: `endpoint()`'s undated fallback was a project-axis offset beside a successor-calendar stored date — an undated predecessor of a 24/7 successor read **8 d / off-path vs 0 / on-path** once dated at its own CPM dates → the fallback instant is re-measured on the successor's calendar when its pattern differs (fully dated files byte-identical; `-m parity` green). MC-02: the third path was the LIVE legacy `compute_sra` (fed by `_risk_events`), multiplying a completed activity's point mass — P50 **8,148 → 12,948** min under a certain ×2 → `done` guard. MC-03: `jcl.py:284`'s absent-actual-as-0 dropped the performed share of budget (EAC **680 vs 1,180**; a 990 jump at 100 %) → the performed share is the point estimate when no actual is recorded, a recorded zero never overridden, `actuals_assumed_count` disclosed on /jcl. MAN-01: on the effective basis the Fuse §E No-Longer-Critical membership is **UID-exact** (the documented 96↔99 swap gone; counts 1 / 34 / 41 / 4 unchanged; manipulation findings 5 = 5) → one shared `effective_critical_incomplete`, the parity pin re-baselined through its own path to the EMPTY symmetric difference. REC-02: true at the engine (`KeyError`) and the small half — `SessionState.scope()` truncated to any non-summary UID, so an inactive target set through POST /target turned **51 of 63 GET routes into 500s** (`/` included; an absent UID 0) → the presence test is the network's membership. Red-first on a pristine worktree **16 failed / 8 passed**; green 23 + the parity pin; seven mutations red by name; engine + parity **1,137 / 1** with the one failure a JCL rounding-edge fixture re-baselined to a RECORDED 0.0 (51 green after).
> **/forecast on the design (ADR-0464).** Artboard 09 recovered by EXECUTING the canvas — with a new recipe step paid for: `file://` blocks the canvas's `crossorigin` React scripts by CORS (blank 6-KB screenshots, the section present but hidden); served over loopback HTTP it rendered in four themes with zero page errors. The drift stepper's own ◀ Prev / label / Next ▶ / ▶ Auto-play are RE-HOMED (moved, not mounted — the same nodes) into `#forecastMaster`, one `.cd-chip` per version (the FIRST on), a pill; `render()` publishes `data-frame`; the ruler panel full width, methods + inputs beside the Carnac cards, the drift panel beside a "How to read this" from the page's explainer; the mock's S-curve is /scurve's and is not ported. Every drift.js edit above the line-136 DD pin is same-line-count. Contract counts measured IDENTICAL pristine → patched (panels 11 / 9 · takes 5 / 4 · chips 5 / 4 · exports 3 / 2 · ⤓ 3 / 2 · ⛶ 5 / 4 · forms 5); the four-theme render census moved only on chips / chipOn / strip / the master's 3 buttons / head order / height, zero page errors, nothing wider than the viewport; red-first 7 / 1; 8 green; six mutations red by name; 410 + 78 across the /forecast-pinning modules and the census + stepper drivers (one premise pin — the r10 take tuple's OLD panel order — re-derived, its assertions unchanged).
>
> ## Operator-facing state
> Re-download once the PR merges; the banner must read **v1.0.237**. ASK (do not assume): (1) on /forecast with two or more files — do the chips and the re-homed Prev / Next / Auto-play in the masthead strip move the drift chart, and does the pill name the version you expect? (2) #635's ask stands: on /integrity with UID 152 as the target, does the working-day count read right against the file's calendar? (3) #631's and #630's asks stand (the parent-folder question on `/`; /trend's chips; I-01 · T-01 · the /analysis lag). UNKNOWABLE and closed: the blank-header banner, the 08-26 incident.
>
> ## Traps paid for this session, by name
> a finder's line number is a DATE — re-derive it from the tree as it read then (`git show <sha>:path`) before believing what it points at now · a floored FINISH must reach the float — compare `LS - ES` with `LF - EF` on every task, the two are equal only when the task is contiguous · two ends of one link must share one ruler — an undated end's fallback axis is part of the measurement · enumerate every loop over the population before saying "guarded" (`.affected` had four callers, three guarded) · an ABSENT figure at the boundary of a formula shows up as a DISCONTINUITY (99 % → 100 %) — probe the boundary · a "documented swap" pinned by a parity test is a measurement waiting to become exact — try the other basis before defending the swap · a finder's row can be the small half of a defect: sweep the whole route table with the hostile state against a control before sizing it · a mis-written test is red on BOTH trees — a red on pristine proves nothing until the same test is green on the fix · a fixture that lost its purpose to the fix is re-baselined to keep its purpose (a RECORDED 0.0), never its number · `file://` is not a browser origin — serve the canvas over loopback HTTP · a premise pin on panel ORDER fires on a re-arrangement — re-derive the order, never the assertions · `--deepen=300` can leave the clone shallow; the installer script refuses only a graft-boundary MPXJ ref — check `git rev-parse --is-shallow-repository` and the resolved ref, not the flag alone.
>
> ## Next — campaign queue
> Branch FRESH from `origin/main` once this PR merges (`git fetch --prune origin && git checkout -B <branch> origin/main`). **WP6b** (the ledger tail, above) → **WP7** (`ai/txlog.py` first — Law 1; RC-02's routes) → **WP8**; **FIRST, the operator's 2026-09-04 feature request — /onepager-compare** (two three-column One-Pager sheets dropped as prior / current; the ADR-0446 slide with every slip drawn and labelled in CALENDAR days, matched on the (swimlane, item) pair the file actually carries; spec and the operator asks in `docs/STATE/NEXT-SESSION-PROMPT.md`) — it takes the design-page slot; then the design page owed each session: **/performance (07 How we execute)** next (recover artboard 07 by EXECUTING the canvas over loopback HTTP; `sfredux-screen` key from the canvas's `setScreen('…')` calls). CI-04 (the /driving-path header-row equality race on #632's docs-only diff) is the remaining CI candidate. Observed, not fixed blind: `evm.py`'s `actual_cost or 0.0` ACWP on a mixed population (an EVM parity row of its own).

# (prior) handoffs — archived

> The earlier handoff sections now live in **[HANDOFF-ARCHIVE.md](HANDOFF-ARCHIVE.md)** (newest-first,
> verbatim), and the full append-only per-session history is in **[SESSION-LOG.md](SESSION-LOG.md)**.
> Per ADR-0246 this file keeps ONLY the current STATUS section above, so the entire live handoff is
> small enough to read in full in one pass every session (and the SessionStart hook auto-injects it). When you
> write the next handoff, MOVE the current section to the top of `HANDOFF-ARCHIVE.md` (demote its
> heading to `(prior) Handoff`) and REPLACE the section above — do not stack another archived heading
> here. This single pointer is intentionally the only such heading in the file; the size guard enforces
> that.
