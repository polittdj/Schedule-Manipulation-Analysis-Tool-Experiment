# Handoff — 2026-09-25 (two §3 rows worked in order — R-48 REFUTED and CLOSED (ADR-0532), R-51 CLOSED (ADR-0533) — **v1.0.293**)

STATUS (current) — branch **`claude/determined-cray-beuym5`**, draft **PR #718** opened this session (the operator merges; never marked ready here). Based on `main` @ **`f1b691f3`** (#717, ADR-0531, v1.0.292). §0's anti-foreign-prompt block was RUN and the tree agreed on every point (f1b691f3, 816 commits, `src` present / `app` absent, both workflows, 1.0.292, highest ADR 0531 on main). **Both draft PRs the previous kickoff called open are MERGED by the operator:** #716 → `d3d1034d` (v1.0.291) and #717 → `f1b691f3` (v1.0.292); `main`'s CI run for f1b691f3 read from its JOBS — floor · cui-guard · test (3.11) · test (3.13) · browser · check all **success** (check 13:19 UTC 09-24), installer-smoke success. **No PR was open at session start.** One remote branch nobody's kickoff named, `test/ch04-stability-oracle` (2026-08-19, one commit on #603's base; its test file reached `main` via #604), is stale and the operator's to delete — not touched. `src/` changed (`engine/metrics/health_extra.py`): wheel + nine installers rebuilt LAST, so **EIGHT checks**. Highest ADR **0533**. Version **1.0.293**. Schema **2.17.0** (unchanged). QC-1 / QC-2 / QC-3 bind every session.

## What landed — the register's two priced OPEN rows above R-21, each premise re-read first

**R-48 REFUTED and CLOSED (ADR-0532).** The row said the library's "8. High Duration" tile carries `IncludeComplete=true`. Both entries (GUIDs c6edc255 / eebc7193) carry **`false`** in BOTH filters of BOTH committed `.aft` snapshots — neither file edited since its upload — and their Remarks say "planned or in-progress". And the row's "no Fuse figure in the repo discriminates" was false since ADR-0518: the Large Test File pair carries **77 / 78 COMPLETED** activities with a Baseline Duration field over 44 days, so a tile admitting them would print **164 / 1,569 and 164 / 1,568** where the Analyst ribbon prints **87 / 927 and 86 / 904** (the count separates both files; the 2-dp ratio ties on File2 at 0.10). No engine change. `tests/parity/test_r48_high_duration_complete_oracle.py` (15): the library pin per file (red by name on a scratch copy with one flag flipped), the discrimination (red with `is_incomplete` patched to admit completed work: 164 vs 87, plus three leak censuses and Project5's completed UID 17 reading 1 vs 0), no completed offender on ten progressed goldens.

**R-51 CLOSED (ADR-0533).** Fuse's "Estimated Duration" is the MS Project *Estimated* flag over **planned-or-in-progress NORMAL** activities (three library entries: `IncludeComplete=false`, `IsEstimated = True`; the two DCMA-report entries `IncludeMilestone=false`), and its ratio is the count over that same population without the flag. The engine's health check "Estimated (placeholder) durations" read the flag over EVERY status — 68 / 68 / 63 / 63 on the four Hard_File snapshots where Fuse prints **68 / 65 / 47 / 41** (ratios 0.80 / 0.82 / 0.81 / 0.79 = 68/85, 65/79, 47/58, 41/52; the with-milestones population prints 0.62). The predicate adds `is_incomplete`; the check's population is the to-go normal count (a one-entry mapping; every other check keeps `n`). Oracle `tests/parity/test_r51_estimated_duration_oracle.py` (7): the AlltheProjects ribbon's count AND ratio on nineteen committed labels (also the rev-5 updated3 41 / 0.79 and the 24-hour save 9 / 0.64; 0 on eleven flag-free labels), the every-status reading refuted on ≥ 3 labels, the two Detailed Metric Reports' X marks BY UID on four sheets (65 / 47 / 47 / 41 — the flagged activities Fuse leaves unmarked are exactly the completed ones, 3 / 16 / 16 / 22; set-equal where the 50-offender citation cap allows, subset-and-count on the 65 sheet). **The STAT scorecard's "Estimated (not-yet-firm) durations" row is deliberately NOT re-scoped** — a raw flag census over every status, labelled by its source (63 beside 47 on updated2); recorded in the ADR so the two are not read as a disagreement.

## How it was verified

QC-3 attacked 13 assumptions across the two ADRs (A1–A5, B1–B8); none fell, two are UNVERIFIED by construction and say so: the milestone clause (no estimated milestone on sixteen fixtures — only `test_health_extra.py` sees it) and percent-complete vs actual-finish at the margin (coincident on every activity, 0 mismatches). Red-first on the pristine engine: R-51's ribbon leg `('Hard_File', 68, 110, 0.8)` (0.62 vs 0.80), the four X-mark legs (68 vs 65, 63 vs 47, 63 vs 47, 63 vs 41), the unit test 2 vs 1 — 6 red, the two oracle-consistency tests green. Mutants, each restored from a scratch copy and the tree verified identical: M1 status clause dropped 6 red · M2 population left at `n` 2 red (the X-mark legs green — they compare counts and UIDs) · M3 milestone clause dropped: parity GREEN, unit red (recorded) · M4 `dcma14.is_incomplete` admitting completed work 5 of 15 red · M5 the flipped library copy red naming c6edc255 / PrimaryFilter. Green: the 27 collected tests of the three modules; `test_evm_acumen_reference`, the health-panel render test, the wp8 register guard 8 / 8, the parity-report sync + tolerance-ledger guards. **Gate on the final tree:** statics clean on `/usr/local/bin/ruff` 0.16.9 (the 0.15.8 on PATH is NOT CI's), mypy strict 165 files, bandit exit 0, `node --check` per script; the full suite **6,005 passed, 5 skipped, 0 failed** (46:56); parity **271 / 271, 0 skipped** (10:11). PR #718's first head dc425f95 read six of eight checks green (test (3.11) in flight) when the docs-only follow-up was pushed — read the FINAL head's eight checks to conclusion.

## Deliberate re-baselines

None — no test had pinned the every-status reading; the register rows R-48 / R-51 read CLOSED with their findings, §1's tier-1 sentence names R-48's refutation.

## Not done (measured, left) · carried forward

The STAT row's every-status census (a distinct, labelled figure) · the milestone clause's oracle (no estimated milestone in any committed save) · the third library entry's primary `IncludeMilestone=true` (not in the DCMA report) · Project5's completed UID 17 (recorded, no ribbon) · the origin of ADR-0473's misreading (not reconstructed) · `IncludeInDCMA=false` on both tile entries (observed, not interpreted). Earlier residuals unchanged: R-71's clamp (UID 187) · R-21's pane (the register's ONLY priced OPEN row now, T4 M) · R-22's encodings · R-32's product finding (the 60-day extension on open) · DCMA-13's pure-branch min over ALL timings · the eleven other exports answering 400 · the AST walker's blind shapes. **UNVERIFIED:** nothing in this handoff beyond the two named clauses — every figure above was re-read from a run. **Next:** R-68 waits on the operator (question (f)); the raw-flag question (ADR-0531); R-21 is the last priced OPEN row; then the HELD rows by tier and the "also open" list in the kickoff.

# (prior) handoffs — archived

> The earlier handoff sections now live in **[HANDOFF-ARCHIVE.md](HANDOFF-ARCHIVE.md)** (newest-first,
> verbatim), and the full append-only per-session history is in **[SESSION-LOG.md](SESSION-LOG.md)**.
> Per ADR-0246 this file keeps ONLY the current STATUS section above, so the entire live handoff is
> small enough to read in full in one pass every session (and the SessionStart hook auto-injects it). When you
> write the next handoff, MOVE the current section to the top of `HANDOFF-ARCHIVE.md` (demote its
> heading to `(prior) Handoff`) and REPLACE the section above — do not stack another archived heading
> here. This single pointer is intentionally the only such heading in the file; the size guard enforces
> that.
