# Handoff — 2026-09-14 (R-60 CLOSED (ADR-0491): a leveling split is honoured inside the task the way the leveling delay is — the converter writes the timephased data, the importer reads a WORK booking's pieces, the engine carries every gap nobody works through; every Hard_File snapshot's finish EXACT; 14 goldens regenerated from their own saves; v1.0.259)

STATUS (current) — `main` @ **`6708cbff`** (#671, ADR-0487 / 0488, v1.0.257; `main`'s runs CI 1849 / installer-smoke 712 success, read from the Actions API). **PR #672 (`claude/confident-johnson-py46p9`, ADR-0489 / ADR-0490, v1.0.258) was OPEN and unmerged at this session's start** — this unit branched from `main`, numbers its ADR **0491** and its version **1.0.259** so both land whichever merges first; if #672 merges first, merge `origin/main` into this branch and resolve `HANDOFF.md` / `SESSION-LOG.md` / `pyproject.toml` / the oracle's slack-population pins (ADR-0490 re-baselined four of them by the inferred zero-slack count; this unit did not touch `tf_n`). **One unit landed on branch `claude/fervent-hawking-7z7kx0`: R-60, ADR-0491.** The kickoff's plan held and then changed under measurement. **(1) Provenance by section diff, not testimony.** The fixture's own save was in git history (`git show af4d154f:…/Hard_File_updated3.mpp`, blob `1908d073`, Revision 2); converted with the vendored converter it differs from the golden on 32 lines — `CurrentDate` and four resource elements the writer resolves at conversion time — and NOT AT ALL in the `<Tasks>` / `<Assignments>` sections. The same test over all 36 historical blobs of the 30 intake `.mpp` paths matched **14 of 16 goldens** to one save each (`tests/fixtures/golden/PROVENANCE.json`, pinned by `tests/guards/test_golden_provenance.py`; the two EVM goldens have no committed source). **(2) The rule.** `MpxjToMspdi.convert()` sets `setWriteTimephasedData(true)` (Large_Test_File +7 % bytes, +0.4 s parse, same JVM wall time under `-Xmx1g`; `setGenerateMissingTimephasedData` throws inside MPXJ 16.2.0 and is refused); `importers/mspdi.py::_timephased_pieces` reads Type 1 / 2 blocks (verified block-for-block against MPXJ's raw remaining / actual accessors) into `Assignment.work_pieces` (new `WorkPiece` model; SCHEMA_VERSION 2.13.0 — and the ledger records ADR-0487's un-bumped `start` / `finish` retroactively); the engine's `_Leg` carries `gaps` — `_split_gaps` measures each gap in WORKING minutes of the leg's calendar (`_recorded_span`) **less what other WORK bookings of the task work inside it** (`_covered_span`), `_leg_finish` / `_leg_retreat` walk piece-gap-piece both ways, the share scales with the leg and the gap does not, `_plan_scaled` keeps a gap still ahead; `CPMResult.split_driven` discloses. **(3) The finding.** The first cut honoured EVERY gap and read Large_Test_File2's UID 5308 chain (24 activities) three weeks late — one of eight bookings on a fixed 228-h duration records a three-week zero window the other seven work through, and MS Project's Duration already spans it; UID 5265 (one booking, daily afternoon-only gaps excluded from its 36.85-h duration) landed exact under the same rule. A gap nobody works is the TASK's split; a gap another booking works through is that booking's contour. Two unit tests pin both sides. **(4) The goldens** were regenerated by `tools/regenerate_timephased_goldens.py` — the exact blob, the shipped converter, the same-save proof inside the tool, the new `<Assignments>` section SPLICED into the old golden so the only change is the timephased data (the header and resources keep their 07-09 / 09-07 values; today's would change two resources' `MaxUnits` for no reason of the save's — R-63). **Measured, pristine → this tree, every golden:** Hard_File / updated / updated2 project finish **−1 d → EXACT**, within-a-day **92 → 103 / 100 → 108 / 93 → 109**, stored slack 3 → 5 / 52 → 60 / 8 → 36, updated2's Critical **80 → 107**; updated3 104 → 106, slack 43 → 46, UID 403 **11-05 09:12 with LateStart 11-25 13:48 and slack 12,888 — the stored values**, 404 exact; Hard_File's UID 14 spans its stored 10-26 20:00 → 10-29 11:00, 401 spans 5.6 d like MS Project; **Large_Test_File 1569 → 1666** (slack 842 → 865, Critical 1682 → 1721), **File2 1589 → 1687** (1686 → 1717), Leveled 1569 → 1645; Project2 / Project5 exact and unmoved; **no activity moved away from its stored finish** except UID 5306's chain on the leveled SSI golden by 8 minutes MS Project's own arithmetic carries (four 2:36 daily gaps; the engine's integer minutes) on a start already a day late. **Verification:** red-first (three modules cannot import on the pristine tree; the oracle's updated3 row red by name at 10-23 15:00 / LateStart 12-08 08:00); the first green run refuted the RIG (a 240-minute piece over a 480-minute window); **mutation battery 18 / 18 RED by name** on shadowed copies — one survivor on the first pass (pieces unsorted across rows), now pinned; the converter mutant ran as a mutated class under a scratch `SF_MPXJ_HOME`; the guard mutant in memory. Both ruff binaries, format, mypy strict clean; the oracle module 10 passed; the full suite and the installer rebuild are in the SESSION-LOG's close entry with the PR number. Version **1.0.259**. Highest ADR **0491**. QC-1/QC-2 bind every session — ADR-0393.

**CARRIED FORWARD, still live and NOT this unit's:** `test_driving_path_whole_schedule_browser.py:104` is width-racy (#667's register); `/settings` scrolls sideways (1,877 px measured 09-12); V-4 and the `"ok": false` lines still owed by the operator.

## UNVERIFIED, stated

- Gap semantics when the START moves: every stored start in the corpus equals its first piece's start, so working-minute vs elapsed cannot be told apart by any golden when a predecessor slips; the file's own backward-pass arithmetic (403's LateStart) is working-minute and the chosen reading is pinned by a named test so a re-pin is deliberate.
- The Windows DPAPI branch of the key store (unchanged; ADR-0488's note stands).

## Traps this session paid for, by name

* **"Honour what the file records" is not a rule until the corpus says WHOSE fact it is** — a golden-level count (1589 → 1605) hid 24 regressions behind 80 improvements on the same file; the per-task toward/away census and the away-mover whose predecessors are not away-movers found the rule's counter-example (5308) in one derivation.
* **Provenance is a diff, not a report** — `git log --all` had the save ADR-0487 called unavailable; the section diff matched 14 goldens to their saves and exposed the converter's wall-clock dependence (R-63) at the same time.
* **`git log --follow` invents history** — two commits listed never carried the file; address a save by its BLOB.
* **The rig can be wrong before the engine is** — recompute every expectation from the rig's own numbers.
* **The compiler is the API reference** — three MPXJ method names written from memory did not exist; `javap` gave the real ones in a second.
* **A `&&` chain with a missing binary printed "started" and started nothing** — the background census never ran until the tool's own runner was used; check the log exists before believing a launch line.

## Next — campaign queue

**Read `git log origin/main` before trusting any sha written here.**

**First:** this PR's checks on its final head (eight — `installer/**` changed) and #672's fate (merge-resolve if it lands first). Then the report's §3 in order: **R-46** (BCWS +150) · **R-47** (SPI(t) 8.24 vs 8.22) · **R-52** (the `.pptx`) · **R-50** (History variants) · **R-61** (a FIXED_DURATION leg on an off-pattern crew) · **R-57** (an assignment's OWN leveling delay — Hard_File UID 398's RA 277 is a split ON a delayed assignment: the gap is honoured now, the delay is not; UID 188) · **R-58** · **R-59** · **R-64** (milestone 387's external link — the one day the Hard_File chain 400 → 404 still sits early) · **R-63** (the converter's conversion-date dependence — `MaxUnits` feeds the loading view) · **R-62** (in #672) · then R-03 · R-04 · R-09 · R-13 · R-18 · R-21 · R-22 · R-32 · R-39. The design queue is 19 artboards. Other residuals: ADR-0488's / 0486's / 0485's / 0483's (unchanged), `/settings` sideways scroll, OR-11b, OR-11d, the working-minute axis, the hint bubble, ADR-0484's in-grid rows.

**Review cover is still absent** — Codex quota EXHAUSTED through #672; the mutation battery and the full gate are all this repo gets.

# (prior) handoffs — archived

> The earlier handoff sections now live in **[HANDOFF-ARCHIVE.md](HANDOFF-ARCHIVE.md)** (newest-first,
> verbatim), and the full append-only per-session history is in **[SESSION-LOG.md](SESSION-LOG.md)**.
> Per ADR-0246 this file keeps ONLY the current STATUS section above, so the entire live handoff is
> small enough to read in full in one pass every session (and the SessionStart hook auto-injects it). When you
> write the next handoff, MOVE the current section to the top of `HANDOFF-ARCHIVE.md` (demote its
> heading to `(prior) Handoff`) and REPLACE the section above — do not stack another archived heading
> here. This single pointer is intentionally the only such heading in the file; the size guard enforces
> that.
