# Handoff — 2026-09-15 (OR-20 SHIPPED (ADR-0497): the driving-path series reports the focus's own stored Finish — the axis its drivers are measured on and the date MS Project / SSI show — and discloses the engine's logic-only finish beside it only where the two disagree by a working day or more; the model's self-diagnosis ("the focus finish was the network finish") REFUTED on the operator's own IMS; `-m parity` 118 / 0 on the pristine tree; v1.0.265)

STATUS (current) — `main` @ **`69607e3b`** (#681, the docs-only merge record of #680 / #679, squash-merged by the operator; tree-identical to its head `7b04f3e8`; before it `970a3bcc` #680 (OR-19, ADR-0496, v1.0.264), `055dfb3f` #679 (R-47, ADR-0495), `e6ff45e1` #678 (OR-18, ADR-0494) — all read from `git log origin/main` this session). **`main`'s own runs for `69607e3b` read this session: CI run 1878 (35014204420) SUCCESS, completed 20:12:05Z; installer-smoke has NO run for that sha — #681 was docs-only and `installer-smoke.yml` is path-filtered to `installer/**` (its latest `main` runs are 735 / 733 / 731 for 970a3bcc / 055dfb3f / e6ff45e1, all success).** **This unit is a draft PR from `claude/beautiful-hypatia-4aefrf`, ONE commit on `69607e3b`** (number in the SESSION-LOG follow-up); eight checks apply (`installer/**` changed).

**What landed — OR-20, the operator's 32-version Ask-the-AI answer.** The operator loaded 32 monthly versions of one IMS (USA IPMR Format 6, July 2023 → January 2026), asked for the driving path to UID 152 ("Ready to Ship") in every version, and the answer tabled a *"driving-path finish"* of **2028-02-22** for `USA IPMR Format 6_January 2024.mpp` where MS Project shows **2026-05-07**. The model's own "correction memo" then diagnosed *"the focus finish was populated with the network CPM finish — byte-for-byte identical in all 32 versions"*. **Measured on the operator's own IMS in the repo** (`ssi_uid152/Large_Test_File.mspdi.xml.gz`, 2,126 tasks, the same "USA OTB Master IMS" family, with SSI's Directional Path export): `_focus_finish` read UID 152's OWN CPM early finish (2026-10-02 = its stored Finish = SSI's exported Finish, serial 46297.5), two years from the network finish (2028-09-28) — **the memo's diagnosis is refuted**; the tool's stored Start/Finish equal SSI's on 76 of 76 path members. **The real defect:** the drivers on each line are measured on the file's stored, progress-aware dates (ADR-0011's SSI axis), but the finish printed beside them was the engine's logic-only CPM early finish, unlabelled — and on this IMS the two disagree by more than a day on **50 of 1,723** scheduled activities (up to 106 d, both directions; 65 / 54 / 35 on the other three saves). For UID 152 they coincide in every save in the repo (latent here; **UNVERIFIED on the operator's 32 files**); wherever the focus is one of those activities the line carried a date neither MS Project nor SSI shows, in the same shape as the SCHEDULE-LOGIC FINISH SERIES line beside it. **Shipped:** the line reports the stored Finish (`UID 152 finishes 2026-05-07`), the CPM finish only where the file stores none (said to be computed), and — where the logic-only finish disagrees by a whole working day or more, compared on the project working-minute axis (`datetime_to_offset` vs `timing.early_finish`, ADR-0310's one ruler) — both dates and the signed gap on the line; the header says whose date it is and that it is NOT the network finish; the movement census runs on the stored Finish and counts the versions carrying a disagreement. **Measured:** red-first 8 of 10 by name on the pristine tree (the defect test printed the logic-only 2025-02-07 where the file stores 2025-05-02); the same-instant milestone test red on the first threshold rule (a wall-clock window reads 480 min between Fri 17:00 and Mon 08:00 on a calendar without day segments) and green on the project-axis rule; 33 green after; **two SSI oracles on the real IMS pinned** (2026-10-02 / 2026-10-09 on the lines, 2028-09-28 nowhere); **mutation battery 7 / 7 red by name on a shadowed copy** (control 22 green; M2 = the memo's allegation → both oracles red); ruff ×2 / format / mypy 165 files / bandit / node clean; **`-m parity` on the pristine tree 118 passed / 0 failed in 4:18** — the executable proof the operator asked for that the committed corpus matches Fuse / SSI / MS Project. **The full suite on the final tree: 5,460 passed / 5 skipped (the environment-gated urlparse and axis-title skips every run carries) / 0 failed in 37:30.** Version 1.0.265, wheel + nine installers rebuilt AFTER the last source edit (the wheel's `driving_facts.py` byte-identical to `src/`, checked). Highest ADR **0497**. QC-1/QC-2 bind every session — ADR-0393.

**What the operator can settle that this session could not (OR-20's open question, not blocking):** whether the Jan 2024 fact line said 2026-05-07 (the model then transcribed the network series into its table) or 2028-02-22 (the engine's logic-only finish for UID 152 disagrees with that file by ~424 working days). The transaction log keeps only a hash of the prompt (Law 1), so the line lives only in the operator's session: the cited facts under the answer, or `/export/{fmt}/ask` of the last exchange. On v1.0.265 the line states both dates wherever they disagree, so the next run answers it directly — ask the same question again on 1.0.265 and read the DRIVING-PATH SERIES / MOVEMENT lines. The mode the answer was produced in is inferred as unrestricted (its arithmetic — 697 / 1,286 / 589 days — was the model's own across two axes; unrestricted is ungated by design, ADR-0361; annotate flags model-derived figures, strict discards them).

**CARRIED FORWARD, NOT this session's:** the 09-11 `"ok": false` transaction-log lines (a 403); an ON-banner screenshot on v1.0.264+; R-45; `test_driving_path_whole_schedule_browser.py:104` width-racy (#667); `/settings` sideways scroll; the residuals of ADR-0488 / 0486 / 0485 / 0483; OR-11b, OR-11d; the working-minute axis; the hint bubble; ADR-0484's in-grid rows. **NEW, registered by ADR-0497:** **OR-20b** — the SCHEDULE-LOGIC FINISH SERIES carries only the CPM network finish; a per-version stored project finish beside it is the like-for-like series (the model's 1,286 days subtracted a CPM finish from a stored one); **OR-20c** — the focus's stored Total Slack per version on the line (513.5 d on Large_Test_File, 0 on File2); the CPM-vs-stored residual census (50 / 65 / 54 / 35 per save, worst-eight UIDs in the SESSION-LOG — the report's §3 mechanisms, no engine number moved); `working_minutes_between` credits a finish day whole on a calendar without day segments (`Calendar()` only; every MSPDI calendar carries segments). **Environment facts re-paid:** the clone arrives shallow (`git fetch --unshallow origin`); no package until the dev install (`uv pip install --python /usr/local/bin/python3 --system -e '.[dev]' build playwright`); CI's ruff 0.16.7 at `/usr/local/bin` only after it — run both; the harness's git identity is the operator's — override per command; a background wrapper "completing" is the shell detaching, not the job finishing — read the job's own log; `pkill -f` matches the shell that runs it — bracket the first letter.

## UNVERIFIED, stated

- Which date the operator's Jan 2024 fact line carried (above); whether the engine's logic-only finish for UID 152 disagrees with any of the operator's 32 files — the v1.0.265 line now says.
- The mode of the operator's answer (inferred unrestricted from the footer and the model's arithmetic; the log does not record it).
- The operator's machine: whether v1.0.265 is installed — theirs to report (every page states its build since ADR-0494).

## Traps this session paid for, by name

* **A model's correction memo is testimony, not a root cause** — its "byte-for-byte identical" claim and its "populated with the network finish" mechanism were both refuted by one probe on the real IMS; read the code and measure before adopting a diagnosis, however confident its prose.
* **Two per-version date lists of one shape in one prompt WILL be cross-wired** — label whose date each line carries, and make the shapes differ (the UID on every line).
* **Drivers and the finish beside them must be on one axis** — the drivers were stored-date (SSI parity); the finish was logic-only; the census (50 of 1,723) says how often that bites on a real IMS.
* **A wall-clock window is not the CPM's ruler** — `working_minutes_between(Fri 17:00, Mon 08:00)` = 480 on a segment-less calendar; compare offsets on the project axis (`datetime_to_offset` vs `early_finish`), where the two are one instant. The same-instant test was red-first on the wrong rule.
* **Rebuild after the LAST source edit** — the threshold fix landed after the first wheel; the lockstep test would have caught it, the ritual says rebuild, and the wheel's file was diffed against `src/` before the suite ran.
* **The transaction log cannot replay a prompt by design** — the cited facts under the answer and the ask export are the only record; say so instead of guessing which line the model read.
* **Verify the parity claim with the gate, not with prose** — the operator asked for proof the numbers match the reference tools; `-m parity` 118 / 0 IS that proof for the committed corpus, and its scope (the committed corpus, not the operator's production files) is the honest boundary.

## Next — campaign queue

**Read `git log origin/main` before trusting any sha written here.** **First:** OR-20's draft PR (eight checks) — after the operator merges, restart the branch with `--prune` + `remote set-head` + `checkout -B`; the operator's re-run of the UID 152 question on v1.0.265 (the DRIVING-PATH SERIES / MOVEMENT lines answer the Jan 2024 question) — and their decision on OR-20b / OR-20c. Then the report's §3 in order: **R-52** · **R-50** · **R-61** · **R-57** · **R-58** · **R-59** · **R-64** · **R-63** · **R-62** · **R-45** · then R-03 · R-04 · R-09 · R-13 · R-18 · R-21 · R-22 · R-32 · R-39. The design queue is 19 artboards. Other residuals: ADR-0488's / 0486's / 0485's / 0483's, `/settings` sideways scroll, OR-11b, OR-11d, the working-minute axis, the hint bubble, ADR-0484's in-grid rows.

**Review cover is still absent** — Codex quota EXHAUSTED; the mutation batteries and the full gate are all this repo gets.

# (prior) handoffs — archived

> The earlier handoff sections now live in **[HANDOFF-ARCHIVE.md](HANDOFF-ARCHIVE.md)** (newest-first,
> verbatim), and the full append-only per-session history is in **[SESSION-LOG.md](SESSION-LOG.md)**.
> Per ADR-0246 this file keeps ONLY the current STATUS section above, so the entire live handoff is
> small enough to read in full in one pass every session (and the SessionStart hook auto-injects it). When you
> write the next handoff, MOVE the current section to the top of `HANDOFF-ARCHIVE.md` (demote its
> heading to `(prior) Handoff`) and REPLACE the section above — do not stack another archived heading
> here. This single pointer is intentionally the only such heading in the file; the size guard enforces
> that.
