# Handoff — 2026-09-05 (b) (/onepager-compare SHIPPED — the operator's two-list One-Pager compare: matched on the only key the sheet carries, every move drawn and labelled in CALENDAR days, the same slide in the browser and in PowerPoint, on the Claude Design layout (ADR-0465); v1.0.238)

> ## STATUS (current) — **This session's draft PR (number in the SESSION-LOG) rides `claude/polaris-audit-resume-e9t5h1`, branched FRESH on `origin/main` @ `46a91fd2` (#638's docs-only merge). `main` is GREEN on the merged tree: run #1737 for the #637 squash `c0800680` concluded `success` at 01:47:27Z, installer-smoke #643 `success`, run #1740 for `46a91fd2` `success`. Banner **v1.0.238** (wheel + nine installers rebuilt after the last source edit; lockstep 68; full gate ruff (whole tree) · format · mypy --strict (163 files) · bandit (exit 0 after its one B101 finding — an `assert isinstance` in the page module — became a typed parameter) · node --check green; pytest **4,855 passed / 5 skipped (the standing env skips) / 0 failed in 33:28**, `tests/test_state_docs.py` run separately after the docs rotated (12 green)). QC-1/QC-2 bind every session — ADR-0393, pinned by `tests/test_standing_rules.py`.**
> Highest ADR **0465**. Campaign queue: **WP6b / WP7** next — the ledger TAIL (CPM-03/04 · MF-03/04/06..10 · MC-04..08 · IMP-02..06 · MAN-02/03 · JS-02..06 · TST-02/03) and RC-02's 3 never-2xx + 15 never-adverse routes, then `ai/txlog.py` first (Law 1) → WP8. The standing one-design-page-per-session ask was **MET** by a page built NEW on the design (/onepager-compare, the One-Pager family's artboard); next: **/performance (07 How we execute)**; /compare (10) is a feature change.
>
> ## What this session shipped
> **/onepager-compare (ADR-0465) — the operator's 2026-09-04 request, built as its own route, page module, painter and .pptx export; /onepager's r11 contract, ADR-0446 intake and painter untouched (`onepager.js` / `reports/onepager.py` md5 and the ADR-0446 twin deck's sha256 identical before and after).** The sheet decided the semantics: NO unique id → rows match on the normalised `(swimlane, item)` pair (`item_key`; the swimlane half is the layout's own ADR-0446 merge key), an unmatched row is **NEW** or **REMOVED by name**, a rename or a swimlane move is one of each (the engine counts the names seen on both sides under different swimlanes and SAYS so, never inferring the move), a name twice under one swimlane in EITHER sheet is a **collision** — reported by sheet and row, every row AMBIGUOUS, compared with nothing; NO calendar → every move is the FINISH delta and the START delta in **calendar days**, the unit on every figure (the CF-01 lesson applied at design time); which list is PRIOR is the operator's choice at a two-slot drop zone (a stray drop is refused with a hint; *Swap prior and current* is one click). The slide is ADR-0446's with the delta carried by SHAPE so it survives print, PowerPoint and all four themes: the current position solid, the prior a dashed ghost (a diamond ghost under a bar when a milestone became an activity), an arrow from the old finish to the new one with `+N cal d` / `−N cal d` on the label, `NEW` / `REMOVED` / `DUPLICATE NAME` tags, a per-swimlane summary column (non-zero counts, the worst slip named) and the encoding-first legend; the packer reserves ghost + shape + arrow + label + tag. The .pptx draws the same shapes natively (dashed `roundRect` / `diamond` ghosts with `noFill`, `triangle`-headed connectors — a pull-in `flipH`-ed with its head at the current finish — two-run labels, tag boxes); `_Slide.shape(dash=)`, `arrow`, `text_runs` added with every existing call byte-identical. The page wears the family (kicker · takeaway · notices · the slide panel · `cd-grid-12` summary panel beside a `cd-read` "How to read this" from the page's own `_EXPLAINERS` entry · `cd-grid-2` slots as `cd-block`s · a `cd-read` rules block); the takeaway quotes only cells the summary table's Total row and the rows table render (r10). The artboard ("Library One-Pager Timeline", `setScreen('op')`) was recovered by EXECUTING the canvas over loopback HTTP in four themes, zero page errors — it is the shape ADR-0446 built /onepager from.
> **Verification.** Engine/layout tests observed RED at import before the module existed; page and browser modules red before the route and painter existed. Green: engine + layout 37 · page 11 · browser 4 · the 17 guard modules the route touched 394 / 3 env skips. Mutation batteries on scratch copies of the FINAL code, each red BY NAME: engine/layout **15/15**, painter/CSS/pptx **12/12**, controls green. Four-theme render census (1440 px, the twin as PRIOR and a re-dated twin as CURRENT): `.panel` 2 · blocks 4 · grids 2 · ghosts 16 · arrows 5 · tags 2 · summary boxes 7 · ⤓ 2 · ⛶ 1 · ▦ 1 · zero page errors · nothing wider than the viewport, identical across themes; /onepager control unchanged; console + daylight renders viewed. Sweeps joined with DELIBERATE re-baselines: M1 census row · oracle labels +30 with the empty fingerprint `{200: 44, 400: 17, 422: 4}` · DD-ledger row (`onepager_compare.js`, 131) · r11 axis sites 29 → 30 (the compare caption WORDED differently on purpose — identical caption bytes hash identically and the freeze rejects a collision) · `VIEW_MODULES` + both whole-view-layer guards · the E501 exemption · shipped assets 68 → 69 · the LIBRARY rail pin · i18n ×8 × 4 (hand-translated, UNVERIFIED as idiomatic). PowerPoint itself was not run here (UNVERIFIED — the operator's first opening settles it).
>
> ## Operator-facing state
> Re-download once the PR merges; the banner must read **v1.0.238**. ASK (do not assume) — THREE rulings the page currently states as its rule: (1) a task that changed swimlane — keep as one REMOVED + one NEW (the page names the count), or treat as MOVED with its deltas? (2) a slip threshold below which an item reads unchanged — none today (any move of one calendar day is a change); want one, and its value? (3) should ⤓ POWERPOINT on the compare page ALSO ship the single-version slides (prior · current · compare = three slides), or stay one slide? PLUS the standing asks: (4) on /forecast with two or more files — do the chips and the re-homed Prev / Next / Auto-play move the drift chart, does the pill name the version you expect? (5) #635's: on /integrity with UID 152 as the target, does the working-day count read right against the file's calendar? (6) #631's / #630's stand (the parent-folder question on `/`; /trend's chips; I-01 · T-01 · the /analysis lag). UNKNOWABLE and closed: the blank-header banner, the 08-26 incident.
>
> ## Traps paid for this session, by name
> two charts that caption identically HASH identically — a content-keyed freeze needs distinct content per site, so a new painter's caption must say what its sibling's does not · a "missed" mutation is a question about the FIXTURE first — the ghost-packing test's newcomer was pushed down by the label anyway; move the data to where only the mutant's path differs · a counting pin must count what it NAMES — `<div class=panel` matched `panel-head`, `<tr><td>` matched a second table, `.opc-summary` matched the table beside the SVG: three superset counts in one session · a module-shared browser session is STATE — a helper that loads "if empty" inherits what the previous test left; load unconditionally · the unit is part of the provenance at DESIGN time too — calendar days because the sheet has no calendar · the installer script refuses a graft-boundary MPXJ ref and `--deepen=300` moved it off the boundary this time (check the resolved ref, `42d92dc9`) · `python -m build` is NOT in the container — `pip install build` before the wheel · state the operator's unanswered rulings ON THE PAGE as the current rule rather than assume them silently · the canvas's One-Pager artboard IS the page ADR-0446 built — executing it settled that in four themes.
>
> ## Next — campaign queue
> Branch FRESH from `origin/main` once this PR merges (`git fetch --prune origin && git checkout -B <branch> origin/main`). **WP6b** (the ledger tail, above) → **WP7** (`ai/txlog.py` first — Law 1; RC-02's routes) → **WP8**; the design page owed each session: **/performance (07 How we execute)** next (recover artboard 07 by EXECUTING the canvas over loopback HTTP — `npm pack react@18.3.1 react-dom@18.3.1 @babel/standalone@7.29.0`, `support.js` patched to `./pkgs/…` with the three `_SRI` constants blanked, seeds `sfredux-screen=<key>` from the canvas's `setScreen('…')` calls, `sfredux-guided=1`, `sfops-boot.skipNext=true`, `sfredux-theme`; `python -m http.server --bind 127.0.0.1`). The compare page's three rulings, once the operator answers, are small edits: `item_key` / the ADDED-REMOVED pairing (1), a threshold constant in `_status` (2), a three-slide `render_onepager_compare_pptx` (3). CI-04 (the /driving-path header-row equality race on #632's docs-only diff) is the remaining CI candidate. Observed, not fixed blind: `evm.py`'s `actual_cost or 0.0` ACWP on a mixed population (an EVM parity row of its own).

# (prior) handoffs — archived

> The earlier handoff sections now live in **[HANDOFF-ARCHIVE.md](HANDOFF-ARCHIVE.md)** (newest-first,
> verbatim), and the full append-only per-session history is in **[SESSION-LOG.md](SESSION-LOG.md)**.
> Per ADR-0246 this file keeps ONLY the current STATUS section above, so the entire live handoff is
> small enough to read in full in one pass every session (and the SessionStart hook auto-injects it). When you
> write the next handoff, MOVE the current section to the top of `HANDOFF-ARCHIVE.md` (demote its
> heading to `(prior) Handoff`) and REPLACE the section above — do not stack another archived heading
> here. This single pointer is intentionally the only such heading in the file; the size guard enforces
> that.
