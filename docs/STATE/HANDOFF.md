# Handoff — 2026-09-03 (e) (the operator's evening batch MEASURED: I-01 root-caused to session population and disclosed on the page, T-01 measured working and pinned, /analysis scroll re-aim incremental + link overlay reused — ADR-0457 + ADR-0458, v1.0.234)

> ## STATUS (current) — **On branch `claude/polaris-batch-0903e-integrity-timescale` (from `origin/main` @ `0f098cce`, the #628 merge, with the docs-only #629 branch merged IN so the ledger/kickoff build on its "Operator answers + new batch" section — if #629 lands first the four state docs merge-resolve trivially; never stack on that branch). Draft PR **#630** (the docs-only #629 MERGED as `main` @ `f2be8a0` mid-session and is merge-resolved into this branch). `main`'s run #1706 for `0f098cce` was STILL `in_progress` at 23:50Z (an hour after the merge — the 3.13 job runs ~53 min); read its conclusion before trusting `main`. QC-1/QC-2 bind every session — ADR-0393, pinned by `tests/test_standing_rules.py`.**
> Highest ADR **0458**; version **1.0.234**; wheel + nine installers rebuilt in lockstep as the LAST step (the operator re-downloads from `main` once the PR merges — banner **v1.0.234**). Campaign queue: **WP5** (BOTH folder-ask builds) is next once this PR merges — branch fresh from `origin/main`, open a new draft PR — plus ONE design page per session (candidates /trend · /forecast · /performance; ADR-0456's method, DESIGN-SYSTEM §9). This session shipped no design page: the operator's batch was three measured defects and took the budget.
>
> ## What this session settled (ledger section "Operator answers + new batch", rows I-01 · T-01 · (c))
> **I-01 (ADR-0457) — the detectors did NOT change; the page's SENTENCES were wrong for two session states.** Differential of the SAME inputs (golden trio, Project2→5, all ten TP4 pairs) through `/integrity?a=&b=` and `detect_manipulation` on worktrees at v1.0.221 / v1.0.229 / this tree → rows IDENTICAL by name (4/7/9 · 5 · 0·0·1·1·0·2·2·2·2·0); `manipulation.py`/`change_effects.py`/`cpm.py`/importers/model byte-identical since v1.0.221; zero page errors, picker/drill/diagram/export work. What empties the page: (1) a second Project (two dropped folders, or loose files with different document Titles) — the page said "Load at least two versions" while two were loaded one switch away; (2) a reduce filter on an UNMAPPED role name (ADR-0450 matches nothing) — the page asserted "No manipulation-pattern findings" over an EMPTY population. Fixed red-first: the page names its Project population (holds k of N files · other Projects by name · switch form back to /integrity), states the in-scope counts under a reduce filter, and an empty side reads "nothing to compare". 6 tests, 4 observed red on a pristine worktree.
> **T-01 (ADR-0457) — MEASURED WORKING, NOT REPRODUCED, PINNED.** Show = Two tiers committed through the dialog on /analysis (TP4 ×5, TP5), /path, /driving-path, /evolution, /sra; at the opening zoom, 3× in, 6× out, Fit and after a reload every scale rendered EXACTLY two `.g-tier` rows, absolute at y 0/18, scale 36 px, `config().show === 2`, `{"show":2}` persisted, zero errors. The ladder, a string `show`, Fit and a stale `timescale.js` (assets are `no-cache` + ETag) are refuted. Two M2 pins; mutation (three tiers for show=2) → both red.
> **(c) (ADR-0458) — measured, root-caused by subtraction, improved; the residue is named.** Quiet-box frame times at 2,280 rows × two files: wheel p50 33 → 17 ms, p95 183 → 83 ms (300-px steps); programmatic 400-px re-aims p50 100 → 83, p95 183 → 150. Before: every re-aim re-sorted 2,280 rows, emptied the tbody, repainted ~115 rows and re-created a 36,729 × 40,937-px link overlay holding EVERY relationship (links ON p50 67–83 → OFF 17). Now: the re-aim touches only the rows that enter/leave (`reaimWindow`), pins entered rows by copying a survivor's sticky offsets (no forced layout), re-trues the spacers to the rendered pitch at the first scroll (the initial paint assumed 18 px; TP5 renders 16.18 — the extent was 11 % tall), and the overlay is ONE reused node drawing only window-visible links. Residue: Chromium's layout of ~700–800 sticky frozen cells — stripping them is the only subtraction reaching p95 50 ms; a separate frozen pane is the next candidate, priced not built.
>
> ## Verification, in the shape QC-1 wants
> Red-first on a pristine worktree (`0f098cce`): I-01's four disclosure tests; the re-aim identity pin (`same=False`); the overlay pin (570 paths on a 605-row file). Mutations on scratch copies of the FINAL code, each reddening its pin by name: `visibleTiers` → three tiers (T-01: 2/2) · re-aim → full repaint (identity) · `freezeLike` removed (identity's frozen-column check) · every link drawn (overlay) · overlay re-created (overlay) · `trueSpacers` removed (contiguity). Green: the six /integrity TestClient modules (40), the windowing module (6/6), the Gantt/timescale browser modules, the r11 byte pins UNCHANGED (`gantt.js` untouched — a first draft touched it and was reverted). Full suite + parity in the SESSION-LOG.
>
> ## Operator-facing state
> Re-download once the PR merges; the banner must read **v1.0.234**. ASK (do not assume): I-01 — which finding disappeared, on which two files, and were the two files dropped as separate folders or do they carry different document Titles (the page now says so itself); T-01 — which page, which zoom, and a screenshot of the dialog beside the header (nothing measured wrong on ≥ v1.0.233); (c) — whether the residual lag is gone on their file, else a Performance-panel recording. Still owed from before: (d) SRA grid checks, (e) /cei chips. UNKNOWABLE, closed: the blank-header banner, the 08-26 incident; CLOSED: the One-Pager opened in PowerPoint.
>
> ## Traps paid for this session, by name
> a differential that comes back IDENTICAL is the finding — it earns the right to call the engine innocent · the page's own sentences on a testimony surface are the defect even when the banners disclose the state · frame times measured while a battery runs are noise (p95 250 vs 83 on the same tree) — quiet box, and say so · isolate native cost by SUBTRACTION in the live page (links off · stub · strip), the profile's `(program)` names nothing · a `bounding_box()` pointer can sit below the viewport — assert `scrollTop` moved before believing 16.7 ms · `pkill -f <pattern>` kills the shell that runs it (exit 144) — sentinel files, never process greps · an estimate baked in before the table is connected survives until something re-measures it (18 vs 16.18 px) · a slice oracle needs the whole population (take order from the page's JSON, guard against the opening window) · a first draft that re-baselines a byte pin should first ask whether the change belongs in that file at all (`gantt.js` reverted; `freezeLike` copies from a survivor instead).
>
> ## Next — campaign queue
> **WP5** (BOTH folder-ask builds — the three 2026-08-21 folder-gesture facts govern, do NOT re-derive) → **WP6** (CPM-01 · CPM-02 · MC-02 · MC-03 · MAN-01 · REC-02; RC-02's endpoints are WP6/WP7 rows) → **WP7** (`ai/txlog.py` first — Law 1) → **WP8**. CI-03 (the caption sweep's `console@0.9` cell, two hits on untouched pages) is queued for its own root-cause PR. Design migration, one page per session: /trend (05), /forecast (09), /performance (07). Grid family candidate from ADR-0458: a separate frozen pane for /analysis — price it as a design change.

# (prior) handoffs — archived

> The earlier handoff sections now live in **[HANDOFF-ARCHIVE.md](HANDOFF-ARCHIVE.md)** (newest-first,
> verbatim), and the full append-only per-session history is in **[SESSION-LOG.md](SESSION-LOG.md)**.
> Per ADR-0246 this file keeps ONLY the current STATUS section above, so the entire live handoff is
> small enough to read in full in one pass every session (and the SessionStart hook auto-injects it). When you
> write the next handoff, MOVE the current section to the top of `HANDOFF-ARCHIVE.md` (demote its
> heading to `(prior) Handoff`) and REPLACE the section above — do not stack another archived heading
> here. This single pointer is intentionally the only such heading in the file; the size guard enforces
> that.
