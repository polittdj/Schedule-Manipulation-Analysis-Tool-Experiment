# Handoff — 2026-09-07 (b) (the plan-forward's R-01 CLOSED and every metric family run against every Fuse workbook in the repo — the multi-project Metric History + EVM ribbon oracle (ADR-0473): the §C compliance block on the Bible's CURRENT dates, MSPDI currency ÷ 100, time-phased BCWS, whole-day Negative Float, CPI's blank actuals disclosed; v1.0.243)

> ## STATUS (current) — **This session's branch is `claude/polaris-audit-plan-forward-3vp28m` (the harness's designated branch), branched FRESH on `origin/main` @ `0408c56` (the #646 squash, docs-only). #645 and #646 were MERGED at session start; no PR was open. This session's draft PR: number in the SESSION-LOG follow-up. Banner **v1.0.243** (wheel + nine installers rebuilt after the last source edit). Highest ADR **0473**. Statics on this tree: ruff check + format (whole tree) · mypy --strict 163 files · bandit exit 0. Pytest re-read on this tree: `pytest -m parity` **72 passed** before the change (10:49); the NEW oracle module `tests/parity/test_fuse_metric_history_oracle.py` **15 passed** and **12 failed / 5 passed by name against a `git archive HEAD src` scratch copy** (red first — the five that pass on both are the oracles where the two date bases coincide); `test_evm.py` + the module **34 passed**; the engine / importer / battery / report-guard set green after the reverts; the full-suite figure is in the SESSION-LOG follow-up. QC-1/QC-2 bind every session — ADR-0393, pinned by `tests/test_standing_rules.py`.**
>
> ## What this session shipped (ADR-0473)
> The operator's instruction: answer the five questions yourself and validate every metric against every reference file. Three Fuse workbooks no test had read — `AlltheProjects - Metric History Report.xlsx` (twelve projects, EVM1/EVM2 among them), the Large Test File / File2 Metric History, the Hard_File update-vs-update History and ribbons (BCWS/BCWP/ACWP/BAC/SPI/CPI/TCPI) — plus the Detailed Metric Reports' per-activity X-marks. A std-lib harness diffed 16 snapshots label by label; every mismatch was root-caused on the Bible's formula + inclusion flags and the X-marks before a line changed. **Fixed, red-first, oracle-exact:** the §C compliance block scores the Bible's CURRENT `Finish`/`Start` (EVM1 Completed On Time 5 where the tool read 0; Large Test File2 159 vs 117; exact on 12 oracles, ten counts + two ratios each) · MSPDI currency is in HUNDREDTHS (Hard_File_updated BAC 133,400 / ACWP 20,800 where the tool summed 13,340,000 / 2,080,000 — the task-info drawer and the JCL showed 100× figures) · BCWS accrues linearly over the baseline span (64,240 / 110,440 exact; 16,150 vs 16,000 documented) · the ribbon Negative Float classifies the stored slack in whole days (Large Test File2 122, was 123) · **R-01 CLOSED**: CPI 0.81 / 0.78 exact under the Bible's blank-as-zero, and the started, budgeted activities with no actual cost now ride CPI/TCPI as count/population/offenders with a sentence on /evm. **Reverted the same hour, each red on a ribbon oracle or a golden:** three "mismatches" were the library's SAME-NAMED metrics with different inclusion sets per workbook section (Insufficient Detail™ 43 vs 22 · Merge Hotspot 156 vs 125 · Lags 5 vs 2) — the engine implements the ribbon tiles and stays exact on them (R-50 registers the variants). The parity report's DCMA08 row now names the row the engine reproduces (High BASELINE Duration, 87/86 UID-exact), not High Planned (124).
>
> ## The five questions — decided (ADR-0473; the report's §5 carries the paragraph)
> (a) the pre-consent catalog probe stays (R-12 CLOSED) · (b) the P6 planned-date basis stays with its disclosure (R-02 HELD on a Fuse-on-XER export) · (c) the three rulings stand; **the `.pptx` — one-pager and compare — does not load in LibreOffice 7 headless**, PowerPoint UNVERIFIED (R-31 CLOSED, R-52 OPEN S) · (d) on the repo's Large Test File pair UID 152 does not move (0 wd / 0 cd, five-day calendar); the operator's 2027-dated pair is not in the repo (R-30 HELD) · (e) chips and the folder ask pinned by 24 tests on two-file corpora; T-01 / I-01 / the lag are the operator's box (R-23/24/29 HELD).
>
> ## Measured, deliberately left (rows R-44..R-54)
> Hard_File's CPM finish is **42 days** later than MS Project's stored finish — resources on 16-hour / 24-hour calendars the base CPM does not model (R-44, L) · updated3's BAC/BCWP vs Fuse and Fuse's ACWP-to-time-now (R-45) · the BCWS +150 (R-46) · SPI(t)–Acumen 8.24 vs 8.22 (R-47) · the DCMA08 tile's IncludeComplete=true (R-48) · MPXJ omits a ZERO TotalSlack — 62 of Fuse's 66 zero-float activities carry none (R-49) · Estimated Duration 63 vs 47/41 (R-51) · TP3's ribbon 8 captured on a differently-progressed `.mpp` (R-53) · the `ssi_uid152` fixture is the underscore sibling `.mpp` (31 vs 41 negative float; R-54, CLOSED).
>
> ## Traps paid for this session, by name
> The library carries the SAME display name with DIFFERENT inclusion sets per workbook section — compare a Fuse figure only to the tile its own section carries, and prove the mapping on a ribbon oracle before changing a rule · a harness's "first occurrence of the label" collides with same-named rows (Started Late at row 398 vs 699) · the compliance ratio at 2 dp is derived from the counts, never from the 1-dp percentage (8/13 → 0.62, not 0.61) · MPXJ writes no zero (Cost, ActualCost, TotalSlack all omitted at 0) · a fixture is not the file Fuse scored until its provenance is checked (`Large_Test_File.mpp` ≠ `Large Test File.mpp`) · a 0.01 tolerance needs 0.0101 against float error · `git archive HEAD src` on `PYTHONPATH` is the cheapest pristine tree for a red-first proof.
>
> ## Next — campaign queue
> Branch FRESH from `origin/main` once this PR is merged. The report's §3 in order: **R-44** (the assignment-calendar CPM pass, L — Hard_File's stored finish is the oracle) · R-49 (the zero-slack inference, SSI gates as arbiter) · R-46 · R-47 · R-52 (the `.pptx` package) · then R-03 / R-04 / R-09 / R-13 / R-18 / R-20 / R-21 / R-22 / R-32 / R-39 as before. PLUS the design page owed each session (/standards or /scorecards). The harness that found this session's rows lives in the ADR as a method; committing it as a tool is priced with R-50.

# (prior) handoffs — archived

> The earlier handoff sections now live in **[HANDOFF-ARCHIVE.md](HANDOFF-ARCHIVE.md)** (newest-first,
> verbatim), and the full append-only per-session history is in **[SESSION-LOG.md](SESSION-LOG.md)**.
> Per ADR-0246 this file keeps ONLY the current STATUS section above, so the entire live handoff is
> small enough to read in full in one pass every session (and the SessionStart hook auto-injects it). When you
> write the next handoff, MOVE the current section to the top of `HANDOFF-ARCHIVE.md` (demote its
> heading to `(prior) Handoff`) and REPLACE the section above — do not stack another archived heading
> here. This single pointer is intentionally the only such heading in the file; the size guard enforces
> that.
