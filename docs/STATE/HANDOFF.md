# Handoff — 2026-09-06 (c) (WP7 COMPLETE — the AI transaction log under Law 1 by execution and the 21 never-adverse routes driven adversely (ADR-0469); /card is the SEVENTH page on the Claude Design layout (ADR-0470); #641's content merged onto main by MERGE after #640; v1.0.241)

> ## STATUS (current) — **This session's branch is `claude/polaris-audit-resume-0gv4bo` (the harness's designated branch), checked out on `origin/main` @ `5b219952` — the #640 squash, merged by the operator at 06:22:01Z (ADR-0466, v1.0.239; `main`'s OWN CI run **#1751** for it concluded `success` at 07:33Z; installer-smoke #651 `success` — `main` is green on the merged tree). Its draft PR is **#643**: the MERGE of #641's head `d61f6395` (WP6b ADR-0467 + /performance ADR-0468; 8/8 green on its final head, marked READY, but `dirty` against `main` in 15 files because it forked from the #639 squash beside #640) onto `main` — resolved by MERGE, never by renumbering (the installers rebuilt from the merged `src`, the five state docs spliced, the archived 2026-09-05 (b) section = `main`'s final version with its ADR-0466 addendum) — PLUS this session's WP7 and /card. **The operator merges #643 and closes #641** (its two commits are the merge's second parent; nothing is lost), or says the word and the identical merge commit `44107596` is fast-forwarded onto #641's branch. The sibling docs-only draft **#642** (`claude/polaris-audit-resume-e9t5h1`, the record of #640's merge) needs a `main` merge whichever lands second. Banner **v1.0.241** (wheel + nine installers rebuilt after the last source edit). Statics: `/usr/local/bin/ruff` 0.16.6 (the PATH `ruff` is a shadowed 0.15.8) check + format (whole tree) · mypy --strict (163 files) · bandit exit 0 · `node --check` 63/63. Pytest: the two WP7 modules **72** (8 + 64) · the card layout 5 with its 151-test guard set · the neighbouring suites **537** · the merge worktree's full suite **4,921 passed / 5 skipped / 0 failed in 50:54**; the final tree's (a worktree of `471b72fa`) **4,997 passed / 5 skipped / 1 failed in 50:34** — the one failure the `&mdash;` sentinel guard fixed in `d9749cd8`. #643's first head `471b72fa` went RED on `floor` (the `&mdash;` sentinel guard — a one-token fix, rebuilt and pushed; see the SESSION-LOG follow-up); read the checks on the FINAL head. QC-1/QC-2 bind every session — ADR-0393, pinned by `tests/test_standing_rules.py`.**
> **23:14Z addendum — #643 MERGED** (`main` @ `dee28ab3`, tree-identical to its green head `72cf8e9b`; `main`'s own CI **#1759** / installer-smoke #657 `in_progress` at this record — read #1759 first). Branch restarted on `dee28ab3`. **#641 → CLOSE unmerged** (its content landed via #643). **#642** (marked ready, conflicting on the three state docs) is merged INTO this branch's docs-only draft PR — merge that PR and close #642. Then the next session branches FRESH.
> Highest ADR **0470**. Campaign queue: **WP8** next — the consolidated report + repair roadmap ordered by testimony risk (every row's verdict and measurement is in `docs/STATE/AUDIT-2026-08-27.md`; WP8 orders and prices them). The standing one-design-page-per-session ask was **MET** (/card, the seventh); **/compare (10) is PRICED — a feature change (M–L)**: its artboard needs the cross-pair evidence ledger ("All five pairs × every edit") and a slip decomposition update by update that no page computes; next candidates by artboard size against an extracted page module: **/wbs** (Library WBS Rollup, 6 KB / 8 headings), /standards, /scorecards.
>
> ## What this session shipped
> **The merge (steward §1, "two PRs in flight").** `git merge-tree --write-tree` named the 15 conflicts; a resolver script asserted every splice; `pyproject` 1.0.240 kept (then bumped here); the nine installers rebuilt from the merged `src` (MPXJ `42d92dc9`, off the graft boundary after `--deepen=300`); SESSION-LOG chronological · the ledger in ADR order · LESSONS newest first · HANDOFF-ARCHIVE's 09-05 (b) section replaced by `main`'s final version. Gate on the merged tree before the push: statics · collect-only 4,926 · 606 + 38 targeted green.
> **WP7 (ADR-0469).** `ai/txlog.py` first: through the REAL `urllib` opener a loopback peer's malformed status line landed VERBATIM in the `generate.done` record (`BadStatusLine(line)` → `str(exc)`) — a misbehaving gateway's reply, model output derived from the prompt, had a path into the audit log → `txlog.error_summary`, a closed vocabulary (status · three fixed transport reasons · class name). Pinned: the ten-key record shape + the bounded error; no transmission before consent on /settings (the operator-initiated catalog probe is body-less, key-bearing, recorded — design, DISCLOSED, an operator ask); the transport inventory by census (six modules); retention under a scratch HOME through the quit-path clear and the wipe; the best-effort completion record; the retention sentence on /settings. RC-02's 15 POSTs + 6 exports driven adversely: `/sra/branch` 500 on `--5` (audit L5's pattern alive on the sibling of the route it fixed) · `/sra/jcl-config` storing a non-finite cost target · `/sra/risk` storing garbage as a `(0, 0, 0)` override — all three CONFIRMED-FIXED red-first; the other 18 pinned fail-soft; 10-mutation battery 10/10 red by name; neighbours 537.
> **/card (ADR-0470).** The cursor is navigation: one link chip per loaded version of the active project, the open one on, the `vN · file · DD` pill, only with two or more versions; both panels and every figure verbatim; the mock's ⤓ EXCEL, verdict and Law-1 footnote NOT ported (named). Four-theme render census identical on `.panel` (3), widest 1440, zero page errors, the click's effect measured. The artboard was read from the canvas markup, not executed — UNVERIFIED by execution, named.
>
> ## Operator-facing state
> Re-download once #643 merges; the banner must read **v1.0.241**. ASK (do not assume): (1) **NEW — WP7 TX-03:** keep the model dropdown's catalog probe firing BEFORE the acknowledgment (a body-less `GET /v1/models` that carries the session's key and is recorded), or gate it behind the acknowledgment too? (2) IMP-05 (ADR-0467): on a P6 XER should HMI/BEI keep reading the PLANNED dates as the baseline, or read N/A until a baseline project is exported? (3) the /onepager-compare rulings (swimlane move · threshold · three slides) and whether its .pptx opens in PowerPoint; (4) #635's /integrity working-day move for UID 152; (5) the standing asks (/forecast's chips with two files; the parent-folder question on `/`; /trend's chips; T-01 · I-01 · the /analysis lag). UNKNOWABLE and closed: the blank-header banner, the 08-26 incident.
>
> ## Traps paid for this session, by name
> `str(exc)` is not a sanitizer — `BadStatusLine` carries the peer's reply line; a durable record's error field gets a closed vocabulary · a boundary rule is a census: grep every `float(` / `int(` outside the helper, and the SIBLING of a fixed route first · garbage that parses to a default is a fabricated figure — refuse a supplied field by name, default only a blank one · `git merge-tree` old-style output prefixes lines, so `grep '^<<<<<<<'` reads 0 on a 15-conflict merge — use `--write-tree` · the harness's "never push to another branch" beats the steward's "second lander re-merges": merge on the designated branch, offer the fast-forward · a page's `.panel` count includes the chrome's Ask panel; a panel head prints the project TITLE · Excel truncates a sheet name to 31 characters — assert the row · `ls` the test path before filtering its run (a guessed module made a whole run "pass" at EXIT=4) · the census `_FAMILY` is a raw pattern · a mock's Law-1 footnote is never a static sentence (ADR-0396) · the PATH `ruff` is 0.15.8; CI's is 0.16.6 at `/usr/local/bin/ruff` · `"&mdash;"` is never a VALUE sentinel (the view-layer guard reads every module's source and printed all of `settings.py` in the floor job's log) — run the source-reading guards before a UI push.
>
> ## Next — campaign queue
> Branch FRESH from `origin/main` once #643 (and #641 / #642, in either order) are merged or closed (`git fetch --prune origin && git checkout -B <branch> origin/main`). **WP8** — the consolidated report + roadmap ordered by testimony risk, from the ledger's rows. PLUS the design page owed each session: **/wbs** next by cost (else /standards, /scorecards); /compare stays a feature. Observed, not fixed blind (own rows): `evm.py`'s `actual_cost or 0.0` ACWP · `path_evolution`'s pure-logic critical list · MF-08's residual (`dcma14`'s float-band integer roundings; 176 `round(` sites outside `engine/metrics`) · JS-05's 56 dead CSS tokens · the other browser modules' EXPRESSION-string waits (ADR-0466 §3) · IMP-05's stricter option · the pre-consent catalog probe (TX-03) · the `workbench` 400 vs 422 · the record's missing schema version / correlation (WP8's design question). CI-04 (the /driving-path header-row equality race) is the remaining CI candidate.

# (prior) handoffs — archived

> The earlier handoff sections now live in **[HANDOFF-ARCHIVE.md](HANDOFF-ARCHIVE.md)** (newest-first,
> verbatim), and the full append-only per-session history is in **[SESSION-LOG.md](SESSION-LOG.md)**.
> Per ADR-0246 this file keeps ONLY the current STATUS section above, so the entire live handoff is
> small enough to read in full in one pass every session (and the SessionStart hook auto-injects it). When you
> write the next handoff, MOVE the current section to the top of `HANDOFF-ARCHIVE.md` (demote its
> heading to `(prior) Handoff`) and REPLACE the section above — do not stack another archived heading
> here. This single pointer is intentionally the only such heading in the file; the size guard enforces
> that.
