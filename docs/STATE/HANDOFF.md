# Handoff — 2026-09-07 (d) (the design page the operator was owed TWICE, delivered first and alone: /standards wears the Control "Standards and Execution Indices" artboard — the selector row ported as navigation that HIDES NOTHING, and the mock's `· 16 / · 14 / · 10` measured to BE the page's own live counts (ADR-0475); v1.0.246)

> ## STATUS (current) — **This session's branch is `claude/handoff-next-session-q74ju8` (the harness's designated branch), started FRESH on `origin/main` @ `5f34c2a8` (the #649 squash) — the tree was already identical to it at session start, nothing to restart. The kickoff's first order was discharged before any work: **`main`'s own runs for the #649 squash are GREEN** — CI **#1786** (34156160638) and installer-smoke **#674** (34156160822), both `success` on `5f34c2a8`; there was no red cell to adjudicate against tree hashes. PR **#650** (the prior session's docs-only merge record) was OPEN at session start and the operator **MERGED it at ~22:18Z while this unit was in flight** — `main` moved `5f34c2a8` → **`fc2ddf7c`**, which turned #651 `mergeable_state: dirty`. Resolved by MERGING `origin/main` into this branch (never a rebase or force-push): the three conflicts were all state docs — `SESSION-LOG.md` kept **both** sides (it is append-only; #650's three (c) follow-ups precede this session's (d) entry), `HANDOFF.md` and the kickoff took this session's newer text, and the ARCHIVE's (c) section was replaced with #650's FINAL wording so its merge record survives. #650 is docs-only, so **no source, wheel or installer is affected** by the new base. This unit is the DESIGN PAGE, owed twice and therefore shipped FIRST and as its own unit (ADR-0475, the ninth page, the first **Control** screen): `/standards` on the executed `setScreen('sd')` artboard. Highest ADR **0475**. Statics on this tree: `ruff check .` + `ruff format --check .` (whole tree, 647 files) · mypy --strict 163 files · bandit exit 0. Red-first 11 failed / 3 passed; the layout module 15 green; neighbouring guard sets 139 + 216 (3 standing skips); **mutation battery 13/13 RED BY NAME**; four-theme render census pristine→patched identical on every structural key, moved only on the design's. The full-suite figure and the draft PR number are in the SESSION-LOG follow-up. QC-1/QC-2 bind every session — ADR-0393, pinned by `tests/test_standing_rules.py`.**
>
> ## What this session shipped (ADR-0475, the design page owed twice)
> The artboard was **executed**, never read: ADR-0464's recipe (`npm pack react@18.3.1 react-dom@18.3.1 @babel/standalone@7.29.0`, `support.js` repointed at the three local files with its SRI constants blanked, served over loopback, `sfredux-screen=sd`) rendered `section[data-screen-label="Control Standards and Execution Indices"]` in console, daylight, apollo and jarvis with **zero page errors**, and each of its three family buttons was CLICKED to learn that the mock swaps one family's table for another's. **The finding that made this a port and not an invention:** the artboard's counts `· 16 / · 14 / · 10` are the page's OWN live counts on the golden Project2+Project5 pair (DCMA 16 rows, Fuse 14, SEM 10) — measured before a line was written. **Ported:** the selector row as `.viz-controls.cd-cursor#standardsFamilies` — one `<a class="cd-chip">` per family carrying its live count, anchored to that family's panel (`#std-dcma`/`#std-fuse`/`#std-sem`), a pill (`40 metrics · Project5.mspdi.xml`) and a `cd-note`; the per-family `.sf-take` the mock gives all three where the page had one, in the DCMA panel's EXISTING idiom; the REF column carrying the ENGINE's `metric_id`; the mock's footnote as the page's own disclosure. **Refused, each named in the ADR:** the tab-HIDING (a family behind a click is one a reviewer cannot Ctrl-F, print or read beside its neighbours — the `cd-note` says "nothing is hidden behind it"); `⤓ EXCEL · ALL FAMILIES` (no export covers Fuse or SEM — ADR-0327's residual; the label would lie); the mock's `INFO` status (the engine's vocabulary is PASS/FAIL/NA); the mock's `01a`/`01b` split of DCMA-01 (the engine scores ONE `DCMA01`); the Continue footer (the chrome's spine).
>
> ## The battery found a weak check of MY OWN — recorded, because that is the point
> The first mutation, "hardcode the DCMA count to the mock's 16", came back **GREEN**. It is not a passing test: the DCMA family always scores exactly 16 checks and SEM always 10, so **no fixture in the repo can distinguish a hardcoded count from a measured one** — the mutation is behaviourally equivalent and the check was theatre. The **Fuse** family does vary (the CEI rows need a prior version: **9 rows with one file loaded, 14 with two**), so the single-version test was strengthened to pin `Acumen-Fuse · 9` and `35 metrics` against the pair's `14` / `40`, and the battery re-aimed there — 13/13 red after. The DCMA and SEM counts stay **UNVERIFIABLE as computed-vs-constant** by any available fixture, said out loud rather than papered over.
>
> ## Measured, deliberately left
> `document.scrollingElement.scrollWidth` 1719 → 1734 with the strip — the KNOWN chrome-wide UI-03 condition (**R-20**, the hidden `[data-sf-hint]::after` box on a right-aligned host), already 1719 on the pristine page. ADR-0471 recorded the **identical** 1719 → 1734 for the identical strip on /wbs, which corroborates the attribution; the fix is R-20's own priced row (a chrome-wide change pinned across 34 page states), not a page migration's.
>
> ## Traps paid for this session, by name
> **A mutation that comes back GREEN is a finding about the TEST, not a pass** — re-aim the pin at a value that varies, or record it unverifiable · **the container's pip playwright expects a browser build the container does not vendor** (`chromium_headless_shell-1234` vs the vendored `-1194`): the repo already owns the cure, `tests/web/browser_chrome.py::chrome_kwargs()` — use it in every scratch probe instead of `playwright install` · **two of my own probe strings were wrong before the tree was** — an assumed source citation (`Acumen Fuse v8.11.0`, really `Fuse v8.11.0 Metric History parity.`) and an arithmetic slip in a pill total (39 for 16+9+10=35): verify a literal against the RENDER, never against memory · **a helper added to an extracted page module is red on the monolith split contract until `app.py` carries its `X as X` re-export** — the seam catches it, as designed · **the token-guardian script copied into the repo root breaks `ruff check .`** (the gate is whole-tree): keep it in the scratchpad · a design mock's status word, decomposition and export label are all claims about the ENGINE, and each must be checked against the engine before it is drawn.
>
> ## Next — campaign queue
> Read #650's state (docs-only, the operator's to merge) and this unit's own draft PR on its FINAL head. Then the report's §3 in order: **R-55** FIRST (progress semantics: pin started work AT its actual start and keep completed work at its actual dates — updated3 reads −6 d / updated3_24hr +17 d today, a scratch-copy pin read −13 d / −2 d; the SSI driving-slack goldens and ADR-0391's TP4 v5 / TP1 pins are the arbiter, `pytest -m parity` unmoved) · R-49 (MPXJ omits a ZERO `TotalSlack`) · R-46 · R-47 · R-52 · R-50 · R-57 / R-58 / R-59 · then R-03 / R-04 / R-09 / R-13 / R-18 / R-20 / R-21 / R-22 / R-32 / R-39. PLUS the design page owed each session — **now current, not owed**: the next Control screen is **/scorecards** (`setScreen('sk')`), next by cost, with 21 artboards remaining (report §6).

# (prior) handoffs — archived

> The earlier handoff sections now live in **[HANDOFF-ARCHIVE.md](HANDOFF-ARCHIVE.md)** (newest-first,
> verbatim), and the full append-only per-session history is in **[SESSION-LOG.md](SESSION-LOG.md)**.
> Per ADR-0246 this file keeps ONLY the current STATUS section above, so the entire live handoff is
> small enough to read in full in one pass every session (and the SessionStart hook auto-injects it). When you
> write the next handoff, MOVE the current section to the top of `HANDOFF-ARCHIVE.md` (demote its
> heading to `(prior) Handoff`) and REPLACE the section above — do not stack another archived heading
> here. This single pointer is intentionally the only such heading in the file; the size guard enforces
> that.
