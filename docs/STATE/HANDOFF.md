# Handoff — 2026-09-15 (OR-17 SHIPPED IN PART (ADR-0493): the installed build is v1.0.255/256 — measured from the screenshot's own wording, NOT v1.0.257+, so nothing OR-16 shipped had reached the operator; the gateway key path proven on the wire and the DPAPI branch pinned on real Windows in CI; a Save now says what it did with each credential and the page states its version; v1.0.261)

STATUS (current) — `main` @ **`ec3adf6c`** (#675 squash-merged 2026-09-15 12:09Z, docs only; its own CI run 1862 was IN PROGRESS at this close — read it first; #674's runs for `9a9e400e` read **CI 1860 success · installer-smoke 723 success**, measured this session). This branch `claude/dazzling-ptolemy-i1zig0` was restarted on `ec3adf6c` and then **fast-forwarded to `04a2f20b`, the head of the previous session's still-open docs-only draft #676** (the OR-17 registration) — so this PR CONTAINS #676; the operator may merge either first (mine supersedes; if #676 lands first, this branch merge-resolves on the state docs). **One unit landed: OR-17, ADR-0493, version 1.0.261.** **(1) The installed build, measured.** The kickoff said "establish the version from the banner"; the page carried no version (only `?v=` on static URLs). The screenshot's own strings settled it: the placeholder *"a key is saved — leave blank to keep it"* and the banner *"could not reach … requires authentication"* exist at v1.0.255 / 0.256 and not at 0.257 (`git show` on the three release commits: 0.257 reads *"— N characters"* / *"refused the credential the tool sent"*; `data-backend-only` 0 / 0 / 5). **The operator never installed v1.0.257+** — the "every backend's fields at once" is that build, not a regression; OR-16b's ledger status was stale (corrected to SHIPPED, ADR-0488). **(2) H1 (the DPAPI branch) refuted on the Python side, pinned on the Win32 side.** `_dpapi_protect` / `_dpapi_unprotect` had never executed under a test (every store test injects a lambda). A fake `crypt32` reading the input blob through the real pointer and length (with `gc.collect()` inside the read) proves the layout, the buffer lifetime, `cbData`, flags 0, one `LocalFree` per call, a tampered blob refused — `tests/ai/test_dpapi_marshaling.py`, 8 tests, six mutants red by name. The REAL Win32 calls now run in `installer-smoke.yml`'s Windows job through the installed venv: two keys (25 / 67 chars) round-trip, no plaintext at rest, one flipped byte comes up keyless with the other credential intact — **its first verdict is the PR's `windows` check, read it before believing this paragraph.** The screenshot itself refutes "saved but empty": a failed unprotect renders *"(none set …)"*, never *"a key is saved"*. **(3) The key path on the wire, first time.** `tests/web/test_gateway_wire.py` drives the REAL `_urllib_gateway_opener` against a loopback fake gateway demanding exactly `Authorization: Bearer <key>`: the header on the socket with a dead corporate proxy in the environment (`NO_PROXY` deleted — the first battery pass survived a proxy-consulting mutant because the CONTAINER's `NO_PROXY` excluded 127.0.0.1), a refused key's challenge quoted, a key pasted once → persisted → a fresh launcher-path app → the catalog AND a generation authenticated, a stale key on disk rendering the photographed banner with *"the saved key, 25 characters"* and the gateway's words; the allowlist pinned to still refuse loopback at construction (the shim rewrites the host only). **(4) The failure the installed build could not show:** two masked fields one above the other (local token above gateway key), blank-keeps on both — a fresh key pasted into the wrong one keeps the OLD key, the page reads *"a key is saved"* + 401, nothing says where the paste went (RED on the pristine tree). v1.0.261: `POST /settings` records a one-shot `_SettingsReceipt` (`SessionState.settings_receipt`, consumed by the GET like `flash`) — *"Saved. Gateway API key: replaced — 25 characters now held / kept (the field was blank; …)"* — plus a `.notice.warn` when a credential lands in a field no selected backend (primary or cross-check) uses; the local-token label says the gateway never sends it; the status line carries `tool version: <installed metadata>` (`data-tool-version`); `http_refusal_detail` quotes a scheme-only `WWW-Authenticate` challenge last (`challenge: Basic realm="…"` — H2's discriminator). Rendered in Chromium in four themes (visible box inside the panel, the theme's notice border, gone on reload). **Verification:** four red-first probes on the pristine tree (three RED, the wire chain GREEN by design); the touched modules 63 passed; the browser test 4 passed; **mutation battery 20 / 20 RED by name** after two instrument fixes (the parametrized-id matcher; the `NO_PROXY` environment); both ruff binaries, format, mypy strict 165, bandit, `node --check` clean; installer tests 68 passed on the rebuilt wheel + nine installers (`tools/mpxj` unchanged, `mpxj_ref` 163d1942). The full suite, `-m parity` and the CI verdict are in the SESSION-LOG's follow-up with the PR number. Highest ADR **0493**. QC-1/QC-2 bind every session — ADR-0393.

**CARRIED FORWARD, still live and NOT this unit's:** the key itself — on 09-12 the same key answered 401 OUTSIDE the tool; **the operator owes V-4** (the gateway's refusal text — on v1.0.261 the banner quotes it, including a scheme-only challenge), **whether the pasted key is the CURRENT Hub key**, and the one `Invoke-RestMethod …/v1/chat/completions` request in OR-17 §4 that settles the catalog-401-masks-generation hypothesis (registered UNVERIFIED; the tool gates routing on `GET /v1/models` by design — do NOT build a completion probe without that evidence). Also carried: the `"ok": false` lines of the 09-11 403 (a prompt-size guard is NOT a unit without them); R-45; `test_driving_path_whole_schedule_browser.py:104` width-racy (#667); `/settings` sideways scroll; ADR-0488's residual (the Ollama default model / `— not installed` wording under the gateway).

## UNVERIFIED, stated

- The real-Windows DPAPI verdict until the `windows` check reads green on this PR (the step can fail: a passthrough, a broken marshaling or an accepted tampered blob each fail an assertion by name).
- Whether the 09-15 paste landed in the wrong field, and whether the pasted key is current — the receipt and the banner on v1.0.261 will say.
- The gateway's refusal text (V-4) and its scheme.

## Traps this session paid for, by name

* **A screenshot's WORDING is a version fingerprint when the page has no version** — `git show` the strings at the release commits before calling anything a regression; then give the page a version.
* **"Verified with injected openers" never put the transport on a socket** — a loopback fake behind the REAL opener is the only proof of the wire; rewrite the host in a shim and pin that the allowlist still refuses loopback.
* **A `pragma: no cover` branch with a lambda stand-in in every test has never run** — a fake of the OS call that reads through the real pointer proves the marshaling; the real call needs the real OS (the Windows runner).
* **A battery green can be the environment's** — the container's `NO_PROXY` excluded loopback, so a proxy-consulting mutant survived until the test deleted it; and a by-name matcher blind to parametrized ids reports a by-name red as "other". Read every non-by-name verdict as a finding about the instrument first.
* **A kickoff's pointer can name a document that is not on the tree** — OR-17's entry lived on an unmerged draft (#676); grep before trusting.
* **A masked field cannot show where a paste landed** — say what the save DID, once, and never move a secret between fields silently.

## Next — campaign queue

**Read `git log origin/main` before trusting any sha written here.**

**First:** after #677 merges, **OR-18** (operator, 2026-09-15 ~14:25Z): the installed version as a server-rendered line at the top of the `/launch` Boot Screen — the full unit shape is OR-18 in `OPERATOR-REQUESTS.md`; the global-header chip was offered, not built. Then: this PR's checks — the `windows` job's DPAPI step first (eight checks apply: `installer/**` and the workflow changed), then the operator's reply to OR-17 §4 (a v1.0.261 screenshot pins the build and shows the receipt + the gateway's own reason). If the receipt reads *replaced* and the banner still refuses: the key or the gateway (V-4 names which) — not this repository's. Then `main`'s run 1862 for `ec3adf6c`. Then the report's §3 in order: **R-47** (SPI(t) 8.24 vs 8.22) · **R-52** (the `.pptx`) · **R-50** (History variants) · **R-61** (a FIXED_DURATION leg on an off-pattern crew) · **R-57** (an assignment's OWN leveling delay — Hard_File UID 398 / 188) · **R-58** (the calendar intersection — also the planned-value proration's rule, `cpm.booking_calendar`) · **R-59** · **R-64** (milestone 387's external link) · **R-63** (the converter's conversion-date dependence) · **R-62** · **R-45** (updated3's BAC / BCWP) · then R-03 · R-04 · R-09 · R-13 · R-18 · R-21 · R-22 · R-32 · R-39. The design queue is 19 artboards. Other residuals: ADR-0488's / 0486's / 0485's / 0483's (unchanged), `/settings` sideways scroll, OR-11b, OR-11d, the working-minute axis, the hint bubble, ADR-0484's in-grid rows.

**Review cover is still absent** — Codex quota EXHAUSTED; the mutation battery and the full gate are all this repo gets.

# (prior) handoffs — archived

> The earlier handoff sections now live in **[HANDOFF-ARCHIVE.md](HANDOFF-ARCHIVE.md)** (newest-first,
> verbatim), and the full append-only per-session history is in **[SESSION-LOG.md](SESSION-LOG.md)**.
> Per ADR-0246 this file keeps ONLY the current STATUS section above, so the entire live handoff is
> small enough to read in full in one pass every session (and the SessionStart hook auto-injects it). When you
> write the next handoff, MOVE the current section to the top of `HANDOFF-ARCHIVE.md` (demote its
> heading to `(prior) Handoff`) and REPLACE the section above — do not stack another archived heading
> here. This single pointer is intentionally the only such heading in the file; the size guard enforces
> that.
