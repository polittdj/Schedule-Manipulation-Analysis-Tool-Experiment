# Handoff — 2026-09-12 (R-56 CLOSED (ADR-0487): a material / cost booking occupies the span the file records for it — updated3's finish 13 d early → EXACT; OR-16 (ADR-0488): the gateway refused the operator's key and the tool now says so in the gateway's own words, and AI Settings shows one backend at a time; v1.0.257)

STATUS (current) — `main` @ **`c31259fe`** (#670, ADR-0486 / v1.0.256, merged 2026-09-12 00:01Z; `main`'s own runs **CI 1846 / installer-smoke 709 both success** — read from the Actions API this session; no merge-record PR, the #667 rule). **Three units landed on branch `claude/jolly-bohr-cs1rze`.** **(1) R-56, ADR-0487.** The kickoff's "duration contour" was a category error, found by executing the register's own first step: the tally was run against the **`.mpp` itself** through the vendored MPXJ (the MSPDI carries ZERO `TimephasedData`, on every golden). The engine's WORK legs were exact to the minute on both heads; MS Project's task finish was the finish of a **MATERIAL / COST booking the engine skipped** (non-WORK resources contributed no leg, ADR-0474), whose span **no stored quantity determines** (302's two bookings, different quantities and rates, share one 36 h span; 385's span 24 h and 125 h) but which the file **records per assignment** (`Assignment/Start` / `Finish`). The recorded window is now a leg of the execution plan (`Assignment.start`/`finish`, `_recorded_span` segment-aware — the contiguous ruler landed 385 one lunch gap late —, `CPMResult.booking_span_driven`; WORK bookings never). Measured on a scratch copy across the whole corpus BEFORE a model field existed: **updated3 −13.00 d → 0.00 d (exact 2026-12-12 17:00), within-a-day 60 → 103 / 110, stored slack 9 → 42 / 68, Critical 103 unmoved; Project2 / Project5, LTF ×4, Hard_File / updated / updated2 / 24hr byte-identical; EVM2 −2.21 → −1.00 d (its Net Finish Impact −20 → −21, Acumen −22 — re-pinned knowingly)**. Chain heads re-derived on the live engine: **five** (385, 403, 302, 188, 300), not seven — a threshold artefact. UID 403 is a **leveling SPLIT** (two work pieces seven days apart in the `.mpp`; the MSPDI cannot carry it) — **R-60**, registered. **Fixture provenance settled (QC-2):** the intake `Hard_File_updated3.mpp` is Revision 5 (07-15), the fixture Revision 2 (07-09), 12 tasks / 12 bookings apart (all re-leveling); Fuse's *update2 vs update3* Forensic Analysis Report records 403 at the FIXTURE's 11-05 09:12 — the fixture IS Fuse's file; NOT regenerated. **(2) OR-16, ADR-0488.** Mid-session the operator reported *"I can't log into Opus 4.8 Thinking even if I put in the API Gateway Key"* (v1.0.256, a key saved, the probe 401). The key path v1.0.254 → v1.0.256 was diffed and proven intact on the real code; the discriminator — the same `GET /v1/models` with the same key from PowerShell, outside the tool — returned **401**: the gateway refuses the key itself (expired / rotated / not the key pasted — the masked paste read 25 characters). What the tool got wrong is fixed: `ai/refusal.py` quotes the server's OWN reason (the `WWW-Authenticate` challenge or the error body, bounded to one line, markup never; the body read once and shared with ADR-0486's 400 fallback) in every probe / generation refusal; the gateway banner names the credential sent (*the saved key, 25 characters* / the env var) and sends the operator for the CURRENT key; the placeholder states the saved key's length; the refusal is word-bounded (ADR-0485's residual closed). **(3) OR-16b — *"as user friendly and simple as possible"*:** every backend-specific row carries `data-backend-only`; `settings.js` shows only the primary or cross-check backend's rows (the `hidden` attribute; every field still posts; no-JS shows all); the Ollama runtime note rides the Ollama group; measured in Chromium in all four themes. **Verification:** red-first everywhere (the engine module and `refusal.py` cannot import on the pristine tree; the oracle 2 failed / 8; the settings pins 4 / 24; the disclosure 3 / 2); **mutation battery 23 mutants / 23 RED by name** (12 R-56 + 11 OR-16) after two instrument findings (a rig with only on-pattern legs could not see a disclosure mutation; an anchor stale after a mypy rename); the perf count-gate caught a first cut hashing the frozen `Calendar` (420 hashes / 20 solves) → identity keys; engine + parity + importers + perf + ai + model **455 passed** (+ the EVM re-pin, 6 passed); `-m parity` **97 passed** (5:04); the AI / settings neighbourhood 160 → 100 with the disclosure; both ruff binaries, format, mypy strict (165 files), bandit exit 0, `node --check` clean; **full suite 5,308 passed / 5 skipped / 2 failed in 41:44 — both reds were pins the change moved on purpose and re-pinned in the same commit with their reasons beside them: the `Assignment` schema freeze (the two recorded-window fields) and the Hard_File → updated3 change-effects sentence 33 → 32 of 33 (restoring UID 384's data-date SNET now moves the target finish −1 wd, because its successor 385 is on the driving chain exactly as in MS Project); the two modules then green**. Version **1.0.257**; wheel + nine installers rebuilt (`SF_MPXJ_REF=42d92dc9…`; the clone unshallowed first — `git log -1 -- tools/mpxj` reads `42d92dc9`). **Draft PR: the SESSION-LOG follow-up records its number and head** (eight checks expected — `installer/**` changed). Highest ADR **0488**. QC-1/QC-2 bind every session — ADR-0393.

**CARRIED FORWARD, still live and NOT this unit's:** `test_driving_path_whole_schedule_browser.py:104` is **width-racy** (registered by #667; its own unit, never fixed by widening a wait) — watch for it on this PR's browser job.

## What the operator still owes, and what the tool will show them

- **V-4** — the gateway's own reason: the step-2 PowerShell (`WWW-Authenticate` + the 401 body) was requested and had not arrived; the v1.0.257 banner shows the same text. A key the gateway accepted at 21:58Z on 09-11 and refuses on 09-12 needs the CURRENT key from the AI Hub; the tool cannot renew it.
- **The `"ok": false` lines** of the 2026-09-11 morning 403 — still owed; still the only thing that decides whether a prompt-size guard is a unit.

## UNVERIFIED, stated

- The Windows DPAPI branch of the key store is exercised only in the field (the POSIX plain branch is what the suite proves); the operator's placeholder read "a key is saved", which is the loaded-config path — consistent with the store working.
- R-56's rule is proven against MS Project's stored dates and Fuse's finish; whether MS Project itself derives those material / cost spans from something the MSPDI omits (a rate scale, a fixed-consumption flag) is unknown — `getVariableRateUnits` read `null` on every booking. The recorded window is the file's fact; the mechanism is MS Project's.

## Traps this session paid for, by name

* **"Contour" was a category error — check what the engine SKIPS before tuning what it computes.** Two ADRs described a duration-contour on the work legs; the work legs were exact and the finish came from bookings the engine never looked at.
* **"The MSPDI cannot explain it" was true of the XML and false of the file.** MPXJ's writer emits no `TimephasedData`; the `.mpp` carried splits, contours and resource types the XML flattened (COST written as Type 0). Read the source binary through the vendored converter before declaring a rule unrecoverable.
* **A register's count is a threshold artefact until re-derived** — "seven heads" was five at the one-day threshold; the two extra were day-boundary milestones.
* **The intake `.mpp` is not the fixture's file** — provenance was settled by the Fuse report's own figure for UID 403, not by dates or revisions. Never regenerate a golden from the intake binary without that check.
* **A first cut keyed a set on the frozen `Calendar` model — the perf count-gate caught it** (420 hashes). Identity keys on a solver the SRA calls a thousand times.
* **A rig whose legs all sit on the project pattern cannot see a disclosure mutation** (the fast path drops them); the green was the instrument's, and the rig now carries a two-shift crew.
* **A 4 KB read limit dropped a long reason instead of bounding it** — the JSON no longer parsed; "bounded" means quote the opening run.
* **A page-wide substring assertion matched a field's TITLE text** (*"requires authentication"* in the OpenAI token's tooltip) — read the banner element. The handoff had already said so; it happened again.
* **The operator's live evidence re-ordered the session correctly:** one request made OUTSIDE the tool (PowerShell against the gateway) settled tool-vs-gateway in a single step — build the discriminator before building a fix.

## Next — campaign queue

**Read `git log origin/main` before trusting any sha written here.**

**First:** the draft PR's eight checks on its final head (the SESSION-LOG follow-up names it); the operator's V-4 line and the `"ok": false` lines. **Then the design page, owed and NOT delivered this session: `/margin` (`setScreen('mg')`, the last Control screen; recipe ADR-0471 / 0475 / 0484).** Then the report's §3 in order: **R-49** (MPXJ omits a ZERO `TotalSlack`) · R-46 · R-47 · R-52 · R-50 · **R-60** (leveling splits via converter `TimephasedData` — UID 403's 12.8 d is the whole of updated3's remaining per-activity gap) · **R-61** (a FIXED_DURATION leg on an off-pattern crew) · R-57 / R-58 / R-59 · then R-03 · R-04 · R-09 · R-13 · R-18 · R-21 · R-22 · R-32 · R-39. Other residuals: ADR-0488's own (under the gateway a saved model the catalog cannot confirm still reads "— not installed", and a fresh config's default model is an Ollama id whatever the backend — the screenshot's dropdown) · ADR-0486's (`max_completion_tokens` as a second attempt; an Ollama output-side disclosure from `done_reason`; the cross-check's `last_completion` not surfaced) · ADR-0485's remaining one (the model dropdown probes with the SAVED token) · ADR-0483's (a live-peer response cut at 5 s) · **`/settings` scrolls sideways — MEASURED this session: 1,877 px in console / apollo / jarvis, 1,641 in daylight at a 1,440 viewport; an over-wide `<select>`, its own UI unit** · OR-11b · OR-11d · the working-minute axis · the hint bubble · ADR-0484's in-grid rows.

**Review cover is still absent** — Codex quota EXHAUSTED through #670; the mutation battery and the full gate are all this repo gets.

# (prior) handoffs — archived

> The earlier handoff sections now live in **[HANDOFF-ARCHIVE.md](HANDOFF-ARCHIVE.md)** (newest-first,
> verbatim), and the full append-only per-session history is in **[SESSION-LOG.md](SESSION-LOG.md)**.
> Per ADR-0246 this file keeps ONLY the current STATUS section above, so the entire live handoff is
> small enough to read in full in one pass every session (and the SessionStart hook auto-injects it). When you
> write the next handoff, MOVE the current section to the top of `HANDOFF-ARCHIVE.md` (demote its
> heading to `(prior) Handoff`) and REPLACE the section above — do not stack another archived heading
> here. This single pointer is intentionally the only such heading in the file; the size guard enforces
> that.
