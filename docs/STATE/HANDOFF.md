# Handoff — 2026-09-04 (WP5 shipped — the three folder gestures named and a parent folder ASKED about (ADR-0459); /trend is the third page on the Claude Design layout and `vol-*` is aliased onto `cd-*` (ADR-0460); v1.0.235; PR #631 MERGED)

> ## STATUS (current) — **On branch `claude/continue-yznv26` from `origin/main` @ `f2be8a0f` (the #629 docs merge; `main`'s run #1706 for the #628 merge commit `0f098cce` concluded `success`). #630 (ADR-0457 + ADR-0458, v1.0.234 — the operator's evening batch: I-01 root-caused to session population, T-01 measured working, /analysis re-aim incremental) **MERGED** to `main` @ `19e74147` at 02:18Z (the operator). At the 03:14Z check-in this branch merged `origin/main` back in (a MERGE commit — never rebase, never stack), re-resolved the five state docs (their 2026-09-03 (e) handoff archived above (d); log entries theirs-then-mine; kickoff carries both DONE blocks), and rebuilt the wheel + nine installers at 1.0.235 on the MERGED tree (the lockstep pin compares the embedded wheel to `src/` byte-for-byte). **PR #631 squash-merged to `main` @ `d9bac11a` at 04:55Z (the operator)** with all eight checks green on its merged head `46a96ec6` (the first head's `browser` job had caught one DOM panel-count pin, 11 → 10, re-baselined — ADR-0460). GitHub deleted the head branch; `claude/continue-yznv26` was restarted on `origin/main` @ `d9bac11a` (`git fetch --prune` + `checkout -B`, never an amend of the squash). `main`'s OWN run for the merge commit, **#1717**, concluded **`failure` on ONE cell** — the `browser` job, `tests/web/test_axis_titles_visual.py::test_captions_survive_every_theme_and_scale`, `console@0.9 /cei: no captions rendered`, 1 failed / 436 passed, every other job green — while `d9bac11a`'s TREE is byte-identical to #631's all-green head `46a96ec6` (`git rev-parse <sha>^{tree}` = `9ab52a4a…` for both): **CI-03's THIRD strike** (/forecast #626 · /curves #629 · /cei `main` #1717), NOT the merge's. Its single re-run (07:07:04Z) was cancelled 40 s later by the workflow's concurrency group when the operator squash-merged this session's docs-only record **#633** to `main` @ `47a5d758` at 07:07:26Z (tree-identical to its head `528cb1a3`). `main`'s run for THAT commit is **#1721** — the identical product code under the same sweep, i.e. the de-facto re-run: its `browser` job was still running at this record (started 07:07:29Z; verdict in the SESSION-LOG's 2026-09-04 (c) follow-up line — read it, then #1721's conclusion, before trusting `main`). #1714 (the #630 merge) concluded `success`. WP0–WP5 + both operator batches ALL MERGED — NO open product PR; this docs-only record rides a new draft PR (number in the SESSION-LOG). The OTHER session's docs-only draft **#632** is superseded (branched before #631; `browser` red on the CI-04 header race) and still open at this record — close it unmerged. QC-1/QC-2 bind every session — ADR-0393, pinned by `tests/test_standing_rules.py`.**
> Highest ADR **0460**; version **1.0.235**; wheel + nine installers rebuilt in lockstep as the LAST step of this session (the operator re-downloads from `main` once the PR merges — banner **v1.0.235**). Campaign queue: **WP6** is next (CPM-01 · CPM-02 · MC-02 · MC-03 · MAN-01 · REC-02; parity-sensitive rows through the metric-parity skill; RC-02's endpoints are WP6/WP7 rows) → WP7 (`ai/txlog.py` first — Law 1) → WP8. Plus ONE design page per session — next candidates **/forecast (09 Where it lands)** and **/performance (07 How we execute)**; /compare (10) is a feature change.
>
> ## What this session shipped (ledger sections "WP5" and "Claude Design migration")
> **WP5 (ADR-0459) — BOTH folder-ask builds, the three 2026-08-21 facts taken as given.** (A) the folder button reads *choose one folder…*, a `.dz-how` legend names the three gestures (Ctrl/⌘/Shift-click for several FILES · one folder per pick · select the folders together and DROP them for several Projects · a parent folder that holds several project folders asks), both buttons carry the facts as hover hints, the two 2026-08-21 pinned phrases survive. (B) `home.js` funnels every way in through `ingest()`; when ONE folder root's schedule files span ≥2 immediate sub-folders the served hidden `#dzAsk` `role=dialog` shell asks — sub-folders BY NAME with counts, *N Projects, one per sub-folder* (focused) · *One Project “X” — N versions* · Cancel/Escape — and the per-sub-folder answer re-roots each companion `rel` at its sub-folder (`Programs/Apollo/a1.xml` → `Apollo/a1.xml`; a two-segment rel is left alone and the box says the file stays the parent's). The server is untouched; a multi-root drop never asks (ADR-0437 byte-for-byte); a single dropped parent asks the same question. Proof: `test_folder_ask.py` (6) + `test_folder_ask_browser.py` (7; the picker path with a REAL directory via Playwright 1.62's `set_input_files`, genuine `webkitRelativePath` values), red-first 3 + 7, four mutations red by name, four-theme render of the open question clean.
> **/trend (ADR-0460) — artboard "05 How it moved", recovered by EXECUTING the canvas** (npm-packed React/Babel, `support.js` patched local, `sfredux-screen=tr` + `sfredux-guided=1` + `sfops-boot.skipNext=true` seeded, four themes rendered and viewed; the intake's `screenshots-v2/05-screen.png` is Chapter 11 under a Chapter-05 name — do not trust it). The page's own ▶ Play all / ⏭ Step all master is re-homed into a masthead cursor strip (`#trendMaster` slot; ids, handlers, the ADR-0275 registration untouched; NO ◂ Back — the page has no master step-back and a new in-family control is new functionality), one `.cd-chip` per version clicks every framed chart's Next (and `#qualNext`) until it shows that version — each stepper publishes `data-frame` on the bar it owns (`trend.js`, `margin.js`, `trend_drill.js`), the active chip + `#trendFrame` pill follow the FIRST framed chart; the Focus form is the options row (byte-exact, still `.panel`); rows: version table beside the signals table (`.cd-grid-12`), the 21 framed charts full width, quality drill beside a `.cd-stack` of the quality sentences + a "How to read this" block from `_EXPLAINERS["Trend"]`, margin burndown full width. `vol-*` cursor vocabulary aliased onto `cd-*` (third caller); `volatility.js`'s r11 byte-freeze re-baselined DELIBERATELY (`bca57830… → 381fec11…`, two selector strings). Every `trend.js`/`margin.js`/`trend_drill.js` edit above an axis-caption pin is a same-line-count edit; the pin lines `[483, 587, 712, 830, 920]`, `[224]`, `[110]` did not move. Proof: `test_trend_design_layout.py` (5) + `test_trend_design_browser.py` (3 — incl. a synthetic two-version margin corpus, because TP4 carries no margin task and the burndown then renders NO stepper), red-first 5 + 2, five mutations red by name (the margin one only after the synthetic corpus existed — a mutation that survives is a claim about the FIXTURE first), 254 TestClient + the M1 census rows for /trend and /volatility + 38 M3 drivers + the caption sweep green; four-theme census moved on chips / order / the master's `.panel` shell only.
>
> ## Operator-facing state
> Re-download once the PR merges; the banner must read **v1.0.235**. ASK (do not assume): (1) on `/`, pick a PARENT folder that holds two project folders — does the question appear, do the two answers land two Projects / one Project as promised, and are the three legend lines clear in your terms? (2) on /trend with ≥2 files — do the version chips move every chart and the drill together, and does the strip read naturally with Play all / Step all inside it? (3) #630's own asks stand (I-01: which finding, which two files, folders or Titles · T-01: page, zoom, screenshot · (c): is the residual /analysis lag gone). UNKNOWABLE and closed: the blank-header banner, the 08-26 incident.
>
> ## Traps paid for this session, by name
> a mutation that SURVIVES is a claim about the fixture before it is a claim about the code (TP4 renders no margin burndown, so the margin frame line was dead until a corpus with a margin task existed) · a real `webkitdirectory` FileList arrives in filesystem-traversal order — sort by name, and let the test that assumed alphabetical order catch it · a line-number-keyed pin (r11's axis captions) is edited with SAME-LINE-COUNT replacements and new code BELOW the last pin, asserting the pin lines in the patch itself · a byte-frozen page script (r11 `PAGE_SCRIPTS`) changes only by a dated re-baseline · an intake screenshot's FILE NAME is testimony (05-screen.png is Chapter 11) — execute the canvas · two sessions in flight: number ADRs after the in-flight PR's, hold the version bump / installers / docs rotation to the end, and plan the re-merge + rebuild · `/root/.local/bin/ruff` (0.15.8) shadows the pinned 0.16 — run `python -m ruff` · `pip install -e '.[dev,browser]'` first; `build` is NOT in the extras (`pip install build`) · the installer build refuses a shallow clone (`git fetch --deepen=300` first).
>
> ## Next — campaign queue
> Branch FRESH from `origin/main` @ `47a5d758` (or later) and open a NEW draft PR. **RECOMMENDED FIRST (operator to confirm — the order is theirs): CI-03's root-cause PR** — the caption sweep's `console@0.9` cell struck three times in 48 h (/forecast · /curves · /cei, the last on `main`'s own run #1717), test-instrument only, red-first (reproduce the race under induced load before touching the wait). Then **WP6** (ledger highs: CPM-01 `cpm.py:1316` · CPM-02 `driving_slack.py:314` · MC-02 · MC-03 `jcl.py:284` · MAN-01 · REC-02; any golden shift = CONFIRMED-DEFERRED, never a silent re-pin) → **WP7** (`ai/txlog.py` first — Law 1) → **WP8** (consolidated report + roadmap by testimony risk); plus ONE design page per session (/forecast 09 · /performance 07 — the `.cd-*` family now carries `cd-grid-12`, `cd-stack`, `cd-master`, `cd-note`; a script-created master mounts into a `#<page>Master` slot). CI-03's ledger row carries the three occurrences and the first candidate fix (measure what the caption wait waits for at 0.9 scale; a `wait_for_function` on the caption selector with the page's own settle signal — never a wider timeout).

# (prior) handoffs — archived

> The earlier handoff sections now live in **[HANDOFF-ARCHIVE.md](HANDOFF-ARCHIVE.md)** (newest-first,
> verbatim), and the full append-only per-session history is in **[SESSION-LOG.md](SESSION-LOG.md)**.
> Per ADR-0246 this file keeps ONLY the current STATUS section above, so the entire live handoff is
> small enough to read in full in one pass every session (and the SessionStart hook auto-injects it). When you
> write the next handoff, MOVE the current section to the top of `HANDOFF-ARCHIVE.md` (demote its
> heading to `(prior) Handoff`) and REPLACE the section above — do not stack another archived heading
> here. This single pointer is intentionally the only such heading in the file; the size guard enforces
> that.
