# Handoff — 2026-09-15 (R-46 CLOSED (ADR-0492): BCWS is the file's own time-phased baseline cost — the importer reads a booking's baseline-cost series, the engine sums it through the status date and prorates a straddling block on the booking's calendar; the three Hard_File ribbons EXACT; one task moved in the whole corpus; v1.0.260)

STATUS (current) — `main` @ **`163d1942`** (#673 squash-merged 2026-09-15: ADR-0491, v1.0.259). **This branch (`claude/exciting-curie-n0wghm`) started ON that commit** (`HEAD^{tree}` == `origin/main^{tree}`; nothing to restart, nothing to merge). **One unit landed: R-46, ADR-0492, version 1.0.260.** The kickoff's step was NOT taken as written, and the reason is the unit. **(1) The file already records the answer.** The register said "prorate the straddler on its crew's calendar"; the Bible says `PV (BCWS) = sum(BCWSPV)` — each activity's BCWS as MS Project stores it — and the goldens ADR-0491 regenerated carry exactly that: the assignment baseline-cost series (`TimephasedData` Type 5, 250 blocks on Hard_File_updated) that nothing in the tool read. A probe summed it by the ribbon's own status-date rule before a line changed: **16,000.00 / 64,240.00** on updated / updated2 (Fuse 16,000 / 64,240; the engine 16,150 / 64,240), and 106,440 + UID 270's straddling merged block (4,800; two of three days = 3,200) + UID 257's 800 of baseline cost that has NO series in updated2 / updated3 (a booking baselined and since removed) = **110,440** on updated3. The 150 was UID 187 alone: a "Customer Service Team" crew on 06–08 / 08–17 / 17–23, its 120 h front-loaded on that calendar — 72 of 120 h (3,600) planned by the status date where the project-calendar proration read 5 of 8 days (3,750). **(2) The rule.** `CostPiece` + `Assignment.baseline_cost_pieces` (SCHEMA_VERSION 2.14.0); `mspdi._baseline_cost_pieces` reads every valued Type-5 block (÷100, time order; a zero / absent block carries nothing; a non-numeric Value fails loud — `parse_float`'s contract); the JSON Save round-trips it strictly; `cpm.booking_calendar` / `cpm.working_minutes_between` make the plan builder's calendar rule and ruler public; `evm._planned_value` sums a task's series through the status date, prorates a block the status date falls inside in working minutes of the booking's calendar, accrues the budget no series carries by ADR-0473's linear rule, and leaves a series exceeding the budget as recorded. **(3) Provenance.** 110,440 was ADR-0473's testimony — pinned by no test — until this session found it in `Hard_File_update2 vs update3_Fuse - Excel .xlsx` (Ribbon View, status 46307), a workbook no test had opened; the oracle module now reads it. **Measured, per task, pristine → this engine, every golden: ONE mover in the corpus** — updated UID 187 3,750 → 3,600; BCWS 16,150 → **16,000**, SPI 1.04 → **1.05** (= the ribbon); updated2 64,240 / updated3 110,440 / the 24h snapshots 133,400 / Hard_File 0 unmoved; EVM1 / EVM2 / Project2 / Project5 / TP4 (no series) equal the old rule exactly. **Rendered:** `/evm` on Hard_File_updated reads SPI 1.05 (header and table) on this tree, 1.04 on `git archive HEAD`. **Verification:** red-first (10 failed / 137 passed + two modules that cannot import; the oracle's `updated` row red by name at `150.0 <= 0.0`); **mutation battery 18 mutants on a shadowed copy (`-p mutcheck`, control 182 passed): 17 red by name first pass, ONE survivor equivalent** (a missing cost defaulted BELOW the guard that refuses it) **re-cut at the guard → red on exactly the strictness test, 18 / 18**; boundary mutants equivalent by arithmetic, named in the ADR. Both ruff binaries, format, mypy strict 165 files, bandit clean; the six touched modules 182 passed; the full suite and `-m parity` on the final head are in the SESSION-LOG's follow-up with the PR number. Wheel + nine installers rebuilt (`tools/mpxj` unchanged). Highest ADR **0492**. QC-1/QC-2 bind every session — ADR-0393.

**CARRIED FORWARD, still live and NOT this unit's:** R-45 (the updated3 ribbon's BAC / BCWP / ACWP 121,800 / 53,715 / 66,245 against the file's task-level fields — only its PV is pinned); `test_driving_path_whole_schedule_browser.py:104` width-racy (#667); `/settings` scrolls sideways (1,877 px, 09-12); V-4 and the `"ok": false` lines still owed by the operator.

## UNVERIFIED, stated

- A status date INSIDE a merged block whose daily amounts are not equal (the writer merges equal days; every corpus status date sits at a day's end) — the ruler is the booking calendar's working time, pinned by name, no oracle.
- A series exceeding the task's baseline cost (none in the corpus): left as recorded, nothing subtracted — pinned as decided, no reference figure.
- The Windows DPAPI branch of the key store (unchanged; ADR-0488's note stands).

## Traps this session paid for, by name

* **The register's step was a hypothesis about the mechanism** — the file carried the output (the Type-5 series) since the last unit and nobody re-priced the row; measure whether the file records the answer BEFORE re-deriving it.
* **A figure with no test behind it is testimony however many ADRs repeat it** — 110,440 lived in a workbook no test had opened; grep the intake for the NUMBER.
* **A number written mid-session is not a measurement** — "18 / 18" went into the audit row before the battery finished; it held only because the row was re-read against the log.
* **A survivor below a guard is an equivalent mutant** — re-cut it AT the guard before calling it a test gap.
* **A wide red can be the contract, not the fixture** — reading durations as numbers fails the import loud on purpose.
* **The environment is cold every time** — shallow clone, no package, CI's ruff only after the dev install; the kickoff's environment block is the checklist, run it before the first measurement.

## Next — campaign queue

**Read `git log origin/main` before trusting any sha written here.**

**First:** this PR's checks on its final head (eight — `installer/**` changed) and its merge (the operator's). Then the report's §3 in order: **R-47** (SPI(t) 8.24 vs 8.22) · **R-52** (the `.pptx`) · **R-50** (History variants) · **R-61** (a FIXED_DURATION leg on an off-pattern crew) · **R-57** (an assignment's OWN leveling delay — Hard_File UID 398 / 188) · **R-58** (the calendar intersection — now ALSO the planned-value proration's rule, `cpm.booking_calendar`) · **R-59** · **R-64** (milestone 387's external link) · **R-63** (the converter's conversion-date dependence) · **R-62** · **R-45** (updated3's BAC / BCWP — the ribbon's per-activity cost columns, if the Detailed Metric Report carries them) · then R-03 · R-04 · R-09 · R-13 · R-18 · R-21 · R-22 · R-32 · R-39. The design queue is 19 artboards. Other residuals: ADR-0488's / 0486's / 0485's / 0483's (unchanged), `/settings` sideways scroll, OR-11b, OR-11d, the working-minute axis, the hint bubble, ADR-0484's in-grid rows.

**Review cover is still absent** — Codex quota EXHAUSTED through #673; the mutation battery and the full gate are all this repo gets.

# (prior) handoffs — archived

> The earlier handoff sections now live in **[HANDOFF-ARCHIVE.md](HANDOFF-ARCHIVE.md)** (newest-first,
> verbatim), and the full append-only per-session history is in **[SESSION-LOG.md](SESSION-LOG.md)**.
> Per ADR-0246 this file keeps ONLY the current STATUS section above, so the entire live handoff is
> small enough to read in full in one pass every session (and the SessionStart hook auto-injects it). When you
> write the next handoff, MOVE the current section to the top of `HANDOFF-ARCHIVE.md` (demote its
> heading to `(prior) Handoff`) and REPLACE the section above — do not stack another archived heading
> here. This single pointer is intentionally the only such heading in the file; the size guard enforces
> that.
