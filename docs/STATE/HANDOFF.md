# Handoff — 2026-09-07 (WP8 COMPLETE — the consolidated report + repair roadmap ordered by testimony risk, PINNED to the tree (ADR-0472); /wbs is the EIGHTH page on the Claude Design layout (ADR-0471); the campaign's nine work packages are closed; v1.0.242)

> ## STATUS (current) — **This session's branch is `claude/polaris-audit-resume-uw726u` (the harness's designated branch), checked out on `origin/main` @ `b1cd1739` — the #644 squash (docs-only). At session start #641–#644 were ALL closed or merged and NO PR was open; `main`'s own CI run **#1759** for the WP7 squash `dee28ab3` concluded `success` (00:24Z), installer-smoke #657 `success`; run **#1763** for `b1cd1739` concluded **`success`** at 01:36Z — `main` is green on the merged tree. This session's draft PR: **#645** (its `floor` job went red once on the TEST-01 guard reading the WP8 guard's own census literal — fixed on the next head; read the verdict on the FINAL head). Banner **v1.0.242** (wheel + nine installers rebuilt after the last source edit; lockstep 68). Statics: ruff 0.16.6 check + format (whole tree, 1,191 files) · mypy --strict 163 files · bandit exit 0 · `node --check` 63/63. Pytest on this tree: the /wbs layout **7** · the WP8 guard **8** · the neighbouring guard set **174** + **225 / 3 skipped** (the standing axis-title env skips) + the census rows **3** · installers **68** · the doc guards green. The FULL suite ran on a SNAPSHOT worktree of this tree's `src` + the layout test with the OLD installers (never measure a tree a session is mutating): **5,004 passed / 5 skipped / 1 failed in 51:22**; its one failure is `test_embedded_wheel_is_in_lockstep_with_the_source_tree`, by construction (the snapshot's installers predate the rebuild; the real tree's 68 pass). QC-1/QC-2 bind every session — ADR-0393, pinned by `tests/test_standing_rules.py`.**
> Highest ADR **0472**. **THE CAMPAIGN IS CLOSED — WP0–WP8 complete (ADR-0440..0472).** The plan-forward is **`docs/STATE/AUDIT-2026-08-27-REPORT.md`** §3: 43 rows by testimony tier (T1 a cited figure could be WRONG … T6 organizational), every OPEN row priced with its first executable step, pinned to the tree by `tests/guards/test_audit_report_wp8.py` (coverage · pricing · tier order · a nine-figure census recomputed by method). The OPEN rows, in the report's order: **R-01** `evm.py`'s ACWP `actual_cost or 0.0` (S-M) · **R-03** `dcma14`'s two parity-mode roundings (S, needs the Acumen oracle) · **R-04** the 325 `round()` sites outside `engine/metrics` (M) · **R-09** the `.catch` conflation in 13 page scripts (M) · **R-13** the transaction record's schema version (S) · **R-18** NUM-01's parity wording — UNVERIFIED, not re-read (S) · **R-20** UI-03 the tooltip-box overflow on every page (S) · **R-21** the /analysis frozen pane at scale (M) · **R-22** the WBS row drill (S) · **R-32** CI-04 the /driving-path header race (S) · **R-39** `workbench` 400 vs 422 (S). ASK rows: R-02 (b) · R-12 (a) · R-23/R-24/R-29 (e) · R-30 (d) · R-31 (c). The standing one-design-page-per-session ask: next **/standards** (`setScreen('sd')`) or **/scorecards** (`'sk'`); /compare stays a feature (M-L); 22 artboards remain (the report's §6).
>
> ## What this session shipped
> **WP8 (ADR-0472).** Every inherited "observed, not fixed blind" item was RE-MEASURED before it was priced (QC-2): the ledger's "176 `round(` sites" is unreproducible by four definitions on `d61f6395` or this tree (AST calls **325** outside `engine/metrics`, 45 inside) and is retired; ADR-0466 §3's "other modules' expression-string waits" is EMPTY (15 sites, 15 function strings); ADR-0451's four-theme residual on /volatility closed by a DOM census; ADR-0470's "artboard not executed" closed by executing `setScreen('ic')` in four themes; TEST-01 and ENG-DEAD-01 closed by grep; DOC-01 fixed (FINAL-REPORT's "COMPLETE and parity-green" headline tempered under a red-first `test_docs` pin). One NEW row, **UI-03**: every page's document scrolls sideways ~280–300 px at 1440 — the hidden `[data-sf-hint]::after` box (340 px, absolute, `visibility:hidden`) on right-aligned hosts (every `.viz-controls` row's Reset-view button); attributed by computed style, priced S, NOT fixed blind. The report: 78 register rows, 43 roadmap rows, 9 census figures with methods, the operator questions, the design queue, the 2026-08-13 plan reconciled (P0 ORG · P1–P4 done · P5 closed here · P6 UNVERIFIED · P7 ORG · P8 closed). The guard was red before the report existed and red by name under four mutations (a census value, a dropped id, an unpriced OPEN row, a tier out of order).
> **/wbs (ADR-0471).** The artboard EXECUTED over loopback HTTP (`setScreen('wr')`, four themes, zero page errors): one card. The page already IS that card in two verbatim panels; the migration is the ORDER (the masthead leads — the pristine page put its Field-roles picker ABOVE its own takeaway), the family's navigation strip for a per-file drill (one link chip per version, the open one on, the `vN · file · DD` pill, two or more versions only), and the picker in the options position, byte-for-byte. `_version_chips` DESCENDED from `card.py` into `components.py` (a second extracted referrer — ADR-0351's rule) with `route` / `cursor_id` / `noun` parameters; /card's render is byte-identical across the move (diffed). Not ported, named: the mock's row click (priced R-22), its bars and colour, its footnote (the ES panel's read-me line states the COUNT basis; the mock says duration-weighted), the Continue footer. Red-first 4 / 3; layout 7; 8-mutation battery 8/8 red by name; four-theme census moved on chips / chipOn / the strip / the height only, the click opening the sibling version with its own chip on.
>
> ## Operator-facing state
> Re-download once this PR merges; the banner must read **v1.0.242**. ASK (do not assume) — restated with the rows each decides in the report's §5: (a) TX-03 the pre-consent catalog probe (R-12); (b) IMP-05 the P6 planned-date baseline (R-02); (c) the /onepager-compare rulings and its `.pptx` in PowerPoint (R-31); (d) #635's /integrity working-day move for UID 152 (R-30); (e) /forecast's chips with two files · the parent-folder question on `/` · /trend's chips · T-01 · I-01 · the /analysis lag (R-23 · R-24 · R-29). UNKNOWABLE and closed: the blank-header banner, the 08-26 incident (R-43).
>
> ## Traps paid for this session, by name
> The preflight's `[ok] python` is an INTERPRETER check — the container had no project install, no pytest, no playwright, and CI's ruff was not at `/usr/local/bin`: `pip install -e '.[dev,browser]'` before measuring anything · a figure without a method is a rumour (the `176`) — pin the method with the number · a census instrument that names the literal it censuses self-matches (the `pgrep -f` trap in a new coat): exclude its own file · the family regex on raw markup: `pan(?!d)` matches `<span` — the census applies it to id and class VALUES · element boxes cannot see a pseudo-element: measure `document.scrollingElement.scrollWidth` (UI-03) · a snapshot worktree's lockstep pin fails by construction once the installers are rebuilt — map an `F`'s index onto `--collect-only` and read the NAME before calling a run red · a shared helper descends the moment a second extracted module refers to it; keep the first caller's bytes identical by parameter defaults and prove it by render diff · a single-card artboard's migration is the ORDER — do not invent a two-column row the artboard lacks · the Library screens boot with the canvas's own keys (`'wr'`, `'ic'`, `'sd'`, `'sk'`) — never from a screenshot's file name · `git fetch --deepen=300` left the clone shallow (377 commits) and the installer build still succeeded; check `--is-shallow-repository` before blaming the graft.
>
> ## Next — campaign queue
> Branch FRESH from `origin/main` once this PR is merged (`git fetch --prune origin && git checkout -B <branch> origin/main`). The campaign is CLOSED; the next session works the report's §3 in order — **R-01 first** (a two-task cost-loaded fixture with one `actual_cost=None`; red test that CPI/TCPI read NA-or-disclosed) — under the same rules: red-first → mutation proofs by name → the full gate → an ADR → the state docs → a draft PR; a parity-locked row (R-03) waits for its oracle, an ASK row for its answer. PLUS the design page owed each session: **/standards** (Control Standards and Execution Indices, `setScreen('sd')`) or **/scorecards** (`'sk'`); the recipe: npm-pack React/Babel, patch `support.js`, serve over loopback, seed the four localStorage keys, screenshot the section in four themes, census its DOM before writing a line. Observed, not fixed blind: UI-03 (R-20) · the 13 `.catch` sites (R-09) · the 325 roundings (R-04).

# (prior) handoffs — archived

> The earlier handoff sections now live in **[HANDOFF-ARCHIVE.md](HANDOFF-ARCHIVE.md)** (newest-first,
> verbatim), and the full append-only per-session history is in **[SESSION-LOG.md](SESSION-LOG.md)**.
> Per ADR-0246 this file keeps ONLY the current STATUS section above, so the entire live handoff is
> small enough to read in full in one pass every session (and the SessionStart hook auto-injects it). When you
> write the next handoff, MOVE the current section to the top of `HANDOFF-ARCHIVE.md` (demote its
> heading to `(prior) Handoff`) and REPLACE the section above — do not stack another archived heading
> here. This single pointer is intentionally the only such heading in the file; the size guard enforces
> that.
