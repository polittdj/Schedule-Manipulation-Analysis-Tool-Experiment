# Handoff — 2026-09-15 (e) (R-52 CLOSED (ADR-0498): the exported `.pptx` DOES load — the register's "LibreOffice refuses it" was an install with **no PresentationML import filter**, which refuses a PowerPoint-authored deck and a Microsoft-authored `.xlsx` with the identical sentence; both decks now load and are pinned by a gate that skips rather than accuses when the instrument is unusable; PowerPoint still UNVERIFIED; v1.0.266)

STATUS (current) — `main` @ **`3e64e3e0`** (#683, the docs-only merge record of #682; **its own CI run 1883 (35033509512) read SUCCESS at 23:44:19Z** — the previous session left it in progress and this session read its conclusion first, as instructed; docs-only, so no `installer-smoke` run exists for that sha). Before it `4b213dd8` (#682 — OR-20, ADR-0497, v1.0.265; `main`'s runs 1881 / 738 success, read last session). Branch `claude/eloquent-allen-2hg06l` restarted on `3e64e3e0`, clean; **this unit shipped as PR #684 (`5a4efce8` + the docs follow-up `9acac1fd`) and is MERGED** — the operator marked it ready 01:39:49Z and squash-merged it at **01:39:53Z**: `main` @ **`93593ef5`**, tree `46de0c02…` == `9acac1fd`'s (`git rev-parse` both). EIGHT OF EIGHT GREEN on BOTH heads: `5a4efce8` — CI 1884 (35038520524) + installer-smoke 739 (35038520476); the final head `9acac1fd` — CI 1885 (35042374478, `cui-guard` 01:01:14Z · `browser` 01:18:36Z · `floor` 01:22:10Z · `test (3.13)` 01:27:50Z · `test (3.11)` 01:39:03Z · `check` 01:39:09Z) + installer-smoke 740 (35042374407); `mergeable_state: clean`, zero review threads. **The `browser` job's two NEW steps ran and did NOT skip on both heads** (Impress install 15 s / 17 s, the interop gate 3 s / 3 s) — the R-52 gate measured on GitHub's runner twice, on two commits. **`main`'s OWN runs for `93593ef5` are READ and BOTH SUCCESS: CI 1886 (35044973862) completed 02:25:40Z, installer-smoke 741 (35044973863) completed 01:44:04Z** — the merge is green on `main` itself, not only on the PR head. Nothing about #684 is outstanding. GitHub deleted the remote branch; it was restarted on the squash (`--prune` + `remote set-head` + `checkout -B`, working tree clean) and the session unsubscribed from the merged PR. Highest ADR **0498**. Version **1.0.266**. QC-1/QC-2 bind every session — ADR-0393.

**What landed — R-52, the next row of the report's §3, and it was REFUTED rather than fixed.** The register carried, since 2026-09-07 (ADR-0473): *"the exported `.pptx` — the one-pager AND the compare deck — does not load in LibreOffice 7 headless (source file could not be loaded); PowerPoint UNVERIFIED"*, with a repair step naming `presProps` / `viewProps` / `tableStyles`. **The finding was the INSTRUMENT.** Reproduced first: both decks refused (`rc=0` — LibreOffice exits **0** when it refuses, so any gate reading the exit code calls a refusal a pass). Then the control the original finding never ran: the same install, same command, refused a deck **PowerPoint wrote** (`00_REFERENCE_INTAKE/mpp/Politte Schedule Tool.pptx`, 53 parts, six slides) *and* a Microsoft-authored `.xlsx`, with the same sentence. `dpkg -l` explained it — `libreoffice-core` + `libreoffice-common` only, **no `libreoffice-impress`**; no `impress.xcd` in the registry. That sentence is an install saying *I have no import filter for this document class*. With the filter installed, **LibreOffice 24.2.7.2 loads BOTH decks**: PDFs 48,275 / 56,460 B beside the control's 150,219; converted to flat ODF the one-pager is **1 `draw:page` · 54 `draw:custom-shape` · 10 `draw:frame` · 18 `draw:connector`**, every label, the CUI banner top and bottom and our selection-pane shape names intact; the PNG render was read by eye (title, year band, month ticks, three lane bands, bars with in-bar labels, milestone diamonds, the red TODAY line, legend, provenance footer). **Shipped:** `tests/reports/test_pptx_libreoffice_interop.py` — it loads the PowerPoint-authored **control** deck BEFORE it will judge ours, and skips with the reason named (no `soffice` · no control deck · the install refuses the control) rather than reddening the writer for the environment's fault; the `browser` job installs `libreoffice-impress` and treats **a skip as a failure**, so the gate cannot go quietly decorative. The register's repair step was run anyway: `presProps` / `viewProps` / `tableStyles` were the **whole** structural delta against a PowerPoint-authored package and are now written — **not as a fix, as the removal of a variable**, because PowerPoint is in no container a session can reach.

**Measured.** Red-first by name: `test_the_package_carries_the_three_parts_powerpoint_always_writes` failed on the pristine writer (*"ppt/presProps.xml is missing from the package"*) before the writer changed. The three parts **change nothing LibreOffice reads** — the flat ODF is **byte-identical before and after** (181,935 bytes both, same shape census); the decks grow 7,706 → 8,636 B and 8,532 → 9,462 B. **Mutation battery 8 of 8 by name on a shadow copy of `src/`** (a `-p mutcheck` plugin asserts the module measured IS the copy): C0 control 2 passed · M1 corrupt main content type → both FAIL *"the package is the problem, not the instrument"* · M2 markings emptied → both FAIL on the CUI banner · M3 shape names dropped → FAIL on the selection pane · M4 delta text emptied → compare FAILS on `+12 cal d` · **M5 the 2026-09-07 instrument (a `soffice` that refuses everything at rc=0) → SKIP naming the missing filter, never a red** · M6 no `soffice` on PATH → SKIP · M7 control deck absent → SKIP · M8 the slide listed twice → both FAIL on the ONE-slide assertion. The CI step was exercised **both ways** under GitHub's shell semantics (`bash -e`, `set -o pipefail`): exit **1** with a crippled `soffice`, exit **0** with the real one. Version 1.0.266, wheel + nine installers rebuilt after the last source edit (the wheel's `pptx.py` byte-identical to `src/`, sha checked). **The full local gate on the final tree: 5,463 passed / 5 skipped (the `urlparse` pair and the three `INCIDENTAL_SVG` axis cases every run carries) / 0 failed in 49:09; `-m parity` 120 passed / 0 failed in 6:32** — 118 was the PREVIOUS unit's pristine-tree count and it then pinned two SSI oracles; `tests/reports/` contributes 0 tests to the parity selection, so this unit did not move that population.

## UNVERIFIED, stated

- **PowerPoint itself has never opened either deck** — no PowerPoint in any container a session can reach. Registered as **V-5** in `OPERATOR-REQUESTS.md`: on v1.0.266 download `/onepager` → ⤓ PPTX and `/onepager-compare` → ⤓ PPTX and double-click each; if it complains, its wording names the part. The package's *shape* is no longer a variable.
- **The 2026-09-07 container's LibreOffice version was not re-created.** The mechanism is a missing import filter and reads the same at any version; claiming a re-measurement of "LibreOffice 7" would be testimony.
- The operator's machine: whether v1.0.265/266 is installed — theirs to report (every page states its build since ADR-0494).

## Traps this session paid for, by name

* **The rig can be wrong before the engine is — and an error message is a statement by the INSTRUMENT, not about the file.** "Source file could not be loaded" named nothing; the one-line control (hand the same tool a file authored by the reference implementation) refuted eight days of a filed defect.
* **A converter that exits 0 on refusal.** `soffice` returned `rc=0` on every refusal. Assert the artifact exists; never the exit code.
* **A register's "first executable step" is a hypothesis about the mechanism** — the second time this repo has paid for it (R-46 was the first). Reproduce before you repair.
* **A mutant that does not change the thing under test proves nothing.** M2 took three cuts: the first patched one call site of two, the second left the bottom CUI strip intact so the probe still found the text. Both were recorded as NON-mutations, not survivors.
* **Prove the CI fragment under the CI shell.** The first local proof exited 0 on a collection error because the subshell lacked `-e`; GitHub's is `bash -e {0}`. The harness was wrong before the step was.
* **Load ≠ render.** The load gate converts to flat ODF, not PDF: a passing assertion then says *Impress parsed these shapes and this text*, not merely *a file opened* — and it needs no text-extraction dependency.
* **Unverifiable conformance padding is not a fix.** The three parts ship as a removal of a variable, said so in the ADR, the writer's comment, the test docstring and the register row — never as the cause of anything.

## Next — campaign queue

**Read `git log origin/main` before trusting any sha written here.** **First:** this unit's draft PR (eight checks — `installer/**` is touched); after the operator merges it, restart the branch with `--prune` + `remote set-head` + `checkout -B`. Then the report's §3 in order: **R-50** · **R-61** · **R-57** · **R-58** · **R-59** · **R-64** · **R-63** · **R-62** · **R-45** · then R-03 · R-04 · R-09 · R-13 · R-18 · R-21 · R-22 · R-32 · R-39. The design queue is 19 artboards. **Operator-owned, none blocking:** V-5 (PowerPoint opens the `.pptx`); OR-20's Jan-2024 fact line or the same question re-asked on v1.0.265+; decisions on OR-20b / OR-20c; the 09-11 `"ok": false` transaction-log lines (a 403); an ON-banner screenshot on v1.0.264+; `test_driving_path_whole_schedule_browser.py:104` width-racy (#667); `/settings` sideways scroll; the residuals of ADR-0488 / 0486 / 0485 / 0483; OR-11b, OR-11d; the working-minute axis; the hint bubble; ADR-0484's in-grid rows.

**Review cover is still absent** — Codex quota EXHAUSTED; the mutation batteries and the full gate are all this repo gets.

# (prior) handoffs — archived

> The earlier handoff sections now live in **[HANDOFF-ARCHIVE.md](HANDOFF-ARCHIVE.md)** (newest-first,
> verbatim), and the full append-only per-session history is in **[SESSION-LOG.md](SESSION-LOG.md)**.
> Per ADR-0246 this file keeps ONLY the current STATUS section above, so the entire live handoff is
> small enough to read in full in one pass every session (and the SessionStart hook auto-injects it). When you
> write the next handoff, MOVE the current section to the top of `HANDOFF-ARCHIVE.md` (demote its
> heading to `(prior) Handoff`) and REPLACE the section above — do not stack another archived heading
> here. This single pointer is intentionally the only such heading in the file; the size guard enforces
> that.
