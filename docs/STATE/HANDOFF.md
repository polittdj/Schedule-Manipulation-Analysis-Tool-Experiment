# Handoff — 2026-09-15 (R-47 CLOSED (ADR-0495): Fuse's SPI(t) population is every started activity with a non-zero actual span, baseline or not — an unbaselined member is the formula's blank-as-0 term; Large Test File / File2 8.24 / 8.17 → 8.22 / 8.14 EXACT over Fuse's own 717 / 726; v1.0.263)

STATUS (current) — `main` @ **`e6ff45e1`** (**#678 — OR-18, ADR-0494, v1.0.262 — marked ready and squash-merged by the operator 2026-09-15 17:02Z**; its tree `5cfd4cd1…` == the PR head `c551cb68`'s tree, verified by `git rev-parse` before this branch was restarted on it; #678's eight checks on `c551cb68` all read **success** — `check` 17:02:29Z, `test (3.11)` 17:02:22Z, `test (3.13)` 16:59:24Z, `floor` 16:36:38Z, `browser` 16:35:25Z, `cui-guard` 16:17:55Z, `linux` 16:17:10Z, `windows` 16:21:23Z; **`main`'s own runs for `e6ff45e1` — NOT yet read, the next session reads them first**; #677's squash `18fa3ffb` read runs 1869 / 728 success this session). **This unit is a draft PR from `claude/elegant-gauss-lpk1a4`, ONE commit on `e6ff45e1`** — it was built stacked on #678's head while #678 was open (a docs follow-up `c551cb68` landed on that branch an hour into this session: the previous session was still stewarding it), and #678 merged while this unit's full suite ran; because the trees are identical the restart onto the squash carried the working tree unchanged and there was nothing to merge-resolve. **What landed: R-47, the next row of the report's §3.** The register said "diff the per-activity ratios against the Detailed Metric Report's SPI(t) column by UID" — that column is **empty on every activity row** in both Detailed reports; it carries only the total (8.22 / 8.14) and a **Record Count, 717 / 726**, and that count was the whole answer: the engine's sum of ratios over Fuse's count reproduces both figures exactly, so the residual was one and three missing 0-term members. The engine (since ADR-0176) required a baseline to admit a started activity; Fuse admits **every started activity with a non-zero actual span** and scores an unbaselined one as (blank − blank) = 0 — UID 7260 (in progress, no `<Baseline>` element) on both files; 7262 / 7551 (completed) on File2, named by the workbook's own `Completed (w/o Baseline Duration)` = 2; the zero-span milestone 7183 stays excluded. **Measured:** red-first 5 by name on the pristine tree (both values, both populations, a six-activity unit test) with the other 17 oracle parametrizations green; **18 Fuse SPI(t) oracles now pinned from three Metric History workbooks** (all exact, N/A ↔ NOT_APPLICABLE) and the population pinned from the Detailed report's Record Count (streamed, stopped at row 14); corpus census 27 fixtures, **5 moved, all the started-unbaselined class, none away** (the two older Large Test File saves → 8.22, consistent with `AlltheProjects`' Leveled sheet but not pinned; TP3 0.53 → 0.48 on UID 34, no oracle, nothing pinned it); **mutation battery 5 / 5 RED by name** on a shadowed copy (control 23 green; the pre-ADR gate itself is M1); the members disclosed on `offender_uids` and as a muted note under the EVM scorecard, **measured in Chromium in four themes at 1,440 / 900** (inside the panel, no sideways scroll added); statics clean on both ruff binaries; installers 68 on the rebuilt wheel. **`-m parity` on the final tree: 118 passed / 0 failed in 3:46** (98 + this unit's 20 parametrizations). **The full suite on the final tree: 5,444 passed / 5 skipped / 0 failed in 35:08.** The PR number and its eight checks: the SESSION-LOG follow-up. Highest ADR **0495**. QC-1/QC-2 bind every session — ADR-0393.

**CARRIED FORWARD, NOT this unit's:** OR-17's operator-owned items (a v1.0.262+ screenshot after pasting the CURRENT Hub key; V-4; the `…/v1/chat/completions` request in OR-17 §4; the 09-11 `"ok": false` lines); R-45; `test_driving_path_whole_schedule_browser.py:104` width-racy (#667); `/settings` sideways scroll; ADR-0488's residual. **NEW, operator-owned, registered in ADR-0495:** the VALUE of a completed-unbaselined member's SPI(t) term is 0 by the formula's own blank-as-0 evaluation; File2's total at 2 dp cannot discriminate it from a substituted duration (two members of 726) — only a Fuse run on a small file carrying a completed, never-baselined activity would; not built on an assumption. **Environment facts re-paid:** the clone arrives shallow (`git fetch --unshallow origin` first); no package until the dev install; CI's ruff 0.16.7 at `/usr/local/bin` only after it, 0.15.8 at `/root/.local/bin` shadows it on PATH — run both; the harness exports the git identity as the operator — override `GIT_AUTHOR_*` / `GIT_COMMITTER_*` per command to the `noreply@anthropic.com` identity; **another session can push to the branch you stacked on while you work — `git fetch origin` before every base decision** (this one found `c551cb68` under `7f5dd32f` an hour in).

## UNVERIFIED, stated

- `main`'s own runs for `e6ff45e1` (#678's squash) — not yet read at this close.
- The operator's machine: whether v1.0.262 or later is installed — theirs to report.

## Traps this session paid for, by name

* **A register's "first executable step" can name a column that is EMPTY** — the Detailed report's per-activity SPI(t) cells hold nothing; the Record Count row (row 14) is the discriminating number, and it settled membership before any ratio was compared.
* **A figure an ADR repeats is testimony until a test reads the cell** — 8.24 / 8.22 had no test behind them; the first act was the workbook's row 328.
* **An oracle's total at 2 dp cannot always discriminate a member's TERM** — two members of 726 move the third decimal; say what the corpus settles (membership) and what it cannot (the term's value), and register the discriminating measurement instead of assuming.
* **Two SpreadsheetGear quirks in one workbook** — cells without `r=` and rows without `r=`; a std-lib reader needs both counters (the repo's oracle helper already had them; the scratch reader learned each by crashing).
* **The branch you stack on can move under you — and MERGE under you** — #678 took a docs follow-up an hour in, then went ready-for-review and squash-merged while this unit's suite ran; fetch before basing and again before committing, and compare tree hashes before restarting (identical here, so the restart cost nothing).

## Next — campaign queue

**Read `git log origin/main` before trusting any sha written here.** **First:** this unit's draft PR (number in the SESSION-LOG follow-up) — its eight checks (installers changed); then `main`'s runs for `e6ff45e1`; then the operator's OR-17 §4 replies. Then the report's §3 in order: **R-52** · **R-50** · **R-61** · **R-57** · **R-58** · **R-59** · **R-64** · **R-63** · **R-62** · **R-45** · then R-03 · R-04 · R-09 · R-13 · R-18 · R-21 · R-22 · R-32 · R-39. The design queue is 19 artboards. Other residuals: ADR-0488's / 0486's / 0485's / 0483's, `/settings` sideways scroll, OR-11b, OR-11d, the working-minute axis, the hint bubble, ADR-0484's in-grid rows.

**Review cover is still absent** — Codex quota EXHAUSTED; the mutation batteries and the full gate are all this repo gets.

# (prior) handoffs — archived

> The earlier handoff sections now live in **[HANDOFF-ARCHIVE.md](HANDOFF-ARCHIVE.md)** (newest-first,
> verbatim), and the full append-only per-session history is in **[SESSION-LOG.md](SESSION-LOG.md)**.
> Per ADR-0246 this file keeps ONLY the current STATUS section above, so the entire live handoff is
> small enough to read in full in one pass every session (and the SessionStart hook auto-injects it). When you
> write the next handoff, MOVE the current section to the top of `HANDOFF-ARCHIVE.md` (demote its
> heading to `(prior) Handoff`) and REPLACE the section above — do not stack another archived heading
> here. This single pointer is intentionally the only such heading in the file; the size guard enforces
> that.
