# Handoff — 2026-09-03 (c) (WP4 COMPLETE — the 08-26 CI "outage" root-caused GitHub-side and REFUTED as a standing condition, the route-coverage instrument committed, Law 1 running in CI; /cei on the Claude Design layout; ADR-0455 + ADR-0456, v1.0.233)

> ## STATUS (current) — **On branch `claude/polaris-audit-wp4-huxuz3` (from `origin/main` @ `10a8c62`, the #627 docs merge). WP0/WP1/WP2/WP3 + the operator batch MERGED; this session is WP4 plus the second page onto the Claude Design layout. Draft PR opened at close (see SESSION-LOG for the number and the gate). QC-1/QC-2 bind every session — ADR-0393, pinned by `tests/test_standing_rules.py`.**
> Highest ADR **0456**; version **1.0.233**; wheel + nine installers rebuilt in lockstep as the LAST step (the operator re-downloads from `main` once the PR merges — banner **v1.0.233**). Campaign queue: **WP5** (BOTH folder-ask builds) is next once this PR merges — branch fresh from `origin/main`, open a new draft PR. The design migration continues **one page per session** (standing operator ask).
>
> ## What WP4 settled (ledger section "WP4", ADR-0455)
> **CI-01 the 2026-08-26 `startup_failure` is GitHub-side.** Run #1656 on the #612 merge was created 247 s after the push, died in 4 s with 0 billable ms and four jobs that never left `queued`; `ci.yml` was byte-identical to the green runs on both sides (last change 08-17); the NEXT push's run took 21 minutes to appear and cancelled the 16:22 manual dispatch through the concurrency group. **CI-02 "event triggers are not firing" is REFUTED**: 50/50 `main` commits since 08-01 have a push run, 48 of them within 4 s. Incident attribution is UNVERIFIABLE from the container (status page egress-blocked) — operator ask (f). Also: `main`'s run for `b631b41f` (#626) concluded `failure` — the manifest guard the six web uploads broke, repaired by #627; **read the merge commit's run, never the PR head's.**
> **RC-01 the instrument is committed** — `tools/route_coverage.py` (inventory · passive recorder hooked on `FastAPI.build_middleware_stack` · gap analysis · CLI), the opt-in `SF_ROUTE_COVERAGE` plugin in `tests/conftest.py`, 11 guards. Population today **148** endpoints (71 page · 34 api · 42 export · 1 static) over the **139** floor. **RC-02 the gap BY NAME** is in the ledger, from the instrumented full run (`SF_ROUTE_COVERAGE=1 python -m pytest -q` → `python tools/route_coverage.py route_coverage.json --floor 139`).
> **HOOK-03 the web-upload / hook-less-clone bypass is closed in CI** — `tools/ci_cui_guard.sh` runs THE hook over `base..HEAD` (the inherited-blob ref aimed at the base); the `cui-guard` job is in `check`'s `needs`: a PR fails on any violation, a push to `main` warns for `00_REFERENCE_INTAKE/` (the operator's sanctioned intake channel, ADR-0152) and fails elsewhere; the last step stages a probe `.mpp` and requires refusal. **WF-01** `installer-smoke.yml` gained `workflow_dispatch`, guarded for every workflow.
>
> ## The design migration, page 2 (ADR-0456 — /cei, Chapter 06 "Work piling up")
> Design truth is `Mission Ops Redesign v2.dc.html` (the operator's intake HANDOFF and github.md, refreshed 2026-09-03, call it THE deliverable and the ASTROLABE map superseded; the bundle README disagrees — recorded, not assumed). The artboard was EXECUTED (npm-packed React/Babel, `support.js` patched local, `sfops-boot.skipNext` + `sfredux-guided` set — the first two shots were the boot overlay and the teaching card) and rendered in four themes before a line was written. Shipped: the cursor strip (Auto-play primary · Prev · Next · one `.cd-chip` per snapshot · the frame pill · Running totals), the two ADR-0268 forms as the options row byte-exact, the `1.1fr .9fr` row with the CEI panel verbatim beside a "How to read this" `.cd-block` (the page's own explainer, three beats), the page-neutral `.cd-*` family in `app.css` (DESIGN-SYSTEM §9). Chips drive the stepper's own `render()` (`goTo`/`syncChips`, all below `cei.js:226` so the DD-ledger and r11 axis pins hold). NOT ported, on purpose: GRAIN chips, the CEI line chart, ▦ DATA, ⊞ EXPLORE, every mock figure.
>
> ## Verification, in the shape QC-1 wants
> Red-first: the four layout tests + the chip driver observed **5 red / 3 green** on the pristine page. Green: **160** across the thirteen TestClient modules that pin /cei; the browser modules (r10 panelkit + chip driver, the fake-clock steppers, the sitewide census, the caption sweep, the DD-line render) — counts in the SESSION-LOG. Render diff, four themes: the DOM census moved on exactly `chips 0→2`, `chipOn []→["0"]`; `.panel` 5, forms 6, bars 38, nothing wider than the viewport, zero page errors; all four renders viewed. Mutations: matcher → UNMATCHED reddens 3 instrument guards by name; dead hook reddens the CI self-test; `workflow_dispatch` deleted reddens exactly `[installer-smoke.yml]`; `goTo()` neutered reddens the chip driver alone (session log). Tree restored from scratch copies after every chain.
>
> ## Operator-facing state
> Re-download once the PR merges; the banner must read **v1.0.233**. Still owed by the operator (ask, do not assume): (a) which version banner was behind the blank-header screenshot? (b) did the One-Pager `.pptx` open in PowerPoint? (c) /analysis with both IPMR files: one row per tier, smooth scroll? (d) SRA grid: blank a Factor + Save clears; a pasted header row is NAMED as rejected; Refresh keeps an unsaved edit. (e) NEW — /cei with ≥2 versions: do the version chips beside Auto-play jump the wave, and does the "How to read this" block sit beside the CEI table? (f) NEW — githubstatus.com history for 2026-08-26 15:27–16:42 UTC: an Actions incident? (settles CI-01's attribution).
>
> ## Traps paid for this session, by name
> a run's CONCLUSION is the measurement (`cancelled` / `startup_failure` / a red merge commit under a green PR head) · put event-to-run latency in a table before believing an outage · a module loaded by `spec_from_file_location` must be registered in `sys.modules` BEFORE `exec_module` when it declares dataclasses under `from __future__ import annotations` (the plugin loader had the same latent bug the guard test exposed) · resolve a route template BEFORE dispatch — a `Mount` rewrites the scope it handles · `page.count("<div class=panel")` counts `panel-head` too — parser census only · the canvas needs THREE localStorage keys (screen, `sfops-boot.skipNext`, `sfredux-guided`) and the bounding box is identical whether or not an overlay covers the section — look at the picture · every CDN is egress-blocked but the npm registry is not · `git reset --soft <base>` stages exactly `base..HEAD` for the hook, and `origin/main` must be aimed at the base or a push to main exempts its own blobs.
>
> ## Next — campaign queue
> **WP5** (BOTH folder-ask builds — the three 2026-08-21 folder-gesture facts govern, do NOT re-derive) → **WP6** (ledger highs: CPM-01 · CPM-02 · MC-02 · MC-03 · MAN-01 · REC-02) → **WP7** (thin dims, `ai/txlog.py` first) → **WP8** (consolidated report + roadmap by testimony risk). Design migration, one page per session: next candidates /trend (Ch 05), /forecast (Ch 09), /performance (Ch 07 Command Deck); /compare (Ch 10) needs /integrity's evidence ledger on the page — a feature change, price it as one; alias `vol-*` onto `cd-*` with the third page. RC-02's by-name gap is a work queue for WP6/WP7, not a floor.

# (prior) handoffs — archived

> The earlier handoff sections now live in **[HANDOFF-ARCHIVE.md](HANDOFF-ARCHIVE.md)** (newest-first,
> verbatim), and the full append-only per-session history is in **[SESSION-LOG.md](SESSION-LOG.md)**.
> Per ADR-0246 this file keeps ONLY the current STATUS section above, so the entire live handoff is
> small enough to read in full in one pass every session (and the SessionStart hook auto-injects it). When you
> write the next handoff, MOVE the current section to the top of `HANDOFF-ARCHIVE.md` (demote its
> heading to `(prior) Handoff`) and REPLACE the section above — do not stack another archived heading
> here. This single pointer is intentionally the only such heading in the file; the size guard enforces
> that.
