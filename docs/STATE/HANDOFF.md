# Handoff — 2026-09-14 (/margin on the Claude Design layout — the Control family COMPLETE (ADR-0489); R-49 CLOSED (ADR-0490): an absent `TotalSlack` on a `Critical` activity is the zero the MPXJ writer dropped; R-60 NOT taken and handed off with the save Fuse analysed found in git history; v1.0.258)

STATUS (current) — `main` @ **`6708cbff`** (#671, ADR-0487 / 0488, v1.0.257, merged 2026-09-13 02:54:59Z; `main`'s own runs **CI 1849 / installer-smoke 712 both success** — read from the Actions API this session, not inherited; no merge-record PR, the #667 rule). **Two units landed on branch `claude/confident-johnson-py46p9`, version 1.0.258.** **(1) /margin, ADR-0489** — the eleventh page onto the design and the LAST Control screen (the family: /standards · /scorecards · /margin). The artboard was EXECUTED over loopback HTTP in four themes (zero page errors; 1 h1 · 17 buttons · 3 inputs · 0 selects · 1 svg) and DRIVEN: its `v0`…`v5` status-date chips re-point every KPI tile (`Effective margin 7 wd` → `44 wd`) — a selector STATE, so under the standing "don't modify any of the functionality" they are served as NAVIGATION: one `<a class="cd-chip">` per solvable version linking to `/analysis/<key>`, where that version's margin set is confirmed (the mock's "Which activities ARE margin?" card IS that page's per-version form and stays there); no chip `on`; the pill names the version the takeaway and the tiles read, built from the SAME `months` list so the two cannot disagree; `_solvable_versions_keyed` is the ONE population rule (the provenance chip's), projected. The glossary is hoisted out of the burn-down panel into the artboard's ⓘ callout (`.cd-block.cd-callout` — moved, never duplicated; `.panel` stays 8); the export bar + rate form + band form sit byte for byte in the options position; the two chart panels go VERBATIM into an EQUAL grid (`cd-grid-11`: two 569-px panels at one top, 687 in daylight; every SVG and every head child inside the panel rect in all four themes; the document 1 440 in all four; heights 3 596 / 3 846 / 4 016 / 3 575 → 2 805 / 2 886 / 3 263 / 2 785); the one mock sentence TRUE of the engine ("a flat or growing margin yields no zero-margin date — suppressed rather than extrapolated backwards": `_erosion` extrapolates only on `slope < 0`) is the erosion panel's read-me, proven on a growing fixture. Refused by name in the ADR: the KPI selector (priced — a client-side re-read of the embedded `#marginDashData` months), the confirm card, ▦ DATA (the page's own refusal), "no second simulation" and "a separate toggle on the SRA workspace" (both FALSE — the panel runs the SRA and carries the Fig 7-43 toggle), the verdict pill / coverage bar / WATCH-CORRECTIVE inputs, the band ON/OFF toggle (no state exists), the tiles' colours, the take, ⛶ PRESENT, the kicker, the footer, every mock figure. **(2) R-49, ADR-0490.** Provenance settled on the BINARY: a Java probe through the vendored MPXJ read the intake `Large Test File2.mpp` — the reader holds `TotalSlack = 0.0d` for all 62 Critical activities the written XML lacks the element for (NULL for none; 786 zeros in memory, 0 literal zeros written; critical threshold 0.0d): the writer drops zero durations. The importer infers 0 for an absent element when the FILE carries `TotalSlack` somewhere AND the task is flagged `Critical` (a negative is always written, so the only absent value a Critical task can carry is 0); a completed task's zero stays `None` (R-62 registered, priced). The register's red-first COULD NOT be the Fuse figure — on every fixture those tasks already recompute to exactly 0, so Zero Days Float 66 / 2 were exact on the pristine tree (pinned so the inference can never move them); the red was the ONE fixture where the bases disagree, `Hard_File` UIDs 241 / 249 (480 / 360 min → 0), plus a synthetic file. 624 metric entries snapshotted on eight goldens before/after: TWO moved — Hard_File's float ratio 3.99 → 3.98 and aggregate 2.82 → 2.80. Four stored-slack population pins re-baselined by exactly the inferred count (Project2 65 → 106, Project5 95 → 99, LTF 842 / 1 022 → 844 / 1 024, LTF2 668 / 936 → 730 / 998), every inferred zero exact; ADR-0430's teeth pin (34 stored-less Critical phantoms on Hard_File) had gone inert on the post-ADR-0474 engine — those recompute to 0 / +480 / +360, never negative — and is re-aimed at the inference. **Verification:** red-first on a pristine worktree of `HEAD` — /margin layout 9 failed + browser 5 failed / 2 "nothing moves" guards green by design; R-49 5 failed / 2 by construction; **two mutation batteries, 24 / 24 and 6 / 6 RED by name** on shadowed copies (the imported module asserted to BE the copy; green controls first); two probes of my own found weak by the red run (a chip-effect test looping over ZERO chips; `"Where it lands"` is the chrome's chapter link); neighbours 370 (28 TestClient modules) + 94 / 3 skipped (browser) + 110 (M1 census, i18n, accessibility, air-gap, r12); importers + engine + model 1 581; `-m parity` **97 passed** after five dated re-pins (5 failed / 92 before them, all by the inferred count); both ruff binaries (0.15.8 on PATH, 0.16.7 = CI), format, mypy strict 165; **full suite 5,333 passed / 5 skipped / 0 failed in 34:24** (the standing loopback-allowlist and axis-title env skips), bandit exit 0, `node --check` clean. Version **1.0.258**; wheel + nine installers rebuilt from the final tree (`git log -1 -- tools/mpxj` reads `42d92dc9`, the clone unshallowed first). **Draft PR: the SESSION-LOG follow-up records its number and head** (eight checks expected — `installer/**` changed). Highest ADR **0490**. QC-1/QC-2 bind every session — ADR-0393.

**R-60 NOT taken — deliberately.** The token guardian read 65 % of the assumed wall before the close, and a leveling-splits unit (converter + importer + engine + regenerated fixtures + corpus re-measure) lands whole or not at all. **Handed off with facts this session found:** the Revision-2 `Hard_File_updated3.mpp` Fuse analysed IS in git history — `git show af4d154f:00_REFERENCE_INTAKE/mpp/Hard_File_updated3.mpp` (uploaded 2026-07-09; deleted `b2517780`, re-uploaded `dcacbf44` on 07-15 as Revision 5) — so ADR-0487's "the same save is not available, NOT regenerated" premise is lifted; the vendored MSPDIWriter 16.2.0 has `setWriteTimephasedData(boolean)` and `setGenerateMissingTimephasedData(boolean)` (`javap`); the converter is `tools/mpxj/MpxjToMspdi.java::convert()` (one `new MSPDIWriter().write(project, output)`); the leveling-delay path the splits should ride is `engine/cpm.py` ~1726 / 1870 / 1947 with `_task_shape` / `_recorded_span` / `_LegShape` / `_Exec`; the importer's assignment parse is `_parse_assignments` (mspdi.py ~1007). Red-first target unchanged: UID 403's stored 2026-11-05 09:12 on the SAME save, `getWorkSplits` 10-19 15:00 → 10-21 12:00 then 10-28 13:00 → 10-30 15:00.

**CARRIED FORWARD, still live and NOT this unit's:** `test_driving_path_whole_schedule_browser.py:104` is **width-racy** (registered by #667; its own unit, never fixed by widening a wait) — watch for it on this PR's browser job.

## What the operator still owes — NOTHING arrived with this kickoff, so all three stand

- **V-4** — the gateway's own reason for refusing the saved key (the step-2 PowerShell line: `WWW-Authenticate` + the 401 body, or the v1.0.257 banner's quoted reason). Not received.
- **(b)** whether a fresh NASA AI Hub key made the gateway catalog load (Opus in the Model dropdown). Not reported.
- **(c)** the `"ok": false` lines of the 2026-09-11 morning 403 from `ai-transactions.jsonl`. Not received — and per the standing rule **no prompt-size guard was built** (nothing shows size correlates with the refusals).

## UNVERIFIED, stated

- The Windows DPAPI branch of the key store is exercised only in the field (the POSIX plain branch is what the suite proves).
- R-56's mechanism (whether MS Project derives material / cost spans from something the MSPDI omits) — unchanged.
- The artboard's status-date selector is the MOCK's behaviour; whether the operator wants that selector in the tool is unasked — priced in ADR-0489, not built.
- R-62's wider inference (every absent slack a zero, completed tasks included) is the faithful reading of the MPXJ probe (NULL for none) but was NOT made; no metric reads a completed task's slack today (a census claim from the consumer grep, not from a mutation).

## Traps this session paid for, by name

* **A chip-effect test that LOOPS over the chips it finds passes on a page with none.** Assert the count first; the red run is what found it.
* **"Where it lands" is the chrome's own chapter link** — grep the artifact FIRST for any string you are about to assert absent. Third time this lesson is written (ADR-0484's `cal d`, ADR-0475's assumed citation).
* **A register's red-first can be impossible on every fixture you own.** Fuse's 66 / 2 were exact on the pristine tree because the fallback also read 0 — census the corpus for the case that DISCRIMINATES before writing the pin (Hard_File 241 / 249 were the only two in 28 files).
* **A "teeth" pin ages with the engine.** ADR-0430's phantom set stopped recomputing negative at ADR-0474 and the pin stayed green for two months with nothing to bite. When an engine rule changes, re-derive every pin that cited the OLD behaviour as its reason.
* **`helper() in page` proves the embedding, not the helper** — the helper's bytes are pinned by their own modules; the mutant for the embedding pin is a MODIFIED embedding.
* **Settle a converter's behaviour on the binary, not the XML** (ADR-0487's lesson, applied again): one `javap` + one 40-line probe against `org.mpxj` answered "omitted zero or unknown?" in a minute.
* **`json.dumps` rewrites a fixture's serialisation** — re-detect indent / `ensure_ascii` from the committed bytes, or the diff is the whole file (870 lines of noise, caught before the push).
* **The mock's chips must be DRIVEN before they are named.** The census called them "status-date chips"; the click showed a KPI selector — a different functionality claim, and a different port.

## Next — campaign queue

**Read `git log origin/main` before trusting any sha written here.**

**First:** the draft PR's eight checks on its final head (the SESSION-LOG follow-up names it); the operator's three owed items above. **Then R-60 with the facts in this handoff** (the Revision-2 save at `af4d154f`; the writer's timephased flag; the converter's one-line change; the importer's per-assignment zero-work gaps; the engine honouring them like the leveling delay; red-first on 403's stored 2026-11-05 09:12; the corpus re-measured; Hard_File's 14 / 401 the same class). Then the report's §3 in order: R-46 · R-47 · R-52 · R-50 · **R-61** · R-57 / R-58 / R-59 · **R-62** (ADR-0490's registered widening) · then R-03 · R-04 · R-09 · R-13 · R-18 · R-21 · R-22 · R-32 · R-39. **The design queue is 19 artboards** (§6) — the Control family is complete; the next screen is the operator's order. Other residuals carried verbatim: ADR-0489's KPI-selector unit · ADR-0488's own (a saved model the catalog cannot confirm reads "— not installed"; a fresh config's default model is an Ollama id) · ADR-0486's (`max_completion_tokens` as a second attempt; an Ollama `done_reason` disclosure; the cross-check's `last_completion`) · ADR-0485's (the model dropdown probes with the SAVED token) · ADR-0483's (a live-peer response cut at 5 s) · **`/settings` scrolls sideways (1 877 px console / apollo / jarvis, 1 641 daylight at 1 440 — an over-wide `<select>`; its own UI unit)** · OR-11b · OR-11d · the working-minute axis · the hint bubble · ADR-0484's in-grid rows.

**Review cover is still absent** — Codex quota EXHAUSTED through #671; the two mutation batteries and the full gate are all this repo gets.

# (prior) handoffs — archived

> The earlier handoff sections now live in **[HANDOFF-ARCHIVE.md](HANDOFF-ARCHIVE.md)** (newest-first,
> verbatim), and the full append-only per-session history is in **[SESSION-LOG.md](SESSION-LOG.md)**.
> Per ADR-0246 this file keeps ONLY the current STATUS section above, so the entire live handoff is
> small enough to read in full in one pass every session (and the SessionStart hook auto-injects it). When you
> write the next handoff, MOVE the current section to the top of `HANDOFF-ARCHIVE.md` (demote its
> heading to `(prior) Handoff`) and REPLACE the section above — do not stack another archived heading
> here. This single pointer is intentionally the only such heading in the file; the size guard enforces
> that.
