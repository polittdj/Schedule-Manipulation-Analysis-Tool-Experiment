# Handoff — 2026-09-06 (WP6b COMPLETE — the ledger TAIL verified by execution: 11 rows CONFIRMED and fixed red-first, 6 REFUTED, RC-02's never-2xx list empty (ADR-0467); /performance is the sixth page on the Claude Design layout (ADR-0468); v1.0.240)

> ## STATUS (current) — **This session's branch is `claude/continue-previous-rsq655`, branched by the harness on `origin/main` @ `b8e8aa42` (the #639 squash; `main`'s own run #1743 concluded `success` at 00:32:28Z, installer-smoke #646 `success`). Draft PR **#640** (`claude/polaris-audit-resume-e9t5h1`, ADR-0466 + v1.0.239) was OPEN when this session began — five checks green, both `test` jobs in progress on its final head `9681d5f0` — so this session's numbers SKIP its claims: ADRs **0467/0468**, version **1.0.240**. If #640 is CLOSED unmerged, 0466 / 1.0.239 stay a gap (documented, harmless); if it merges after this PR, the operator resolves the docs (HANDOFF / SESSION-LOG / LESSONS / kickoff / installers) by merge, never by renumbering. Banner **v1.0.240** (wheel + nine installers rebuilt after the last source edit). Statics: ruff (whole tree) · format · mypy --strict (163 files) · bandit exit 0 · `node --check` green. Pytest: the 15 new + 3 repaired WP6b modules **90 passed**; the 108-module web/ai/guard subset the changes touch **1,120 passed / 3 skipped** after the I-01 regression was fixed; the design page's guards 216 + 5 + 50 green; **the full suite's single figure is recorded in the follow-up commit** (the first engine+parity run died without a summary under five concurrent jobs; re-run alone). QC-1/QC-2 bind every session — ADR-0393, pinned by `tests/test_standing_rules.py`.**
> **2026-09-06 (c) addendum — the resume session (`claude/polaris-audit-resume-0gv4bo`), ~07:00Z.** **#640 MERGED** by the operator at 06:22:01Z → `main` @ `5b219952` (ADR-0466, v1.0.239; per #642's record `5b219952^{tree}` == `9681d5f0^{tree}`, `aef52f7e…` — `origin/main`'s tree re-read here as `aef52f7e…`); `main`'s OWN runs for that squash: CI **#1751** (id 34016299655, `in_progress` at this record — read its conclusion first), installer-smoke **#651** `success`. **#641** (`claude/continue-previous-rsq655`, this section's work) went **8/8 green on its FINAL head `d61f6395`** (CI #1750, installer-smoke #650) and the operator marked it READY, but it is `dirty` against `main` (15 files: `pyproject.toml`, the nine installers, five state docs) because it forked from the #639 squash beside #640. **Resolved by MERGE, never by renumbering:** this branch is `origin/main` @ `5b219952` + a merge of `d61f6395` — version **1.0.240** kept, the wheel + nine installers REBUILT from the merged `src` (the lockstep pin), SESSION-LOG both 2026-09-06 entries in order, the ledger's CI-red (0466) then WP6b (0467) sections, LESSONS both entries newest first, this handoff = the WP6b rotation + this line, the archived 2026-09-05 (b) section = `main`'s final version WITH its ADR-0466 addendum. A session may not push to another PR's branch, so the merge rides THIS branch's draft PR (number in the SESSION-LOG follow-up): the operator merges it and closes #641, or says the word and the identical merge commit is fast-forwarded onto #641's branch. The docs-only draft **#642** (`claude/polaris-audit-resume-e9t5h1`, the sibling session's record of #640's merge) is in flight and needs a `main` merge whichever lands second.**
> Highest ADR **0468**. Campaign queue: **WP7** next — thin dimensions, `ai/txlog.py` first (Law 1), then RC-02's 15 never-adverse POSTs (`/sra/*`, `/project/*`, `/margin/*`, `/dcma/scope`, `/fields/roles`) and the six never-adverse exports → **WP8** (consolidated report + roadmap by testimony risk). The standing one-design-page-per-session ask was **MET** (/performance, 07); next: **/compare (10 What changed)** is a feature change (needs /integrity's evidence ledger on the page) — price it before building; the remaining migrated candidates go one per session.
>
> ## What this session shipped
> **WP6b (ADR-0467) — the 27-row ledger tail + RC-02's three never-2xx routes, by the WP6 method** (re-derive the finder's line as of `1b833c6a`, build the refuting check, fix as verified). Red-first on the pristine tree: 21 failed + 1 module red at import; a 23-mutation battery on scratch copies under `PYTHONPATH` red by name 23/23 — after the battery was caught reporting an un-landed mutation as green (an anchor missed a blank line; the runner now aborts loudly on a failed patch). **CONFIRMED-FIXED (11):** CPM-03 (the ADR-0391 actual-start floor now applies under an MSO/MFO pin in both forward passes — MS Project's stored Start == Actual Start on 240/240 started fixture tasks; no golden moves) · CPM-04 (an activity-less network no longer prints the project start as a finish on 8 surfaces — header sentence, brief sentence, resolvers skip an activity-less FILE by name, `cpm_finish: null`; **never a filter that emptied the scope** — the first cut broke the I-01 disclosure, now mutation M23) · MF-03/MF-04 (formula text for `critical`, lags, leads describes the basis the code uses; dictionary regenerated) · MF-08 (`round_half_up` at the 30 MetricResult-feeding sites — CEI 1/8 read 0.12, Critical 1/8 read 12 %; parity unmoved) · MC-05 (an SRA focus outside the network is refused by name, never the project finish in silence) · MC-07 (a factor table not covering 1..5 cannot construct; the setup restore keeps the session's table) · IMP-03 (the five saved-view models frozen) · IMP-06 (an empty calendar registry round-trips empty) · MAN-02 (a prior-only activity's booking cited to the prior file) · MAN-03 (a None→N remaining-work figure is not "burn-down") · JS-03 (three drill filters keep the caret; `driving_tiers.js`'s r11 digest re-baselined, dated) · JS-06 (band labels `≤ 5 …`, the drill heading spells the open lower bound, the export mirror matches) · TST-02/TST-03 repaired. **IMP-05 CONFIRMED by an independent oracle** (MPXJ 16.2.0 maps `target_*` to PLANNED, never baseline) and DISCLOSED as an import note, values kept. **REFUTED (6):** MF-06 (the finder's basis would empty the CEI numerator; the XER N/A is honest and now said), MC-04, MC-06, IMP-02 (promised invariants hold; tolerated gaps named), JS-02 (29/29, no growth), JS-04 (live since ADR-0446). **UNVERIFIABLE as filed:** MF-07/09/10, MC-08. **NON-REPRODUCED:** IMP-04 (the only XER carries no `status_code`). **MEASURED, left:** JS-05 (56 dead CSS tokens, listed). RC-02's three never-2xx routes were green on first drive — coverage, not defects.
> **/performance on the Claude Design layout (ADR-0468).** Artboard 07 recovered by EXECUTING the canvas over loopback HTTP (`setScreen('px')`, four themes, zero errors). The fourteen tiles stay VERBATIM inside the ONE grid the enlarge/export contracts span, regrouped under five numbered `.cd-band` headings in the design's order; the server-rendered stepper is RE-HOMED into a masthead strip (chips, the SELECTED file on, a pill); every step publishes `data-frame` and syncs the cursor in the same task (ADR-0466, no observer needed — one function); the reading block reuses the page's own three `_explain` beats (hoisted into constants, re-exported `X as X`). The r10 contract's POSITIONAL tile tuple was a premise pin on order — re-derived, dated. Four-theme census pristine → patched: `.panel` 18 · tiles 14 · forms 5 · hosts 14 · cf-bars 14 identical; moved on the design's keys only; zero page errors; nothing wider than the viewport.
>
> ## Operator-facing state
> Re-download once the PR merges; the banner must read **v1.0.240**. ASK (do not assume): (1) IMP-05 — on a P6 XER, should HMI/BEI read the PLANNED (target) dates as the baseline at all (P6 shows them as BL dates only with no baseline project assigned), or read N/A until a baseline project is exported? A Fuse export on an XER would settle the parity leg. (2) the /onepager-compare rulings (swimlane move · slip threshold · three-slide export) stand as stated on the page. (3) the standing asks from #635/#631/#630 (the /integrity working-day count for UID 152; /forecast's chips with two files; the parent-folder question on `/`; /trend's chips; T-01 · I-01 · the /analysis lag).
>
> ## Traps paid for this session, by name
> a mutation must LAND before its verdict counts — an anchor that misses a blank line runs the test on an unmutated copy and reads GREEN; make the runner abort on a failed patch · a resolver that skips "no schedulable activity" must ask whose emptiness it is — a FILE with none is skipped by name, a FILTER that emptied the scope is the page's own disclosure (I-01 went red on the first cut) · a finder's proposed basis can be wrong in the other direction (MF-06: `is_effective_critical` excludes completed work and would empty a numerator) — measure the fix, not just the claim · two resolver bodies that are byte-identical defeat an exact-match patcher — anchor on the line that differs · `pgrep -f`/`pkill -f` match your OWN shell's command line — a "still running" process can be the grep · five concurrent pytest/mypy/browser jobs killed one run without a summary — re-run the long suite alone · a contract test that zips tiles to mounts POSITIONALLY is a premise pin on order — re-derive the tuple, never the assertions · a served tile spans lines (its hint attribute carries real newlines): `re.S` · the canvas seeds are raw strings except `sfops-boot.*` (JSON) · a module-level name added to an extracted page module must be re-exported from `web.app` `X as X` or the monolith split contract goes red · guess no test-module path — `ls` it first (two "silent" runs were `file not found`).
>
> ## Next — campaign queue
> Branch FRESH from `origin/main` once this PR (and #640, in either order) is merged (`git fetch --prune origin && git checkout -B <branch> origin/main`). **WP7** (`ai/txlog.py` first — Law 1; the 15 never-adverse POSTs and 6 exports as one parametrised empty-session/adverse test each) → **WP8**. The design page owed each session: **/compare (10)** priced as a feature change first, else the next migrated candidate by referrer walk. Observed, not fixed blind: `evm.py`'s `actual_cost or 0.0` ACWP · `path_evolution`'s pure-logic critical list · MF-08's residual (`dcma14`'s float-band integer roundings; 176 `round(` sites outside `engine/metrics`) · JS-05's 56 dead CSS tokens · the other browser modules' EXPRESSION-string waits (ADR-0466 §3) · IMP-05's stricter option. CI-04 (the /driving-path header-row equality race) is the remaining CI candidate.

# (prior) handoffs — archived

> The earlier handoff sections now live in **[HANDOFF-ARCHIVE.md](HANDOFF-ARCHIVE.md)** (newest-first,
> verbatim), and the full append-only per-session history is in **[SESSION-LOG.md](SESSION-LOG.md)**.
> Per ADR-0246 this file keeps ONLY the current STATUS section above, so the entire live handoff is
> small enough to read in full in one pass every session (and the SessionStart hook auto-injects it). When you
> write the next handoff, MOVE the current section to the top of `HANDOFF-ARCHIVE.md` (demote its
> heading to `(prior) Handoff`) and REPLACE the section above — do not stack another archived heading
> here. This single pointer is intentionally the only such heading in the file; the size guard enforces
> that.
