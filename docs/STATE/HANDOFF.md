# Handoff — 2026-09-07 (c) (R-44 CLOSED — resource calendars and leveling delay in the base CPM, MS Project's own stored dates as the oracle (ADR-0474): Hard_File +42 d → −1 d, Project2 / Project5 exact with every stored slack reproduced, Net Finish Impact −134 = Fuse; the latency amendment — /api/sra 4.27 → 2.0 s, byte-identical; v1.0.245)

> ## STATUS (current) — **This session's branch is `claude/polaris-audit-plan-forward-3vp28m` (the harness's designated branch), restarted FRESH on `origin/main` @ `dcd64509` (the #648 squash, docs-only; tree-identical to its head `c9c264be`). #647 and #648 were MERGED by the operator this session; no PR was open at the restart. This unit's draft PR is **#649**; its browser job went RED on the first heads (`/sra` captions timed out — `compute_cpm` on the leveled goldens 1.3 → 3.7 ms, `GET /api/sra` 1.58 → 4.27 s) and the **latency amendment to ADR-0474** fixes it in the same PR: every schedule-only structure is derived once per schedule OBJECT and found by identity (`_Ruler` per calendar with memoized day cores, `_PlanShape` per task, `_Network` per schedule; the backward target hoisted; one-leg plans skip the generator) — byte-identical on a 7.7 MB dump of every field on every fixture, measured 1.8–2.1 ms / 2.0 s, three count gates in `tests/perf/test_perf_regression.py` red-first on the pristine engine, the ADR-0474 head and a three-mutation scratch copy. The `floor` job then surfaced SEVEN pins the cancelled test jobs never reached, all adjudicated in the same PR: the Large Test File's pure-logic critical count **2 → 33 = MS Project's stored Critical flags exactly** (the dashboard hashes re-pinned; DCMA-12 NA → FAIL because a critical path now exists), the 188→187 counterfactual **+15 → +12 wd** (engine-derived, still non-zero), and a PRODUCT DEFECT fixed — the Driving Path `ignore_leveling` re-solve had gone inert because it cleared stored dates but not the stored leveling delay the base CPM now honours (2 / 0 targets diverged; 50 / 56 after the fix). The browser proof then lost the LAST theme's three `/sra` cells on the CI runner (9 of 12 green): every load of `/sra` re-ran the identical seeded thousand-solve simulation, so `SessionState.sra_result` now memoizes the legacy `/api/sra` result per input set (identity on the scoped schedule object, value-equality on config / overrides / risks, single-flight on a stripe, wiped by default; the same result object → byte-identical), with a red-first count gate. Banner **v1.0.245** (wheel + nine installers rebuilt after the last source edit). Highest ADR **0474**. Statics on this tree: ruff check + format (whole tree) · mypy --strict 163 files · bandit exit 0. The gate figures are in the SESSION-LOG follow-up. QC-1/QC-2 bind every session — ADR-0393, pinned by `tests/test_standing_rules.py`.**
>
> ## What this session shipped (ADR-0474, R-44)
> Every MSPDI carries MS Project's own computed Start / Finish / Early / Late / TotalSlack / Critical per activity — the CPM's oracle was in the file. Hard_File's 42-day gap had two causes, both honoured now: **an assignment runs on the crew's calendar** (78 of 87 bookings span the task duration on the 16-hour / 24-hour crew calendar; a FIXED_UNITS booking spans work / units — UID 200 —, a fixed-duration / fixed-work booking the whole task — Large Test File2's 117 fixed-work bookings), and **a resource-leveling delay is elapsed time added after the calendar admits the task** (tenths of a minute; 12 / 16 / 18 / 16 delayed activities explained exactly on Hard_File / updated / Project2 / Project5). Slack is measured on the TASK's calendar (UID 178's stored 240 = project minutes; the crew calendar reads 720); a predecessor's late finish is the leveled successor's late start less its delay, snapped back to a finish instant. New model fields: `Resource.calendar_uid`, `Task.task_type` / `ignore_resource_calendar` / `leveling_delay_minutes` (schema 2.12.0); the registry carries assigned crews' off-pattern calendars. **After:** Hard_File / updated / updated2 within 1 d of the stored finish, Critical agreed 108 / 110 / 80 of 110 (was 54 / 59 / 73); Project2 / Project5 finish EXACT (was −15 d / −1 d), stored slack 65 / 65 and 95 / 95 exact (was 7 / 8); the Large Test Files unmoved. **Every parity pin that moved, moved onto the reference:** Net Finish Impact −148 → −134 = Fuse's HSD10 (the bridge `-134 = -134 - 0 + 0`), the SN04 96↔99 swap closed, forecast finishes 2027-09-14 / 2028-01-26 (stored), FUSE-VALIDATION's "Project2 differs (08-30 vs 09-14)" closed. Not behind a flag: `IgnoreResourceCalendar` is MS Project's own per-task off switch.
>
> ## Measured, deliberately left (rows R-55..R-59)
> **R-55 (T1, M)** — updated3 −6 d / updated3_24hr +17 d are progress semantics: a started activity is floored, not pinned, at its actual start (completed UIDs 291–298 land 36 days late by logic) and a completed milestone's actual instant is snapped to the project calendar; a scratch-copy pin read −13 d / −2 d · **R-56 (HELD)** — four bookings the MSPDI cannot explain (UID 14: 40 h for 24 h at 200 %; 401; 403's twelve-day contour; 385) put the three early snapshots one day early · **R-57** assignment-level LevelingDelay unread · **R-58** the task ∩ crew calendar intersection approximated · **R-59** the /analysis disclosure names task calendars only.
>
> ## Traps paid for this session, by name
> `LevelingDelay` is tenths of a minute (Project2 UID 37 = 28800, not the 2880 the model shows) — read the RAW value before calling a unit inconsistent · the finish-role vs start-role grid point: a fast-path successor's late-start need arrives as Monday 08:00 and must be snapped BACK to Friday 17:00 before it is a late finish · a booking rule proven on one file (work / units, Hard_File) broke another (Large Test File2's fixed-work contours) — test a scheduling rule by TASK TYPE across every golden before shipping it · the pristine-src red-first proof is the cheapest mutation proof, but a targeted mutation (drop the delay; run every booking at the duration) names the rule that died · a re-pin is legitimate only toward the reference: each moved pin was checked against the stored value it now equals · the full suite takes ~40 min — start it in the background the moment the engine settles, re-run only the changed files after · **an engine change is priced on the SRA before it ships** (`compute_cpm` runs 1 000× per `/api/sra`; time the served golden pair — 1.6 s is the number to keep) · **never key a hot memo on a frozen pydantic model** — `lru_cache` on `Calendar` paid a full-model hash + equality per lookup (8 607 hashes in 20 solves); find the object by identity, retire the entry by weakref · a ratio gate against a baseline the fix also speeds up proves nothing — pin COUNTS, record the latency · **"unmoved" in one measure is not unmoved** — the Large Test Files' finishes did not move while their critical SET went 2 → 33 (onto the stored flags); only a byte pin saw it · **an option that EMULATED a feature goes inert the day the engine implements it** — re-read every toggle named after the thing you just built (`ignore_leveling`) · read the `floor` job's log even when the `test` jobs are cancelled: it runs the whole suite without coverage and finishes first. · **a page that re-runs a seeded simulation on every load is a latency bug waiting for a slower runner** — a pure function of the session's inputs is memoized per input set, identity on the object, value on the frozen inputs, single-flight.
>
> ## Next — campaign queue
> Read #649's verdict on its FINAL head first (eight checks; the browser job is the one that was red); branch FRESH from `origin/main` once the operator merges it. The report's §3 in order: **R-55** (progress semantics: pin started work at its actual start, keep completed work at its actual dates — the SSI goldens and ADR-0391's TP4 v5 / TP1 pins are the arbiter; measured −13 d / −2 d in a scratch copy) · R-49 (the zero-slack inference) · R-46 (BCWS +150 on a crew calendar — now a plan-aware proration) · R-47 · R-52 · R-50 · R-57 / R-58 / R-59 (the ADR-0474 residuals, S each) · then R-03 / R-04 / R-09 / R-13 / R-18 / R-20 / R-21 / R-22 / R-32 / R-39. PLUS the design page owed each session (/standards or /scorecards — the recipe in ADR-0471/0472; not delivered this session either: two units shipped, the page is owed twice).

# (prior) handoffs — archived

> The earlier handoff sections now live in **[HANDOFF-ARCHIVE.md](HANDOFF-ARCHIVE.md)** (newest-first,
> verbatim), and the full append-only per-session history is in **[SESSION-LOG.md](SESSION-LOG.md)**.
> Per ADR-0246 this file keeps ONLY the current STATUS section above, so the entire live handoff is
> small enough to read in full in one pass every session (and the SessionStart hook auto-injects it). When you
> write the next handoff, MOVE the current section to the top of `HANDOFF-ARCHIVE.md` (demote its
> heading to `(prior) Handoff`) and REPLACE the section above — do not stack another archived heading
> here. This single pointer is intentionally the only such heading in the file; the size guard enforces
> that.
