# Deep-dive audit — 2026-08-16 (LIVE, in progress)

Operator directive (2026-08-16): *"a complete deep dive audit of the entire repository …
identify any errors or mistakes or anything that needs to be updated … create tests, both
pass and fail, to verify the validity of your findings … create solutions … test those in a
sandbox to verify prior to implementing any changes. Triple verify everything in
independent ways. Do all cleanup and housekeeping necessary. Test, pass and fail tests
required for all functionality of all pages. Make sure ALL functions work properly. Skip
nothing. Verify everything."*

Run under **ADR-0240** (Ultracode fan-out; the lead re-verifies every finding against code
and executable tests before it is reported or acted on) and **QC-1/QC-2** (ADR-0393).

## Status legend — READ THIS BEFORE TRUSTING A ROW

| mark | meaning |
| --- | --- |
| **LEAD-VERIFIED** | the lead reproduced it with an executable probe **and** an independent oracle (docs, reference library, or a second instrument). Actionable. |
| **REPORTED** | a finder claimed it; **no lead verification yet**. A hypothesis — this round has already produced finder claims that were wrong in both directions (see Method notes). |
| **FIXED** | landed with red-first tests + mutation proof + full gate. |

## Method notes — why REPORTED ≠ true

Two measured facts from this very audit, both arguing against acting on unverified claims:

1. Round 1's `security_cui` verifier died, so the orchestration script's "no verdict =
   refuted" rule filed a **critical, real** CUI bypass as *refuted*. The lead re-verified it
   anyway and it was genuine (HOOK-02 → ADR-0409).
2. That finder's **suggested fix was wrong**. Sandbox measurement before implementing showed
   the proposed `--` separator left three bypass shapes open; the shipped fix differs.

## FIXED

### HOOK-02 — the CUI guard failed open on git-magic filenames (**critical**, Law 1) · ADR-0409
`.githooks/pre-commit` read staged bytes via `git show ":$path"`, so a name beginning `!`,
`^`, `<digit>:` or `:(` parsed as git magic and returned **empty output** — read by every
content detector as "clean". A real schedule committed silently under `!plan.json`; a plain
`git add -A` stages such names. Fixed by resolving the index OID. Triple-verified: 6 tests
red by name → module 98 green · an independent bash battery (10 hostile shapes blocked, 4
benign controls still allowed, ADR-0152 exception intact) · mutation battery **4/4 by name**.

## LEAD-VERIFIED — awaiting fix

### MC-01 — a fired register OPPORTUNITY zeroes the activity's duration (**critical**, Law 2) · **FIXED, ADR-0414**
`engine/sra.py:1701` (and the twin in `engine/jcl.py`): a fired risk REPLACES the sampled
duration — `overrides[u] = max(0, impact)`. `ScheduleRisk.impact_days` is documented at
`sra.py:708` as *"**additive** working days when it fires (>=0 risk, **<0 opportunity**)"*, and
opportunities are a first-class product feature with their own 5x5 Opportunity matrix. For a
negative impact, `max(0, impact)` is **0** — the activity is replaced with a zero-duration
task instead of being shortened.

**Executable proof** (20-day driver → 5-day successor, focus = uid 2, one certain −5 d
opportunity on the driver):

| run | P50 |
| --- | --- |
| no register | 25.0 wd |
| −5 d opportunity | **5.0 wd** |
| correct if honoured | 20.0 wd |

The finish collapsed by the **full 20 days** of the driver, not by the opportunity's 5 — a
**15-working-day optimistic error** in a figure an SRA quotes. Law 2 calls a fast wrong number
worthless; an optimistic one in a testimony deliverable is worse than worthless.

**Why this is NOT a blind fix.** ADR-0359 made a fired risk *replace* the duration, and that
was measured against SSI's own export — but only for **positive** impacts. Replacement is
meaningless for a negative one, and the field's own docstring says **additive**. The natural
correction is: positive → replacement (ADR-0359, parity-locked); negative → subtract from the
sampled duration, floored at 0. That is defensible from the documented semantics, but the
**parity** question — what SSI itself reports for a fired opportunity — needs the reference
export as its oracle, exactly like MF-05. Implement with red-first tests and, if an SSI
opportunity export exists, pin against it; if it does not, ship the documented-additive
semantic and say plainly in the ADR that the parity leg is UNVERIFIED.

**Also check the JCL twin**: `engine/jcl.py` replicates the same replacement block, so an
opportunity almost certainly zeroes there too — the fix and its tests must cover both engines
(ADR-0408 made them mirror each other deliberately).

### MF-02 — the EVM workbook exports a fabricated `0.0` for every NOT_APPLICABLE index (**high**, Law 2) · **FIXED, ADR-0411** (row was stale; re-verified ADR-0439)
`web/app.py:2983` writes each index as
`indices[k].value if k in indices and indices[k].value is not None else ""`.
That guard **never fires**: `_na_index()` (`engine/metrics/evm.py:259`) builds the NA result
with `value=0.0`, not `None` — and its own docstring reads *"NA, never a fabricated 0."*

**Executable proof** (schedule with no `budgeted_cost` at all):

| index | status | value | workbook cell |
| --- | --- | --- | --- |
| SPI | NOT_APPLICABLE | 0.0 | **0.0** |
| CPI | NOT_APPLICABLE | 0.0 | **0.0** |
| TCPI | NOT_APPLICABLE | 0.0 | **0.0** |

An analyst opening the hand-out sees CPI = 0.00 — which reads as catastrophic cost
performance — where the truth is "this file carries no cost data". That inverts the design
system's standing rule (*missing shows an em dash, never a fabricated figure*) and Law 2,
in an exported artefact that leaves the tool and can be quoted.

**FIXED, ADR-0411** — and this row sat in the queue for a week AFTER it shipped. The fix is
`web/app.py::_export_cell`, which gates on `status is CheckStatus.NOT_APPLICABLE` rather than on
`value is not None`. Re-measured 2026-08-25 (ADR-0439) against the shipped workbook bytes, not the
source: on a cost-free file the engine still reports `spi/cpi/tcpi status=NA value=0.0` (the
precondition is unchanged and correct), and the exported cells read `'' / '' / ''`. Restoring the
old guard puts `0.0 / 0.0 / 0.0` back in the sheet, so the probe is not blind.

**The `value is not None` CLASS is closed too**, which this row demanded and nobody had checked:
an AST census of `<expr>.value is/is not None` across `src/` returns exactly three sites, and all
three are correct — `_export_cell` and `web/performance.py:144` both gate on `status` FIRST (the
`value` check is belt-and-braces), and `web/app.py:7415` is `_Magnitude.value`, a genuine
`float | None`. `MetricResult.value` is declared `float`, so any guard of that shape on a
MetricResult is dead by construction — which is what makes the class mechanically detectable.

## FIXED (2)

### MF-01 — TCPI was scored with its PASS direction INVERTED (**high**, Law 2) · ADR-0410
`engine/metrics/evm.py:332` — `_index()` hardcodes `Direction.GE` for all three indices.
SPI/CPI are correctly "higher is better"; **TCPI is inverted by definition**: TCPI > 1 means
the remaining work needs *better* efficiency than achieved so far.

Three independent confirmations:
1. **The repo's own published definition contradicts the code** — `web/help.py:571`:
   *"TCPI **<= 1.0** means the remaining work can complete within budget … > 1.0 requires
   better-than-planned performance"*, and the formula line says `(pass <= 1.0 …)`.
2. **Executable probe**: a project needing 1.6× efficiency → `verdict=PASS`; a comfortable
   project at 0.25× → `verdict=FAIL`. Exactly backwards.
3. **Two existing tests PIN THE DEFECT** (ADR-0385 stale-guard class, 3rd instance) and must
   be repointed in the same commit:
   - `tests/test_projects/test_pass_fail_battery.py:801` — asserts a **blown** (over-budget)
     project's TCPI is `PASS` at 1.17;
   - `tests/engine/metrics/test_evm.py:364` — asserts TCPI `0.5` (comfortable) is `FAIL`.

**Impact:** the affordability dimension shows green precisely on the projects that cannot
afford their remaining work — the worst possible failure in a testimony context.
**FIXED (ADR-0410, v1.0.208):** `_index()` takes an explicit direction; TCPI is `LE` at
1.0. THREE oracles pinned the defect and were repointed. Sandbox-measured before the
tree was touched (4 failures / 430, all bug-pinning); mutation battery 4/4 by name.
The NASA `.aft` confirms the FORMULA was always right — only the verdict was wrong, so
no parity value moves and `help.py` needed no edit.

## REPORTED — not yet lead-verified (do NOT act on these as written)

From round 2's finders. Counts: CPM 4 · metric-formula 10 · Monte-Carlo 8.

**CPM / float** — CPM-01 (*critical*, `cpm.py:1316`) progress-override floor vs the project
axis · CPM-02 (*high*, `driving_slack.py:314`) `endpoint()` measuring a link's two ends on
different rulers when one end is dated · CPM-03 (*medium*) docstring states the ADR-0391
rule unconditionally · CPM-04 (*low*) `network_finish` default 0 fabricates a finish.

**Metric families** — MF-02 (*high*, `web/app.py:2983`) the EVM workbook writes a fabricated
`0.0` for NOT_APPLICABLE cost indices · MF-03/04 (*medium*) published formulas for
`critical`/`negative_float` and the lag/lead counters describe a basis the code does not use ·
MF-05 (*medium*) `_pct_result` returns PASS on an empty population — **mechanism confirmed
by the lead** (`percent()` yields 0.0 when population is 0, which then evaluates PASS
against an LE bar), but **whether that is a DEFECT is unresolved and must not be "fixed"
blind**: if Acumen also reports 0%/PASS for an empty population, matching it is *correct*
under Law 2 and changing it would BREAK parity. Needs the reference export as the oracle,
per metric, before any edit · MF-06 (*medium*)
`cei_critical` reads `stored_is_critical` instead of `is_effective_critical` · MF-07/08/09/10
(*low*) docstring contradictions, banker's rounding, and two axis/unit mismatches.

**Monte-Carlo** — MC-01 (*critical*, `sra.py:1701`) a register **opportunity** (negative
impact days) mishandled · MC-02 (*high*) ADR-0308's "finished work cannot be delayed" rule
missing from a third code path · MC-03 (*high*, `jcl.py:284`) a missing `actual_cost` on an
in-progress task treated as 0 spent · MC-04…08 (*medium/low*) branch endpoint gating,
`_finish_of` silent project-finish substitution, a docstring guarantee, and a silent
`(0.0, 0.0)` fallback in `RiskFactorTable.for_factor`.


## FIXED (2026-08-17 (c)) — the kickoff's resume order, items 0-4

Every row below was re-verified by the lead with an executable probe BEFORE any edit, fixed
red-first, and mutation-proven by name. Two mutants initially SURVIVED and were battery defects,
not code defects (see LESSONS-LEARNED 2026-08-17 (c)).

### REC-01 — the disclosure DID reach quantified recovery surfaces · **ADR-0413**
**It holds, and ADR-0407 is corrected rather than defended.** ADR-0407's category justification
was verified against `web/risks.py` only; `ai/briefing.py` has no category gate. Measured on
v1.0.210: briefing §5.2 **"Potential recovery" = 20 wd**, §6 **"Expected effect" = 20 wd**, §6.2
*"up to about 20 workday(s) … potentially recoverable"*, `/risks` card **20/25 rk-extreme** —
on a finding whose recovery is zero by its own wording. Fixed with `Finding.is_disclosure`
(declared, not inferred: `driving_path` is INFO/OPPORTUNITY too and IS a lever). Mutation 7/7.

### MC-01 — **FIXED, ADR-0414** (both engines)
Reproduced independently (25.0 → 5.0 wd, expected 20.0). Sign-of-summed-impact: net risk
REPLACES (ADR-0359 untouched, `+5 d → 10.0 wd` control), net opportunity SUBTRACTS, floored at 0.
`jcl.py`'s twin confirmed defective and fixed identically. **Parity leg UNVERIFIED** — no
committed SSI export exposes a fired negative impact. Parity gate re-run 72/15/exit 0: unmoved.

### TST-01 — **FIXED, ADR-0415** — and the finder's DIAGNOSIS was wrong
The fact is right (`_CPM_HOLDERS` stale: `web.state` absent, `web.app` dead-and-silently-skipped;
24 modules bind `compute_cpm`, the tuple named 10). The cause is not: with the sweep repaired the
injected solves ARE counted (2→4) and the test still passed, because `after_page` is a
**self-baseline**. Two defects. Now a computed sweep (ADR-0352's promised standing sweep) plus a
build-count ceiling. Blind proven first, sighted proven after; sweep guard mutation-proven 2/2.

### JS-01 — **FIXED, ADR-0416** — confirmed in real Chromium
Clicking the parity checkbox changed nothing; console: *"Refused to execute inline event handler
… `script-src 'self'`"*. ADR-0268's `chrome.js` already existed for this but matched
`select[data-sf-autosubmit]` only. Widened to `[data-sf-autosubmit]` **plus a real submit button**
so the toggle needs no JS. Re-probe: toggles, **zero CSP violations**. A view-layer census proves
this was the **only** inline handler in the tree — a single site, not a class.

### SRA-EXPORT-STALE-SCOPE — **FIXED, ADR-0417**, one leg UNVERIFIED
`_sra_reuse_key` omitted the scope while `_sra_selected` returns `analysis.scoped`. Observed:
`scope_signature()` moved `A=1` → `F=(…)A=1`, the key stayed **identical**, the export served
**the same result object**. Fixed. **UNVERIFIED:** that a filter visibly moves the *exported
percentiles* — the shipped example is degenerate for SRA (all percentiles on one date). Needs a
fixture whose filtered population changes the focus distribution.


## FIXED (2026-08-17 (d))

### BROWSER-ORPHAN-01 — **FIXED, ADR-0418**, and the row below UNDER-COUNTED it by 19 modules
The ledger recorded "four browser modules". Four were the modules that FAILED; the modules that
never RAN were **23** (only ADR-0406's own resolves properly), holding **94 tests**. Measured by
bind-mounting an empty dir over `/opt/pw-browsers` in a mount namespace — a runner-shaped
filesystem: **86 passed, 94 skipped**, versus 175 passed / 5 failed with the vendored browser
present. Read the population as a claim, not a headline: the "four" came from counting failures.

**The two causes were unrelated, and one was the opposite of the hypothesis.** The four panelkit
assertions were indeed stale — ADR-0360 replaced navigation with fetch+blob, so
`expected_path in download.url` fails on a WORKING button; repaired by asserting on the network
(the endpoint was really requested), which is stronger, with the `200` leg documented as secondary
because a dead endpoint actually surfaces as the download wait timing out.

The histogram failure was **not** a stale assertion and **not** a lost halo. The halo is painted
(computed style `paint-order: stroke`, 3px white; stashing it moves pure-white pixels 17.6% → 1.3%)
and the caption is legible at true 1×, verified visually. The defect was the ORACLE: `_modal_color`
takes the mode of the whole caption box, which is the dominant REGION, not the glyphs' backdrop —
and on a caption straddling the degenerate one-bin bar it returned **1.17:1 with the halo AND
1.17:1 without it**. It failed a correct render and could not have detected a broken one. Replaced
by a 1px glyph-backdrop ring: **3.06:1 haloed / 1.17:1 stashed**. A second defect in the same test
scored captions against OTHER captions' pixels (document-wide probe joined to `#ssiCharts`
screenshots by non-unique caption text — which is why one straddling caption reported as three
failures); probe and screenshot now come from the same element handle.

**Both halves shipped**: all 23 modules repointed at one resolver whose fallback is playwright's own
resolution, AND CI's browser job now COMPUTES its population (`tools/browser_modules.py`) instead of
naming one module by hand, with the skip-is-a-failure guard covering the whole set. Guard
mutation-proven **7/7 by name**, every mutation confirmed to have landed first — an 8th "survived"
was a battery defect (a `sed` that never applied) which exposed a genuinely weak assertion of mine.
`src/` untouched: tests + workflow only, so no wheel/installer rebuild and no version bump.

**The first CI run closed the runner leg and surfaced TWO MORE orphans** (seven pre-existing
failures in total, not five). `browser` reported **203 passed, 0 skipped** on a real runner — the
`{}` fallback works. It also failed `test_float_tip_dismiss` / `test_float_tip_scroll`. Those
looked runner-specific and were NOT: instrumented and looped, the scroll test fails **8 in 20
locally** — the earlier local passes were luck, and "only fails on CI" was a conclusion from a
sample of one. Three hypotheses refuted first (headless shell · missing `tabindex` · `focus()`
scrolling). The real mechanism was caught in the act by a sequence probe:
`TIP-SHOWN 55ms → scroll 57ms → tip-hidden 67ms`. `scroll_into_view_if_needed()` delivers its
scroll event **asynchronously** (57-70ms), and the product hides tips on scroll BY DESIGN, so
focusing immediately races it. Fixed by waiting for scroll quiescence: **12/20 → 20/20**. The
product was never wrong.

<details><summary>the original row, as filed</summary>

### BROWSER-ORPHAN-01 — four browser modules never execute in CI, and 5 of their tests FAIL (**high**)
Found by accident: installing `playwright` to settle JS-01 turned four previously-skipped modules
ON, and the full suite went from 4100-ish clean to **5 failed, 4224 passed**. All five failures
reproduce on a pristine worktree of `origin/main` (ff89731) with none of this session's changes —
they are **pre-existing**, not regressions.

**Why they never run.** `tests/web/test_r11_panel_contract.py` resolves chromium through
`_chrome()`, made runner-compatible by ADR-0406. The other four hardcode the pattern ADR-0406
identified as the defect:

    _PW_CHROMES = sorted(Path("/opt/pw-browsers").glob("chromium*/chrome-linux/chrome"))
    CHROME = _PW_CHROMES[0] if _PW_CHROMES else Path("/opt/pw-browsers/absent/chrome")
    pytestmark = pytest.mark.skipif(not CHROME.exists(), ...)

A GitHub runner installs browsers to `~/.cache/ms-playwright`, so `/opt/pw-browsers` is absent and
all four SKIP in the CI matrix. CI's dedicated `browser` job runs **only**
`test_r11_panel_contract.py`. So neither CI path ever executes them — `test_ch05_panelkit`,
`test_r10_cei_panelkit`, `test_axis_titles_visual`, `test_ribbon_scorecards_panelkit`.

CI's own comment on that job names this failure class exactly: *"A requirement enforced only by a
test that never executes is the exact round-10 failure class this ADR pair exists to retire."*
ADR-0406's fix and the `browser` job's skip-is-a-failure guard were both applied to ONE module.

**The five failures** (all on base `ff89731`):
- `test_ch05_panelkit::test_panelkit_click_census_and_jarvis[/trend]`, `[/curves]`, `[/scurve]`
- `test_r10_cei_panelkit::test_panelkit_click_excel_and_census`
- `test_axis_titles_visual::test_the_degenerate_single_bin_histogram_is_still_legible`

**Diagnosed one, NOT all** (the rest are UNVERIFIED as to cause): the four panelkit ones assert
`expected_path in download.url` while the download arrives as a **client-side blob** —
`blob:http://127.0.0.1:.../<uuid>` with `suggested_filename='trend.xlsx'`. The download *works* and
carries the right filename, so this reads as a **stale test assertion** about URL shape rather
than a product defect — but that must be proven per-test before any of them is touched, and the
histogram failure has not been diagnosed at all.

**Do NOT "fix" by loosening the assertions until each is diagnosed.** The fix has two parts and
the second is the one that matters: repoint the four modules at a runner-compatible chromium
resolver AND put them in a CI job, or they will rot again the moment they are green.

</details>

## FIXED (2026-08-17 (e)) — IMP-01, and the three MIXED-POPULATION rows were EIGHT sites

### IMP-01 — **FIXED, ADR-0419** — re-derived from source, and it is LATENT
The row's detail was lost with the round-3 pool, so the claim was rebuilt from the importer rather
than confirmed from testimony. `mspdi.py::_parse_calendar` reads a `DayWorking=1` weekday with no
usable `<WorkingTimes>` **two ways four lines apart**: `if minutes > 0` drops it from the day
census, while `dominant_day_minutes(...) or MINUTES_PER_DAY` declares that construct worth 480.
Uniform calendars hide the disagreement; a MIXED one exposes it, because the invisible days cannot
outvote the visible one. **Measured: Mon 4 h + Tue-Fri default → 240 min/day, an 80-working-hour
task displaying as 20.00 days instead of 10.00** — a 2x error on every duration-in-days figure.
Zero-length spans (`08:00 → 08:00`) reach the same hole.

**Reachability measured separately: ZERO occurrences in 56 real MSPDI documents** (every committed
MSPDI-rooted file plus 25 MPXJ conversions of the reference `.mpp`s) — MPXJ always emits
`WorkingTimes`. Genuinely wrong, genuinely latent; both stated because either alone misleads.
Fixed; **proven a no-op on the corpus** (parsed calendars of all 83 documents diffed patched vs
pristine — identical) and parity **72 passed**. Mutation **5/5 by name**. The open question — is an
implicit day 480, or the file's `<MinutesPerDay>`? — stays **UNVERIFIED** and is unchanged by the
fix, which only collapses two readings into one.

### The three MIXED-POPULATION rows — **FIXED, ADR-0420**, and the class was 8, not 3
`ANALYSIS-HEADER-MIXED-POPULATION`, `RIBBON-MIXED-POPULATION` and
`ANALYSIS-EXPORT-QUALITY-UNSCOPED` all confirmed by ONE differential probe (flip the scope, diff
two surfaces — ADR-0417's technique, as predicted), with `analysis.scoped` as a control proving the
filter bit. Under an `Activity Type: Normal` reduce filter (9 → 8) on the shipped example:

| surface | unfiltered | filtered | |
| --- | --- | --- | --- |
| `analysis.scoped` size — **control** | 9 | 8 | MOVED |
| `/analysis` header `stack-foot` | 9 | **9** | STATIC (raw) |
| `/analysis` grid takeaway | 9 | 8 | MOVED (scoped) |
| `/api/analysis` `tasks` vs `activities[]` | 9 = 9 | **9 vs 8** | disagree in ONE payload |
| `/ribbon` missing-logic / logic-wired | 2 / 7 | **2 / 7** | STATIC — filter ignored outright |
| workbook Schedule-quality row | 2 of 9 | **2 of 9** | honest scoped is **5 of 8** |

**A computed AST census then found five MORE route functions the ledger never named** —
`schedule_card` (x2), `standards_view`, `_schedule_facts` (x2 — the fact sheet **the AI is allowed
to cite**), `export_ribbon`, `export_ribbon_drill`. Leaving `export_ribbon` would have re-created
the very screen-vs-export split being closed. All eight fixed by passing `analysis.scoped`
(`filter_to_uids` preserves file identity; `scope()` is the identity unfiltered, so each change is
a literal unfiltered no-op). Mutation **5/5 by name**, and a **standing computed census guard**
(empty-set contract + a synthetic positive control) separately proven to fire against the real tree
**2/2 by name**. **Read this row as a correction to the ledger's own granularity**: three named
surfaces were three members of a class of eight, and the named ones were not the worst.



## FIXED (2026-08-18) — the route x test census SETTLED, and the AI figure-gate dimension opened

### The coverage census — measured DYNAMICALLY, and both prior numbers are refuted
The route population holds at **137** for a fourth derivation, and the sub-split is now explained
rather than merely repeated: **136 paths carry HTTP methods + 1 `StaticFiles` mount**, and
`/settings` carries **two** (GET and POST), so the number that matters for coverage is
**137 (path, method) endpoints**. The ledger's 65/34/38 vs a live-app 64/34/39 differ by exactly
one boundary call — whether `/download/{name}` is a page or an export.

The sub-counts were re-derived with a **dynamic** instrument instead of a source scan: a pytest
plugin hooks `FastAPI.build_middleware_stack` (a CLASS method, so immune to import-timing) and
records every HTTP request the WHOLE suite makes — the route template resolved by the app's own
matcher, the status, and how many schedules were loaded at request entry. **15,338 requests**
observed across a full 4244-passed run.

| | ledger | 2026-08-17 (e) instrument | **measured (dynamic)** |
| --- | --- | --- | --- |
| no success test | 5 | 7 | **3** |
| no failure/adverse test | 16 | 66 | **25** |

The three with no success response ever observed: `/export/{fmt}/resource-drill` and
`/export/{fmt}/ribbon-drill/{name}` (reached ONLY into a 4xx) and `POST /sra/factor-table` (never
reached at all). **Zero 5xx anywhere in the suite.** All 34 `api` routes have adverse coverage;
the 25 gaps are 19 `page` (almost all `POST /sra/*`) and 6 `export`.

**Read the two numbers as a bracket, not a correction.** The dynamic instrument sees traffic, not
assertions, so a route exercised without being asserted counts as covered: **25 is a LOWER bound**
on the true gap. The source-text instrument only counted `status_code == 4xx` literals, so **66 is
an UPPER bound**. The honest statement is 25 <= gap <= 66, and the population is settled.

### `ISDIGIT-INT-500` — LEAD-VERIFIED and it was 12 routes, not one · **ADR-0423**
The census pointed straight at the never-adversely-tested `POST /sra/*` surface. Fuzzing those 25
routes found **6** routes answering 500 to a superscript; fuzzing EVERY field of EVERY route found
**12**, across 5 sites — routes that DO have adverse coverage carry the same bug in another field.
Fixed with `str.isdecimal()`, which across all 788 numeric code points disagrees with `int()` on
**zero** (`isdigit()` disagrees on 128). **The first fix was wrong the other way** — `isascii() and
isdigit()` disagrees on 650 and would have silently stopped resolving Arabic-Indic digits — and it
was the change's own guard-the-guard test that caught it, not review. Before: 12 routes raised
across 255 field slots. After: **0 across 290**. Parity **72 passed**, unmoved.

### AI figure-gates — the never-audited dimension, first pass · **ADR-0421 / ADR-0422**
* `AI-DRIVE-01` (**new**, high): `/api/ask/{name}` and `/api/ask`'s single-file branch paired the
  RAW schedule with the scoped `analysis.cpm`, so under ANY active filter `compute_driving_slack`
  raised `KeyError` on a filtered-out task and `driving_path_summary`'s bare `except` swallowed it
  — **every engine driving-path fact silently vanished**, for in-scope activities too, with a 200.
  That is precisely the failure `ai/driving_facts.py` exists to prevent. **The hypothesis on entry
  ("it will answer about a filtered-out activity") was REFUTED**; the truth was worse and quieter.
  The product supplied its own oracle: with two files loaded and a filter on, `/api/ask` (correct)
  and `/api/ask/{name}` (broken) answered the same question with contradictory evidence.
* `ASK-UNRESTRICTED-WRONG-VERSION` — **REPORTED row now LEAD-VERIFIED and fixed.** `/api/ask`
  resolved the newest version's activity table by matching `Schedule.name`; successive updates of
  one project share that name, so it returned the OLDEST file. Facts from the newest version, data
  from the oldest — in the one mode that is deliberately **ungated**.
* A **standing computed census** now guards the whole raw-vs-scoped class
  (`tests/web/test_scoped_pairing_census.py`): it wraps all 61 engine callables taking a
  `Schedule` and a `CPMResult` and asserts `set(tasks_by_id) == set(cpm.timings)` at call time
  across every parameterless GET plus the Ask routes. Unfixed tree: 2 violations, named. Fixed
  tree: 0. Unfiltered: 0 in both — differential, not blind.
* **Checked and CLEAN** (negative results worth recording): `/briefing`, `/api/ai/narrative`,
  `_unrestricted_data_block`'s rows, `manipulation_forensics_facts`, `_pair_versions`,
  `_solvable_versions`.
* **Latent, NOT fixed:** `citations.reattach` rebuilds each `CitedStatement` without carrying
  `pinned`, so ADR-0392's population-frame flag would be dropped. Measured unreachable today —
  `pinned` is set only in `version_facts.py` and `reattach`'s three call sites are narrative /
  briefing paths that never carry those facts. Reported, not repaired, because the safe fix is a
  behaviour no test can currently exercise.

### Still open after this session
`MF-05` stays **do-not-fix-blind**. Remaining REPORTED: CPM-01..04 · MF-03/04/06..10 ·
MC-02..08 · IMP-02..06 · MAN-01..03 · REC-02 · JS-02..06 · TST-02/03. (**MF-02 left this list on
2026-08-25**: it had shipped as ADR-0411 and the row was never updated. An ADR-vs-queue census run
at the same time — every open row ID searched across all 440 ADR files — found MF-02 was the ONLY stale
entry of the 35, so the rest of the queue is honest. That census is a LOWER bound: an ADR can fix
a row without naming its ID.) Never audited: **page
modules A/B** and **docs/config/CI**; the AI figure-gate dimension got its FIRST pass here, not a
complete one — the strict/annotate role gate and the Layer-B derivation verifier were read but not
adversarially probed.

## FIXED (2026-08-25) — PAGE MODULES get their first census, and it is CLEAN (ADR-0439)

The dimension the ledger has carried as **never audited** since round 2 now has a verdict, and it
is a *measured* one. Question asked: what happens to an operator's activity name — legally
`Pour slab <2m> & cure` — on its way to a served page and to an exported workbook?

**Result: clean on both legs.** 81 scored server responses · 33 rendered pages in real Chromium ·
44 export archives — zero leaks. The vendored JS builds its DOM with `createElement` +
`textContent` (structurally immune); the nine non-clearing `innerHTML` sinks in the tree are
static literals, already-escaped text, host telemetry, or server-built HTML. Two standing censuses now hold it:
`tests/web/test_operator_content_escaping.py` and `tests/web/test_operator_content_dom_browser.py`
(auto-discovered by `tools/browser_modules.py`, so CI carries it with no workflow edit).

**Read the clean verdict only because the instruments were proven able to dirty it.** BOTH first
drafts returned "clean" and both were wrong to:

* the page census **hand-wrote its route list** — `/analysis` and `/wbs` are `/{name}` routes, so
  two of its 28 pages 404'd and **scored as escaped**, and every parameterized route (where names
  render densest) was skipped outright. Now enumerated from the app object, `{name}` filled from
  real session keys, and a non-success response is **never scored**;
* the browser oracle's positive control **injected at the wrong level** — a `<td>`'s own
  `innerHTML` cannot produce the row break (the parser discards stray `</td></tr>` there); only a
  whole-table string assigned to a container can. The teeth test caught it.

Mutation battery, all on the REAL product, all red by name: `_e()` removed on the `analysis.py`
activity row → page census names `/analysis/{name}` · `path.js` `el()` `textContent`→`innerHTML` →
browser census names `/path` + `/driving-path` (`img=5, onerror=5`) · `_esc()` neutered in BOTH
report writers → 6 archives unparseable · `_export_cell`'s status gate removed → `0.0/0.0/0.0` back
in the EVM sheet. A fifth correction was to the guard itself: the export census first counted only
well-formed archives as its population, so a corruption shrank the population and the FLOOR
assertion fired first — *a red for the wrong reason is not a red*; the substantive assertion now
runs first.

**Also settled, and deliberately NOT changed** (see ADR-0439 for the full reasoning):

* **MF-02 was stale in this very ledger** — shipped as ADR-0411, still filed as *"Not yet
  implemented"*, and repeated in the kickoff's standing queue. Corrected above. An ADR-vs-queue
  census over all 35 open row IDs found it was the **only** stale entry — a LOWER bound, since an
  ADR can close a row without naming its ID.
* **the `value is not None` class is closed**: `MetricResult.value` is declared `float`, so the
  guard is dead by construction and mechanically detectable — an AST census returns 3 sites in
  `src/`, all correct.
* **6 dead E501 per-file-ignores** (`scurve`, `standards`, `brief`, `briefing`, `curves`,
  `workbench`; two independent oracles agree). **Not removed** — the stated policy attaches the
  exemption to what a module *is*, not what it currently contains; removing them fights the
  documented intent. Do-not-fix-blind, MF-05 style.
* **`evolution.py`'s completed-on-path table renders `0%` for an absent activity** where the cell
  beside it renders `—`, in a table whose heading asserts those activities completed. **Measured
  unreachable** (`completed_on_path` is filtered against the same `schedules[i]` the view indexes,
  and the one caller computes `ev` from that same list). Latent, reported, not repaired.

**Still never audited: docs/config/CI got a PARTIAL pass, not a deep one.** CLAUDE.md's documented
gate was checked against `.github/workflows/ci.yml` and `[tool.mypy]` and they agree (bare `mypy`
== `mypy src/` strict, via `files = ["src"]`). Two stale pyproject comments were found and fixed.
The rest of that dimension — the hooks, the installer workflow, `constraints/`, the docs guards —
is untouched.

## Coverage census — the every-page pass+fail requirement

The route inventory was built **twice, independently** (the lead from the live app object,
the census agent from the source) and both give **137 routes** — 65 page · 34 api · 38
export. Against the current test tree:

- **5 routes have no success test**
- **16 routes have no failure-mode test** (no 4xx / empty / gated-state coverage)
- 15 gap items listed, plus the JS↔page contract pins

This is the work queue for the operator's "pass AND fail tests for all functionality of all
pages" requirement. It is **not yet closed** — see the session's live handoff for progress.

### Re-derivation attempt, 2026-08-17 (e) — the ROUTE COUNT holds, the SUB-COUNTS do not

A third independent instrument (routes read off the live FastAPI app object; test-tree request
URLs resolved back to route templates by the app's OWN router, so no hand-written mapping) gives:

* **137 routes — 65 page · 34 api · 38 export. CONFIRMED**, now by three independent derivations.
* **no success-shaped test: 7** (ledger says 5) — `/download/{name}`, `/export/{fmt}/resources`,
  `/export/{fmt}/resource-drill`, `/export/{fmt}/ribbon-drill/{name}`, `/export/{fmt}/risks`,
  `/openapi.json`, `/sra/factor-table`.
* **no failure-shaped test: 66** (ledger says 16).

**Do NOT bank 7/66.** The instrument classifies a test as failure-shaped only when its function
body contains a literal `status_code == 4xx/5xx`, so a failure test that asserts on an error body,
an empty-state string, or a gated-state banner is **invisible to it** — the 66 is an upper bound on
the gap, not the gap. The first version of this same instrument was also caught over-reporting
(39/73) because it probed every URL with `method: "GET"` and mis-resolved every POST-only route;
that bug was found only because `/api/ask` appeared simultaneously as *unresolved* and *untested*.

So the **sub-counts remain UNVERIFIED in both directions** and the honest state is: the route
population is settled, the coverage gap is not. Whoever works this next needs an oracle for
"is this test asserting a failure mode?" that does not depend on a status-code literal — and should
treat both 5/16 and 7/66 as hypotheses.


## REPORTED — round 3 (2026-08-17), 2 of 5 dimensions returned before the pool stalled

Same rule as every REPORTED row: **a finder claim, not a verified defect.** This audit has
produced finder output wrong in both directions; nothing below has been lead-verified.

**web core + state (8)** — `SRA-EXPORT-STALE-SCOPE` (*critical*): `_sra_reuse_key` is said to
omit the session scope signature, so `/export/{fmt}/sra` could serve a stale PRE-filter result
— the same family as MF-02 (an export disagreeing with the screen) and worth verifying FIRST ·
`ASK-UNRESTRICTED-WRONG-VERSION` (*high*): `/api/ask` unrestricted mode resolving "newest" by
name-matching · `ANALYSIS-HEADER-MIXED-POPULATION`, `RIBBON-MIXED-POPULATION`,
`ANALYSIS-EXPORT-QUALITY-UNSCOPED` (*high*): counts computed on the RAW schedule while the page
claims the scoped one — three separate surfaces · `ISDIGIT-INT-500` (*medium*): `str.isdigit()`
gating `int()` accepts superscripts/other Unicode digits that `int()` rejects → a 500.

**importers + model (10)** — `IMP-01` (*high*): two importer interpretations that "materially
move every displayed number" (a fallback path) — verify before anything else here · `IMP-02`:
`Schedule`'s docstring guarantees an inconsistent schedule is *unconstructable*, allegedly not
enforced · `IMP-03`: the schema change-control gate covers 6 of 11 model classes · `IMP-04`:
`_percent_complete` inferring finished/started from raw presence of `act_end_date` · `IMP-05`:
XER mapping `target_start_date`/`target_end_date` (P6 *current plan*) as baseline · `IMP-06`
(*low*): save→reopen not model-identical when the calendar registry is empty.

**Round 3 COMPLETED all 5 dimensions** (the earlier "stalled" note was premature — it finished
after ~79 min, 5/5 agents, 0 errors). The remaining three dimensions returned 22 more claims:

**tests that cannot fail (3)** — the finder ran REAL mutation probes and quoted the output, so
these are the strongest REPORTED rows in this ledger:
- `TST-01` (*high*, `tests/web/test_single_compute.py:36`): `_CPM_HOLDERS` is stale —
  `web.state` (which hosts the PRIMARY solve) is **absent** and `app_mod` no longer binds
  `compute_cpm` at all, so the single-CPM regression gate is blind to extra network solves.
  **Probed:** injecting two extra `compute_cpm` calls into `state.py`'s warm path left
  `test_single_compute.py` **passing**. ADR-0352 fixed this exact coupling elsewhere and never
  reached this module; its promised "standing sweep" exists only as prose.
- `TST-02` (*medium*): `test_summary_only_schedule_yields_cited_findings_never_empty` asserts
  inside `for finding in recommend(s):` where `recommend(s) == ()` — **zero iterations, zero
  assertions**. Probed: making `recommend()` return `()` unconditionally still passes. NOTE the
  honest fix may go RED and expose that the documented terminal-citation fallback never fires.
- `TST-03` (*medium*): `assert sum(v.actual_finishes) == sum(v.actual_finishes)` — a
  self-comparison that cannot fail; its own comment names the property it meant to assert.

**findings / trend / manipulation (8)** — `REC-01` (*high*) **challenges ADR-0407, shipped by
this same session**: it claims the `actual_start_driven` INFO/OPPORTUNITY disclosure DOES reach
a quantified recovery surface, contradicting the ADR's "never becomes a threat row or a recovery
action". **Verify this before trusting ADR-0407's claim** — if true, that ADR overstated and
must be corrected, not defended · `MAN-01` (*high*): `_critical_incomplete` scores on pure-logic
`t.is_critical` instead of `is_effective_critical`, i.e. it ignores the source tool's
progress-aware flag — a parity-relevant manipulation signal · `MAN-02` (*high*): a resource-edit
finding cites the CURRENT file for a task that exists only in the PRIOR version · `REC-02`
(*high*): `recommend(schedule, target_uid=<inactive activity>)` calls `compute_driving_slack`
unguarded · `MAN-03` (*medium*): an ABSENT remaining-work figure read as 0 (the None-vs-0 rule) ·
plus a /risks test that cannot fail, dead code, and two unreachable trend branches.

**static JS / CSS (6)** — `JS-01` (***critical***, `web/analysis.py:1204`): the **Acumen-Fuse
parity-mode checkbox is a dead control in any CSP-honouring browser** — an inline handler the
tool's own strict CSP blocks. A visible control that silently does nothing, on the parity page ·
`JS-02`: a document-level `fullscreenchange` listener added per framed chart and never removed ·
`JS-03`: in four drill panels the text filter is destroyed by its own keystroke · `JS-04`:
panelkit's data-drawer branch is dead (no page emits `data-sf-data`) · `JS-05`: CSS rules
matching nothing · `JS-06`: `bucketOf` mislabels fractional float at band edges.

**Never audited at all:** page modules A/B · docs/config/CI · AI figure-gates.

### Suggested order — items 0-4 are DONE (2026-08-17 (c)); what remains is below

**IMP-01 and all three MIXED-POPULATION claims are CLOSED (ADR-0419/0420).** Next: **the route
x test gap-fill** — and re-derive its "137 routes / 5 / 16" with a computed census FIRST; it is
the last ledger count nobody has recomputed, and this audit has now twice found a stated
population to be smaller than the real one. Then the never-audited dimensions: page modules A/B
· docs/config/CI · AI figure-gates. MF-05 stays do-not-fix-blind.

**A population correction worth carrying:** BROWSER-ORPHAN-01 was filed as "four modules" because
four modules FAILED; twenty-three never RAN. When a row states a count, check whether it counts the
symptom or the thing — and prefer a computed census to a number in prose. The same question is
open for the route × test gap-fill's "137 routes / 5 / 16", which was at least enumerated twice.

<details><summary>the original order, for the record</summary>

0. **REC-01 FIRST** — it disputes an ADR this session shipped (0407). An audit that will not
   audit its own output is worthless; if REC-01 holds, correct ADR-0407 rather than defend it.
1. **MC-01** (LEAD-VERIFIED above, critical, reproduction included) — needs the SSI oracle for
   the parity leg, or ship documented-additive with that leg marked UNVERIFIED.
2. **SRA-EXPORT-STALE-SCOPE** — verify by rendering a filtered page and diffing its figures
   against the export, the technique that caught MF-02 in one probe.
3. **IMP-01**, then the three MIXED-POPULATION claims (one probe can settle all three).
4. **JS-01** (a dead control on the parity page) and **TST-01** (a regression gate that is
   already proven blind by mutation).
5. The route x test gap-fill: 5 routes with no success test, 16 with no failure-mode test.

</details>

## Still outstanding

Round-2 dimensions not yet returned: findings/trend/manipulation · importers · web core ·
page modules A/B · static JS · **the test suite itself** (green-tests-that-cannot-fail) ·
docs/config/CI · AI figure-gates — plus the completeness critic. Round 1 lost 15 of 16
agents to credit exhaustion; if round 2 degrades the same way, the remaining dimensions get
audited in-session and this ledger will say plainly which received the deep treatment and
which received a lighter pass.
