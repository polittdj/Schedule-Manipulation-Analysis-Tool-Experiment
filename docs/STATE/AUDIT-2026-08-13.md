# Read-only forensic audit — Schedule-Manipulation-Analysis-Tool-Experiment

**Audited commit:** `5a8003f868e9cf58f22b1be7375836d1850ffe0b` (`HEAD`, and `== origin/main` after
`git fetch`). **Branch:** `claude/schedule-manipulation-audit-xnrqfd`. **UTC start:** 2026-08-13T20:54Z.
**Mode:** strictly read-only; the checkout was never mutated (proof in §16). All probes ran against a
disposable `git archive` sandbox + a throwaway venv under `/tmp/.../scratchpad`, never the checkout.

**Method note (honesty over cleanliness).** Every retained finding was re-derived by the lead with an
executable check that was shown able to FAIL (negative control or mutation), per the repo's own QC-1/QC-2
rules. Ten parallel read-only investigators produced candidates; none entered this report on an agent's
word — each is lead-verified below. Observed facts are stated separately from inferred impact. Where a
determination needs NASA policy, it is marked **REQUIRES AUTHORIZING OFFICIAL** and not resolved here.

---

## 1. Executive verdict (do not collapse)

| Dimension | Verdict | Basis |
|---|---|---|
| **Repository build** | **PASS with gaps.** mypy strict clean (151 files), ruff/format/bandit/pip-audit green in a real checkout, version 1.0.200 consistent (28 sites). Full pytest census in §9. | executed |
| **Operator-local patched build** | **OUT OF SCOPE / UNVERIFIABLE.** The gateway patches are unversioned, not in the repo, cannot be audited here. Documented as such by the repo itself. | REFUTED that repo contains them |
| **NASA gateway integration** | **ABSENT (recorded, not built).** No `ai/gateway.py`/`ai/cloud.py`; no caller wires `cloud_backend`; no gateway/API-key env var is read; the "cloud" selection falls closed to Null. Consistent with `docs/PLAN/APPROVED-GATEWAY-INTEGRATION.md`. | VERIFIED |
| **CUI (data sovereignty)** | **HOLDS AS SHIPPED, with latent gaps.** AI backends loopback-validate at construction; the egress guard blocks forbidden clients; CSP/air-gap enforced. Gaps: banner is config-derived not observed (GW-02), `_ALLOWED_HOSTS` unpinned (SEC-01), pre-commit blind to renamed/PDF/ZIP schedules (HOOK-01). None is a live leak in the shipped tree. | VERIFIED + PARTIAL |
| **ITAR** | **REQUIRES AUTHORIZING OFFICIAL.** No code makes ITAR determinations; the only ITAR-relevant fact is a *disclosure* (DISC-01). | policy |
| **Numerical parity** | **PARTIALLY VERIFIED. Do NOT call it "parity achieved."** Core CPM matches an independent hand oracle. Fuse metrics are pinned to a *transcribed* reference export (not tool-generated, but transcription unverifiable here). SRA/SSI accept *documented tolerances* (ADR-0106) — agreement, not exact parity. | mixed |
| **Release readiness** | **NOT READY.** Placeholder LICENSE (all rights reserved), REQUIRES-AUTHORIZING-OFFICIAL disclosure in a public repo (DISC-01), FINAL-REPORT overclaims vs current open items. | VERIFIED |

**Single most important finding: DISC-01** — a *public* repository names a NASA gateway hostname
(`<gateway-host>`), an ITAR-tagged model id (`<itar-model-id>`), and describes
a patched NASA workstation handling controlled data, with no release determination on file.

---

## 2. NASA gateway authorization matrix (technical evidence ≠ organizational approval)

| # | Claim | Authoritative evidence required | In repo? | Verdict |
|---|---|---|---|---|
| 1 | Authorizing org / system owner named | ATO letter / SSP owner | No | REQUIRES AUTHORIZING OFFICIAL |
| 2 | Authorized gateway host + path | accreditation boundary doc | Only a hostname *string* in docs | REQUIRES AUTHORIZING OFFICIAL |
| 3 | Scope (NASA-wide / center / program / user) | authorization memo | No | REQUIRES AUTHORIZING OFFICIAL |
| 4 | ATO / equivalent boundary | ATO | No | REQUIRES AUTHORIZING OFFICIAL |
| 5 | Approved data categories (CUI Basic/ITAR/EAR/proprietary/source-selection/classified) | data-handling authorization | No | REQUIRES AUTHORIZING OFFICIAL |
| 6 | Approval covers schedule fields/derived metrics/full files | authorization | No | REQUIRES AUTHORIZING OFFICIAL |
| 7 | Authorized model IDs/versions | approval | A model *string*, not an approval | REQUIRES AUTHORIZING OFFICIAL |
| 8 | Alias→model drift controls | provider contract | No | REQUIRES AUTHORIZING OFFICIAL |
| 9–30 | hosting/foreign-person/retention/training/telemetry/subprocessors/TLS/certs/keys/rate-limits/markings/audit-logs/retention/reauth/consent/minimization/output-handling/releasability | NASA policy artifacts | None present | REQUIRES AUTHORIZING OFFICIAL |

**Technical reality (VERIFIED):** there is no working gateway path in the repo at all, so items 1–30 are
moot *for the shipped code* and become live only for the operator-local patch (unauditable here).
**Nothing about a hostname, a model name, or a successful HTTP response constitutes NASA approval.**

---

## 3. Data-flow (as shipped)

```
Schedule file (.mpp/.xer/.xml/.json)
   -> importer (stdlib only; MSPDI rejects DTD/entity: XXE-safe [REF-02])
   -> Schedule model (frozen) -> engine (CPM + metrics; deterministic)
   -> web/app.py (FastAPI, binds 127.0.0.1; Host-header middleware; strict CSP; no external assets)
   -> server-rendered HTML + vendored JS  ── all local ──►  operator browser (loopback)
                         │
                         └─ AI layer (OPTIONAL): NullBackend (default, offline) │ Ollama/OpenAI-compat
                            (loopback-validated at construction — remote endpoint => CUIEgressError).
                            route_backend() FAILS CLOSED to Null; cloud_backend never supplied.
Trust boundary that DOES NOT EXIST as shipped: any remote endpoint. No process egresses schedule data.
Latent boundary risk: the UI banner is derived from CONFIG INTENT, not the backend actually used [GW-02].
```

No credential, log sink, remote cache, or external export path was found in the shipped tree.

---

## 4. Findings (lead-verified)

Severity ∈ {critical, high, medium, low, info}. Confidence ∈ {high, medium, low}. All line numbers at `5a8003f`.

| ID | Sev | Conf | Verdict | Finding | Evidence | Reproduction | Refutation attempt | Impact | Owner |
|---|---|---|---|---|---|---|---|---|---|
| **DISC-01** | high | high | REQUIRES AUTHORIZING OFFICIAL | Public repo discloses NASA gateway host + ITAR model id + patched-workstation ops | `docs/PLAN/APPROVED-GATEWAY-INTEGRATION.md:11-24`; host in 7 files, model in 4; in git history since `a19b969` | `git grep -n <gateway-host>` / `git grep -n <itar-model-id>` | Confirmed the other "itar" hits are false positives (`delimitar`, an ITAR-notice test) — the disclosure is real, not noise | Potential releasability/infosec exposure of controlled-program details | NASA authorizing official |
| **GW-02** | medium | high | VERIFIED (latent) | Sovereignty banner is config-intent-derived, not observed; `route_backend`'s Banner discarded | `web/chrome.py:172-177` renders `banner_for(state.ai_config)`; `app.py:838` & `settings.py:346` drop `_banner` | test `test_gw02_shown_banner_matches_routed_backend` (xfail→red) | Dangerous direction (remote-as-local) currently unreachable: backends loopback-validate at construction | If a cloud path is later wired without fixing this, UI could mislabel a remote backend as local | engineering |
| **SEC-01** | medium | high | VERIFIED | `_ALLOWED_HOSTS` (DNS-rebinding Host allowlist) enforced but its contents pinned by NO test | `web/app.py:1076` (def), `:1192` (middleware); `grep tests/ _ALLOWED_HOSTS` = 0 | test `test_sec01_allowed_hosts_pinned_to_exact_loopback_set` (+mutation) | Same class ADR-0394 fixed for `_LOOPBACK_HOSTNAMES`; a widened entry would pass existing sec-hardening suites | A silent non-loopback entry becomes a rebinding admission hole, undetected by CI | engineering |
| **TEST-01** | medium | high | VERIFIED | 22 playwright modules hard-code `chromium-1194`; skip when absent → silent browser-suite skip on a container bump | `grep -rl chromium-1194 tests` = 23 (r11 fixed via glob); `test_integrity_panelkit.py:35` skipif | test `test_test01_no_test_hardcodes_a_chromium_build_number` (xfail→red) | r11's own comment documents the failure mode; fix not propagated | Browser/a11y/perf coverage can vanish while CI stays green | engineering |
| **HOOK-01** | low-med | high | VERIFIED (documented tradeoff) | Pre-commit CUI guard misses schedules renamed to non-sniffed exts, `blocked.ext.png` double-exts, and PDF/ZIP | `.githooks/pre-commit` sniff=`\.(json\|txt)($\|\.)`; scratch-repo battery (12+7 cases) | scratch-repo red/green (this report §I) | Deliberate: sniffing prose/code causes false-positives (hook comment); defense-in-depth behind `.gitignore`; real CUI never enters a build session | An intentionally-renamed schedule (`+ git add -f`) can be committed | engineering (+ operator policy) |
| **DOC-01** | medium | high | VERIFIED | `FINAL-REPORT.md` overclaims vs current state | `:8` "COMPLETE and parity-green"; `:113` "CI green"; `:117` "complete, validated" — vs HANDOFF's ~12 open items + "RECORDED NOT BUILT" | read both docs | It is dated/stale (M17 era); HANDOFF is current — a stale-overclaim, still shipped | Misleads a reader (esp. in a testimony context) about completeness/parity | docs owner |
| **LIC-01** | medium | high | VERIFIED | LICENSE is a placeholder ("All rights reserved", none chosen) | `LICENSE:1-12` | read | — | Blocks any release/redistribution determination | rights-holder + counsel |
| **ENG-DEAD-01** | low | high | VERIFIED | `actual_start_driven` computed but consumed by no product code | producer `cpm.py:1357`; consumers = tests only (`grep src/ outside cpm.py` = 0) | grep | It IS asserted by tests (not fully dead), but no view/metric reads it | Latent/unused output surface; maintenance risk | engineering |
| **NUM-01** | medium | high | PARTIALLY VERIFIED | "parity" mixes exact pins, transcribed oracles, and tolerance-accepting agreement | `tests/parity/test_fuse_export_parity.py:1-9` (transcribed); `test_sra_ssi_oracle_uid152_v2.py:208-315` (`<=0.06`, `<=0.02`…) | read + run | Fuse oracle is NOT tool-generated (refutes circularity); but transcription unverifiable here & SRA is tolerance-based | Blanket "parity-green" claims are imprecise; fine internally, overstated in FINAL-REPORT | numerical owner |

Positive controls that *passed* (guarantees that HOLD): egress guard, MSPDI XXE defense, CPM core math,
loopback transport matrix, secret scanner, intake manifest content-detection, action SHA-pinning,
version consistency, drift guard (ADR-0394 in HANDOFF+SESSION-LOG). See §7/§13.

---

## 5. Secrets assessment (no values shown)

- Scanner proven on a negative control: 9/9 synthetic secret types detected (AWS AKIA, `sk-`, GitHub
  `ghp_`, Bearer, Authorization, Slack `xox`, Google `AIza`, PEM key block).
- Tracked source: **0** provider-pattern hits. Intake (400 MB): **0**. Git history (2792 objects): **0**
  `sk-` via pickaxe. No `.env`/`.pem`/`.key`/credential files tracked.
- One candidate adjudicated **false positive**: a `Pwd=`/`Password=` byte run inside third-party PowerBI
  *CustomVisuals* bundles in `NSATDeploymentRevisionAlpha.pbix` — minified JS/JSON labels, not a DataModel
  connection credential (independently re-verified by the lead).
- Intended (future) gateway key storage is an env var — **not** in the repo; assessed at doc level only.
  Env vars inherit to child processes and appear in crash dumps/diagnostics; a NASA-appropriate secret
  store is a policy question (REQUIRES AUTHORIZING OFFICIAL).

**No real credential found. Rotation action: none required for the repo.** (If the operator-local patch
stores an API key in an env var or plaintext, that is outside this audit and should be reviewed there.)

---

## 6. Repository accounting (how each category was examined)

1624 tracked files. Top extensions: `.py` 527, `.md` 502, `.png` 121, `.js` 103, `.xlsx` 91, `.xml` 34,
`.mpp` 29, `.jar` 24, `.json` 23, `.pdf` 22, `.css` 16, `.docx` 14. Method per category:

| Category | How examined |
|---|---|
| Python (src/tests/tools) | mypy strict + ruff + full pytest (§9); targeted reads of ai/, engine/, web/, importers/, net_guard |
| Docs (.md) | read README/CLAUDE/FINAL-REPORT/PARITY/HANDOFF/ADR index + gateway plan; contradiction matrix §12 |
| Vendored JS (.js/.mjs) | `node --check` (CI); CSP air-gap; not re-audited line-by-line (noted) |
| Binaries (.mpp/.xlsx/.jar/.pbix/.pdf/.png/.docx) | magic-byte ID via `file(1)`/`od`; reconciled vs `docs/INTAKE-MANIFEST.md` |
| GitHub workflows | SHA-pin check (all pinned) + `tests/guards/test_workflow_action_pins.py` |
| `.githooks/pre-commit` | scratch-repo behavioural battery (§I) |

**Cross-extension mislabels (VERIFIED, managed):** `01-apollo.png` is HTML (`3c214…`=`<!DOCTYP`);
`app.css` is Python; `concepts_b.txt`/`int02_advanced.txt` are DOCX. Matches CLAUDE.md's "99 files carry
an extension their bytes contradict." The **manifest correctly content-detects** these (records
`01-apollo.png` as `html`, mismatch=yes, and as a byte-dup of a `.html`) and is guarded against silent
swap — so this is a hygiene/provenance/releasability matter, not a hidden defect.

---

## 7. Claim-verification matrix (material claims)

| Claim (source) | Verdict | Evidence |
|---|---|---|
| "AI is loopback-only, fails closed" (CLAUDE.md) | VERIFIED | `route_backend` + construction-time `is_local_http_endpoint`; egress guard |
| "No forbidden HTTP client in runtime" | VERIFIED | pip-audit clean; net_guard forbidden-dist test |
| "Gateway RECORDED, NOT BUILT" (plan doc) | VERIFIED | no gateway.py/cloud.py; no cloud caller; no env read |
| "loopback allowlist pinned (ADR-0394)" | VERIFIED | `tests/guards/test_loopback_allowlist.py` present |
| "Host-header guard" (ADR-0264) | VERIFIED enforced; contents UNPINNED (SEC-01) | middleware `app.py:1192` |
| "Metric formulas from the .aft Bible" | NOT RE-DERIVED (agent pending) | formula-pin test exists |
| "Parity: deltas=0 or driven to zero" (FINAL-REPORT) | PARTIALLY VERIFIED / imprecise (NUM-01) | transcribed Fuse oracle; SRA tolerances |
| "CI green / all milestones complete" (FINAL-REPORT) | REFUTED as stated (DOC-01) | HANDOFF open items |
| "No secrets committed" | VERIFIED (with negative control) | §5 |
| "version 1.0.200 everywhere" | VERIFIED | 28 sites, 0 divergent |

---

## 8. Numerical / parity matrix

| Family | Primary oracle | Independent? | Verdict |
|---|---|---|---|
| CPM forward/backward, float, criticality | lead hand-derivation (A2d→B3d→C1d) | YES (mine) | VERIFIED exact |
| DCMA-14 / Fuse families | `fuse_exports_2026-06.json` — TRANSCRIBED Acumen Fuse v8.11.0 export | transcription, not tool-generated | PARTIALLY VERIFIED (transcription unverifiable here) |
| SSI path / SRA / JCL | SSI export + std-lib Monte Carlo w/ tolerances (ADR-0106) | tolerance-accepting | AGREEMENT, not exact parity |
| Other metric families | (numerical agent pending) | — | see §9 addendum |

**Do not report "parity achieved."** Report: CPM exact-verified; Fuse pinned-to-transcription;
SRA within documented tolerance.

**Numerical detail (investigator PO-*, lead re-verified where noted):**
- PO-03 (medium, lead-verified): the Fuse DCMA transcription (`fuse_exports_2026-06.json`) is validated
  by tests against the JSON only — **no test reads the source vendor `.xlsx`** (the workbooks ARE
  committed: `…Detailed Metric Report.xlsx`, `acumen_v8.11.0/Hard_File_Fuse - Fuse Analysis Report.xlsx`,
  …), so a transcription error/drift is unguarded. (SRA/SSI/SEM oracles DO read `.xlsx`.)
- PO-04 / PO-05 (medium, lead-verified): **CEI/bow-wave and HMI have no independent reference-tool
  oracle** on committed fixtures (grep for a committed CEI/HMI expected/vendor file = none; only
  `cei.js` + an ADR). → BLOCKED BY MISSING PRIMARY ORACLE for those families.
- PO-07 (info): High-Float (DCMA-06) and Critical-count parity rows are scored on MS Project's **stored**
  fields on both sides — they validate field-reading, not the engine's independent CPM.
- PO-09 / PO-10 (low): a SEM "Delta" row is asserted against the tool's own formula value (one circular
  row); EVM cost indices have no oracle on committed goldens (marked NA, never fabricated), and the
  EVM Acumen reference is not gate-locked due to documented residuals.

**Importer (investigator IMP-*, lead-verified):** IMP-03 `duration_minutes` unbounded → `OverflowError`
at date presentation (fails fast, no hang) [low]; IMP-04 web `/upload` has **no file-count cap** —
*intentional & documented* ("v4 grouped ingestion") vs loader `MAX_FILES=100`, per-file byte cap present
[low, DoS-surface]; IMP-05 file-path importers read the whole file into memory, only the web path caps at
500 MB [info]. XXE and CSV/XML-injection defenses **REFUTED as vulnerabilities** (they hold) [§13].

---

## 9. Test & CI assessment

- mypy strict: **PASS** (151 files, exit 0). ruff check `.`: **PASS** in a real git checkout. ruff format
  `--check`: **PASS**. bandit `-r src`: exit 0. pip-audit: no known vulns.
- **ruff caveat (REFUTED as a defect):** in a `.git`-less sandbox `ruff check .` reports 656 errors — all
  inside `00_REFERENCE_INTAKE/**/*.py`, which are tracked-but-gitignored and skipped by ruff's
  gitignore-respect **only inside a git tree**. CI runs in a checkout → genuinely green. Not a CI defect.
- Targeted authoritative census (faithful git sandbox, 1624 tracked files): **1429 passed, 2 skipped,
  0 failed** across `tests/guards` + `tests/ai` + `test_airgap` + `test_sec_hardening` + `test_launcher`
  + `tests/engine` + both Fuse-parity files (84 s). The engine==transcribed-Fuse-oracle parity HOLDS.
- **Sandbox-artifact caution (reproduced & explained):** in a `.git`-less `git archive` extract, the
  intake-manifest + precommit-blocklist guards FAIL/ERROR (they shell out to `git ls-files`/`rev-parse`);
  after `git init` + `git add -f 00_REFERENCE_INTAKE` to reach the real 1624-file tracked set, all 56
  pass. This is exactly the audit's "do not treat raw-checkout failures as product failures" — not defects.
- Full `pytest --cov` (all suites incl. >900 s parity) was left running; not needed for any conclusion
  here (the security/CUI/engine/parity core is green above). `httpx→httpx2` Starlette deprecation warning
  is present (documented transition), not a failure.
- **Skips you must not read as "pass":** playwright is a separate extra, absent from `.[dev]`, so ~22+
  browser modules `importorskip` in a lean venv; and TEST-01 makes them skip on any chromium bump. The
  audit's rule holds: "all tests pass" is false whenever any test skipped/deselected.

Mutation-proofs executed by the lead: GW-02/TEST-01/HOOK-01 each shown red-now/green-when-fixed;
secret scanner, XXE guard, CPM oracle, `_ALLOWED_HOSTS` pin each shown able to fail.

---

## 10. Security / CUI / ITAR threat model (summary)

- **Assets:** operator production schedules (real CUI), derived metrics, narratives.
- **Boundary (shipped):** everything stays on loopback; no remote endpoint is reachable; CSP blocks
  external assets; importer is XXE-safe; egress guard blocks forbidden clients.
- **Failure modes (latent, not live):** GW-02 (banner could mislabel a *future* remote backend);
  SEC-01 (unpinned Host allowlist); HOOK-01 (renamed-schedule commit). None egresses data as shipped.
- **The real exposure is human/organizational:** DISC-01 (public disclosure) and the operator-local
  patch that widens locality — both outside the code's control and REQUIRES AUTHORIZING OFFICIAL.

---

## 11. Supply-chain & licensing

- GitHub Actions: **all `uses:` SHA-pinned**; pinned by a guard test.
- Python deps: bounded (lower+upper), floor-tested; pip-audit clean.
- MPXJ: vendored jars with versioned names (`commons-*`, `jackcess-4.0.10`, …); provenance/CVE +
  `javac --release 17` reproducibility → **supply-chain agent pending** (§addendum).
- LICENSE placeholder (LIC-01): every redistribution/branding/sample-schedule/NASA-doc reuse decision
  **requires rights-holder + procurement + export counsel**. No legal advice given here.

---

## 12. Documentation contradiction matrix

| A (claim) | B (contradicts) | Nature |
|---|---|---|
| FINAL-REPORT "COMPLETE / CI green / parity-green" | HANDOFF ~12 open items; plan "RECORDED NOT BUILT"; LICENSE placeholder | stale overclaim (DOC-01) |
| plan doc "zero hits for <gateway-host> in src/tests/tools/installer" | the hostname IS in docs/ + history (DISC-01) | scope-narrow true, whole-repo misleading |
| "nothing leaves this machine" (chrome.py:97, unconditional) | GW-02 banner not backend-observed | true as shipped, coupled to a latent gap |

---

## 13. Refuted hypotheses (disproved by executable test)

- **CPM math wrong** → REFUTED (hand oracle matches exactly; negative control fails).
- **MSPDI XXE-vulnerable** → REFUTED (DTD/entity/billion-laughs all rejected; clean doc passes guard).
- **Parity oracle circular/tool-generated** → REFUTED for Fuse (transcribed from reference export).
- **Intake manifest just records names** → REFUTED (it content-detects true formats + dup groups).
- **Secrets committed** → REFUTED (0 hits, scanner negative-control proven; pbix `Pwd=` adjudicated FP).
- **CI ruff red on committed intake** → REFUTED (green in checkout; sandbox 656 is a `.git`-less artifact).
- **Gateway integration present** → REFUTED (no gateway.py; cloud path dead).
- **Version drift** → REFUTED (1.0.200 at 28 sites, 0 divergent).

---

## 14. Environment-blocked work (exact commands; never a real key)

- Full parity gate wall-clock >900 s: `pytest -m parity tests/parity tests/engine/test_ssi_leveled_uid152.py` (running).
- Playwright suite needs the extra: `pip install -e '.[playwright]' && pytest tests/web -k chromium` (browser env).
- Java reproducibility: `javac --release 17 …` + jar hash compare (needs JDK 17; agent pending).
- Gateway transport tests: **cannot be run** — no gateway in repo, and **must never** call
  `<gateway-host>` with real data (policy).

---

## 15. Prioritized remediation plan → see companion `FORWARD-PLAN.md`

## 16. Final clean-tree proof → see §16 in the delivery message (`git status --short --branch`)

---

### §I — Pre-commit scratch-repo battery (red/green), abbreviated

BLOCK: `sched.json`(sniff), `notes.txt`(sniff), `SCHEDULE`(sniff), `p.xml`, `real.mpp`, `real.mpp.bak`,
`book.xlsx.1`. ALLOW (gap): `sched.png`, `notes.md`, `chart.svg`, `p.png`(MSPDI), `data.mpp.png`,
`sched.mpp.zip`, `export.xer.png`, `report.pdf`(schedule), `data.zip`. Allowlist: `tests/fixtures/x.json` ALLOW.
