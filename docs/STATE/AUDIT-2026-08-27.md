# POLARIS² full-tool audit — 2026-08-27 (LIVE, in progress)

Operator directive (2026-08-27, re-issuing the 2026-08-16 deep-dive with new emphasis):
interactive UI functionality — zoom, timescale changes, everything clickable — with **PASS and
FAIL tests for all of it**, a report, and a plan forward. Operator decisions: **SOLO lead** ·
**fix-as-verified** (small confirmed defects land with red-first tests + mutation proofs + ADRs;
large/parity-blocked go into the plan-forward) · both folder-ask builds. The operator reports a
**live defect on their installed v1.0.221**: /path, /driving-path, /evolution — "controls do
nothing" + "renders wrong".

Run under **ADR-0240** (the operator chose SOLO lead for this campaign) and **QC-1/QC-2**
(ADR-0393). Every row below cites its executable proof; a row changes state only with one.

## Status legend — READ THIS BEFORE TRUSTING A ROW

| mark | meaning |
| --- | --- |
| **CONFIRMED-FIXED** | reproduced with an executable probe, fixed, landed with red-first tests + mutation proof by name + ADR. |
| **CONFIRMED-DEFERRED** | reproduced with an executable probe; fix is large/parity-blocked — priced (S/M/L) with its first executable step in the plan forward. |
| **REFUTED** | the claim was probed and did not hold; the probe is cited. |
| **NON-REPRODUCED** | an honest probe could not produce the reported symptom; what would settle it is stated. |
| **UNVERIFIABLE** | cannot be settled from this side (e.g. state on the operator's machine); the settling observation is named. |

---

## Phase 0 — live-defect chase (WP0, this session) · ADR-0440

### Verdict

**CONFIRMED-PLAUSIBLE-ROOT-CAUSE, and the mechanism is CONFIRMED-FIXED.** A persisted
out-of-range/garbage `localStorage["sf.timescale.v1"]` reproduces BOTH reported symptoms
("controls do nothing" + "renders wrong") on exactly the three reported pages, with **zero
console/page errors** — invisible to a fresh-profile probe, matching the planning session's
clean-render measurement — and it survives Reset-view and the launch wipe **by design**
(`persist.js:24-26,64-65` exempts the Timescale key as an operator preference). The unclamped
load path was a confirmed defect regardless (`timescale.js` pre-fix `:60-77` merged persisted
values with only a null-check; the 25–1000 Size clamp existed only on dialog edits; every
consumer guarded only non-positive factors — verified in `app.js:477-480`, `path.js:158-160`,
`driving_path.js:65-67`, `path_evolution.js:158-160`, `sra_grid.js:27`).

**That the operator's machine holds this exact state remains UNVERIFIABLE until their reply.**
Three-line operator ask:

1. A screenshot of /path as it misrenders.
2. DevTools → Console: any red errors on /path?
3. DevTools → Console, paste and send the output of:
   `localStorage.getItem("sf.timescale.v1")` and `localStorage.getItem("sf-ui:/path")`

If (3) shows a `size` outside 25–1000 (or a non-numeric one, or a tier `units` not in the
dialog's list), the chase is closed: the fixed build self-heals that state on first load. If it
shows a clean config, the A-row mechanism is not their defect and the chase reopens from the
B/C/D rows (all currently NON-REPRODUCED as symptom sources on this tree).

### Reproduction matrix — pre-fix tree (v1.0.221 static/), five-version TP4 corpus, target=26

Probe: `probe_matrix.py` (session scratchpad; the committed, re-runnable form of the A/B rows is
`tests/web/test_timescale_dialog_browser.py`). State seeded via `context.add_init_script` —
timescale.js reads localStorage at script PARSE time, so post-load seeding is vacuous. Oracles:
pageerror count · sane geometry (track ∈ [200, 60000] px, widest task bar ≥ 4 px, tier rows
rendered) · Tier-2 control effect (the page's own zoom control must measurably move a bar).
Baseline row A0 passed all three pages before any hostile cell was trusted (the oracle-calibration
control).

| cell | seed | /path | /driving-path¹ | /evolution | reading |
| --- | --- | --- | --- | --- | --- |
| A0 baseline | none | PASS (track 475, bar 124) | PASS (track 1056, bar 318) | PASS (track 670, bar 116) | oracles calibrated |
| A1 | `{"size": 1}` → 0.01× | **FAIL** track 120 (floor), bars at 2px floor | **FAIL** track 120 | **FAIL** track 120, zoom moves bar 2→3px | BOTH symptoms, 0 errors |
| A2 | `{"size": 100000}` → 1000× | **FAIL** track 476,000px | **FAIL** track 1,056,000px | **FAIL** track 670,000px | renders wrong; zoom visually inert, 0 errors |
| A3 | `{"size": 24}` (just outside) | **FAIL** track 120 | PASS (marginal) | **FAIL** track 161 | degrades mildly — clamp floor 25 is sane |
| A4 | `{"size": "600000"}` string | **FAIL** track 2.85M px | **FAIL** track 6.34M px | **FAIL** track 4.02M px | strings coerce downstream → 6000× |
| B1 | `{"show": 0}` | PASS² | PASS² | PASS² | ladder's else-branch paints 3 tiers; config stays poisoned |
| B2 | hostile tier (`units:"bogus"`, `count:1e9`) | **FAIL** pageerror `Cannot read properties of undefined (reading 'fn')` | **FAIL** same ×3 after step-back | **FAIL** blank chart + misleading "Failed to load the path-evolution data." (`path_evolution.js:515` catch swallows a RENDER crash) | second confirmed defect: `labelDef` lacked the fallback `UNITS` has |
| B3 | corrupt JSON `{not json` | PASS | PASS | PASS | parse-catch → defaults (pre-existing, pinned) |
| C1 | `sf-ui:/path` slider replay | PASS | — | — | persist restore is guarded |
| C2 | stale `sf-qs:/path` | PASS | — | — | volatile-param cleaner holds |
| D1 | single version | PASS | PASS (fallback) | PASS ("load at least two analyzable versions") | legitimate empty states |
| D2 | no target | PASS | PASS (fallback) | PASS | legitimate |
| D3 | target=999999 | — | PASS (fallback) | — | legitimate |
| E | A-cells × daylight/jarvis × 1024×700 | **FAIL** (same numbers) | — | — | state-borne; theme/viewport-independent |

¹ /driving-path opens on the NEWEST version and TP4 v5's corridor for 11→26 is legitimately
empty ("No driving corridor in this version."), so the probe steps back one version before
measuring — the first probe run flagged the baseline itself until this was understood (a red for
the wrong reason is not a red).
² B1 PASS is geometric only: `visibleTiers()`'s else-branch happens to catch garbage `show`;
the poisoned value still reached `config()` and the dialog pre-fix.

### The fix (CONFIRMED-FIXED) — `timescale.js` load-path sanitizer + `labelDef` belt

Ranges coerce-then-clamp (`size` → [25,1000]; tier `count` → [1,999] floored); enums must be
members or the field keeps its default (`show` ∈ {1,2,3}, `fyStartMonth` ∈ integers 0–11, tier
`units`/`label`/`align`, nonworking `draw`/`pattern`, `color` pinned to the dialog's own
`#rrggbb(aa)` shape before it reaches CSS strings); healing is in-memory (idempotent per load;
storage heals on the next OK). `labelDef` gains the same months-fallback `tierBands` already had
for `UNITS`, so the B2 crash is dead even for a future unvalidated path. The load moved below the
UNITS/LABELS tables (it needs them to validate; nothing reads CFG before they exist).

Proof chain (QC-1):
- **Red first**: all 8 hardening tests in `tests/web/test_timescale_dialog_browser.py` observed
  to FAIL on the pre-fix tree, each on the defect it names (size 1 kept, 100000 kept, `"600000"`
  kept as a string, `"abc"` kept, `bogus` units kept, show 0 kept, bars pinned at the 2px floor,
  670,000px track).
- **Green**: 16/16 after the fix (≈37s — inside the browser-job budget).
- **Mutation battery, by name**: size clamp removed → the 6 size/matrix tests red;
  tier sanitize bypassed → hostile-tier test red on the units assert while the `labelDef` belt
  still prevented the crash; belt ALSO reverted → red on the crash channel with the exact
  original pageerror text (the test was restructured to force a tier REBUILD first — the crash
  lives in `tierBands`, and a load-time-only check can outrun the error collector);
  show/fy passthrough → its test red. Seed-vacuity is excluded by the pre-fix reds themselves
  (a vacuous seed cannot produce `assert 0 == 3` or `'600000' == 1000`).
- **End-to-end**: the matrix probe re-run on the fixed tree — A2/A4/B2 heal on all three pages
  (A2: tracks 4,750–10,560px, live controls, 0 errors); A1/A3 heal to size 25, where the
  measured geometry (track 120, bar 16–25px) is byte-class-identical to the DIALOG's own legal
  25% (measured separately: track 120, bar 24, factor 0.25) — those residual probe rows are the
  smallest-legal zoom, **REFUTED as defects**.

### Observations logged, not repaired (do not "fix" blind)

- **Legal 25% on /path renders a 120px-floor track** (the fit-fill base × 0.25 with
  `Math.max(120, …)`). It is the dialog's own smallest choice, recoverable in-dialog and by
  slider; whether the floor should be larger is a design question for the UI map (WP1), not a
  defect row.
- **`path_evolution.js:515`'s catch labels a RENDER crash "Failed to load the path-evolution
  data."** — with B2 fixed there is no known way to reach it, but the message shape (a catch
  that misattributes) is the `citations.reattach`-pin shape: reported here, unrepaired.
- **/driving-path on TP4 opens on an empty corridor (v5)** with only the muted line — correct
  data, but the page offers no "step back to see the corridor" hint. UI-map candidate, not a
  defect.

---

## Phase 0 addendum — the operator answered the ask (2026-08-28) · ADR-0441

**The A-row is REFUTED for the operator's machine.** Their `sf.timescale.v1` is a byte-clean
default config (`size:100, show:3`, stock tiers), `sf-ui:/path` is `null`, console clean. The
ADR-0440 mechanism stays CONFIRMED-FIXED as hardening; it was not their defect. Their two
screenshots re-aimed the chase at the axis the whole matrix missed: **span/scale** — a 12.3-year,
2,301-activity IPMR (data date 2026-01-16).

Reproduced at that scale (synthetic 2,280-task look-alike; numbers pre-fix), then fixed
(ADR-0441, all four red-first with a five-mutation battery — see the ADR for the table):

| row | verdict | measurement (pre-fix → post-fix) |
| --- | --- | --- |
| **S1 stale timeline column on reflow** (`SFColResize` sizes the `.g-head` th only at ATTACH; zoom/Fit go through `reflow()`) | **CONFIRMED-FIXED** | after Fit: 1,918 bars painted in a 969px track inside a **40,104px column**, pane scrolled 24,206px into dead space, track rect left **−24,205px** → column follows the axis every reflow |
| **S2 header degrades instead of promoting units** | **CONFIRMED-FIXED** | fitted months tier **165 bands / 0 labeled / 5.9px** (the operator's picket fence); DOM 427,795 nodes → density adaptation (`MIN_BAND_PX=14`, render-only, MS-Project-style Years/Quarters stack; gridlines follow); DOM 176,829 |
| **S3 whole-schedule opens at slider default regardless of span** | **CONFIRMED-FIXED** | no-target /path = 40,104px (~38-page) track, ~0–2 marks visible → opens FITTED above 16 pages; ADR-0438's zoomed-and-seated opening preserved below (first try at 3× broke the measured seat contract on a 7.5-page schedule — caught by `test_path_whole_schedule_browser`, threshold re-anchored) |
| **S4 slider input freezes** | **CONFIRMED-FIXED (debounce)** | 5,692 ms synchronous per input event × dozens per drag → 0 ms per event, one trailing rebuild per gesture |
| **S5 one-shot rebuild cost at 2,280 rows** | **CONFIRMED-DEFERRED (M)** | Fit one-shot 1,417 ms post-fix (was 5,580); first executable step: windowed `paintRows` (viewport slice ± overscan) — WP1 |

New standing instruments: `tests/web/test_long_span_gantt_browser.py` (5 tests, auto-joins the
browser job) + fixture `TP5_LongSpan_Synthetic.xml` (121 tasks, 2017–2029 — the SPAN is the
payload; provenance-pinned). The operator ask below replaces the Phase-0 one — **ANSWERED
2026-08-28; no further reply owed on it.** /evolution on their machine remains unprobed at their
scale (their current session loads ONE file; /evolution needs ≥2 versions) — watch on their next
multi-version load.

---

## Campaign ledger skeleton (appended per-WP — never batch-written)

### WP1 — M1 control-effect census (`test_ui_control_effect_census.py`) — COMPLETE (2026-08-31, ADR-0442, v1.0.224)
**Full M1 LANDED: the census is sitewide and computed** — 34 page states (33 HTML routes from
the app's own route table + the `/driving-path?trace` state), 65 in-family id'd controls, 358
id-less instances (220 chartframe buttons / 138 sf-frame steppers), 8 structural floor families
(sitewide: 76 chart hosts · 55 cf-bars · 109+33 legend controls · 49 resize grips · 7 sticky
proxies · 267 drill triggers · 180 enlarge buttons), zero pageerrors pinned on every page.
57 tests ~2:30; 17-mutation battery red BY NAME (7 census halves + 10 driver falsifications),
plus the 5-mutation windowing battery and the recorded pre-fix reds. The family recognizer runs
on id+className only — free text false-positives measured ("Fit-Out" in a schedule name,
"dis-play", "s-pan"; `tooltips.js` moves `title=` at load) — and `pan(?!d)` excludes "expand".

**Three defects the new drivers caught on their first run, all CONFIRMED-FIXED red-first
(ADR-0442):**

| row | verdict | measurement (pre → post) |
| --- | --- | --- |
| **UI-01 column drag-resize grips unhittable** (Chromium ignores top/bottom/%-height on abs-pos children of table cells) | **CONFIRMED-FIXED** | grips 7×**0**px at the cell's static position (~44px down, LEFT edge) on every frozen Gantt header — feature dead on arrival under passing byte-pins → `sizeGrip` measured geometry: 7×77px on the right edge; real-mouse drag +60px ±6, clamp = JS 28px style + ~53px Chromium min-content floor |
| **UI-02 sticky proxy scrollbar tracks content by race** (attach at DOMContentLoaded observes an EMPTY pane; the table arrives by async fetch) | **CONFIRMED-FIXED** | first zoom: inner width pinned at fitted **1118px** while the pane scrolled 8747px — dead slider unless the fetch beat the boot → childList observer adopts the table whenever it (re)appears; mirror AND drive measured both directions |
| **S5 one-shot paintRows at row scale** (the ADR-0441 deferral, priced M) | **CONFIRMED-FIXED** | 2,280 rows: Fit rebuild 1,623 ms median → **49 ms** (33×); first paint 7.8s → 0.5s; DOM 104,728 → 19,066 nodes. Windowed slice ±40 rows at ≥400 rows, flat output only; full-paint escapes pinned: groups · Show-links · Find · beforeprint. Two sub-defects caught by probe: the tbody clear clamps scrollTop to 0 BEFORE the slice (window stuck at top; pre-fix repaints lost the position too) and stale-pitch spacers undershoot jump-to-bottom |

New standing instruments: the sitewide census (57 tests) · `tests/web/scale_schedule.py`
(deterministic row-scale MSPDI generator) · `tests/web/test_path_row_windowing_browser.py`
(5 tests, 900/300 generated rows).

#### The WP1 UI map — every interactive family, per-row status

| # | surface | where | status |
| --- | --- | --- | --- |
| 1 | /path zoom slider `#pathZoom` | census+DRIVEN | ≥1.5× bar growth |
| 2 | /path fit `#pathFit` | census+DRIVEN | fitted-track shrink |
| 3 | /driving-path zoom `#dpZoomIn/Out` | census+DRIVEN | ±steps, 40px ceiling + 1px floor no-op clicks |
| 4 | /driving-path fit `#dpFit` | census+DRIVEN | fits corridor to mount |
| 5 | /evolution zoom `#evoZoomIn/Out/Reset` | census+DRIVEN | ×1.6, ceiling, floor-recover, reset ±10% |
| 6 | /evolution pan `#evoPanL/R` | census+DRIVEN | scrollLeft round trip |
| 7 | /analysis zoom `#zoomIn/#zoomOut` (the #vizZoom surface) | census+DRIVEN | 1.25× steps, measured on `.g-bar` |
| 8 | /analysis fit `#fitBtn` | census+DRIVEN | scale shrinks to page |
| 9 | /sra grid zoom `#ssiGridZoom` | census+DRIVEN | ≥1.5× bar growth |
| 10 | /sra grid fit `#ssiGridFit` | census+DRIVEN | scale ≤ pane width |
| 11 | Timescale Size % on all 5 consumer pages | DRIVEN | ~2.0× axis (1.94–2.0 measured) |
| 12 | Timescale dialog (open/tabs/preview/OK/Cancel/Reset/Esc/persist) | DRIVEN (WP0 M2) | 16 tests + 8 hardening pins |
| 13 | chartframe −/＋/Reset (220 buttons, 55 bars) | census+DRIVEN | SVG width ×1.25, label 125%→100% |
| 14 | chartframe ⤢ full screen | census+DRIVEN | API or cf-max fallback, enter+leave |
| 15 | legend toggles (109) + show-all (33) | floors+DRIVEN | display:none round trips |
| 16 | column drag-resize (49 grips) | floors+DRIVEN | **was DEAD (UI-01)** — now widen+clamp by real mouse |
| 17 | sticky-scrollbar sync (7 proxies) | floors+DRIVEN | **was RACY (UI-02)** — mirror + drive both ways |
| 18 | bar-click drills (267 sf-drill) | floors+DRIVEN | overlay opens populated, Esc closes |
| 19 | enlarge ⛶ (180) then print | floors+DRIVEN | overlay → static under print; noprint/cf-bar hidden |
| 20 | /path row windowing at scale | DRIVEN (S5) | window/scroll/tail/Find/links — 5 tests |
| 21 | dp version steppers `#dpPrev` | census; step-back exercised by 3 drivers | full clock-stepped drive → WP2:M3 |
| 22 | dp `#dpNext`/`#dpPlay` autoplay | census'd, DEFERRED | WP2:M3 |
| 23 | /evolution steppers+autoplay (`#prevEvo/#nextEvo/#evoPlay`, embedded `#volPrev/Next/Play`) | census'd, DEFERRED | WP2:M3 |
| 24 | per-page steppers: cei snap · scurve · volatility · performance · forecast drift · trend/mission qual | census'd, DEFERRED | WP2:M3 |
| 25 | sf-frame trios (138 buttons, 46 charts) + masters `#sfPlayAll/#sfStepAll/#missionPlay/#missionStep/#autoPlay` | census'd, DEFERRED | WP2:M3 clock-stepped |
| 26 | real `#themeSelect` across 4 themes · language · taskinfo dblclick | not in family; queued | WP2:M5 |
| 27 | SRA grid edit / paste-from-Excel / save round trip | DRIVEN (WP3 M4) | **six defects, all CONFIRMED-FIXED** (ADR-0454) — 17 drivers, real clipboard, digest + status oracles |

Observed, logged, deliberately NOT fixed (do not fix blind): **/mission serves 30 chart hosts
but only 9 chartframe bars** — the 21 async tiles are never framed (stable at settle; the wall
has its own tile-expand system; a WP2:M5 design question, floor-pinned at 30/9) · the Name
column's 200px CSS floor and Chromium's ~53px min-content floor both out-floor the JS 28px
resize clamp (working behavior, documented in the driver) · `test_gantt_sticky_scrollbar.py` /
`test_bar_drill.py` docstrings claimed "Chromium-verified" with no committed browser half — the
census now makes the claims true rather than editing them.

### WP2 — M3 stepper/autoplay drivers + M5 chrome controls — COMPLETE (2026-08-31, ADR-0443, v1.0.225)

**Both milestones LANDED, and both first runs caught real defects.** Two new browser modules,
both driven on Playwright's **fake clock** (autoplay is 1100–1800 ms; a wall-time wait would make
the module a race, not a measurement — the WP1 sticky-scrollbar lesson):
`tests/web/test_ui_stepper_autoplay_browser.py` (59 tests, ~1:52) and
`tests/web/test_ui_chrome_controls_browser.py` (8 tests, ~30s), plus the route-level
`tests/web/test_ui_language_redirect_guard.py` (10 tests, no browser).

**Two oracles per control, both required** — the label the stepper rewrites AND a DOM-shape-agnostic
digest of the chart it repaints. A control that flips its own caption while the visual stands still
is the WP0 defect class, and a label-only assertion would certify it.
`test_chart_digest_is_stable_and_sensitive` pins that the digest holds still across a no-op and
moves on a real step; without it every "chart moved" assertion is unfalsifiable noise.
`test_the_fake_clock_does_not_perturb_the_page` pins the instrument against the artifact.

| row | verdict | measurement |
| --- | --- | --- |
| **M3-01 `/mission`'s wall master was never registered with the ADR-0275 coordinator** | **CONFIRMED-FIXED** | the layout emits `chartframe.js` (which defines `window.SFPlayAll`) AFTER `<main>` — measured DOM script indices `mission.js` **20** vs `chartframe.js` **24** — so `mission.js:76`'s `if (window.SFPlayAll)` guard was always false and skipped registration in silence. `SFPlayAll.stopAll()` asked directly did nothing; a real click on a chart's own control left the wall playing → order-independent registration (stub queue + `_pending` adoption) |
| **M3-02 `/curves`'s `#sfPlayAll` never called `register()` at all** | **CONFIRMED-FIXED** | zero `SFPlayAll` occurrences in `curves.js`; same symptom. `/trend` was the ONLY page where the coordinator worked, and only by the accident of registering after a `fetch` — now on the same explicit idiom |
| **M3-03 `driving_path.js` ignored `prefers-reduced-motion`** | **CONFIRMED-FIXED** | the only animated module of **twelve** with zero handling; under reduced motion Play rewound to 1/5 and a 1100 ms timer carried the corridor to 5/5 → advances one version, no timer |
| **M5-01 the Mission wall framed 9 of its 30 chart tiles** | **CONFIRMED-FIXED** | settled by measurement, not taste: a manual `SFChartFrame.scan()` on the settled wall took the count **9 → 30**, so it is the UI-02 attach-vs-fetch race, not a design choice; the 9 existing toolbars were confirmed FUNCTIONAL first (zoom grew their svg 536 → 670 px) → childList observer adopts late hosts |
| **M5-02 the Language selector always returned the operator to `/`** | **CONFIRMED-FIXED** | `/language` trusted a `Referer` the app's own `Referrer-Policy: no-referrer` strips — measured absent, and the landing measured as `/` from four different pages, contradicting the route's docstring → `data-sf-nexturl-submit` + a server-validated `next_url`, the idiom the banner Project switcher already used |

**The A2 reduced-motion pin was failing OPEN.** M3-03 survived because `test_accessibility.py`
checked a hand-written list of FIVE module names while twelve are animated. Now COMPUTED from the
shipped JS with a documented EXCLUSION list (pollers/audio), so a new animated module is RED by
default, plus a second guard that fails on a stale exemption.

**Proof.** Red-first observed: M3 **5 failed / 54 passed** (the four coordinator failures named
`[mission]` and `[curves]`; the reduced-motion failure named `[driving-path]` and was unpredicted),
M5 **3 failed / 5 passed** (theme, page-scale and Task Information already working). Green: M3
59/59, M5 8/8, redirect guard 10/10, census 57/57 with `/mission` re-pinned 30/30.
**19-mutation battery, every one RED BY NAME** — including the chart oracle proved independently
of the label oracle (volatility captions while `#volChurn` stops redrawing; an `sf-frame` bar
captions without `frames.draw()`), and the coordinator's `isTrusted` check, whose removal would
make Play-all halt on its own first beat.

**The first push was RED on CI and every failure was this change's** — recorded rather than tidied
away, because two of the three causes are process. A botched mutation restore (`git checkout --`
reverts to HEAD) had wiped three files; re-applying them put back only HALF of `chartframe.js`, so
the coordinator's assignment stayed conditional, `mission.js`'s `stopAll`-less stub survived, and
`/mission` threw 20 page errors — worse than the pre-PR code, while the "59/59 green" being quoted
described a tree that no longer existed. Four byte-freeze pins also fired that the pre-flight grep
could not see (it searched for filenames; the pins hash whole files and index call sites by line):
re-baselined deliberately, every axis CAPTION md5 verified identical first. Plus a guard that froze
the very literal this work replaced (re-expressed against the computed population), a stale embedded
wheel after a late whitespace edit, and a FALSE `playwright>=1.44` floor — `page.clock` does not
exist below 1.45, measured from both wheels. See ADR-0443's own table.

#### The WP1 UI map — rows closed by WP2

Rows **21–26** move from `census'd, DEFERRED` to DRIVEN: dp steppers/autoplay · `/evolution`
steppers + embedded volatility · the per-page cei/scurve/volatility/performance/forecast/quality
steppers · the 138 `sf-frame` trio buttons and the three masters · and the real `#themeSelect` /
language / taskinfo. Every `WP2:M3` marker in the census is discharged; driver values are now
module-qualified (`<module>.py::<test>`) and `test_every_declared_driver_exists` imports the
sibling module to resolve them, so a misspelled cross-module driver is RED (both spellings
mutation-proved).

**The `/mission` 30-vs-9 design question is CLOSED, not deferred.** Three measurements settled it:
a manual re-scan takes the count to 30; `mission.py` marks tiles it does NOT want framed with
`chart-note` rather than `chart-host`, making the class the wall's own deliberate switch; and the
existing toolbars demonstrably zoom, so this restores a working affordance to 21 tiles rather than
spreading dead chrome. Census floor `(30, 9, …)` → `(30, 30, …)`.

**Not a defect, and recorded so it is not re-chased:** `#uiScale` measured "dead" against
`document.body` (full-bleed at every zoom) and is fine measured on a heading's box
(212 → 265 → 371 → 191 px across 1.0/1.25/1.75/0.9).

### Operator defect (between WP2 and WP3) — the diagonal timeline header — CLOSED (2026-09-01, ADR-0444 + ADR-0445, v1.0.227)

| row | verdict | measurement |
| --- | --- | --- |
| **HDR-01 edge bands drawn a full unit wide** (ADR-0444) | **CONFIRMED-FIXED** | `2017` 0..81px over `2018` @47 (34px overlap); last band 57px past a 969px axis → both edges clamped, rights tile `47 … 945, 969`. Real, and NOT the operator's symptom. |
| **HDR-02 the tooltip anchor re-positions every Gantt element** (ADR-0445) | **CONFIRMED-FIXED — THE operator's defect** | `[data-sf-hint]{position:relative}` out-cascaded `.g-band/.gantt-bar/.g-ms {position:absolute}` on every titled element: 60/62 bands, 73/73 bars, 11/11 milestones `relative`; rendered band tops `[18,36,54,72,90…]` for one tier — the staircase. Since 2026-07-11, all themes, any file count → `:where([data-sf-hint])`. |

| **HDR-03 `sizeGrip` (ADR-0442 UI-01) mis-seated the grip once the hijack was fixed** | **CONFIRMED-FIXED** | grip `[1,0,7,59]` (left edge) with the patch, `[47,0,7,58]` (right edge, full height) on CSS alone → `sizeGrip` deleted; driver now asserts absolute · flush-right · full-height · reachable. **Corrects ADR-0442's UI-01 diagnosis:** the "Chromium ignores top/bottom on abs-pos children of table cells" claim was this hijack — the grip was `relative`, not absolute, and the pre-fix grip sat at x=7 (wrong edge) under a passing test. |
| **OBS sticky controls bar overlays the sticky header at the top scroll position** | **OBSERVED — do not fix blind** | `#pathControls.sf-freeze-bar` z6 vs `thead` z3 / frozen th z4: `elementsFromPoint` at a correctly seated grip returns a filter button in the bar. The bar being "frozen" above the grid is by design; whether it may cover the header is a cross-page z-order question. The driver now scrolls the grid to viewport centre and proves reachability. |

**Oracle lesson, promoted to a standing rule:** every header oracle before this (ADR-0441 widths,
ADR-0444 inline styles, WP1 zoom drivers) was structurally blind to positioning mode — widths and
inline styles are identical broken or fixed. Positioning is proved by rendered `top` and computed
`position`. The two new tests pin both sides (Gantt elements absolute; static hint hosts relative).

### WP3 — M4 SRA grid edit / paste-from-Excel / save round-trip — CLOSED (2026-09-03, ADR-0454, v1.0.232) — MERGED #626 @ `b631b41f`

The last queued UI-map row. Before this WP the grid's coverage was TestClient plumbing plus a grep of
the served JS for `"paste"` — nobody had typed into a cell, pasted a clipboard, pressed Save grid or
read the status line. `tests/web/test_sra_grid_edit_browser.py` drives every control in Chromium with a
REAL clipboard (`navigator.clipboard.writeText` + Ctrl+V, Excel-shaped CRLF/tab payloads) and two oracles
(the status line; a grid digest that includes each input's live value and each radio's checked state,
pinned stable across reloads/servers and sensitive to a typed value). 9 pass-side drivers green on the
pre-fix tree; 6 defects, each observed RED by name before a line changed:

| # | surface | measured on v1.0.231 | verdict | shipped |
| --- | --- | --- | --- | --- |
| M4-01 | Refresh grid · post-run reload (`sf-ssi-run`) | `pending = {}` on every load: "1 task(s) with unsaved edits." → "145 tasks.", value gone, server never saw it | **CONFIRMED-FIXED** — the map survives a reload; `beforeunload` guard | ADR-0454 |
| M4-02 | a blanked cell | sent as `""`, route skipped it: "Saved 0 change(s)." and the old value came back — no way to clear a factor/range from the grid | **CONFIRMED-FIXED** — a blank clears; a blanked range side re-derives from the ranking | ADR-0454 |
| M4-03 | paste `Factor / 12,5 / 7` | "Pasted 4 value(s)" → "Saved 7" of 10 deltas; three cells silently back to blank; 7 silently 5 | **CONFIRMED-FIXED** — `rejected` + `clamped` in the reply, read back by uid/field/value/reason (ADR-0313's rule, on the grid) | ADR-0454 |
| M4-04 | "Saved N change(s)." | overwritten by the reload's row count after one fetch round trip | **CONFIRMED-FIXED** — the summary rides the reload | ADR-0454 |
| M4-05 | typing `e` into a factor cell | Chromium `badInput`, value `""` → queued as an edit (a silent CLEAR once M4-02 is fixed) | **CONFIRMED-FIXED** — refused at the cell, named | ADR-0454 |
| M4-06 | the SSI panel's own forms (POST + redirect) | a pending edit rode out with no prompt | **CONFIRMED-FIXED** — `beforeunload` while the map is non-empty | ADR-0454 |

Mutation proof: original JS + fixed route → exactly the six JS-side drivers red, the route-side
blank-clear driver green; the four new route pins red against the original route, the 20 existing
green. Oracle trap paid for: the envelope's `title=` is moved to `data-sf-hint` by `tooltips.js` at
load (WP1's trap, met again — read either).

### WP4 — CI event-trigger outage · committed route-coverage instrument · Law 1 in CI — COMPLETE (2026-09-03, ADR-0455, v1.0.233)

| row | verdict | measurement |
| --- | --- | --- |
| **CI-01 the 2026-08-26T15:31:22Z `startup_failure`** | **CONFIRMED — GitHub-side, ~70 min; NOT a repository fault** | run #1656 (id 32985375962) on the #612 merge: created **247 s** after the push, `startup_failure` in **4 s**, **0 billable ms**, four jobs never left `queued`, `check` skipped; job logs 404; `ci.yml` byte-identical to the green runs before (#1655, 08-25) and after (#1659, 08-26 16:42Z), last changed 08-17; the NEXT push's run took **1,281 s** to appear (05abadc5 committed 16:20:55Z, run 16:42:16Z) and cancelled the 16:22 manual dispatch (#1657) through the concurrency group. Attribution to a githubstatus.com incident: **UNVERIFIABLE** from the container (egress-blocked) — operator ask (f). |
| **CI-02 "event-triggered CI is not firing"** | **REFUTED** | 50/50 `main` commits since 2026-08-01 have a push run; event-to-run latency **2–4 s** for 48 of 50 (15 s max, 09-03's six-upload burst); every merged PR head had a `pull_request` run. Trap: `main`'s run for `b631b41f` (#626 merge, #1700) concluded `failure` — the intake-manifest guard the uploads broke (#627) — while the PR head was green; the six upload commits' runs were all `cancelled` by the next push. |
| **RC-01 the route-coverage instrument** | **COMMITTED** — `tools/route_coverage.py` · `SF_ROUTE_COVERAGE` plugin · 11 guards | population **148** endpoints (71 page · 34 api · 42 export · 1 static) over the **139** floor (2026-08-27's figure); Starlette's implicit HEAD not counted (`/openapi.json` declares the only one); recorder on `FastAPI.build_middleware_stack` (class attribute), templates by the app's own router resolved BEFORE dispatch, three buckets (full · partial · unmatched), loaded-schedule count at entry (a 200 empty state = adverse); passive (wrapped/unwrapped byte-identical). Mutation: matcher forced to UNMATCHED → **3 red by name**, 8 green. |
| **RC-02 the gap by name** | **MEASURED** — see "RC-02" below (appended from the instrumented full run of this tree) | `SF_ROUTE_COVERAGE=1 python -m pytest -q` → `python tools/route_coverage.py route_coverage.json --floor 139`. Read as the census did: traffic is not assertions — "reached" is a LOWER bound on tested; the list is a work queue (WP6/WP7), not a floor. |
| **HOOK-03 a web upload / a hook-less clone bypasses the pre-commit guard** | **CONFIRMED-FIXED** — `tools/ci_cui_guard.sh` + `cui-guard` job in `check`'s `needs` | THE hook (never a copy) over `base..HEAD` via `git reset --soft`, the inherited-blob ref aimed at the base (a push to main would otherwise exempt its own blobs); `pull_request` = every violation fails; `push` to main = warning under `00_REFERENCE_INTAKE/` (ADR-0152's sanctioned channel — disclosure), error elsewhere; `--self-test` stages a probe `.mpp` and REQUIRES refusal (refused on the real tree, exit 1). 8 scratch-repo tests, incl. the dead-hook mutation. |
| **WF-01 `installer-smoke.yml` had no manual trigger** | **CONFIRMED-FIXED** | `workflow_dispatch:` added; `test_every_workflow_offers_a_manual_trigger` over every workflow's `on:` block (a trigger elsewhere in the file does not count); mutation (deleted) → exactly `[installer-smoke.yml]` red, 7 green. |

#### RC-02 — the gap by name (instrumented full run of this tree, 2026-09-03 20:48–21:29Z: 4,730 passed · 28,900 requests recorded)

`SF_ROUTE_COVERAGE=<path>.json python -m pytest -q -p no:cacheprovider` → `python tools/route_coverage.py <path>.json --floor 139` (exit 0):

| bucket | count | endpoints |
| --- | --- | --- |
| population | **148** (34 api · 42 export · 71 page · 1 static) | floor 139 holds |
| never reached | **0** | — |
| reached, never a 2xx/3xx | **3** | `GET /export/{fmt}/resource-drill` · `GET /export/{fmt}/ribbon-drill/{name}` · `POST /sra/factor-table` — **the same three the 2026-08-18 census named**: the committed instrument re-derives the uncommitted one |
| reached, never adversely (no 4xx/5xx, no empty-state 2xx) | **21** (2026-08-18: 25) | exports (6): `GET /export/{fmt}/evm` · `mission` · `ribbon` · `risks` · `scurve` · `workbench` — pages (15): `POST /dcma/scope` · `POST /fields/roles` · `POST /margin/band` · `POST /margin/confirm` · `POST /project/combine` · `POST /project/exclude` · `POST /project/select` · `POST /sra/auto-calc` · `POST /sra/branch` · `POST /sra/conditional` · `POST /sra/correlation-matrix` · `POST /sra/jcl-config` · `POST /sra/load-from-schedule` · `POST /sra/risk` · `POST /sra/risk-register` |
| partial matches (path known, method not served) | 3 | `DELETE /cei` (the guard's own probe) · `GET /example` · `GET /session/wipe` (POST-only routes probed as GET by existing tests — each answered 405, which is what those tests assert) |
| 404 by absence | 1 key | `GET <unmatched>` (the deliberate unknown-URL probes) |

Reading, unchanged from the census: **traffic is not assertions** — "reached" is a lower bound on tested,
and "never adversely" means no test drove a 4xx/5xx or an empty-state 2xx through the route, not that no
adverse test exists in another shape. The three never-2xx routes and the fifteen never-adverse POSTs
(`/sra/*`, `/project/*`, `/margin/*`, `/dcma/scope`, `/fields/roles`) are the WP6/WP7 work queue; the six
exports are one parametrised empty-session test away.

### Claude Design migration — one page per session (standing operator ask, 2026-09-03)

| page | artboard (v2 canvas) | shipped | status |
| --- | --- | --- | --- |
| /volatility | 04 How stable is the path | ADR-0451 (2026-09-02) — five numbered panels, one cursor, ten tiles verbatim | MERGED #623 |
| /cei | 06 Work piling up | **ADR-0456** — cursor strip (Auto-play primary · Prev · Next · a `.cd-chip` per snapshot · frame pill), options row (the two ADR-0268 forms byte-exact), `1.1fr .9fr` row (CEI panel verbatim · "How to read this" block from the page's explainer); `.cd-*` family (DESIGN-SYSTEM §9); chips drive the stepper's `render()` | this PR — red-first 5/3, 160 TestClient green, four-theme render census moved on the chip keys only |
| /trend | 05 How it moved | **ADR-0460** (2026-09-04) — the page's own ▶ Play all / ⏭ Step all master re-homed into a masthead cursor strip with one `.cd-chip` per version (a chip clicks every framed chart's Next until it shows that version; the active chip + `v3 · file · DD` pill follow the first chart); the Focus form as the options row; `1.2fr .8fr` row (version table beside the signals table), the 21 framed charts full width, `1.1fr .9fr` row (quality drill beside a stack of the quality sentences + "How to read this" from the page's explainer), margin burndown; `vol-*` cursor aliased onto `cd-*` (third caller) | red-first 5 + 2; 254 TestClient green; census rows /trend + /volatility green; 38 M3 drivers; four-theme census moved on chips / order / the master's shell only; artboard recovered by EXECUTING the canvas (npm-packed React/Babel) |
| next | 09 Where it lands (/forecast) · 07 How we execute (/performance, Command Deck) | — | /compare (10) needs /integrity's evidence ledger on the page: price as a feature change |

Design truth: `Mission Ops Redesign v2.dc.html` — the operator's intake HANDOFF/github.md (refreshed 2026-09-03) call it THE deliverable and the ASTROLABE map superseded; the bundle README (2026-07-27) says the opposite. Recorded in ADR-0456, not resolved by assumption. Recovery recipe (paid for): npm-pack React/Babel, patch `support.js` to local paths, seed `sfredux-screen`, `sfops-boot.skipNext`, `sfredux-guided`, screenshot the `section[data-screen-label]`.

### WP5 — folder-ask builds A+B — COMPLETE (2026-09-04, ADR-0459, v1.0.235) — operator chose BOTH

The three 2026-08-21 facts governed and were NOT re-derived: (1) the folder DIALOG cannot
multi-select (`webkitdirectory` overrides `multiple`); (2) dropping N folders works, N Projects
(ADR-0437); (3) Ctrl/Shift on FILES already works via *choose files…*.

| row | build | what shipped | proof |
| --- | --- | --- | --- |
| WP5-A | clearer labels | the folder button reads *choose one folder…*; a `.dz-how` legend names the three gestures in the operator's terms (several FILES = Ctrl-click / ⌘-click / Shift-click; ONE folder per pick; several Projects at once = select the folders together and drop them; a parent folder that holds several project folders asks); both buttons carry the facts as hover hints; the two 2026-08-21 pinned phrases and both button ids survive | `test_folder_ask.py` (red-first 3/3 on the pristine page; the two SERVER facts pinned — a parent pick = ONE Project, re-rooted rels = one Project per sub-folder — were green before and stay true) |
| WP5-B | parent-folder question | `home.js`: `ingest()` funnels every way in; `subfolderPlan()` returns a plan only when ONE folder root's schedule files span ≥2 immediate sub-folders (schedule extensions from the file input's own `accept`, which equals `supported_extensions()`); the served hidden `#dzAsk` `role=dialog` shell is filled with `textContent` (title · sub-folders BY NAME with counts · the never-guess rule · *N Projects, one per sub-folder* (focused) · *One Project “X” — N versions* · Cancel/Escape); `reroot()` drops the parent segment from every 3+-segment rel and leaves a 2-segment rel alone (a schedule directly under the parent keeps the parent as its Project either way, and the box says so); the server is untouched; a multi-root drop never asks (ADR-0437 byte-for-byte); a single dropped parent asks the SAME question | `test_folder_ask_browser.py` (7; the picker path with a REAL directory through Playwright 1.62's `set_input_files` — genuine `webkitRelativePath` values — and the drop path on ADR-0437's fake-entry machinery); red-first 7/7; mutations on scratch copies each red by name: `reroot` no-op (2), plan always `null` (5 red / 2 never-ask green), threshold `<2`→`<1` (1), sort removed (1); four-theme render of the open question (zero errors, nothing wider than the viewport, focus on the per-sub-folder button) |
| WP5-G | guards | the ask's ids/classes are asserted OUTSIDE the M1 census family regex with the census module's own `_FAMILY`; `home.js` keeps exactly ONE `innerHTML` sink; `hum('prime')` stays at four (the question's buttons never prime) | `test_folder_ask.py`; the M1 `/` census row and route-table guard green; `test_launch_sequence` / `test_preread_concurrency` / `test_home_shell` / `test_multi_folder_*` / `test_landing` / `test_header_and_loading` / `test_i18n` / `test_accessibility` / `test_r11_panel_contract` / `test_responsive` 94 green |

### WP6 — ledger rows verify-or-refute (CPM-01 · CPM-02 · MC-02 · MC-03 · MAN-01 · REC-02 →
then the tail) — PENDING. Do-not-fix-blind rows stay untouched: MF-05, MC-01 parity leg,
ADR-0417/0419 fixtures, the `citations.reattach` pin, the 6 dead E501s, the evolution 0% cell.

### WP7 — thin dimensions (`ai/txlog.py` first — Law 1) — PENDING

### WP8 — consolidated report + repair roadmap ordered by testimony risk — PENDING

### Operator batch (between WP2 and WP3) — six items, 2026-09-02 — CLOSED at v1.0.229 (ADR-0447..0451)

Six operator reports arrived with screenshots; each was MEASURED before it was believed (QC-1), fixed only
where the measurement said so, and every fix landed red-first with the failing run observed.

| # | operator said | measured | verdict | shipped |
| --- | --- | --- | --- | --- |
| 1 | "timeline header still not correct… act accordingly when the user zooms" (blank header screenshot) | current tree renders 3 labeled absolute tiers @8 px/day, 2 at Fit; their screenshot = the pre-v1.0.227 hijack signature (bands stacked below a 63-px clip, DD line alone; dark thead predates v1.0.197). Zoomed IN the stack never demoted: 907-px month bands @30 px/day | **blank header UNVERIFIED on ≥1.0.227 — ask for the banner**; **demote-on-zoom CONFIRMED-FIXED** | ADR-0447 · 4 tests |
| 2 | "Work Piling Up must always show the Target UID(s)" | axis clamped to status+12 months; UID 152 finishes +21 → `target_scheduled_index` None → no mark | **CONFIRMED-FIXED** (pinned months, cap holds) | ADR-0448 · 1 test |
| 3 | "I don't see the OnePager page" | route, rail link, wheel (236 entries, 4 onepager files, 6 route refs) and all nine `origin/main` installers carry it; the LIBRARY rail entry sits at y=938 in a 620-px rail scroller (needs a rail scroll at 1152-px viewports) | **PRESENT — a discoverability question, not a build gap**; installed version UNVERIFIED | none (design question logged) |
| 4 | "running like shit… terrible lag switching pages / scrolling" (two files) | server 0–0.9 s every route; /analysis 1,801,557 DOM nodes (743 gridline + 80 holiday divs PER ROW), 41.6 s to interactive, 36.9 s long tasks, 5 fps scroll; every other page 17 ms/frame | **CONFIRMED-FIXED**: 26,926 nodes · 4.7 s · 0.8 s long tasks · 33 ms/frame (p95 83) | ADR-0449 · 6 tests |
| 5 | "select the field for the WBS… filter for Cost Account and Work Package" | only float erosion honoured a custom WBS field; no CA/WP notion anywhere; **/wbs, /api/wbs, /export wbs read the RAW schedule** (ignored the session scope every banner promises) | **BUILT**; the unscoped WBS pivot **CONFIRMED-FIXED** | ADR-0450 · 7 tests |
| 6 | "convert /volatility to the Claude Design page, don't modify functionality" | artboard recovered by executing the canvas template (support.js blocked by the proxy); five numbered panels, one cursor with chips, cursor-cumulative KPI | **DONE** — ten tiles verbatim inside the five panels; census 66/66; r11 contract preserved | ADR-0451 · 1 test |

**Residuals, stated:** the four themes were not each screenshotted on the restyled /volatility · the
/analysis residue at operator scale is the ~130 materialized rows' sticky cells (a class-based freeze is
the next candidate, not done blind) · the One-Pager `.pptx` in PowerPoint itself is still the operator's
open question · the operator's build version behind the header screenshots is UNVERIFIED.

| 7 (follow-up on 1.0.229) | "I should be seeing three tiers and I am only seeing two" + J/F/M or 1..12 month labels | months → Quarters at ~5 px collided with the configured Quarters tier and ADR-0441 DROPPED it; the labels existed but the 14-px floor ignored them | **CONFIRMED-FIXED**: promote-collision pushes coarser (Y / H / Q), `COARSER` quarters→halfyears, label `fitPx` floor | ADR-0452 · 2 tests |

| 8 | (on 1.0.230) "bottom tier should show months, shows Quarters, won't change"; "Reset to default does not work" | 7.4 px/month vs an 8-px glyph floor AND a 9-px label-blanking rule; Reset worked (select + persisted) but default and custom promote to identical rows — the preview could not show the effect | **CONFIRMED-FIXED** (floor 7, blanking follows the label, glyph class) · **Reset: NOT a defect — explained** (`effectiveNote`) | ADR-0453 · 2 tests |

### Operator answers + new batch — 2026-09-03 (evening) — OPEN (next session, measure before believing)

| # | operator said (2026-09-03, verbatim gist) | status |
| --- | --- | --- |
| (a) | "I don't know which version banner produced the blank-header screenshot" | **UNKNOWABLE** — the screenshot stays unversioned; do NOT chase; a header report on ≥ v1.0.227 is a NEW defect (ADR-0445) |
| (b) | "The one pager did open in PowerPoint" | **CLOSED** — ADR-0446's open question answered; no `reports/pptx.py` change owed |
| (c) | "/analysis scrolls better but there is still some lag" | **MEASURED, IMPROVED, RESIDUE NAMED (ADR-0458, v1.0.234).** Quiet-box frame times at 2,280 rows × two files (1600 × 1000): wheel 300-px steps p50 33 → 17 ms, p95 183 → 83; programmatic 400-px re-aims p50 100 → 83, p95 183 → 150. Root cause by SUBTRACTION in the live page: every re-aim re-sorted the population, emptied the tbody, repainted the whole ~115-row window and re-created a 36,729 × 40,937-px link overlay holding EVERY relationship (links ON p50 67–83 ms → OFF 17). Now: incremental re-aim (`reaimWindow`), entered rows pinned by copying a survivor's sticky offsets, spacers re-trued to the rendered pitch (the initial paint's 18-px estimate inflated the extent 11 %), ONE reused overlay drawing only window-visible links. Residue: Chromium's layout of ~700–800 sticky frozen cells (stripping them → p95 50 ms) — a separate frozen pane is the next candidate, priced not built. 3 pins (2 observed red on a pristine worktree), 5 named mutants each reddening its pin. Measured while a battery ran the same tree read p95 250 — never measure frame times on a busy box |
| (f) | "I don't know if there was a GitHub event on 08-26" | **UNVERIFIABLE stays** — CI-01 rests on the repository's own run evidence (ADR-0455) |
| **T-01** | "The timeline doesn't change when I tell it to — when I tell it I want two tiers it doesn't work correctly" | **MEASURED WORKING on v1.0.233, NOT REPRODUCED — pinned (ADR-0457).** Chromium drove the dialog's Show menu to *Two tiers* + OK on /analysis (TP4 ×5, TP5), /path, /driving-path, /evolution, /sra; at the opening zoom, 3× zoom-in, 6× zoom-out, Fit / View entire project and after a reload every scale rendered EXACTLY two `.g-tier` rows, `position:absolute` at y 0/18 (18/36 under a caption), scale 36 px, `config().show === 2` and `{"show":2}` in localStorage, zero page errors; 1 and 3 tiers again through the same menu. The ladder (ADR-0441/0452/0453), a string `show` and Fit are REFUTED as causes; a stale `timescale.js` is refuted (static assets are `Cache-Control: no-cache` + ETag). Two M2 pins added (`test_t01_*`), mutation (`visibleTiers` returning three tiers for show=2) → both red by name. ASK: which page, which zoom, a screenshot of the dialog beside the header |
| **I-01** | "The schedule integrity page is not picking up the same findings that it once did and it's not working correctly now" | **ROOT-CAUSED to session POPULATION, detectors proven unchanged (ADR-0457, v1.0.234).** Differential: the golden trio (3 pairs), Project2→5 and all ten TP4 pairs through `/integrity?a=&b=` AND `detect_manipulation` on worktrees at v1.0.221 / v1.0.229 / this tree → detector rows IDENTICAL by name (4/7/9 · 5 · 0·0·1·1·0·2·2·2·2·0); `manipulation.py`, `change_effects.py`, `cpm.py`, importers, model byte-identical since v1.0.221. Every code-side hypothesis (ADR-0450 / 0421 / 0422 / 0371 / 0424) REFUTED for a clean session; the page renders with zero page errors, picker/drill/diagram/export work. What narrows it is STATE: (1) the ADR-0258 active Project — two dropped folders, or loose files whose document Titles differ, split the files into Projects and the page said "Load at least two versions" while two were loaded one switch away; (2) a reduce filter — a role name with no mapping (ADR-0450) leaves an EMPTY population and the page asserted "No manipulation-pattern findings". FIXED red-first: the page names its Project population (holds k of N files · other Projects by name · switch form returning to /integrity) and, under a reduce filter, states the in-scope counts; an empty side reads "nothing to compare", never "no findings". 6 TestClient tests (4 observed red on a pristine worktree). ASK: which finding, which two files, and whether they were dropped as separate folders / carry different Titles — the page now says so |
| **CF-01** | "On the schedule integrity page in the Counterfactual — WITHOUT THESE CONDITIONS … it doesn't make sense that if the Target UID is 152 the CPM finish would be 2029-10-29 instead of 2029-09-28 — 31 working day(s) — and then Target UID 152 would have finished 2027-12-30 instead of 2027-10-04; the CPM should be calculating to UID 152 so there should not be two different dates" (2026-09-04) | **TWO DEFECTS, FIXED red-first (ADR-0462, v1.0.236).** (1) the "31 working day(s)" was a calendar-DATE subtraction printed under a working-day label (2029-09-28 → 2029-10-29 IS 31 calendar days; ~21 working days on a five-day calendar) while the change-effects table beneath it was already working days; the engine now reports the CPM's working-minute move over the calendar's day for both the project finish and the target. (2) the two dates are two ACTIVITIES — the network's last finish (2029) and the target milestone (2027); the panel never said which activity "the project finish" was and gave the target bare dates. Now: the finish activity is NAMED (`finish_uid`/`finish_name`), the target line carries its own working-day move (recovery / pushed out / no change), and one muted line says the two are different activities. Engine 2 tests red (7 vs 5; missing field) → green; page 3 pins red on a pristine scratch copy → green; the one data pin on the old number re-baselined 7 → 5 with the reason. ASK: on v1.0.236 does the working-day count read right against the file's calendar |

Ask the operator (do not build on assumed answers): for I-01, which finding disappeared and on which two files; for T-01, on which page (/analysis or /path) and at which zoom; for (c), a Performance-panel recording or the frame-time numbers the ADR-0449 probe prints.

| **CI-03 the caption sweep's `console@0.9` cell is a real intermittent race, not two flakes** | **CLOSED — ROOT-CAUSED and FIXED (2026-09-04, ADR-0461, v1.0.236).** Three strikes in 48 h (/forecast #626 · /curves #629 · /cei `main` #1717), always the sweep's FIRST cell, always a fetch-driven page. NOT a slow paint: every fetch-driven chart module is a sync script inside `<main>` that draws in its fetch callback, and `chartframe.js` (the only `SFChartFrame`) was emitted AFTER `</main>` — the parser yields while downloading a sync script, so the callback could run first, throw `SFChartFrame is not defined`, and the module's `.catch` printed "Failed to load the … data." with zero captions. R1 (chartframe.js held 1.5 s): /cei /curves /forecast /scurve /trend → 0 captions + the false sentence; the `defer`red pages survived. R2 (NO delay, 6 CPU hogs + CDP 8×, fresh browser, 12 cold loads × 3 routes): 1/36 → `Failed to load the forecast-drift data.`; time-to-first-caption max 2,494 ms — the 5-s wait never bit. FIX: `chartframe.js` emitted in the layout HEAD (after `tooltips.js`); five premise pins re-derived; the sweep's zero-caption line now carries `ready/frame/hosts/svgs/failed/pageerrors`. `test_chartframe_load_order_browser.py` (8: layout + 6 routes + a slow-asset Chromium proof with a teeth check) red 8/8 on the pristine tree, green after; sleep-neutralised mutation red by name. The `.catch` conflation (fetch failure vs render throw) stands — a UI-map row |
